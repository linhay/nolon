# Skills & Workflows 迭代复盘 - 2026-02-04

## 0. 对话概览
- 目标：改进 Codex 多账号用量卡片（读取/刷新/切换/性能/展示），并按 `build.sh` 进行构建验证。
- 关键约束：账号文件统一放在 `~/.nolon/codex/auth/`；切换账号要写“干净”的 `auth.json` 到 `.codex`；不要为用户安装 CLI；非当前账号不强刷以避免卡顿。
- 关键转折：用户补充多项 UI/性能要求，并指出编译错误（main actor/未初始化）需要修复。

## 1. 本次使用的 Skills / Workflows
- Skill：`stfilepath`（文件监听/Watcher）
- Skill：`preferences`（通用偏好）
- Workflow：`update-skills`

## 2. 负反馈记录
- “不要为用户安装”：上下文为 `codex` 命令找不到；根因是存在安装倾向/建议；影响是违反用户操作边界。
- “bug: 卡片的邮箱都显示相同的”：上下文为多账号用量卡片；根因是账号摘要/身份信息未按账号区分或被复用；影响是账号识别错误。
- “为什么有卡片不显示账号信息? 报错的话要显示报错信息”：上下文为卡片渲染；根因是缺少 fallback 与错误细节展示；影响是卡片不可用/排障困难。
- “多账号卡片太大、点击要支持切换、当前和选中重叠”：上下文为卡片交互与选中态；根因是卡片设计/标识逻辑；影响是交互不清晰、视觉冲突。
- “刷新太慢/非当前账号不刷新”：上下文为加载策略；根因是对所有账号并发刷新；影响是整体性能下降。

## 3. 自动扫描结果
- 用户提供编译报错定位：`ProviderUsageView.swift` main-actor 调用与 `usageWatcher` 初始化顺序问题，需要优先修复。

## 3.5 习惯/风格学习（偏好沉淀）
- 新增偏好：当用户反馈 CLI 不存在时，只给诊断与手动指引，不执行安装/升级。

## 4. 现有 Skills / Workflows 迭代建议
- 更新 `.agent/skills/preferences.md`：补充“不要自动安装 CLI”与“命令缺失只给诊断指引”规则（已执行）。

## 5. 新增 Skill / Workflow 建议
- 暂无新增需求；现有偏好可覆盖。
