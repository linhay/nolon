# Skills & Workflows 迭代复盘 - 2026-04-27

## 0. 对话概览

- 用户要求让本仓库具备与 GetTokens 一致的“整理”能力。
- 核心要求不是普通总结，而是正式会话沉淀：先提炼模式，再判断落位到 `skill`、`docs`、`memory` 或 `AGENTS.md`，最后执行 `qmd update` 与 `qmd embed`。

## 1. 本次使用的 Skills / Workflows

- 参考了现有 `.agent/workflows/update-skills.md` 的复盘与沉淀思路。
- 本次新增项目级 skills：
  - `gettokens-session-organize`
  - `gettokens-space-governance`
  - `gettokens-doc-writeback`
  - `gettokens-agents-governance-sync`

## 2. 负反馈记录

- 仓库虽然已经在 `AGENTS.md` 中写了“优先使用 `gettokens-*` skills”，但实际文件不存在。
- “整理”缺少正式定义，容易退化成普通总结，不能稳定触发写回与检索闭环。

## 3. 自动扫描结果

- `.agent/skills/` 中原先不存在任何 `gettokens-*` 项目级 skill。
- `docs-linhay/scripts/check-docs.sh` 原先只校验文档目录结构，不校验治理入口引用的 skill 文件是否存在。

## 3.5 习惯/风格学习（偏好沉淀）

- 对本仓库而言，“整理”是正式沉淀动作，不是可选总结风格。
- 当用户说“整理”时，必须默认执行落位判断、写回与 `qmd` 同步，不再重复澄清“只是总结还是要沉淀”。

## 4. 现有 Skills / Workflows 迭代建议

- 用 `gettokens-session-organize` 作为“整理”总入口。
- 用 `gettokens-doc-writeback` 承接 `docs-linhay/dev/`、`docs-linhay/memory/` 与 `qmd` 闭环。
- 用 `gettokens-agents-governance-sync` 控制哪些规则允许提升到 `AGENTS.md`。

## 5. 新增 Skill / Workflow 建议

- 已直接新增上述 4 个项目级 skills。
- 后续若出现更细分的固定沉淀动作，再继续拆分 supporting skills，但仍由 `gettokens-session-organize` 统一编排。
