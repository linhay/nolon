# Skills & Workflows 迭代复盘 - 2026-02-03

## 0. 对话概览
目标：将 `/nolon` 内“厂商相关定义/设置（除 GUI 外）”下沉到 `libs/Providers/Sources` 统一维护，并把 `nolon/Skills/Views/Provider` 的账号/用量 UI 合并为通用视图，数据统一读取 `libs/Providers` 封装结果；同时为 Codex 建立 `import Testing` 的 TDD，并支持读取用户 env 的 live tests。

结果：定位到用量展示与 CodexBar 不一致的核心原因（used% vs left% 口径差异），以及账号/plan/credits/cost 缺失是“Providers 未透出/未映射到通用视图”的问题；另外确认 GUI/测试环境出现 “Codex CLI missing” 多数是 PATH/env 继承问题（终端可运行但子进程找不到），需要在自研 `SKProcessRunner` 侧补齐 login shell env 合并策略。

## 1. 本次使用的 Skills / Workflows
- `/update-skills`
- `preferences.md`（用于约束“只改 .agent/*”与输出语言）

## 2. 负反馈记录
1. **用量数据与 CodexBar 对不上**：Nolon 显示为 `usedPercent`，CodexBar 显示为 `remainingPercent`（xx% left），导致 UI 看起来互相矛盾。
2. **account / plan / credits / cost 没有显示**：说明 `libs/Providers` 对外模型/descriptor 未提供这些字段或 Nolon 通用视图未消费，造成信息缺失。
3. **GUI/测试环境提示 “Codex CLI missing” 但终端可运行**：子进程拿到的 PATH/env 不完整，导致 `codex` 在 app/test 里找不到。
4. **编译错误（module import/依赖缺失）**：`UsageMonitorSettingsStore.swift` 报 “Initializer ... missing import of defining module 'ProviderUsage'”，暴露出跨 target 依赖/`import` 约束在迁移过程易被遗漏。
5. **CodexBar 仅作参考，不直接集成**：需要在流程中明确“对齐行为/数据口径，但不引入依赖”。

## 3. 自动扫描结果
- 交互确认中多次出现 `<turn_aborted>`：下一次遇到中断重入，应优先验证当前 repo 状态（避免重复操作/基于过期假设继续）。

## 3.5 习惯/风格学习（偏好沉淀）
- **架构边界（仓库级）**：厂商定义/解析/抓取等非 GUI 逻辑统一下沉到 `libs/Providers/Sources`，App 侧只消费 “Providers 输出的通用模型”。
- **测试偏好（仓库级）**：Providers 单测优先使用 Swift Testing（`import Testing`）；live tests 允许读取用户 env，但必须有开关并可在缺少 env 时跳过。
- **进程运行偏好（仓库级）**：凡是需要调用外部 CLI（如 `codex`）的 provider，实现必须通过 `SKProcessRunner` 合并 login shell env（尤其 PATH），避免 GUI/test 与终端行为不一致。

## 4. 现有 Skills / Workflows 迭代建议
### `preferences.md`
- 增补 “外部 CLI 的 PATH/env 合并” 与 “live tests gating（env 开关 + 缺失时跳过）” 两条可执行规则，减少后续反复定位同类问题。

### `/verify-build`
- 建议追加 Providers 侧验证步骤：`swift test --package-path libs/Providers`（仅在相关改动时），避免 “App 编过但 Providers 逻辑/模型不一致” 的返工。

## 5. 新增 Skill / Workflow 建议
### 新增 Workflow：`/providers-migration`
- 目的：固化 “App Provider 视图通用化 + 厂商逻辑下沉 libs/Providers + 数据口径对齐（used vs left）+ live tests + env/PATH” 的标准流程。
- 验证：`swift test --package-path libs/Providers` + `./build.sh`（或项目约定的 build 验证）通过，并能在 UI 侧显示 account/plan/credits/cost（若 env 可用）。

