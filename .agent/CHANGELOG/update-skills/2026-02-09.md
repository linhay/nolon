# Skills & Workflows 迭代复盘 - 2026-02-09

## 0. 对话概览
- 目标：完成 Codex 多账号切换/CLI 登录流程落地，并修复账号卡片交互与监听刷新问题，最后统一卡片在深色模式下的可读性。
- 状态：已完成一轮实现与多次回归修正，用户持续通过日志与交互反馈推动收敛。

## 1. 本次使用的 Skills / Workflows
- `swiftui-ui-patterns`
- `update-skills`（本次复盘）

## 2. 负反馈记录
1. “CLI 重新登录应该在右键菜单里”
   - 触发上下文：账号卡片顶部按钮布局讨论后。
   - 根因：交互入口放置与用户既定操作习惯不一致。
   - 影响范围：账号卡片交互一致性与误触风险。
2. “进入 tab 会不停刷新账号 / 避免触发刷新”
   - 触发上下文：监听 `~/.nolon/codex/auth` 目录时大量 `renamed` 事件触发。
   - 根因：未充分区分临时文件（`.dat.nosync*`）和真实 auth 变更事件，抑制窗口与去抖策略不够细。
   - 影响范围：性能、日志噪音、误刷新。
3. “点击 cli 登录后关闭浏览器不能重新点击登录了”
   - 触发上下文：CLI 登录中止/取消后再次登录。
   - 根因：登录任务状态与 UI 可点击态未完全解耦，取消路径状态清理不完整。
   - 影响范围：CLI 登录可恢复性。
4. “CLI 登录 未能完成操作（Swift.CancellationError错误1）”
   - 触发上下文：切账号或重复点击导致任务取消。
   - 根因：把用户主动取消与异常失败统一作为 alert 错误路径处理。
   - 影响范围：错误提示准确性与用户信任。
5. “卡片背景和文字颜色在深色模式下看不清”
   - 触发上下文：账号卡片及后续 skill/workflow/mcp 卡片。
   - 根因：默认卡片背景与边框对比不足，且样式分散在多个组件未统一治理。
   - 影响范围：深色模式可读性与视觉一致性。

## 3. 自动扫描结果
- 扫描 `.agent/skills`、`.agent/workflows` 后，已存在 `preferences.md` 作为偏好集中维护点，适合直接迭代。
- 本次无新增 workflow 文件，采取“更新偏好 + 落 changelog”的最小改动策略。

## 3.5 习惯/风格学习（偏好沉淀）
- 偏好 1：涉及同类卡片样式问题，不接受“只修当前 tab”，应默认跨 `skills/workflows/mcp` 一次性统一。
- 偏好 2：深色模式是硬验收项，不是附加项；卡片背景、边框、正文颜色要有明确对比度策略。
- 偏好 3：持续使用中文直接沟通（本次对话中用户多次强调中文与直接执行）。

## 4. 现有 Skills / Workflows 迭代建议
- 更新 `.agent/skills/preferences.md`：
  - 新增“同类卡片跨 Tab 一次性统一”规则。
  - 新增“深色模式可读性验收”规则。
  - 在 `validation` 增加“收尾时列出已覆盖组件清单”的可验收要求。

## 5. 新增 Skill / Workflow 建议
- 暂不新增。现有 `preferences.md` 足以承载本轮偏好，避免过度拆分。

