# 2026-04-02

- 发布流水线改造：`.github/workflows/release.yml` 从 `macos-latest` 切换为 `self-hosted, macOS, ARM64`，用于在 `mini.lan` runner 上执行发版。
- 密钥加载策略调整：workflow 新增 runner 本地密钥文件读取（`~/.nolon/release-secrets/release.env`），并保留 GitHub Secrets 回退。
- 新增脚本：`scripts/sync-release-secrets-to-mini.sh`，用于将本机 `.env` 与当前 shell 的发版变量同步到 `mini.lan`。
- 远程同步执行结果：已同步 `SIGNING_IDENTITY`、`APPLE_ID`、`APPLE_APP_PASSWORD`、`TEAM_ID`；缺失 `SPARKLE_PRIVATE_KEY`、`MACOS_CERTIFICATE_P12_BASE64`、`MACOS_CERTIFICATE_P12_PASSWORD`、`NOTARY_PROFILE`。
- 文档更新：`docs-linhay/dev/ops/github-actions-release-runbook-2026-03-21.md` 与 `docs-linhay/spaces/github-actions-release/README.md` 已补充 self-hosted runner 与本地密钥方案。
- Claude Code 账号页新增“编辑账号”能力：卡片菜单支持编辑动作，新增账号编辑弹窗（名称/鉴权类型/密钥/Base URL），保存后写回账号快照；若编辑的是激活账号会同步写回 Claude `settings.json`。
- 测试更新：`ProviderUsageUnifiedAccountsPipelineTests` 补充 Claude 编辑动作可见性与触发编辑态用例；执行 `xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS'`，结果 499 tests（1 skipped）通过。
- 参考仓库同步：`docs-linhay/references/cc-switch` 已 `git pull --ff-only` 到 `origin/main` 最新（HEAD `14a3799f`）。
- Claude Code 数据库迁移 Phase 1：在 `ClaudeAccountManager.ensureStorage()` 增加 `nolon.sqlite3` schema 初始化，新增 `claude_accounts`、`claude_active_accounts` 两张表，并插入默认 `scope=default` 行；当前读写仍保持 JSON 通路，未切换运行时数据源。
- Claude Code 账号数据库迁移继续推进：`ClaudeAccountManager` 的账号读写与 active 读写已切换到 `nolon.sqlite3`（`claude_accounts` / `claude_active_accounts`），并保留 JSON 镜像写入；首次启动在 SQLite 为空时会从旧 `accounts.json` 与 `active-account.json` 自动回填（幂等）。
- 回归验证：执行 `xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS'`，结果 499 tests（1 skipped）通过。
- Claude Code 账号迁移收口：已移除运行时 JSON 镜像写入（`saveAccounts`/`setActiveAccountID` 不再写 `accounts.json`、`active-account.json`），运行时改为纯 SQLite 主路径；仍保留“SQLite 为空时从旧 JSON 首次回填”的升级兼容。
- 新增测试：`ClaudeAccountManagerTests` 增加“切换到 SQLite 后不再生成 legacy JSON 快照”用例。
- 回归验证：执行 `xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS'`，结果 499 tests（1 skipped）通过。
- Codex 账号动态分组修复：`ProviderUsageEngine.applyRefreshedCodexOutcome` 在刷新成功后改为“只要拿到有效 `identity.plan` 就覆盖旧值”，不再仅在 `summary.plan` 为空时写入，解决账号升级/降级后分组仍停留旧套餐的问题（如 Plus -> Pro）。
- 新增回归测试：`CodexAccountDisplaySectionsTests.testBDD_GivenCodexAccountPlanChanged_WhenRefreshingOutcome_ThenGroupingUsesLatestPlan`，先复现红灯（分组仍是 Plus），再验证修复后为绿灯（分组更新为 Pro）。
- 验证结果：定向执行 2 条测试通过：
  1) `-only-testing:nolonTests/CodexAccountDisplaySectionsTests/testBDD_GivenCodexAccountPlanChanged_WhenRefreshingOutcome_ThenGroupingUsesLatestPlan`
  2) `-only-testing:nolonTests/CodexAccountDisplaySectionsTests/testBDD_GivenMixedCardKinds_WhenGroupingByTypeInfo_ThenChatGPTUsesPlanAndConfigsUseProvider`
- Claude 配置项对齐 cc-switch：账号编辑页新增并贯通 `ANTHROPIC_MODEL`、`ANTHROPIC_DEFAULT_HAIKU_MODEL`、`ANTHROPIC_DEFAULT_SONNET_MODEL`、`ANTHROPIC_DEFAULT_OPUS_MODEL` 四个字段，激活账号时同步写入 `settings.json`，并清理旧键 `ANTHROPIC_SMALL_FAST_MODEL`。
- 默认值（Cloud Code）已内置：`ANTHROPIC_MODEL=gpt-5`、`ANTHROPIC_DEFAULT_HAIKU_MODEL=gpt-5(minimal)`、`ANTHROPIC_DEFAULT_SONNET_MODEL=gpt-5(medium)`、`ANTHROPIC_DEFAULT_OPUS_MODEL=gpt-5(high)`。
- SQLite 迁移：`claude_accounts` 新增四个模型列并支持老库自动补列（ALTER TABLE）；读取兼容旧配置回退链（含 `ANTHROPIC_SMALL_FAST_MODEL`）。
- 测试：`xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS'` 通过（500 tests，1 skipped，0 failures）。
- Claude 账号级字段继续对齐：新增 `ANTHROPIC_REASONING_MODEL` 全链路（账号模型 / SQLite 列 / 编辑页 / settings 写回 / cc-switch 导入解析），账号级配置项已完整覆盖主模型+推理模型+三档默认模型。
- 回归验证：`xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS'` 通过（500 tests，1 skipped，0 failures）。
- Claude 账号编辑弹窗修复：`ProviderUsageAccountsViewModel.ClaudeState` 补充 `@Observable`，修复点击“编辑”后 `isShowingEditor/editorDraft` 变更未触发 SwiftUI 刷新、导致编辑页不弹出的问题；新增观察回归用例 `testBDD_GivenClaudeCardEdit_WhenOpeningEditor_ThenClaudeStateObservationUpdates`，定向测试 `ProviderUsageUnifiedAccountsPipelineTests` 6/6 通过。
- Claude 账号编辑页交互调整：按需求将密钥输入从 `SecureField` 改为明文 `TextField`；工具栏取消按钮改为右上角关闭按钮，使用标准 UI 组件 `NolonUI.ProviderUsageCloseActionLabel`（`ToolbarItem(.primaryAction)`，兼容 macOS）。
- 验证：`xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/ProviderUsageUnifiedAccountsPipelineTests` 通过（6/6）。
EOF && qmd update && qmd embed- Claude 编辑页关闭按钮样式对齐 Skill Detail：移除 toolbar 关闭入口，改为 `ZStack(alignment: .topTrailing)` + `NolonUI.FloatingCloseButton`（与 SkillDetailScaffold 同款右上角浮动按钮，支持 cancel 快捷键）。
EOF && qmd update && qmd embed- Claude 编辑页头部样式调整：去掉 Navigation 标题栏，改为自定义大标题 Header（`font size 30`）+ 顶部 Save，避免默认下划线分隔感。
- 崩溃修复：`ClaudeAccountEditorSheet.binding` 移除 `fatalError("ClaudeAccountEditorSheet binding requested while draft is nil")`，改为 fallback draft 安全读取；当 draft 为空时 set 分支 no-op，避免空态渲染期间崩溃。
- 验证：`xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/ProviderUsageUnifiedAccountsPipelineTests` 通过（6/6）。
EOF && qmd update && qmd embed
## Codex 账号分组持久化（2026-04-02 17:43）
- 背景：账号页分组在切换后会恢复，根因是 `codexAccountGroupingOption` 未持久化。
- 变更：
  - `ProviderUsageMonitorSettings` 新增 `codexAccountGroupingOptionRawValue`，默认 `typeInfo`，并接入 Codable。
  - `ProviderUsageEngine` 初始化/更新设置时读取该字段；新增 `setCodexAccountGroupingOption(_:)`，切换时同步写回 settings store。
  - `ProviderUsageSubViewModels` 中分组选项 setter 改为调用 engine 持久化方法。
- 回归：
  - 通过：`CodexAccountDisplaySectionsTests/testBDD_GivenCodexAccountPlanChanged_WhenRefreshingOutcome_ThenGroupingUsesLatestPlan`
  - 通过：`ProviderUsageSubViewModelWiringTests` 全套（含分组持久化用例）
  - 未通过路径：`swift test --package-path libs/Providers` 在当前工具链下触发第三方依赖编译错误（swift-collections typed throws），因此 `ProviderUsageMonitorSettingsTests` 暂未在该路径验证。
- Claude 菜单新增“添加账号”入口：在 `ProviderUsageMenuButtonAction` 增加 `claudeAddAccount`，菜单点击后调用 `claude.beginCreateAccount()` 打开编辑页。
- Claude 编辑页支持“创建模式”：`AccountEditorDraft` 增加 `mode(create/edit)`，`saveEditor()` 在 create 模式走 `engine.createClaudeAccount`（底层 `ClaudeAccountManager.addAccount`），edit 模式保持原更新逻辑。
- 验证：`ProviderUsageUnifiedAccountsPipelineTests` 新增 “Claude menu add action” 用例后，定向测试 7/7 通过。
EOF && qmd update && qmd embed- Codex 分组“重启后回退”根因确认：`UsageMonitorSettingsStore` 使用 `provider.id` 作为 key，但 `ProviderSettings.syncWithTemplates()` 会在启动时将 original vendor 的 id 迁移到模板稳定 id（本地命中 `codexXcode`：`981B5574-...` -> `34D152D6-...`），导致旧 key 读取不到，表现为分组恢复默认。
- 修复：`UsageMonitorSettingsStore` 改为 original vendor 使用 template-scoped key（`nolon.usage.settings.template.<templateId>` / `nolon.usage.multi_accounts.template.<templateId>`），并在读取时自动迁移 legacy id key。
- 兼容迁移：内置 legacy id 映射已补 `codex`（`E7D873DA-...`）与 `codexXcode`（`981B5574-...`）。
- 回归：`xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/ProviderUsageSubViewModelWiringTests` 通过（6/6）。
- Codex auth_mode 收敛：在 `CodexAuthManager` 中将 legacy `chatgptAuthTokens` 统一归一化为 `chatgpt`；写入路径统一输出 `chatgpt`，读取继续兼容 legacy 值，避免重启后 metadata/auth.json 出现旧类型导致分组与类型判断不一致。
- Codex SQLite metadata 增强：`codex_account_metadata` 新增 `plan_type` 字段并完成迁移（ALTER TABLE），`CodexAuthManager` 在 upsert metadata 时写入 `plan_type`，从 SQLite 重建 auth JSON 时回填 `plan_type/plan`，用于重启后更快更稳定的套餐分组。
