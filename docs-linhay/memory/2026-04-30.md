# 2026-04-30

- 决策：先下线 Codex iCloud / CloudKit 同步能力，而不是继续带着降级逻辑上线。
- 实施：在 `nolonApp.swift` 增加产品级关闭开关，启动期不再创建 CloudKit coordinator；高级配置页移除 `Cloud Sync` 入口；账号卡片隐藏全部云同步 tag 与 trailing text；`nolon.entitlements` 清空 CloudKit/APS。
- 验证：`xcodebuild -project /Users/linhey/Desktop/FlowUp-Libs/nolon/nolon.xcodeproj -scheme nolon-app -configuration Debug -destination 'platform=macOS' CODE_SIGNING_ALLOWED=NO test -only-testing:nolonTests/CodexCloudSyncPlacementTests -only-testing:nolonTests/CodexiCloudSyncPresentationTests` 通过。
- 发布：将版本号提升到 `2.2.1 (202604301234)`，使用 Xcode 官方签名链路和 `nolon Developer ID` provisioning profile 成功生成 `release/nolon-arm64.dmg` 与 `release/nolon-x86_64.dmg`。
- 公证：`nolon-arm64.dmg` notarization submission `a14c86fa-d29e-4140-83ae-6b0fab25b985` 已 `Accepted` 并 `staple` 成功；`nolon-x86_64.dmg` notarization submission `0fe352b1-5f8d-4e52-b9ee-00dc233d6734` 已 `Accepted` 并 `staple` 成功。
- 新结论：`2.2.1` 的新崩溃已确认不是 CloudKit 回归，而是 `MenuBarExtra -> CodexQuickSwitchMenuBarView` 动态菜单在 macOS 26.4.1 上触发 SwiftUI/AppKit 菜单 diff 崩溃。
- 热修复：临时移除菜单栏入口，版本提升到 `2.2.2 (202604301330)`，重新打出 `release/nolon-arm64.dmg`，notarization submission `70d6c849-bcb9-47f6-a73c-7645a2585bf6` 已 `Accepted` 并 `staple` 成功。
- 本机安装：`/Applications/nolon.app` 已替换为 `2.2.2`；旧版本保留为 `/Applications/nolon-2.2.1-backup.app` 与 `/Applications/nolon-2.1.20-backup.app`；从 `2026-04-30 13:38:39 +0800` 开始的本地启动验收中，进程持续驻留且未生成新的 `nolon-*.ips`。
