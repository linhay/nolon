# 2026-03-29

- 修复 Codex Binary 下载资产筛选：`CodexBinaryManager.fetchRemoteReleases` 不再使用“`contains(arch)+tar.gz`”宽松匹配，改为优先精确选择 `codex-<arch>.tar.gz`，并兼容回退 `codex.tar.gz`/`codex-<arch>.tgz`，避免 `rust-v0.117.0` 误选 `argument-comment-lint-*` 或 `codex-responses-api-proxy-*` 导致 `Cannot find Codex binary in archive.`。
- 新增回归测试：`CodexBinaryManagerReleaseAssetSelectionTests` 覆盖混合资产精确命中、旧包回退、缺失返回 `nil` 三类场景。
- 当前风险：`ProvidersTests` 存在既有编译错误（`ProviderSettings` 缺少 `pathFolder/path`），导致 `xcodebuild test -scheme Providers-Package` 在测试执行前失败，需先修复该基线问题后再跑完整回归。
