# 2026-04-27

## Codex config.toml 单一读写收拢
- 新增 `CodexConfigStore`，按 `config.toml` 路径串行化 read-modify-write，减少多模块并发/交错写入导致的配置丢失。
- 第二阶段补上跨进程 `.lock` 文件互斥和原子写，避免子进程或仓外同机写入绕过进程内锁。
- 已将 `CodexBinaryManager`、`MCPConfigManager`、`CodexActiveProviderConfigManager`、`NolonCodexCLI.prepareIsolatedLoginHome`、Codex 高级配置保存迁移到统一 store。
- 后续审计又补掉两个 app 侧旁路：`ProviderUsageEngine.prepareCLILoginHomeDirectory` 和 `CodexConfigEditorView` 全文编辑保存，现在 Codex 生产写路径都经过 `CodexConfigStore`。
- 继续排查发现切账号仍会丢项，根因在 `CodexActiveProviderConfigManager.restoreManagedConfig`：它会把 relay 激活时缓存的 `originalRawConfig` 整份回写，覆盖 relay 激活后 app 内新增的 MCP 等片段。已改为“从当前文件剥离 managed fragment，再补 baseline 受控键”。
- 继续排查 MCP 用户反馈，确认 Codex provider 原始 `config.toml` 会在启动 repair 和标准化写回中丢失官方 MCP 字段。已停止对 Codex provider 的被动 repair 重写，并补齐 `supports_parallel_tool_calls` / `default_tools_approval_mode` / `experimental_environment` / `tools.*.approval_mode` 的解析与渲染。
- 继续推进后确认仅补官方字段还不够：`setEnabled` 一类局部写回仍会吃掉未知 server 键、未知工具字段和未知子表。已把 Codex MCP 写回改成基于现有 TOML block 的局部 merge，明确遵循“preserve unknowns”策略，并新增回归覆盖未知字段保留。
- 基于这轮排查与修复，单独创建 `docs-linhay/spaces/20260427-codex-config-governance/`，把 Codex 配置写入收口、MCP unknown-preserving、旁路审计与门禁需求升级为正式治理 space。
- 继续推进治理落地时又发现一个真实旁路：`ProviderMcpGridView` 在创建原生 MCP 配置文件时会直接写 `defaultMcpConfigPath`，对 Codex 即直写 `config.toml`。已改成共享入口 `MCPConfigManager.ensureNativeConfigScaffold(for:)`，并新增 `scripts/tests/codex-config-governance-smoke.sh` 扫描典型 Codex 配置直写旁路。
- 回归验证：
  - `swift test --package-path libs/Providers --filter CodexConfigStore`
  - `swift test --package-path libs/Providers --filter NolonCodexCLIPrepareIsolatedLoginHomeTests`
  - `xcodebuild test -scheme nolon-tests -destination 'platform=macOS' CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO -only-testing:nolonTests/CodexAdvancedConfigRoleDraftTests -only-testing:nolonTests/CodexBinaryAutoUpdateBehaviorTests`

## “整理”正式沉淀治理落地
- 约定用户说“整理”时，默认触发正式会话沉淀流程，不再视为普通总结。
- 本次补齐 4 个项目级 skills：`gettokens-session-organize`、`gettokens-space-governance`、`gettokens-doc-writeback`、`gettokens-agents-governance-sync`。
- 将“先提炼可复用模式，再判断落位到 skill/docs/memory/AGENTS，最后执行 qmd update 与 qmd embed”的规则提升到 `AGENTS.md`。
- 同步增强 `docs-linhay/scripts/check-docs.sh`，防止 `AGENTS.md` 再次引用不存在的项目级 skills。

## Codex iCloud 同步入口迁移
- 用户要求把 Codex iCloud 同步从账号页移到设置页，避免账号列表页承载同步开关、清云和冲突处理。
- 已新增 `CodexCloudSyncSettingsView`，并挂到 `CodexAdvancedConfigView` 顶部的设置区域。
- `ProviderUsageView` 的 Codex 账号页不再挂载 iCloud 同步卡，只保留账号管理与账号列表。
- 已补 `CodexCloudSyncPlacementTests`，锁定“设置页可见、账号页不挂载”的页面边界。
- 继续收尾时已删除 `ProviderUsageView` 中残留的 iCloud 同步源码、冲突 sheet、清云确认和云同步摘要，避免本地分支后续误把同步能力重新带回账号页。

## Codex 启动期漂移账号默认退选
- 用户要求：如果启动时当前选中的 Codex 账号对应 auth 内容已被修改，不要继续默认选中，也不要继续套用该账号的 relay/provider 配置。
- 已把 `preflightManagedAuthIfNeeded` 的被动预检场景细分：`usage_load` 与 `background_poll` 发现 active symlink 漂移时，不再沿用原来的“恢复并继续保持 active”策略，而是清空 active selection、移除 provider `auth.json`、并通过 `restoreManagedConfig` 撤回托管配置。
- 手动/显式修复场景仍保留旧行为：`detect_drift`、`post_activate_detect_drift` 这类路径会继续恢复原 active 快照，并把漂移 payload 另存为新/匹配快照。
- 新增回归 `startupPreflightClearsModifiedActiveAccountSelectionAndRestoresConfig`，覆盖“启动时 relay 账号被改过后默认退选且 `config.toml` 还原”的行为。
