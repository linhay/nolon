## 16:30 NolonUI 卡片视图复用收敛（Remote + ProviderCards）
- 目标：减少相似视图实现，统一右上角菜单样式与上下文菜单按钮，降低后续维护成本。
- 决策：在 `NolonUI` 内新增共享视图组，不在主工程侧做桥接样式。
- 新增复用：
  - `Shared/EllipsisMenuButton.swift`：统一 `ellipsis` 菜单按钮样式。
  - `Shared/ContextMenuCommonButtons.swift`：统一 `View Details / Show in Finder / Install / Delete` 菜单项。
  - `Remote/ResourceCardReusableGroups.swift`：统一资源卡片 header/summary/context menu 组装。
  - `ProviderCards/ProviderCardReusableGroups.swift`：统一 reveal+delete 菜单组；新增 `ProviderCardTitleMenuRow` 统一标题+菜单头部行。
- 调整范围：
  - Remote：`ResourceSkillCardView`、`ResourceWorkflowCardView`、`ResourceMcpCardView`、`ResourceCardScaffold`。
  - ProviderCards：`WorkflowCardView`、`RuleCardView`、`AgentDocCardView`、`SkillCardView`、`McpServerCardView`、`ProviderSkillCardView`。
- 结果指标：
  - 重复菜单文案命中（`View Details/action.show_in_finder/action.install/action.delete`）从 `25 -> 12`。
  - ellipsis 菜单样式重复命中降到 `0`。
- 验证：
  - `xcodebuild build-for-testing -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS'`：`0 error`。
  - `xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS'`：`** TEST SUCCEEDED **`。

## 17:30 Autoresearch 前台循环：菜单破坏性动作统一收敛
- 模式：`$codex-autoresearch` foreground（loop）。
- 目标：减少相似视图、提高复用率、统一样式（聚焦 ProviderCards/Remote 菜单破坏性动作与确认弹窗收敛）。
- 机械指标：`rg -n "\\.confirmationDialog\\(|Button\\(role: \\.destructive\\)" libs/NolonUI/Sources/NolonUI/Components/ProviderCards libs/NolonUI/Sources/NolonUI/Components/Remote | wc -l`
- 基线：`4`
- 最佳：`0`（4 轮 keep）
- 守护：`xcodebuild build-for-testing ... | rg "error:" | wc -l` 持续为 `0`。
- 收敛动作：
  - `ProviderSkillCardView`：内联 `.confirmationDialog` 改为 `destructiveConfirmationDialog`。
  - 新增 `ContextMenuDestructiveButton`（Shared）并在 `SkillCardView` / `ProviderSkillCardView` / `RepositoryRowContextMenuView` 接入，消除内联 destructive 菜单项。
- 完整验证：`xcodebuild test ...` 通过（`** TEST SUCCEEDED **`）。

## 17:48 Autoresearch 前台循环 v2：可选预览块统一
- 新指标：`duplicated_optional_preview_blocks_count`
- 机械验证：`rg -n "if !.*preview\\.isEmpty" libs/NolonUI/Sources/NolonUI/Components/ProviderCards | wc -l`
- 基线：`2`
- 最佳：`0`
- 单轮 keep：抽 `ProviderCardOptionalPreviewBlock`，替换 `RuleCardView` 与 `AgentDocCardView` 重复分支。
- 统一策略：在保证核心功能一致前提下统一视图表现（preview 文本统一 tail 截断）。
- 验证：`build-for-testing` guard 通过；`xcodebuild test` 全量通过。

## 18:10 Autoresearch 前台循环 v3：图标+文案行复用
- 新指标：`duplicated_icon_caption_rows_count`
- 机械验证：`rg -n "HStack\(spacing: DesignSystem\.Metrics\.spacingS - 2\)" libs/NolonUI/Sources/NolonUI/Components/ProviderCards | wc -l`
- 基线：`3`
- 最佳：`0`
- keep：新增 `ProviderCardIconCaptionRow`，替换 `AgentDocCardView` + `SkillCardView` 中重复 icon+text 行。

## 18:18 Autoresearch 前台循环 v4：计数标签样式复用
- 新指标：`duplicated_meta_count_label_style_chains`
- 机械验证：`rg -n "\.dsIconLabelButton\(foreground: DesignSystem\.Colors\.Text\.secondary, font: \.caption2\)" libs/NolonUI/Sources/NolonUI/Components/ProviderCards | wc -l`
- 基线：`2`
- 最佳：`0`
- keep：新增 `ProviderCardMetaCountLabel`，替换 `SkillCardView` 内两处重复计数 Label 样式链。

## 18:26 Autoresearch 前台循环 v5：徽章动作样式复用
- 新指标：`duplicated_workflow_badge_style_chain_lines`
- 机械验证：`rg -n "\.fontWeight\(\.semibold\)\n\s*\.dsBadge\(" libs/NolonUI/Sources/NolonUI/Components/ProviderCards/SkillCardView.swift -U | wc -l`
- 基线：`4`
- 最佳：`0`
- keep：新增 `ProviderCardActionBadge`，收敛 `SkillCardView` 中 workflow/link action 的重复 semibold+badge 样式链。
- 验证：各轮 `build-for-testing` guard 通过。

## 18:15 ViewModel 驱动扩展（ProviderCards）
- 目标：推进 `data -> viewModel -> view`，减少 view 直接持有业务数据与样式派生。
- 本轮完成：
  - `AgentDocCardViewViewModel` / `RuleCardViewViewModel` 承载输入数据与展示派生字段；对应 view 改为 `@State viewModel` 驱动。
  - `WorkflowCardViewViewModel` / `SkillCardViewViewModel` 同步扩展输入数据与展示派生；对应 view 移除大部分原始字段。
- 提交：`88998e9`、`8e0a39a`
- 验证：`build-for-testing` 通过；`xcodebuild test` 全量通过。
