## 16:53 Codex session 窗口 0% 立即刷新策略修复
- 背景：DS 账号卡片出现 `session now 0%` 时，用户预期应立即刷新以拉取重置后的额度状态。
- 变更：在 `ProviderUsageEngine.codexScheduledRefreshDecision` 增加短窗口 reset 触发的一次性立即刷新决策：
  - 条件：最短窗口 `remainingPercent <= 0` 且 `resetsAt <= now`。
  - 行为：若该窗口重置后尚未刷新，则返回 `shouldRefresh = true`（即时触发）；若已在该重置点后刷新过，则返回 `already_refreshed`，避免连续抖动。
  - 保护：仅当存在“更长窗口且仍为 0%”时才抑制该立即刷新；单窗口账号不会被抑制。
- 测试：`ProviderUsageEngineManualRefreshTests` 新增
  - `GivenShortestWindowResetReachedAtZero...ThenTriggersOneImmediateRefresh`
  - `GivenSingleWindowResetReachedAtZero...ThenStillTriggersImmediateRefresh`
- 修复 Codex Binary 下载进度字节显示错误：`CodexBinaryManager` 不再把 `URLSessionTask.progress` 的归一化 `completedUnitCount/totalUnitCount` 直接当作字节数展示，优先使用 `countOfBytesReceived/countOfBytesExpectedToReceive`，当仅有 `5/100` 这类百分比单位时只保留 fraction、不再显示伪造的 `5 B / 100 B`。
- 新增库侧回归测试 `CodexBinaryDownloadProgressTests`，覆盖“归一化进度不应显示假字节大小”和“有真实字节计数时应优先显示真实字节”两个场景。

## 19:05 Codex Advanced 配置页补齐官方配置覆盖
- 背景：Codex Advanced tab 先前只覆盖少量顶层字段，缺少 history/compaction/runtime 控制；role 保存还会误删部分官方支持字段。
- 变更：
  - 新增结构化配置字段：`history.persistence`、`history.max_bytes`、`hide_agent_reasoning`、`model_auto_compact_token_limit`、`compact_prompt`、`experimental_compact_prompt_file`。
  - 补齐 role 级字段：`model_reasoning_summary`、`model_verbosity`、`personality`、`web_search`。
  - 修正 UI 值域：移除废弃的 `approval_policy = on-failure`，`personality` 改为 `none | friendly | pragmatic`，role `model_reasoning_effort` 增加 `xhigh`。
  - 结构化 patch 增加 raw fallback 保留，避免保存时误删官方支持但当前 UI 未建模的原始写法，例如 granular approval policy。
  - 按 section 挂接 OpenAI 官方 Codex 文档入口，覆盖 config basics/reference、sandboxing、approvals、安全、subagents、models。
- 测试：
  - `CodexAdvancedConfigRoleDraftTests.testTDD_GivenOfficialRuntimeControls_WhenSavingStructuredConfig_ThenPersistsHistoryAndCompactionFields`
  - `CodexAdvancedConfigRoleDraftTests.testTDD_GivenGranularApprovalPolicy_WhenSavingOtherFields_ThenUnsupportedOfficialSyntaxIsPreserved`
  - `CodexAdvancedConfigRoleDraftTests.testTDD_GivenRoleAdvancedFields_WhenSavingStructuredConfig_ThenPreservesAndUpdatesOfficialRoleOptions`
  - 定向测试通过：`xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -derivedDataPath /tmp/nolon-codex-advanced-tests -only-testing:nolonTests/CodexAdvancedConfigRoleDraftTests`

## 19:30 Codex Advanced 配置项中文说明与本地化补齐
- 变更：
  - 为 `approval_policy`、`sandbox_mode`、`web_search`、`model_provider`、`profile`、`personality`、`model_reasoning_summary`、`model_verbosity`、`history.persistence`、`history.max_bytes`、`hide_agent_reasoning`、`model_auto_compact_token_limit`、`compact_prompt`、`experimental_compact_prompt_file` 补齐中文标签与说明。
  - 为 multi-agent 编辑器内的 `name`、`description`、`config_file`、`model`、`model_reasoning_effort` 等字段补齐中文标签。
  - 为官方选项值补齐本地化：`personality.none|friendly|pragmatic`、`model_reasoning_effort.xhigh`、`history.persistence.save-all|none`，并保留旧 personality 值域的兼容展示。
  - 模型缓存区的 reasoning effort 选择器改为复用同一套选项本地化映射。
- 验证：
  - `python3 -m json.tool nolon/Localizable.xcstrings >/tmp/nolon-localizable-validated.json`
  - `xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -derivedDataPath /tmp/nolon-codex-advanced-tests -only-testing:nolonTests/CodexAdvancedConfigRoleDraftTests`
  - 结果：10 个定向测试全部通过。

## 23:10 Codex Advanced 输入交互便捷化
- 背景：仅靠右侧单个输入框或下拉菜单效率较低，尤其是数值阈值、路径字段、长文本和常用枚举配置。
- 变更：
  - 枚举项增加快捷按钮行，支持一键选择或取消选择，保留原菜单作为兜底。
  - 数值项增加常用预设按钮，并保留手动输入与清空。
  - `hide_agent_reasoning` 从单一 toggle 改为三态选择：`未设置 / 关闭 / 开启`，保留 nil 语义。
  - `experimental_compact_prompt_file` 与 role `config_file` 增加文件选择器、清空和 Finder 打开。
  - `compact_prompt` 改为摘要预览 + 多行编辑 sheet，而不是在主列表里挤单行文本框。
  - 普通文本项补充了清空按钮，避免手动全删。
- 验证：
  - `python3 -m json.tool nolon/Localizable.xcstrings >/tmp/nolon-localizable-validated.json`
  - `xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -derivedDataPath /tmp/nolon-codex-advanced-tests -only-testing:nolonTests/CodexAdvancedConfigRoleDraftTests`
  - 结果：10 个定向测试全部通过。

## 19:18 Codex Binary tab 接入每版官方 Release Notes
- 背景：`codex - binary` 已经具备 GitHub Releases 版本列表与下载能力，但页面无法查看每一版的官方更新日志。
- 变更：
  - `CodexBinaryManager.fetchRemoteReleases` 扩展解析 GitHub Releases API 的 `html_url`、`published_at`、`body`，并注入 `ReleaseDataLoader` 便于测试。
  - `CodexBinaryManifest` 缓存最近一次远端 release 的 GitHub 页面、发布时间、release notes 文本，使 Binary tab 冷启动也能直接显示最近版本说明。
  - Binary tab 版本表增加选中态驱动的 release notes 面板；远端版本直接展示 notes，本地已安装版本会按 `sourceURL` 或版本号回连到对应 release。
  - 新增 `CodexBinaryReleaseNotesData` 与共享 UI `CodexBinaryReleaseNotesView`，支持 Markdown 渲染和跳转 GitHub。
  - 补齐 `Localizable.xcstrings` 的 release notes 文案键：标题、发布时间、副按钮、空态。
- 测试：
  - 新增 `CodexBinaryReleaseNotesTests`，覆盖远端 releases 解析 notes，以及 update check 会把 release notes 写入 manifest 缓存。
  - 新增 `CodexBinaryReleaseNotesViewModelTests`，覆盖远端行与本地已安装版本行都能生成正确的 release notes 展示数据。
  - `swift test --package-path libs/Providers --filter CodexBinaryReleaseNotesTests` 仍被外部依赖 `swift-collections` 的当前工具链兼容问题阻塞，非本次改动引入。
  - `xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/CodexBinaryReleaseNotesViewModelTests -only-testing:nolonTests/CodexBinaryAutoUpdateBehaviorTests -only-testing:nolonTests/CodexBinaryTabConfigurationTests` 已消除本次实现引入的两个编译错误（`subtitleText` 作用域与 `String??`），但整条工程测试链仍在重编大依赖图，未拿到最终通过结果。
