- 修复 Codex 套餐持久化链路：账号刷新成功后原先只更新内存 `summary.plan`，未回写 auth/SQLite，导致 `codex_account_metadata.plan_type` 为空。新增 `CodexAuthManager.upsertPlanType(for:plan:)` 并在 `ProviderUsageEngine.applyRefreshedCodexOutcome` 成功分支调用，确保刷新后落库 `plan_type`。
- Providers 详情页新增 skills-link 联动态：当开启 "Link this provider's skills folder to ~/.nolon/skills" 后，`Skills` tab 改为显示“已链接到 Nolon”的占位图卡片，并提供 `Go to Nolon` 按钮；按钮通过 `MainSplitView` 新增的 `onSelectNolon` 回调切换到主侧栏 `.nolon` 页面，避免用户在 provider 内重复管理同一份全局 skills。
- 调整 Provider Skills 页浮动添加按钮门禁：当 `skillsLinkEnabled == true` 且当前 tab 为 `skills` 时，隐藏右下角添加按钮，避免在已切换到全局 `~/.nolon/skills` 管理模式下重复触发 provider 内安装入口。
