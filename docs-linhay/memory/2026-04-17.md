## Micro-Sync 09:30
- 继续整理 `docs-linhay/` 文档系统，重点收口历史 debate 目录与少量截图命名。
- 已完成的 debate 目录重组：
  - `20260415` 收口为 `codex-mcp`、`codex-sessions`、`provider-usage`、`resource-center`
  - `20260416` 收口为 `codex-sessions`、`provider-usage`
  - 将原先按话题命名的目录并回稳定模块目录，并同步修正文档内引用路径
- 已完成的截图安全重命名：
  - `20260309-codex-usage-after-refresh-v08.png` -> `20260309-codex-usage-refresh-after-v08.png`
  - `20260309-codex-usage-after-ax-refresh-v09.png` -> `20260309-codex-usage-ax-refresh-after-v09.png`
- 当前明确暂缓的项：
  - `during / launch / after-click / probe` 这类文件名还需要结合图像语义判断，不能机械改名
  - 截图重复哈希的去重还没做，避免误删仍有引用价值的历史留档
- 本轮属于文档治理，不涉及代码行为改动，未新增测试。

## Micro-Sync 11:50
- 完成 Claude 账号编辑页 `Credential` 明文展示 debate，文档落地到：
  - `docs-linhay/debate/20260417/provider-usage/20260417-claude-credential-plaintext-v01.md`
- 本轮 debate 收敛结论：
  - 当前“密文”现象只来自 `ClaudeAccountEditorSheet` 里的 `SecureField`
  - `credentialValue` 在草稿、JSON 预览、SQLite 落盘和激活写回链路里一直是明文字符串
  - 存储加密应拆为独立安全增强议题，不阻塞本次 UI 修复
- 已完成实现：
  - `ClaudeAccountEditorSheet` 将 `Credential` 输入从 `SecureField` 改为 `TextField`
  - `ClaudeAccountEditorSheetSnapshotTests` 新增 BDD 回归测试，验证编辑态不存在 `NSSecureTextField`，且明文 credential 出现在普通可编辑文本框
- 验证结果：
  - 红灯：`xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/ClaudeAccountEditorSheetSnapshotTests` 首次失败，确认旧实现仍为 `NSSecureTextField`
  - 绿灯：同一命令在视图改动后通过（3 tests）
- 过程备注：
  - `Claude Code` 在 debate 首轮因默认模型不可访问被淘汰，后续由 `Copilot` 补位参与收敛

## Micro-Sync 16:40
- 完成 `Codex Sessions` 搜索与 usage 第二轮 debate，文档落地到 `docs-linhay/debate/20260417/codex-sessions/20260417-codex-sessions-search-usage-v01.md`。
- 本轮最终收敛的实现决策：
  - 搜索字段首版覆盖 `title / summary / displayID / cwd / provider`
  - 搜索 UI 复用 `SearchField`，首版即时过滤，不做 debounce
  - compact row 接 usage 时，窄宽只显示 total，不显示 `in/out`
- 讨论过程中的环境结论：
  - `Claude Code` 仍因默认模型不可访问被淘汰
  - `Copilot CLI` 本机未登录认证，首轮即淘汰
  - 由替补独立审查参与者 `Curie` 与 `Gemini` 完成第二轮收敛
- 该轮仅更新文档与记忆，尚未进入代码实现与测试。

## Micro-Sync 16:55
- 已按新规格开始实现 `Codex Sessions` 搜索与 compact usage：
  - `CodexSessionsTabViewModel` 新增 `searchQuery`，在 `allRows -> rebuildSectionStates()` 之间过滤，并在搜索态绕过默认 `5` 条预览限制
  - `CodexSessionsTabView` 接入显式 `SearchField`
  - `UnifiedCodexSessionViews` 的 compact row 接入 usage，并在窄宽下只显示 total
- 已补测试：
  - `CodexSessionsTabViewModelTests` 新增 `displayID` 搜索、provider raw/friendly 搜索、搜索态绕过预览限制、清空搜索后保留展开态
  - `CodexSessionsCardSnapshotTests` 新增 `narrow-width-project-list-loaded-usage`
- 验证结果：
  - `xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/CodexSessionsTabViewModelTests` 通过
  - `xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/CodexSessionsCardSnapshotTests` 通过
- 过程备注：
  - 中途曾并发触发两路 `xcodebuild`，出现一次 `DerivedData ... build.db locked`；随后改为串行执行并通过，不属于代码逻辑回归。

## Micro-Sync 17:25
- 补齐 `Codex Sessions` 会话组排序契约：
  - feature 规格新增约束：会话组必须按“组内最新会话时间”倒序，而不是按组名字典序
  - `CodexSessionsTabViewModelTests` 新增 provider 分组显式排序测试，锁定 `project/provider` 两种分组都遵循最新会话时间排序
- 实现结论：
  - 业务代码原本已满足该需求，本轮未改排序实现，只修正了新增测试中的 key path 写法
- 验证结果：
  - `xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/CodexSessionsTabViewModelTests` 通过

## Micro-Sync 17:45
- 收紧 Gemini session usage 的默认扫描边界：
  - `GeminiTokenTrendService` 与 `GeminiIntradayUsageService` 统一复用 `GeminiSessionUsageSupport`
  - 默认扫描根从 `~/.gemini` 收紧为 `~/.gemini/tmp`
  - 仅枚举 `**/chats/session-*.json`，避免误扫非会话文件与无关目录
- 缓存策略补强：
  - `GeminiSessionUsageStore` 在单文件 fingerprint 缓存之上，新增基于“文件列表 + fingerprint”的 merged snapshot 内存缓存
  - 当 session 文件集合与指纹不变时，重复查询不再重新读文件和合并 daily/event 数据
- 文档与验证：
  - feature 规格已同步改写为 `~/.gemini/tmp/**/chats/session-*.json`
  - `GeminiSessionUsageSupportTests` 覆盖默认根目录解析与 chats/session 枚举规则
  - `GeminiTokenTrendServiceTests` 覆盖 merged snapshot cache 命中
  - `xcodebuild test -scheme Providers-Package -destination 'platform=macOS' -only-testing:ProvidersTests/GeminiSessionUsageSupportTests` 通过
  - `xcodebuild test -scheme Providers-Package -destination 'platform=macOS' -only-testing:ProvidersTests/GeminiTokenTrendServiceTests` 通过

## Micro-Sync 20:35
- 完成 `Codex API Key` 明文展示 debate，文档落地到：
  - `docs-linhay/debate/20260417/provider-usage/20260417-codex-api-key-plaintext-v01.md`
- 本轮代码结论：
  - `CodexConfigEditorSheet` 中 `API Key` 字段的密文展示仅由 `SecureField` 导致
  - 将该字段切换为 `TextField` 即可满足“取消密文”的最小正确实现，不影响 `auth.json` 写回链路
  - 配套 BDD 回归测试锁定为“不存在 `NSSecureTextField` 且能看到明文 API key”
- 已完成实现：
  - `nolon/Skills/Domain/Providers/Usage/Views/Codex/CodexConfigEditorSheet.swift`
  - `nolonTests/CodexConfigEditorSheetSnapshotTests.swift`
- 验证结果：
  - `xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/CodexConfigEditorSheetSnapshotTests` 通过（3 tests）
- 过程备注：
  - 两名独立审查参与者在首轮均因 `unknown provider for model gpt-5.1-codex-mini` 被淘汰，最终由主持人基于代码证据直接裁定并完成实现

## Micro-Sync 21:05
- 跟踪并修复“Codex 改 API key 后重启出现新账号”问题。
- 根因定位：
  - `CodexAuthManager.matchAccount(...)` 对 API-key-only 账号的重建匹配只认 `apiKey` 本身，不认已有的 `nolon.account.id`
  - 当外部/重启链路拿到“同一个 managed 账号但 API key 已轮换”的 payload 时，会在 `upsertSnapshotFromProviderData(...)` 中落到 `createSnapshotAccount()`，从而新建第二个账号
- 修复实现：
  - `libs/Providers/Sources/ProviderUsage/CodexAuthManager+SnapshotHelpers.swift`
  - 新增 `matchManagedAPIKeySnapshotByNolonAccountID(...)`，仅对 `account_id/email` 都为空、但存在 `nolon.account.id` 的 managed API-key payload 生效
  - 在 `matchAccount(...)` 中将该分支放在 `strict identity` 之后、`apiKey` 直接匹配之前
- 新增回归测试：
  - `libs/Providers/Tests/ProvidersTests/CodexTests/CodexAuthManagerTests+PreflightAndHealing.swift`
  - 用例：`Given detached managed apikey auth keeps nolon account id but changes api key, when reconciling provider payload then existing snapshot is updated instead of creating a new account`
- 验证结果：
  - `swift test --package-path libs/Providers --filter reconcileUpdatesExistingManagedAPIKeySnapshotWhenOnlyAPIKeyChanges` 通过
  - `swift test --package-path libs/Providers --filter preflightKeepsOfficialAPIKeyWithoutRelayProvenance` 通过
  - `swift test --package-path libs/Providers --filter preflightUpgradesToRelayProfileWhenManagedRelayProvenanceMatches` 通过
- 额外观察：
  - 扩大到 `swift test --package-path libs/Providers --filter preflight` 时，已有几条 OAuth drift 相关测试因依赖旧的 `auth/<id>.json` 文件镜像假设而失败；从代码分支上看，这些失败不经过本次新增的 managed API-key 匹配逻辑，暂未纳入这次修复范围

## Micro-Sync 21:20
- 对“Codex 改 API key 后重启出现新账号”继续追踪后，确认第二轮修复仍不完整，真实问题还覆盖 active symlink drift 链路。
- 新根因定位：
  - `reconcileProviderAuthWithSnapshotsIfNeeded(...)` 在 provider `auth.json` 仍为 symlink 时，只要 target 内容和当前 active registry 不一致，就会第一时间把 symlink 重链回旧 active，导致外部写进 symlink target 的新 payload 在第二阶段 drift repair 之前就被抹掉
  - `reconcileActiveSymlinkDriftIfNeeded(...)` 原来计算 fingerprint 和后续 upsert 时读取的是 SQLite snapshot，而不是 symlink target 当前字节，因此“通过 symlink target 改 API key / 改账号”的真实内容无法被消费
- 本轮修复：
  - `libs/Providers/Sources/ProviderUsage/CodexAuthManager+ProviderSync.swift`
  - symlink 分支改为“只修坏链，不提前处理内容漂移”；内容漂移统一交给 `reconcileActiveSymlinkDriftIfNeeded(...)`
  - drift repair 改为贯穿使用 symlink target 的当前 payload
  - 当 drift payload 与 backup 属于同一身份，或属于当前 active self-managed API-key 账号的无身份 API-key-only payload 时，直接原地更新 active snapshot，不再拆出新账号
- 测试修正与补充：
  - `libs/Providers/Tests/ProvidersTests/CodexTests/CodexAuthManagerTests+PreflightAndHealing.swift`
  - 把旧的 drift 测试从写 `auth/<id>.json` 调整为写 provider symlink target，对齐当前 `active-auth/<provider>/auth.json` 真实运行路径
  - 新增 symlink 场景用例：`preflightUpdatesActiveManagedAPIKeySnapshotWhenSymlinkTargetReplaced`
- 验证结果：
  - `swift test --package-path libs/Providers --filter preflightRestoresActiveSnapshotAfterExternalDrift` 通过
  - `swift test --package-path libs/Providers --filter preflightOverwritesExistingSnapshotWhenDriftIdentityMatchesStrictly` 通过
  - `swift test --package-path libs/Providers --filter preflightCreatesNewSnapshotWhenDriftEmailDiffersUnderSameAccountID` 通过
  - `swift test --package-path libs/Providers --filter reconcileUpdatesExistingManagedAPIKeySnapshotWhenOnlyAPIKeyChanges` 通过
  - `swift test --package-path libs/Providers --filter preflightUpdatesActiveManagedAPIKeySnapshotWhenProviderAuthFileReplaced` 通过
  - `swift test --package-path libs/Providers --filter preflightUpdatesActiveManagedAPIKeySnapshotWhenSymlinkTargetReplaced` 通过

## Micro-Sync 23:35
- 收口 `Codex API Key` 编辑保存“卡一会儿”的问题。
- 根因：
  - `ProviderUsageEngine.saveCodexConfigEditor()` 会等待 `CodexAuthManager.updateConfiguredAccount(...)` 与当前激活账号的 `refreshActiveProviderConfigIfNeeded(...)` 完成后才关闭弹框。
  - 这段等待链路是正确语义，但 `CodexConfigEditorSheet` 之前没有任何 saving feedback，用户体感上像“点保存后卡住”。
- 本轮实现：
  - `CodexConfigEditorSheet` 的 `API Key` 输入继续保持明文 `TextField`。
  - `ProviderUsageEngine` / `ProviderUsageSubViewModels` / `ProviderUsageView` 增加 `isSavingCodexConfigEditor` 透传。
  - `CodexConfigEditorSheet` 在保存中显示 `ProgressView`，并禁用关闭、取消、校验与重复保存操作。
  - 为底部按钮补了稳定的 accessibility identifier，便于后续 UI 自动化复用。
- 文档已同步：
  - `docs-linhay/features/codex-account-usage-tab-update-2026-03-02.md`
- 验证结果：
  - `xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/CodexConfigEditorSheetSnapshotTests` 通过（4 tests）
  - `xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/ProviderUsageEngineValidateConfiguredAccountTests` 通过（11 tests）

## Micro-Sync 23:50
- 修复 `Codex API Key` 编辑后重开仍显示旧值，以及删空字段时编辑器主体消失的问题。
- 根因：
  - `CodexConfigEditorSheet` 对 `@Binding var draft: CodexConfigEditorDraft?` 使用了 `draft?[keyPath:] = value` 与 `draft?.modelProvider = option` 这种可选 struct 的隐式局部写回。
  - 该写法不会稳定把整个 draft 回写到 `ProviderUsageEngine.codexConfigEditorDraft`；界面临时显示的新值、保存值与重开回填值会出现漂移，删除为空字符串时则会暴露为编辑器主体消失。
- 本轮实现：
  - `CodexConfigEditorSheet` 新增 `updatedDraft(...)`，把 `Name`、`API Key`、`Base URL` 和 `model_provider` 的更新统一改为显式整包写回。
  - 保留前一轮 `API Key` 明文展示与 saving feedback 改动，不回退保存态交互。
- 测试与验证：
  - `nolonTests/CodexConfigEditorSheetSnapshotTests`
    - 新增 `BDD: Given optional codex draft when updating api key then explicit writeback keeps the draft and new value`
  - `nolonTests/ProviderUsageEngineValidateConfiguredAccountTests`
    - 新增 `testBDD_GivenEditedConfiguredAccount_WhenSavingNewAPIKey_ThenReopeningLoadsUpdatedAPIKey`
  - `xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/CodexConfigEditorSheetSnapshotTests` 通过（5 tests）
  - `xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/ProviderUsageEngineValidateConfiguredAccountTests` 通过（12 tests）
- 结论：
  - 这轮没有修改 SQLite 持久化协议，问题根因在编辑页 draft 写回链路，当前已用“保存后重开”回归测试锁住。

## Micro-Sync 19:09
- 收口 `Codex` 配置型账号保存链路的前台重活，只保留“编辑单账号”真正必要的同步。
- 用户边界确认：
  - `Usage` 已经拆到其他页面，账号编辑页理论上不再需要为了保存去重算 usage。
  - 本轮优化按 `1 + 2 + 3` 执行：
    - 去掉保存后的手动全量 `reloadCodexFromDisk(false)`
    - active account 判定复用已加载 `codexAccounts`
    - active 账号保存时按字段 diff 决定同步范围
- 根因：
  - `ProviderUsageEngine.saveCodexConfigEditor()` 原先会串行等待 `updateConfiguredAccount(...)`、`refreshActiveProviderConfigIfNeeded(...)`、`reloadCodexFromDisk(false)`。
  - 其中 `reloadCodexFromDisk(false)` 会全量重建 `accounts / summaries / outcomes / grouping / active id / watcher`，而同一次 SQLite 写盘还会触发 GRDB observation 再刷新一轮，造成重复重载。
  - `CodexAuthManager.activeAccountId(for:)` 旧实现内部还会再次 `loadAccounts()`，把已在内存里的账号列表又扫了一遍 SQLite。
- 本轮实现：
  - `ProviderUsageEngine+CodexManagement`
    - 保存成功后改为本地 patch `codexAccounts / summaries / outcomes`，不再手动全量 reload
    - 新增 `CodexActiveRuntimeRefreshScope`
    - active 配置型账号编辑时：
      - 只改 `name` -> `.none`
      - 只改 `API Key` -> `.authOnly`
      - relay 相关配置改动 -> `.authAndConfig`
  - `ProviderUsageEngine` / `ProviderUsageEngine+CodexRefresh`
    - active account 判定统一改成复用内存里的 `accounts`
    - 单账号 cached outcome 组装抽成 helper，避免为本地 patch 再走整页重算
  - `CodexAuthManager+ActiveProviderConfig`
    - 新增 `refreshActiveProviderFilesIfNeeded(..., syncRuntimeConfig:)`
    - 保留原包装方法兼容旧调用
    - provider migration 增加 source 去重，避免相同 providerID 的重复 session migration
- 测试与验证：
  - `xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/ProviderUsageEngineValidateConfiguredAccountTests/testBDD_GivenEditedConfiguredAccount_WhenSavingNewAPIKey_ThenImmediateReopenUsesLocalPatchedState` 通过
  - `xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/ProviderUsageEngineValidateConfiguredAccountTests/testBDD_GivenActiveOfficialAPIKeyAccount_WhenSavingNewAPIKey_ThenItRefreshesActiveAuthWithoutRewritingConfig` 通过
  - `xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/ProviderUsageEngineValidateConfiguredAccountTests/testBDD_GivenActiveRelayAccount_WhenSavingNewBaseURL_ThenItRewritesManagedConfig` 通过
  - `xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/ProviderUsageEngineValidateConfiguredAccountTests` 通过（15 tests）
  - `xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/CodexConfigEditorSheetSnapshotTests` 通过（5 tests）
- 过程备注：
  - 中途确认有一条单测失败仅是 `auth.json` 字符串断言过严，已改为 JSON 语义断言，不涉及产品逻辑回退。
  - 本轮测试全程改为串行执行，避免再次命中 `DerivedData ... build.db locked`。
