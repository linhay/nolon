# 2026-02-28

## 发布 2.0.0（清理参考项目后）
- 依赖调整：`libs/Providers/Package.swift` 将 `STJSON` 从本地路径依赖切换为线上仓库 `https://github.com/linhay/STJSON.git`（`from: 1.4.4`）。
- 发布脚本修复：
  - `scripts/build-dmg.sh` 使用 `nolon-app` scheme 构建 DMG。
  - 增加 `create-dmg` 失败时的 `hdiutil` 回退，避免 DMG 生成中断。
- 验证：`./build.sh` 通过；发布流程完成双架构构建、签名、公证、staple、appcast 更新。
- 结果：`v2.0.0` 已发布，资产包含 `nolon-arm64.dmg`、`nolon-x86_64.dmg`。
- 关键提交：
  - `483c50c` chore(deps): switch STJSON to remote package
  - `3ce77a0` fix(release): use nolon-app scheme for DMG build
  - `284d84a` fix(release): fallback to hdiutil when create-dmg fails
  - `5efd54f` chore(release): v2.0.0

## 修复 Codex 用量“假正常”
- 问题确认：`nolon codex auth usage --refresh` 在刷新失败时会保留旧 `usage_cache`，导致界面仍显示历史配额，看起来“正常”。
- 根因：刷新失败只写 `lastSyncFailedAt/lastSyncFailureMessage`，未失效缓存；汇总统计继续计入旧缓存账号。
- 修复：
  - `NolonLiveCodexCLIService.refreshUsageCaches` 失败分支改为 `clearUsageCache` + 记录失败元数据。
  - `buildAuthUsagePayload` 在本次刷新失败账号上强制不读缓存，并输出账号级失败时间/原因。
  - CLI 文本新增 `[刷新失败]` 分区，summary 增加“刷新失败账号”统计。
- 验证：
  - `swift test --package-path libs/Providers --scratch-path /tmp/providers-spm-refresh-stale --filter NolonCodexCLIServiceTests`
  - `swift test --package-path libs/Providers --scratch-path /tmp/providers-spm-refresh-entry --filter authUsageRendersRefreshFailureSection`

## Codex 账号卡片错误态简化（状态点 + 等宽操作）
- 决策：去掉账号卡片“正常/失败”横幅条，仅保留账号名前红/绿/灰状态点；错误态保留摘要，不显示配额进度条。
- 交互：`复制错误` / `重新登录` 双按钮时等宽平分；仅有 `复制错误` 时按钮全宽。
- 实现：
  - `ProviderUsageView` 新增 `CodexUsageCardPresentationPolicy`（状态映射 + 按钮布局规则）。
  - 卡片 UI 改为标题前状态点；移除状态横幅文案区。
- 测试：新增 `CodexUsageCardPresentationPolicyTests` 覆盖状态映射与按钮布局策略。
- 验证：`xcodebuild test -scheme nolon-app -only-testing:nolonTests/CodexUsageCardPresentationPolicyTests` 与相关回归测试通过，`./build.sh` 通过。
- 代码：`a591b5b` 并已推送 `origin/main`。

## Codex 账号卡片右击菜单重构（状态/排障/危险操作）
- 菜单重构为四段：状态信息（只读）、主操作（刷新/激活/重新登录/复制错误）、排障操作（Finder/复制账号ID/复制auth路径）、危险操作（删除账号）。
- 新增删除确认链路：卡片右击可触发删除，弹窗文案包含账号名（若有邮箱则拼接展示），删除失败显示统一错误提示。
- ViewModel 增加可测试删除注入点：`codexDeleteAction` 与 `postDeleteLoadAction`，并补齐复制账号ID/路径能力。
- 测试新增 `ProviderUsageViewModelDeleteTests`，覆盖请求删除、删除成功、删除失败三条 BDD 场景；并回归通过登录/激活/卡片策略相关测试。

## Codex 账号卡片复制动作统一反馈
- 交互补充：账号卡片内所有复制动作（复制错误/复制账号ID/复制auth路径/复制登录URL）在成功写入剪贴板后统一显示右下角 toast“已复制”，展示约 1.2 秒。
- 实现：`ProviderUsageViewModel` 增加 `isShowingCopyToast/copyToastMessage` 与统一 `showCopyToast`；`ProviderUsageView` 根视图增加 `ToastView` overlay。
- 测试：更新 `ProviderUsageViewModelCLILoginTests.testBDD_GivenFailureMessage_WhenCopyErrorText_ThenPasteboardContainsOriginalText`，新增断言验证复制后会触发 toast 状态。

## Codex app-server 账号与鉴权能力补全（CodexAccountRuntimeService）
- 时间：2026-02-28
- 结论：补全并验证了账号链路 `account/read`、`account/login/start`、`account/login/cancel`、`account/logout`、`account/rateLimits/read` 与服务端请求 `account/chatgptAuthTokens/refresh`。
- 关键修复：
  - 修复 `logout()` 使用 `NSNull` 顶层 JSON 导致的序列化崩溃，改为发送空对象 `{}`。
  - `chatgptAuthTokens/refresh` 回包新增 `chatgptAccountId` 透传，避免多账号场景下 runtime 失去账号绑定信息。
- 测试：
  - 新增并通过 `CodexAccountRuntimeServiceTests` 中 5 个鉴权用例（登录启动、登录完成等待、登录取消、登出、refresh 回调完整载荷）。
  - 回归通过该 Suite 共 7 项测试。

## STJSON JSON-RPC 接入（JsonRPCKit）
- 时间：2026-02-28
- 决策：在 `JsonRPCKit` 入站消息解析层接入 STJSON 1.4.9 的 `JSONRPC` 协议校验，而不是先大改上层 API。
- 变更：
  - `JsonRPCKit` target 增加 `STJSON` 依赖；
  - `STJSON` 版本升级到 `1.4.9`；
  - `JsonRPCLineProcessSession` 对入站 `method` 消息使用 `JSONRPC.decodeInbound(from:)` 进行 strict 校验，并做 `params` 还原。
- 验证：
  - 新增失败测试 `JsonRPCKitTests.reservedRPCPrefixNotificationIsIgnored`，实现后转绿；
  - 与 `CodexAccountRuntimeServiceTests` 联合回归通过。

## STJSON JSON-RPC 接入（二轮：出站约束）
- 时间：2026-02-28
- 继续下沉：`JsonRPCLineProcessSession` 的 `request/notify` 改为先构造 `JSONRPC.Request` 再发送。
- 效果：出站 method 也执行 strict 校验，`rpc.*` 被本地拒绝，不再写入管道。
- 测试：新增 `reservedRPCPrefixOutboundMethodIsRejected`，并与既有 `JsonRPCKit/CodexAccountRuntimeService` 套件联合通过。

## STJSON JSON-RPC 接入（三轮：响应 strict 校验）
- 时间：2026-02-28
- `JsonRPCKit` 响应解析从松散字典映射收敛到 `JSONRPC.Response` 协议模型。
- 增强：非法响应（同时含 result+error、id 异常、jsonrpc 非 2.0）会直接 fail pending request。
- 新增测试：`invalidResponseWithResultAndErrorIsRejected`，并联合回归通过。

## STJSON JSON-RPC 接入（四轮：参数编码收敛）
- 时间：2026-02-28
- `JsonRPCLineProcessSession.encodeParams` 改为 `JSONRPC.Params` 编码，移除 `JSONSerialization` 对 AnyCodable 的崩溃风险。
- 新增兼容：支持 `AnyCodable` 嵌套 params 编码。
- `decodeOutboundParams` 改为 `JSONDecoder.decode(JSONRPC.Params.self, from:)`。
- 测试：`encodeParamsSupportsAnyCodablePayload` 新增并通过；联合回归通过。

## STJSON JSON-RPC 接入（五轮：server-request 回包）
- 时间：2026-02-28
- `JsonRPCLineProcessSession.handleServerRequest` 回包由 `JSONSerialization` 切换为 `JSONRPC.Response` 编码。
- 修复崩溃：handler 返回 `AnyCodable` 时不再触发 `Invalid type in JSON write (__SwiftValue)`。
- 测试：新增 `serverRequestReplySupportsAnyCodablePayload`，并与全套 JsonRPCKit/CodexAccountRuntimeService 回归通过。

## STJSON JSON-RPC 接入（六轮：入站分发去 JSONSerialization）
- 时间：2026-02-28
- `handleIncomingLine` 改为 Decodable + STJSON 分流，不再依赖 `JSONSerialization` 字典解析。
- 语义兼容：method+id:null -> notification；response+id:null -> ignore。
- 回归：JsonRPCKit + CodexAccountRuntimeService 联合通过。

## STJSON JSON-RPC 接入（七轮：结构化错误）
- 时间：2026-02-28
- `JsonRPCKit` 新增 `JsonRPCSessionError` 并在会话层统一抛出结构化错误。
- 上层收益：不再依赖错误字符串解析，可按 case 做稳定分流（protocol/response/params/transport/shutdown）。
- 测试同步为 case 断言，回归通过。

## STJSON JSON-RPC 接入（八轮：错误分类透传）
- 时间：2026-02-28
- `CodexAppServerSession.mapError` 对 `JsonRPCSessionError` 增加分类映射，输出稳定前缀（protocol_violation/invalid_response/invalid_params/...）。
- 价值：保持 `CodexCLIError` 边界不变，同时提升上层可观测性与可分流性。
- 测试：新增 `mapsProtocolViolationCategory` 并通过；JsonRPCKit + CodexAccountRuntimeService 回归通过。

## STJSON JSON-RPC 接入（九轮：CodexAppServerKit 分类测试补齐）
- 时间：2026-02-28
- 新增 `CodexAppServerKit` 三类错误映射测试（invalid_params / invalid_response / protocol_violation）。
- 作用：锁定 `mapError` 分类契约，防止后续退化为字符串散落。
- 回归：JsonRPCKit + CodexAccountRuntimeService + 3 条 CodexAppServerKit 映射测试联合通过（18 tests）。
