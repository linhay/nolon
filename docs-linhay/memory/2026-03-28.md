# 2026-03-28

## 决策
- 统一布局体系正式收敛为 `SplitLayoutScaffold + SplitLayoutProfile`。
- 移除 `ThreeColumnScaffold/ThreeColumnScaffoldMode/ThreeColumnSidebarWidth` 旧桥接别名，避免双轨 API。
- 将核心页面布局入口统一为 profile 驱动：
  - 主界面：`SplitLayoutProfiles.main`
  - 账号视图：`SplitLayoutProfiles.accounts`
  - 资源中心：`SplitLayoutProfiles.resourceCenter(isTwoColumn:)`
  - Skill 详情：`SplitLayoutProfiles.skillDetail`
  - 设置页：`SplitLayoutProfiles.settings`

## 验证
- `xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -derivedDataPath /tmp/nolon-dd-codex` 通过。

## 后续
- 若继续收敛，下一步优先将布局 profile 参数进一步 token 化（列宽/可见性策略），再做跨页面布局行为测试。
