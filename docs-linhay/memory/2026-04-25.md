# 2026-04-25

- 完成 `docs-linhay` 向 `spaces/` 结构迁移：旧 `features/` 文档已迁到 `docs-linhay/spaces/<space-key>/README.md`，相关 `plans/`、`debate/`、`screenshots/` 已归档到对应 `space`。
- 新增文档治理脚本：
  - `docs-linhay/scripts/create-space.sh`
  - `docs-linhay/scripts/migrate-docs-to-spaces.sh`
  - `docs-linhay/scripts/check-docs.sh`
- 新增 smoke test：`bash scripts/tests/docs-space-governance-smoke.sh`，覆盖建 space、迁移和结构校验。
- 新增治理说明：`docs-linhay/dev/docs-linhay-spaces-migration-2026-04-25.md`。
- 验证结果：
  - `bash scripts/tests/docs-space-governance-smoke.sh` 通过
  - `bash docs-linhay/scripts/check-docs.sh` 通过
- 风险边界：少数历史截图 / debate 缺少 feature 显式引用，本轮按模块和文件名回退映射到稳定 space；若后续需要更细粒度归档，再二次拆分 space。
- 新建 `docs-linhay/spaces/codex-account/`，作为 “Codex - 账号” 主题 space，已生成标准 `README.md`、`plans/`、`screenshots/`、`debate/` 目录并通过 `bash docs-linhay/scripts/check-docs.sh` 校验。
- 为 `docs-linhay/spaces/codex-account/README.md` 产出 `Codex 账号支持 iCloud 同步` 需求稿：
  - 推荐方案：`CKSyncEngine + CloudKit Private Database`
  - 明确不采用：`NSUbiquitousKeyValueStore`、iCloud Drive 文件同步
  - 明确边界：`v1` 只同步账号库，不同步激活态、provider `auth.json`/`config.toml` 接管态、运行时会话与 `runtime-home`
  - 冲突规则：`accountID` / auth hash 自动合并；同邮箱不同凭据进入人工冲突处理
  - 风险结论：同步 auth 快照属于高敏感数据，功能默认关闭，必须显式告知会同步到用户个人 iCloud 容器
- 发起 `debate gemini` 审查 `Codex 账号 iCloud 同步` 需求，结论为“主方案成立，但需收紧两处边界”：
  - Gemini 认可“只同步账号库，不同步当前激活态”，原因是现有激活链路会联动 provider `auth.json`、active registry 和 `config.toml`
  - 需补充 tombstone 删除后的本地残留处理规则，避免形成脱离托管的孤儿 `auth.json`
  - 需补充 `accountID` 缺失但 auth hash 命中时的元数据合并规则，避免旧同步时间覆盖新值
  - debate 记录已写入 `docs-linhay/spaces/codex-account/debate/20260425-codex-account-icloud-sync-v01.md`
- 与 Gemini 完成第 2 轮收敛并达成共识：
  - 主方案保留：`v1` 继续维持“只同步账号库，不同步当前激活态”
  - README 已补充 3 组收紧规则：tombstone 删除后的本地残留治理、`accountID` 缺失但 hash 命中时的字段级元数据合并、relay 参数“只入库不自动写本地 `config.toml`”
  - 当前剩余开放问题仅为：是否要引入单独的 `recordUpdatedAt` / payload 更新时间字段；该问题不阻塞需求级共识
- 与 Gemini 协作完成 `Codex 账号 iCloud 同步` 技术方案：
  - 新增 `docs-linhay/dev/codex-account-icloud-sync-implementation-2026-04-25.md`
  - 方案结论：CloudKit engine bootstrap 在 app，业务同步协调 / 合并 / tombstone / preflight 拦截在 `libs/Providers`
  - 明确本地模型扩展：`cloud_record_name`、`record_updated_at`、`last_synced_at`、`sync_status`、`is_tombstone`
  - 明确 CloudKit record 模型：`CodexAccount` + `accountPayload` + `metadataJSON` + `recordUpdatedAt`
  - 明确测试拆分：schema/state、merge policy、tombstone + preflight、CKSyncEngine 接线四个 phase
- 开始落地 `Codex 账号 iCloud 同步` 第 1 批实现，当前已完成“本地状态面 + app bootstrap 骨架”：
  - `libs/Providers` 新增 `CodexAuthManager+CloudSync.swift`
  - SQLite 已扩展 cloud sync 字段：`cloud_record_name`、`cloud_record_zone`、`record_updated_at`、`last_synced_at`、`sync_status`、`is_tombstone`
  - `preflightManagedAuthIfNeeded` 与 `activateAccountAndMarkActive` 已拦截 tombstone / `invalidPending` 账号
  - `managementStatus(for:)` 已聚合 cloud sync 摘要计数
  - `nolonApp.swift` 已新增 `CodexCloudSyncBootstrapService`，负责本地开关与 `CKAccountStatus` 探测
  - `ProviderUsageEngine` 已加载每个账号的 cloud sync state，管理摘要已显示 iCloud 同步概况
- 验证结果：
  - `swift test --package-path libs/Providers --filter 'CodexAuthManagerTests/(sqliteSchemaIncludesCloudSyncColumns|loadCloudSyncStateDefaultsToLocalOnly|markPendingCloudDeletionPersistsState|managementStatusAggregatesCloudSyncCounters|mergePolicyPreservesLatestSyncMetadataForHashMatchedAccounts|preflightThrowsForInvalidPendingCloudState|applyCloudTombstoneRemovesNonActiveAccount)'` 通过
  - `xcodebuild -project nolon.xcodeproj -scheme nolon-app -configuration Debug -sdk macosx build CODE_SIGNING_ALLOWED=NO` 通过
- 当前边界：
  - 还未接 `CKSyncEngine` 真正 push / pull
  - 冲突处理 UI、手动同步入口、“激活失效待处理”恢复入口仍待下一阶段实现
- 补完 `Codex 账号 iCloud 同步` 第 2 批实现，范围从“骨架”推进到“可见状态 + 本地开关闭环 + 工程能力”：
  - `libs/Providers` 新增 `cloudSyncOverview()`，统一输出总记录数、待处理数、冲突数、待修复数、最近成功同步时间与最近错误摘要
  - 修正 `managementStatus(for:)` 统计口径，隐藏于列表的 `pendingDelete` tombstone 现在仍会计入待处理数，避免 UI 误报为“无待同步”
  - `nolonApp.swift` 已把 `CodexCloudSyncBootstrapService` 升级为 `CodexiCloudSyncService`：
    - 改为读取 `CodexAuthManager` 的持久化开关，默认关闭
    - 归约 `disabled / syncing / synced / paused / conflict / failed` 六种 UI 状态
    - 暴露 `setEnabled(_:)`、`syncNow()`、`refresh()` 给 UI / engine
  - Codex 账号页已新增 `iCloud 同步` 卡片，展示当前状态、iCloud 可用性、上次成功同步时间、待同步变更数、最近错误摘要，并接入：
    - 开启同步
    - 关闭本机同步
    - 立即同步
  - Codex 账号卡片 footer 已显示单账号云状态 tag：`仅本地 / 已同步 / 待上传 / 待删除 / 冲突 / 待修复`
  - 工程已补 `nolon/nolon.entitlements` 与 app target `CODE_SIGN_ENTITLEMENTS`，加入：
    - CloudKit container entitlement
    - APS entitlement
- 本轮验证结果：
  - `swift test --package-path libs/Providers --filter 'CodexAuthManagerTests/(sqliteSchemaIncludesCloudSyncColumns|loadCloudSyncStateDefaultsToLocalOnly|cloudSyncConfigurationDefaultsToDisabled|addAccountMarksPendingUploadWhenCloudSyncEnabled|updateAccountRefreshesPendingUploadStateWhenCloudSyncEnabled|markPendingCloudDeletionPersistsState|managementStatusAggregatesCloudSyncCounters|deleteAccountMarksPendingDeleteWhenCloudSyncEnabled|cloudSyncOverviewIncludesHiddenPendingDeleteAndLatestError|mergePolicyPreservesLatestSyncMetadataForHashMatchedAccounts|preflightThrowsForInvalidPendingCloudState|applyCloudTombstoneRemovesNonActiveAccount)'` 通过
  - `xcodebuild -project nolon.xcodeproj -scheme nolon-tests -configuration Debug -sdk macosx test CODE_SIGNING_ALLOWED=NO -only-testing:nolonTests/ProviderUsageAccountsViewModelParityTests -only-testing:nolonTests/CodexiCloudSyncPresentationTests -quiet` 通过
  - `xcodebuild -project nolon.xcodeproj -scheme nolon-app -configuration Debug -sdk macosx build CODE_SIGNING_ALLOWED=NO -quiet` 通过
- 当前剩余边界：
  - 远端 `CKSyncEngine` / CloudKit record push-pull 尚未接线，因此“立即同步”当前完成的是状态刷新与本地可用性闭环，不是云端 record 传输闭环
  - 冲突处理与“激活失效待处理”仍只有状态暴露，没有专门恢复流
  - entitlement 已入库，但真实 iCloud 容器与签名仍需在有效 Apple 开发环境下做运行态验证
- 完成 `Codex 账号 iCloud 同步` 第 3 批实现，把“立即同步”推进到真实 CloudKit 最小闭环：
  - `libs/Providers` 新增正式 cloud sync API：
    - `cloudSyncPendingChanges()`
    - `markCloudSyncSent(...)`
    - `markCloudSyncFailed(...)`
    - `applyRemoteCloudRecord(...)`
  - 开启同步后，已有 `localOnly / synced` 本地账号会统一转为首轮 `pendingUpload`
  - app 侧新增 `CKSyncEngine` 最小执行器：
    - 固定 private database zone `CodexAccounts`
    - 本地持久化 `CKSyncEngine.State.Serialization`
    - 本地生成稳定 `device-id`
    - `syncNow()` 会真实执行 `sendChanges()` + `fetchChanges()`
    - `sentRecordZoneChanges` / `fetchedRecordZoneChanges` 已回写 provider cloud state
  - 新增 CloudKit codec 测试，验证 pending save/delete 映射、`CKRecord` 编码、远端 payload 解码
- 本轮验证结果：
  - `swift test --package-path libs/Providers --filter 'CodexAuthManagerTests/(sqliteSchemaIncludesCloudSyncColumns|loadCloudSyncStateDefaultsToLocalOnly|cloudSyncConfigurationDefaultsToDisabled|addAccountMarksPendingUploadWhenCloudSyncEnabled|updateAccountRefreshesPendingUploadStateWhenCloudSyncEnabled|markPendingCloudDeletionPersistsState|managementStatusAggregatesCloudSyncCounters|deleteAccountMarksPendingDeleteWhenCloudSyncEnabled|cloudSyncOverviewIncludesHiddenPendingDeleteAndLatestError|mergePolicyPreservesLatestSyncMetadataForHashMatchedAccounts|preflightThrowsForInvalidPendingCloudState|applyCloudTombstoneRemovesNonActiveAccount|enablingCloudSyncQueuesExistingAccountsForUpload|cloudSyncPendingChangesIncludesHiddenPendingDeletes|applyRemoteCloudRecordCreatesLocalAccountAndMarksSynced)'` 通过
  - `xcodebuild -project nolon.xcodeproj -scheme nolon-tests -configuration Debug -sdk macosx test CODE_SIGNING_ALLOWED=NO -only-testing:nolonTests/ProviderUsageAccountsViewModelParityTests -only-testing:nolonTests/CodexiCloudSyncPresentationTests -quiet` 通过
  - `xcodebuild -project nolon.xcodeproj -scheme nolon-app -configuration Debug -sdk macosx build CODE_SIGNING_ALLOWED=NO -quiet` 通过
- 当前剩余边界更新：
  - 冲突中心 UI 仍未实现，目前 `serverRecordChanged` 只会把账号标记为 `conflict`
  - “查看冲突 / 处理激活失效待处理 / 清空 iCloud 云副本” 三个动作仍缺专门入口
  - 真实 iCloud 容器权限、签名与跨设备联调仍需带有效 Apple 开发配置做运行态验证
- 完成 `Codex 账号 iCloud 同步` 第 4 批补完，已把 README 剩余 `v1` 动作补到最小可用闭环：
  - Codex 账号页 `iCloud 同步` 卡片新增：
    - `查看冲突`
    - `清空 iCloud 云副本`
  - 新增 `CodexCloudAttentionSheet`，列出 `conflict / invalidPending` 账号，并支持：
    - `保留本地并重试上传`
    - `清理本地残留`（仅 `invalidPending`）
  - app 侧 `CodexiCloudSyncService.clearCloudData()` 已实现“删云端 records + 删 zone + 清本地 sync state + reset provider metadata + 关闭本机同步”
  - `ProviderUsageEngine` 已补：
    - `retryCodexCloudAttentionAccount(id:)`
    - `discardCodexInvalidPendingAccount(id:)`
    - `clearCodexCloudData()`
- 本轮验证结果：
  - `xcodebuild -project nolon.xcodeproj -scheme nolon-app -configuration Debug -sdk macosx build CODE_SIGNING_ALLOWED=NO -quiet` 通过
  - `swift test --package-path libs/Providers --filter 'CodexAuthManagerTests/(sqliteSchemaIncludesCloudSyncColumns|loadCloudSyncStateDefaultsToLocalOnly|cloudSyncConfigurationDefaultsToDisabled|addAccountMarksPendingUploadWhenCloudSyncEnabled|updateAccountRefreshesPendingUploadStateWhenCloudSyncEnabled|markPendingCloudDeletionPersistsState|managementStatusAggregatesCloudSyncCounters|deleteAccountMarksPendingDeleteWhenCloudSyncEnabled|cloudSyncOverviewIncludesHiddenPendingDeleteAndLatestError|mergePolicyPreservesLatestSyncMetadataForHashMatchedAccounts|preflightThrowsForInvalidPendingCloudState|applyCloudTombstoneRemovesNonActiveAccount|enablingCloudSyncQueuesExistingAccountsForUpload|cloudSyncPendingChangesIncludesHiddenPendingDeletes|applyRemoteCloudRecordCreatesLocalAccountAndMarksSynced)'` 通过
  - `swift test --package-path libs/Providers --filter 'CodexAuthManagerTests/(retryCloudSyncUploadResolvesInvalidPendingToPendingUpload|discardInvalidPendingManagedAccountRemovesLocalResidue|resetCloudSyncMetadataAfterRemotePurgeMakesAllRowsLocalOnly)'` 通过
  - `xcodebuild -project nolon.xcodeproj -scheme nolon-tests -configuration Debug -sdk macosx test CODE_SIGNING_ALLOWED=NO -only-testing:nolonTests/ProviderUsageAccountsViewModelParityTests -only-testing:nolonTests/CodexiCloudSyncPresentationTests -quiet` 通过
- 当前明确未完成边界：
  - 不是完整冲突中心；“采用云端覆盖本地”与“两者都保留并自动重命名”还没做
  - 真实跨设备 / 真容器 / 推送触发运行态验收还没做
- 完成 `Codex 账号 iCloud 同步` 第 5 批补完，冲突处理从“仅标红”推进到“可采用云端覆盖本地”：
  - app 侧现在会从 `serverRecordChanged` 的 `CKError` 提取 `serverRecord`
  - provider 侧 `markCloudSyncFailed(..., conflictPayload:)` 会把远端 payload 落到 `cloud_conflict_payload_json`
  - 新增 `adoptRemoteCloudConflict(accountID:providers:)`
  - Codex 冲突 sheet 新增 `采用云端覆盖本地`
  - 若冲突账号当前正处于激活态，采用云端版本后会同步刷新本机 provider-facing `auth.json`
- 本轮验证结果：
  - `swift test --package-path libs/Providers --filter 'CodexAuthManagerTests/(markCloudSyncFailedStoresConflictPayloadForLaterResolution|adoptRemoteCloudConflictOverwritesLocalSnapshotAndActiveAuthFile|retryCloudSyncUploadResolvesInvalidPendingToPendingUpload|discardInvalidPendingManagedAccountRemovesLocalResidue|resetCloudSyncMetadataAfterRemotePurgeMakesAllRowsLocalOnly)'` 通过
  - `swift test --package-path libs/Providers --filter 'CodexAuthManagerTests/(sqliteSchemaIncludesCloudSyncColumns|loadCloudSyncStateDefaultsToLocalOnly|cloudSyncConfigurationDefaultsToDisabled|addAccountMarksPendingUploadWhenCloudSyncEnabled|updateAccountRefreshesPendingUploadStateWhenCloudSyncEnabled|markPendingCloudDeletionPersistsState|managementStatusAggregatesCloudSyncCounters|deleteAccountMarksPendingDeleteWhenCloudSyncEnabled|cloudSyncOverviewIncludesHiddenPendingDeleteAndLatestError|mergePolicyPreservesLatestSyncMetadataForHashMatchedAccounts|preflightThrowsForInvalidPendingCloudState|applyCloudTombstoneRemovesNonActiveAccount|enablingCloudSyncQueuesExistingAccountsForUpload|cloudSyncPendingChangesIncludesHiddenPendingDeletes|applyRemoteCloudRecordCreatesLocalAccountAndMarksSynced|retryCloudSyncUploadResolvesInvalidPendingToPendingUpload|markCloudSyncFailedStoresConflictPayloadForLaterResolution|discardInvalidPendingManagedAccountRemovesLocalResidue|resetCloudSyncMetadataAfterRemotePurgeMakesAllRowsLocalOnly|adoptRemoteCloudConflictOverwritesLocalSnapshotAndActiveAuthFile)'` 通过
  - `xcodebuild -project nolon.xcodeproj -scheme nolon-tests -configuration Debug -sdk macosx test CODE_SIGNING_ALLOWED=NO -only-testing:nolonTests/CodexiCloudSyncPresentationTests -only-testing:nolonTests/ProviderUsageAccountsViewModelParityTests -quiet` 通过
  - `xcodebuild -project nolon.xcodeproj -scheme nolon-app -configuration Debug -sdk macosx build CODE_SIGNING_ALLOWED=NO -quiet` 通过
- 当前明确未完成边界更新：
  - README 冲突三选项只剩“两者都保留，并对导入侧自动重命名”未做
  - 真实跨设备 / 真容器 / 推送触发运行态验收还没做
- 完成 `Codex 账号 iCloud 同步` 第 6 批补完，README 最后一条冲突分支已落地：
  - provider 侧新增 `splitCloudConflictKeepingBoth(accountID:)`
  - 行为是保留本地账号 A，同时把云端 conflict payload 物化为第二条账号 B
  - B 会自动稳定命名为 `原名 (iCloud)` / `原名 (iCloud 2)` ...
  - A/B 都转回 `pendingUpload`，由现有同步链路继续推回 CloudKit
  - 本机当前激活态仍留在 A，不自动切到 B
  - Codex 冲突 sheet 已补第三个动作：`两者都保留`
- 本轮验证结果：
  - `swift test --package-path libs/Providers --filter 'CodexAuthManagerTests/(splitCloudConflictKeepingBothCreatesDuplicatedPendingUploadAccount|splitCloudConflictKeepingBothUsesIncrementingSuffixWhenNeeded|markCloudSyncFailedStoresConflictPayloadForLaterResolution|adoptRemoteCloudConflictOverwritesLocalSnapshotAndActiveAuthFile)'` 通过
  - `swift test --package-path libs/Providers --filter 'CodexAuthManagerTests/(sqliteSchemaIncludesCloudSyncColumns|loadCloudSyncStateDefaultsToLocalOnly|cloudSyncConfigurationDefaultsToDisabled|addAccountMarksPendingUploadWhenCloudSyncEnabled|updateAccountRefreshesPendingUploadStateWhenCloudSyncEnabled|markPendingCloudDeletionPersistsState|managementStatusAggregatesCloudSyncCounters|deleteAccountMarksPendingDeleteWhenCloudSyncEnabled|cloudSyncOverviewIncludesHiddenPendingDeleteAndLatestError|mergePolicyPreservesLatestSyncMetadataForHashMatchedAccounts|preflightThrowsForInvalidPendingCloudState|applyCloudTombstoneRemovesNonActiveAccount|enablingCloudSyncQueuesExistingAccountsForUpload|cloudSyncPendingChangesIncludesHiddenPendingDeletes|applyRemoteCloudRecordCreatesLocalAccountAndMarksSynced|retryCloudSyncUploadResolvesInvalidPendingToPendingUpload|markCloudSyncFailedStoresConflictPayloadForLaterResolution|discardInvalidPendingManagedAccountRemovesLocalResidue|resetCloudSyncMetadataAfterRemotePurgeMakesAllRowsLocalOnly|adoptRemoteCloudConflictOverwritesLocalSnapshotAndActiveAuthFile|splitCloudConflictKeepingBothCreatesDuplicatedPendingUploadAccount|splitCloudConflictKeepingBothUsesIncrementingSuffixWhenNeeded)'` 通过
  - `xcodebuild -project nolon.xcodeproj -scheme nolon-tests -configuration Debug -sdk macosx test CODE_SIGNING_ALLOWED=NO -only-testing:nolonTests/CodexiCloudSyncPresentationTests -only-testing:nolonTests/ProviderUsageAccountsViewModelParityTests -quiet` 通过
  - `xcodebuild -project nolon.xcodeproj -scheme nolon-app -configuration Debug -sdk macosx build CODE_SIGNING_ALLOWED=NO -quiet` 通过
- 当前明确剩余边界：
  - 业务分支已齐，剩下的主要是运行态联调：真实跨设备 / 真容器 / 推送触发
- 完成 `Codex 账号 iCloud 同步` 第 7 批补完，补齐真实 CloudKit change tag 所需的 record metadata 持久化：
  - provider cloud state 新增 `recordSystemFieldsBase64`
  - SQLite metadata 新增 `cloud_record_system_fields_base64`
  - `markCloudSyncSent(...)` 会持久化成功返回 record 的 system fields
  - `applyRemoteCloudRecord(...)` 会把 fetched server record 的 system fields 一并落回本地
  - outbound save 会优先从 system fields 恢复 `CKRecord`，避免真实云端更新反复触发 `serverRecordChanged`
  - conflict 后 `retry / adopt remote / keep both` 三条路径也都会消费最新远端 system fields
- 本轮验证结果：
  - `swift test --package-path libs/Providers --filter 'CodexAuthManagerTests/(sqliteSchemaIncludesCloudSyncColumns|loadCloudSyncStateDefaultsToLocalOnly|cloudSyncConfigurationDefaultsToDisabled|addAccountMarksPendingUploadWhenCloudSyncEnabled|updateAccountRefreshesPendingUploadStateWhenCloudSyncEnabled|markPendingCloudDeletionPersistsState|markCloudSyncSentPersistsRecordSystemFields|managementStatusAggregatesCloudSyncCounters|deleteAccountMarksPendingDeleteWhenCloudSyncEnabled|cloudSyncOverviewIncludesHiddenPendingDeleteAndLatestError|mergePolicyPreservesLatestSyncMetadataForHashMatchedAccounts|preflightThrowsForInvalidPendingCloudState|applyCloudTombstoneRemovesNonActiveAccount|enablingCloudSyncQueuesExistingAccountsForUpload|cloudSyncPendingChangesIncludesHiddenPendingDeletes|applyRemoteCloudRecordCreatesLocalAccountAndMarksSynced|retryCloudSyncUploadResolvesInvalidPendingToPendingUpload|markCloudSyncFailedStoresConflictPayloadForLaterResolution|discardInvalidPendingManagedAccountRemovesLocalResidue|resetCloudSyncMetadataAfterRemotePurgeMakesAllRowsLocalOnly|adoptRemoteCloudConflictOverwritesLocalSnapshotAndActiveAuthFile|splitCloudConflictKeepingBothCreatesDuplicatedPendingUploadAccount|splitCloudConflictKeepingBothUsesIncrementingSuffixWhenNeeded)'` 通过
  - `xcodebuild -project nolon.xcodeproj -scheme nolon-tests -configuration Debug -sdk macosx test CODE_SIGNING_ALLOWED=NO -only-testing:nolonTests/CodexiCloudSyncPresentationTests -only-testing:nolonTests/ProviderUsageAccountsViewModelParityTests -quiet` 通过
  - `xcodebuild -project nolon.xcodeproj -scheme nolon-app -configuration Debug -sdk macosx build CODE_SIGNING_ALLOWED=NO -quiet` 通过
- 当前真实剩余边界：
  - 代码层冲突流与 CloudKit metadata 链路已齐
  - 仍待真实跨设备 / 真容器 / 推送触发运行态验收
- 运行态预检新增结论：
  - 用带签名的 `xcodebuild -project nolon.xcodeproj -scheme nolon-app -configuration Debug -sdk macosx build -quiet` 做真环境预检时失败
  - 失败不是代码问题，而是当前 `Mac Team Provisioning Profile: *` 不具备：
    - Push Notifications capability
    - iCloud capability
    - `iCloud.nolon.overloaded.com` container 支持
    - `com.apple.developer.aps-environment / com.apple.developer.icloud-container-identifiers / com.apple.developer.icloud-services` entitlements 对应的 profile 权限
  - 当前工程侧已声明：
    - `PRODUCT_BUNDLE_IDENTIFIER = nolon.overloaded.com`
    - `DEVELOPMENT_TEAM = 3L8RM3MDLS`
    - `CODE_SIGN_ENTITLEMENTS = nolon/nolon.entitlements`
  - 结论：`Codex 账号 iCloud 同步 v1` 的代码层已完成，但真容器 / 真签名 / 真推送联调当前被 Apple Developer 侧 capability/profile 配置阻塞
