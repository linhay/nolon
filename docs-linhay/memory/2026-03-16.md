## AccountSummaryCard 标题提前截断修复（2026-03-16）
- 问题：`AccountSummaryCard.swift` 中卡片标题在右侧 badge/meta 存在时被过早截断。
- 决策：在 `headerSection` 提升左侧标题区布局优先级，限制右侧 meta 为单行并固定右侧区域水平自适应，避免右侧信息抢占过多宽度。
- 变更：
  - `nolon/Skills/Views/Components/AccountSummaryCard.swift`
  - `nolonTests/AccountSummaryCardSnapshotTests.swift`（新增回归快照测试）
- 验证：
  - `xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/AccountSummaryCardSnapshotTests` 通过。
- 风险：新快照文件位于 `nolonTests/Snapshots/...`，当前被 `.gitignore` 规则忽略，若需纳入版本控制需强制 add。

## Codex 账号卡右键菜单扩展（2026-03-16）
- 需求：在 `AccountSummaryCard` 的卡片右键菜单增加 `复制 auth.json` 与 `编辑 auth.json`。
- 实现：
  - 扩展 `AccountCardActionID`：新增 `.copyAuthJSON`、`.editAuthJSON`。
  - Accounts 页 Codex 快照卡菜单新增两项，并在 `NolonAccountsView` 动作分发中接入。
  - Usage 页 Codex 卡菜单同步新增两项，保持跨页面一致。
  - 新增 `NolonAccountsViewModel.copyCodexAccountAuthJSON` 与 `editCodexAccountAuthJSON`；`edit` 通过注入 `openURLAction`（默认 `NSWorkspace.open`）打开目标文件。
  - 新增 `ProviderUsageViewModel.editCodexAccountAuthJSON` 并接入 Usage 卡菜单。
- 测试（TDD）：
  - 先改 `NolonAccountsViewModelTests` 菜单断言与新增用例，再补实现。
  - 验证 `xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/NolonAccountsViewModelTests` 通过（17/17）。
- 风险：新文案 key（`codex.accounts.menu.copy_auth_json` / `codex.accounts.menu.edit_auth_json`）当前通过 `NSLocalizedString` 默认值兜底，若需要完整双语资源可再补入 `Localizable.xcstrings`。

## AccountSummaryCard 内部布局梳理（2026-03-16）
- 需求：整理 `AccountSummaryCard.swift` 中 `cardContent` 的卡片内部布局，提升 header/body/details/actions 的层次与节奏。
- 实现：
  - `AccountSummaryContentCard` 收敛为更明确的 header/body/supplementary section 结构，补齐 body/details/actions 的统一纵向节奏。
  - 行内容、详情内容、主操作内容统一落入轻量 panel 容器，使用 subtle fill + border 提高区块辨识度。
  - header 右侧 badge/meta 改为条件 trailing 栈，继续保留长标题可读宽度。
- 测试：
  - `xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/AccountSummaryCardSnapshotTests` 通过。
  - 新增完整内容快照场景 `grouped-card-content`，并更新长标题回归快照基线。
- 追加：`AccountSummaryCard` 的 body/detail/footer/action 行均接入 page marker，便于逐行复制定位路径。
- 调整：按反馈移除 `AccountSummaryCard` 内层 section panel 的卡片化样式（不再 card-in-card），保留逐行 page marker（row/action/footer）。
