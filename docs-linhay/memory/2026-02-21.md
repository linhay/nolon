# 2026-02-21

- CLI: 优化 `nolon skills search` 文本可读性，针对大结果集（>8）自动省略 summary，避免每条重复安装指令，改为顶部统一安装模板。
- CLI: 结果项改为条目间空行分隔，保留 `slug/version/updated`，并增加 `--limit 5` 缩小范围提示。
- 测试: 新增多结果场景测试 `runner omits summary in skills search text list when result is large`，并更新现有 search 文本渲染断言。
- CLI: `nolon skills list --show-fixes` 降噪，移除逐条修复命令，改为聚合命令输出（orphaned/broken/all）并补充明细查看指令。
- CLI: `workflow/mcp` 命令面完成与 `skills` 对齐，统一为 `list/sync/search/add/remove`，旧命令 `discover/install/uninstall` 返回迁移提示。
- CLI: 引入统一来源模型 `NolonResourceOrigin`，`skills/workflow/mcp list --verbose` 支持 `origin` 展示；历史未回填条目默认 `origin=unknown`。
- 测试: `NolonCoreCLIKitTests` 新增 legacy 命令迁移提示覆盖，并修正 `NOLON_HOME` 场景下 repositories 路径断言。
