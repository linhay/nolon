## 2026-03-25

- 关键修复：账号页新增“隐藏报错账号”筛选，并与“隐藏无额度账号”并行生效。
- 关键修复：账号分组标题计数改为“(当前可见 / 总数量)”，统计时忽略占位空卡片（`.empty`）。
- 实现决策：无额度判定优先取最长显式额度窗口（`windowMinutes` 最大），若无显式窗口则回退到 `primary/secondary/tertiary`，以兼容仅有 `usedPercent` 的快照数据。
- 回归：`xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/NolonAccountsViewModelTests` 通过（20 tests, 0 failures）。
- 纠偏：用户提到的“账号页”实际指 Provider Usage 的 Codex 账号页（原本已有“隐藏无额度账号”按钮），已在该页面补齐“隐藏报错账号”并支持与无额度筛选叠加。
- 关键修复：Codex 分组标题由绝对数量改为“(当前可见 / 总数量)”，总数按同分组未筛选结果计算。
- 回归：`xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/CodexAccountDisplaySectionsTests -only-testing:nolonTests/NolonAccountsViewModelTests` 通过（71 tests, 0 failures）。
- 关键修复：`隐藏报错账号` 已接入 `UsageMonitorProviderSettings` 持久化链路（Codable + UserDefaults store + ViewModel 初始化/写回），重建页面后状态可恢复。
- 回归：`xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/CodexUsageTabPresentationTests -only-testing:nolonTests/CodexAccountDisplaySectionsTests` 通过（112 tests, 0 failures）。
- 环境说明：`swift test --package-path libs/Providers --filter ProviderUsageMonitorSettingsTests` 受本机 swift toolchain 未选择影响，未能执行；相关覆盖由上面的 xcodebuild 测试路径验证。
- 问题定位：账号有效性校验（Claude）使用 `URLSession.shared.data(for:)` 直连，在系统层抛出 `invalid reuse after initialization failure` 时会直接透传到 UI。
- 关键修复：`ClaudeAccountManager.validateEffectiveness` 增加 invalid-reuse 检测与 `URLSessionConfiguration.ephemeral` 重试兜底，避免该类瞬时初始化故障直接影响账号状态。
- 测试补充：`ClaudeAccountManagerTests` 新增 direct/wrapped 两个 invalid-reuse 识别用例，覆盖裸错误与 `NSUnderlyingErrorKey` 包装错误。
- 回归：`cd libs/Providers && xcodebuild test -scheme Providers-Package -destination 'platform=macOS' -only-testing:ProvidersTests/ClaudeAccountManagerTests` 通过（6 tests, 0 failures）。

## ProviderUsageViewModel 业务域拆分（2026-03-25）
- 决策：采用 `ProviderUsageRootViewModel + ProviderUsageStateStore + 5 子 VM` 的桥接迁移方案，先保证行为等价与可回滚。
- 范围：覆盖三个消费者（`ProviderUsageView`、`NolonAccountsViewModel`、`CodexQuickSwitchMenuBarViewModel`）切换到 Root 入口。
- 实施：新增 Root/State/Sub-VM/Store；新增策略组件 `ProviderUsageErrorFormatter`、`ProviderUsageOutcomeFilter`、`CodexAccountSectionBuilder`、`CodexConfigDraftCodec`；`AccountCardModels` 与 `ProviderUsageView` 改用新 formatter/filter。
- 测试：新增 Root Store、Sub-VM wiring/parity、消费者集成测试骨架。
- 风险：当前 `ProviderUsageView` 仍通过 legacy bridge 访问大部分状态与动作，后续需继续按业务域替换为子 VM 直连。
- 偏好变化：用户明确要求“后续都用 xcodebuild”，已写入 `AGENTS.md` 的 COMMANDS 规则；后续默认不再使用 `swift test`。
- 去桥接化（外部消费者）：`NolonAccountsViewModel` 与 `CodexQuickSwitchMenuBarViewModel` 已移除对 `rootViewModel.legacyViewModel` 的直接读取，统一通过 `accountsViewModel` 显式接口（含 `settings` 注入与 `activateCodexAccount` 确认链路）。
- 回归：`xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/NolonAccountsViewModelTests -only-testing:nolonTests/NolonAccountsViewModelIntegrationTests -only-testing:nolonTests/CodexQuickSwitchMenuBarViewModelTests -only-testing:nolonTests/ProviderUsageSubViewModelWiringTests` 通过（相关子集全绿）。
- 去桥接化（ProviderUsageView 第二阶段）：`login / import / tokenTrend` 三块已改为通过 `rootViewModel` 子 VM（`loginFlowViewModel`、`importExportViewModel`、`tokenTrendViewModel`）访问，不再直接读取旧 VM 对应状态与动作。
- 实施细节：新增子 VM 显式接口（登录 sheet/URL 动作、导入流程全链路、趋势骨架判定），并在 `ProviderUsageView` 抽出 sheet binding 以避免 SwiftUI body type-check 超时。
- 回归：`xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/ProviderUsageSubViewModelWiringTests -only-testing:nolonTests/NolonAccountsViewModelIntegrationTests -only-testing:nolonTests/CodexQuickSwitchMenuBarViewModelTests` 通过。
- 去桥接化（ProviderUsageView 第三阶段-第一批）：账号/网关交互主路径已迁移到子 VM。
- 账号域：`codexOutcomeCard` 的激活判定、多选切换、刷新/登录动作、删除确认执行已改走 `accountsViewModel`。
- 网关域：卡片折叠、卡片列表、激活卡片、网关成员候选选择、卡片增删改、拖拽加入账号等主流程已改走 `gatewayCardsViewModel`（个别展示型字段仍留在旧 VM）。
- 回归：`xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/ProviderUsageSubViewModelWiringTests -only-testing:nolonTests/NolonAccountsViewModelIntegrationTests -only-testing:nolonTests/CodexQuickSwitchMenuBarViewModelTests` 通过。
- 去桥接化（最终）：移除 `ProviderUsageViewModelStore`、移除 `ProviderUsageRootViewModel.legacyViewModel` 对外门面、`ProviderUsageAccountsViewModel` 删除 `@dynamicMemberLookup` 并改为显式接口，`ProviderUsageStateStore` 改为仅暴露 `engine` 内部实现。
- 测试与门禁：`xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS'` 全量通过（530 tests, 1 skipped, 0 failures）；`./build.sh` 通过（build + unit tests）。
- 结构收敛：`ProviderUsageAccountsViewModel` 将平铺的 `codex*` 只读字段收敛为 `codex` 聚合（`CodexState`），统一访问面为 `accountsViewModel.codex.{accounts,accountOutcomes,accountSummaries,...}`。
- 迁移范围：`ProviderUsageView`、`CodexQuickSwitchMenuBarViewModel`、`ProviderUsageSubViewModelWiringTests`、`ProviderUsageAccountsViewModelParityTests` 已切换到 `codex` 聚合访问，旧平铺字段已移除。
- 回归：`xcodebuild build -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS'` 通过；`xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS'` 通过（本轮输出含 25 tests in 16 suites）。
- 进一步收敛：`ProviderUsageAccountsViewModel` 顶层已移除剩余 codex 前缀状态/动作，统一归入 `codex` 域对象（引用型 `CodexState`，支持双向绑定写入）。
- 接口迁移：`ProviderUsageView` 的 codex 交互已全量切换为 `viewModel.codex.*`（包括激活/删除确认、排序/筛选、配置编辑、网关批量、复制错误等）。
- 回归：`xcodebuild build -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS'` 与 `xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS'` 均通过（本轮输出 25 tests in 16 suites）。
- 最佳形态推进：`ProviderUsageAccountsViewModel` 新增 `claude` / `gemini` 域对象，形成 `accountsViewModel.{codex,claude,gemini}` 三域结构，provider-specific 状态与动作按域收敛。
- 接口迁移：`ProviderUsageView` 已将 Claude/Gemini 读取与操作改为 `viewModel.claude.*`、`viewModel.gemini.*`，并移除 Accounts 顶层对应旧接口。
- 回归：`xcodebuild build -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS'` 与 `xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS'` 均通过（本轮输出 25 tests in 16 suites）。
- `ProviderUsageViewModel.swift` 瘦身启动：将 Claude/Gemini 账号管理与 Gemini 导入确认相关实现迁出到 `ProviderUsageViewModel+ClaudeGemini.swift` 扩展文件，主文件删除对应实现块。
- 迁移兼容：为支持跨文件扩展访问，`claudeAccountManager` 与 `geminiAuthStore` 由 `private` 调整为模块内可见；`startGeminiOAuthLoginFlow` 对扩展可见。
- 回归：`xcodebuild build -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS'` 与 `xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS'` 均通过（25 tests in 16 suites）。
- 继续瘦身 `ProviderUsageViewModel.swift`：
  - 新增 `ProviderUsageViewModel+CodexPresentation.swift`，迁出 Codex 展示策略/分组排序/配置文案与 key-value 解析等纯策略函数。
  - 新增 `ProviderUsageViewModel+CodexConfigImportHelpers.swift`，迁出 Codex HTTP usage query 草稿编解码与导入候选连接测试辅助链路。
  - `ProviderUsageViewModel+ClaudeGemini.swift` 继续承接 Gemini 登录候选检查与 OAuth 登录流程（含 run loop）。
- 可见性调整（为跨文件扩展迁移）：`logger`、`cliLoginTask`、`cliLoginSessionId`、`cliLoginTimeoutSeconds`、`geminiLoginHandle`、`finalizeCLILoginSessionIfNeeded` 及若干工具方法改为扩展可访问。
- 兼容修复：迁移后恢复 `{{baseURL}}/usage` 模板 URL 的合法性校验，避免误伤 HTTP usage draft 测试。
- 回归：`xcodebuild build -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS'` 与 `xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS'` 均通过。
- 体量变化：`ProviderUsageViewModel.swift` 行数由 4672 -> 3741。
- 目录重构：按业务域重排 `nolon/Skills`，采用 `Domain + Shared` 结构。
- 新结构：
  - `nolon/Skills/Domain/App`（Core/Infrastructure/Models）
  - `nolon/Skills/Domain/Accounts`
  - `nolon/Skills/Domain/Providers`（Views/Usage/Sidebar/Models）
  - `nolon/Skills/Domain/Resources`
  - `nolon/Skills/Domain/SkillsDetail`
  - `nolon/Skills/Domain/Onboarding`
  - `nolon/Skills/Domain/Plugins`
  - `nolon/Skills/Domain/Settings`
  - `nolon/Skills/Domain/MenuBar`
  - `nolon/Skills/Shared`（UI/Adapters）
- 实施方式：全部使用 `git mv`，保留文件历史；同时清理了 `Skills` 下 `.DS_Store`。
- 验证：`xcodebuild build -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS'` 与 `xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS'` 均通过。

## Provider Usage 最终收口（去桥接化）
- 将 `ProviderUsageViewModel` 全量更名为 `ProviderUsageEngine`，移除 Usage 域内旧 VM 命名残留。
- 文件收口完成：
  - `ProviderUsageViewModel.swift` -> `ProviderUsageEngine.swift`
  - `ProviderUsageViewModel+ClaudeGemini.swift` -> `ProviderUsageEngine+ClaudeGemini.swift`
  - `ProviderUsageViewModel+CodexConfigImportHelpers.swift` -> `ProviderUsageEngine+CodexConfigImportHelpers.swift`
  - `ProviderUsageViewModel+CodexPresentation.swift` -> `ProviderUsageEngine+CodexPresentation.swift`
- Root/State/SubVM 体系继续保留，`ProviderUsageEngine` 作为底层执行内核；上层调用面不再出现 `ProviderUsageViewModel`。
- 验证：`xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS'` 通过。

## Provider Usage 目录按组件整理
- 将 `nolon/Skills/Domain/Providers/Usage` 重组为组件目录：
  - `Codex/`：Codex 纯组件与数据构建器（section builder / draft codec / drop payload）
  - `Engine/`：ProviderUsageEngine 及按职责拆分的 extension
  - `Root/`：Root VM / Store / StateStore / SubViewModels / SupportTypes
  - `Policies/`：error formatter / outcome filter / skeleton policy
  - `Views/Common/`：通用 usage 视图与 section/sheet
  - `Views/Codex/`：Codex 专属 sheet
- 本次仅做文件组织，不改业务行为；`xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS'` 全绿。

## ProviderUsageView 最终形态推进：卡片映射下沉到 VM
- 新增 `ProviderUsageAccountsViewModel+CardModels.swift`：
  - `ClaudeState.makeCardModel(account:)`
  - `GeminiState.makeCardModel(account:providerName:liveOutcome:isLoading:)`
  - `CodexState.makeUsageCardModel(outcome:hasActiveGatewayCardSelection:isRunningCLILogin:)`
  - `CodexState.handleUsageCardAction(_:model:)`
- `ProviderUsageView` 不再直接调用 `AccountRecordBuilder` / `AccountCardViewDataMapper`，改为消费 VM 产出的 `CardModel`。
- codex 卡片菜单/动作分发从 View 下沉到 `accountsVM.codex`，View 仅负责渲染和薄交互转发。
- 清理了 `ProviderUsageView` 内已废弃的 codex 映射 helper（旧映射/动作函数）。
- 验证：`xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS'` 通过（530 tests, 0 failures）。

## ProviderUsageView 最终收口（视图组件化拆分）
- 将原 `ProviderUsageView+ProviderSections.swift` 拆分为 3 个组件文件：
  - `ProviderUsageView+ClaudeGeminiSection.swift`
  - `ProviderUsageView+CodexGatewaySection.swift`
  - `ProviderUsageView+CodexCompactList.swift`
- `ProviderUsageView.swift` 保持壳层路由 + header/common wiring；provider-specific 大块内容迁移到组件扩展文件。
- 处理跨文件访问控制：对跨文件调用点采用 `internal` 可见性，保留局部 helper 为 `private`。
- 回归：`xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS'` 全绿（530 tests, 0 failures）。

## ProviderUsage 统一账号渲染入口（去硬分支）
- 将 `ProviderUsageView` 主内容路由收口为单入口 `usageContent`，去掉入口层 `codexContent/nonCodexContent` 二选一分支。
- 结构调整为“统一滚动容器 + 能力型 section 组合”：
  - codex 能力 section：管理卡片、网关卡片、codex 账号 section；
  - 非 codex 账号 section：统一卡片模型渲染（Claude/Gemini）；
  - 趋势 section：按 provider 能力开关决定是否展示。
- 目标是让 provider 差异尽量下沉到 section/VM，主 View 不再按 provider 写大块分支逻辑，便于后续继续统一。
- 回归：`xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS'` 通过。

## ProviderUsage 最终收敛（capabilities + section state + 卡片动作闭包）
- 新增 `ProviderUsageCapabilities`，集中 provider 能力判定（是否 codex 家族、是否展示趋势、是否 unified skeleton、是否导入 callout）。
- `ProviderUsageUnifiedAccountCardModel` 增加 `onTap/onAction` 异步闭包，View 不再基于 provider switch 分发账号卡片动作。
- `Claude/Gemini` 卡片中间模型收敛为统一 card data 构造函数，减少同构类型。
- `nonCodexAccountsSection` 与 `codexAccountsSection` 都改为状态机渲染（loading/empty/content），并抽取 codex section header 组件函数，减少重复分支。
- `ProviderUsageView` 中 codex 入口判断改为基于 `capabilities.isCodexFamily`。
- 全量回归：`xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS'` 通过（530 tests，1 skipped，0 failures）。

## ProviderUsage 最终收口再前进一步（Header/Menu/SectionState 合并）
- Header 非 codex 动作改为策略化数组 `nonCodexHeaderActions` + `nonCodexHeaderActionButton(_:)`，去掉主视图里的 if/else 动作分支。
- `actionsMenu` 拆为 `codexActionsMenuContent` 与 `nonCodexActionsMenuContent`，保留系统与危险操作区块，降低单函数复杂度。
- 账号 section state 合并为泛型状态机：
  - `ProviderUsageAccountsSectionState<Content>`（`loading/empty/content`）
  - `ProviderUsageSectionEmptyState`（统一 empty 数据）
  - Claude/Gemini 与 Codex 两侧复用同一状态表达，不再维护两套并行枚举。
- 修复泛型枚举参数编译问题：`.content` 改为 tuple 传递（`((cards, outcomes))`）。
- 验证：
  - 定向测试通过：
    - `ProviderUsageUnifiedAccountsPipelineTests`
    - `ProviderUsageAccountsViewModelParityTests`
    - `ProviderUsageSubViewModelWiringTests`
    - `NolonAccountsViewModelTests`
    - `CodexQuickSwitchMenuBarViewModelTests`
  - 全量 `xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS'` 在当前工作区失败，原因为 `ResourceCenterTabViewModel` 不在作用域（`ResourceCenterTabView.swift`），属于 ProviderUsage 之外的在制改动基线问题。
