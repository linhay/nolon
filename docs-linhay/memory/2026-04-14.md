# 2026-04-14

## Micro-Sync
- 新增 UI/UX debate 文档：`docs-linhay/debate/20260414/codex-sessions/20260414-codex-sessions-tab-ui-ux-v01.md`
- 主题：`codex-sessions` tab 的信息语义、动作可发现性、列表扫描效率。
- 关键结论：
  - 这页当前更像“实现细节可见”，还不够像“迁移工作台”。
  - `P0` 优先级集中在四件事：
    - provider display name 优先，raw id 降级
    - 行级 action 统一，并补齐 accessibility
    - section 默认能力 banner 降噪，只保留例外状态强提示
    - 表格列优先级重排，`rolloutPath` 下沉为次级信息
  - `P1` 再讨论：
    - overview metrics 重排
    - `Group By` 提升为更持久可达的导航控件
    - section 首屏保底配额从 1 提升到更可感知的值
- 开放问题：
  - `Sessions` 页最终定位是迁移工作台还是技术诊断页
  - provider display name 的稳定数据源
  - `DB row count` / `rolloutPath` 是否默认降级隐藏

## Micro-Sync 2
- 补充一轮“共识前置”结论到 `codex-sessions` UI/UX debate。
- 关键调整：
  - 页面定位从“纯迁移工作台”修正为 `迁移优先，诊断可达`
  - provider 呈现从“display name 优先 + tooltip raw id”修正为“display name 主显、raw id 次显、同屏可校验”
  - `rolloutPath` / `DB row count` 改为“降级但保持一跳可见”，不主张直接完全隐藏
  - section banner 原则收敛为“默认降噪，例外高亮”
  - section 首屏保底先定原则，不先写死为 `3`
- 执行前建议先锁定的 5 个共识：
  - 页面定位
  - provider 呈现原则
  - section 语义原则
  - 行级动作原则
  - 第一轮只做信息层级和动作可发现性

## Micro-Sync 3
- 基于新发言继续补充 UI/UX debate。
- 新收敛点：
  - section accent color bug 升级为明确的 `P0`，不再视为实现细节
  - confirmation dialog 升级为 display name + raw id 的双标签校验点
  - `rolloutPath` / `DB row count` 的“一跳可见”不采用纯 contextMenu 方案，倾向显式 `Details/More` 入口，contextMenu 仅作补充
- 新调整：
  - section 保底条数仍先停留在“更可感知的值”原则，不把 `Live/Archived` 混合保底并入第一轮
  - 增加一条统一的信息梯度：
    - 第一层默认浏览与迁移判断
    - 第二层显式次级入口查看诊断信息
    - 第三层 confirmation dialog 做最终校验

## Micro-Sync 4
- 再次调整 `Actions` 列设计口径。
- 新观点：
  - `Actions` 列存在明确的动作预算，不能同时塞进过多常驻控件
  - 因此把先前的 `Finder 常驻` 修正为 `主动作常驻 + 显式次级入口常驻`
  - 第一轮更倾向 `Move/Move Session + More` 的两段式结构
- 对应收口：
  - `Show in Finder` 改为稳定落在可见 `More` 入口中，而不是单独常驻控件
  - `rolloutPath`、`Copy Path`、`DB rows` 也一并收敛到 `More`
  - `contextMenu` 继续作为补充，不做唯一入口

## Micro-Sync 5
- 基于最新发言继续补充 debate。
- 新收敛点：
  - `信息梯度` 正式升级为执行前第六条共识
  - `More` 入口收敛为紧凑图标菜单，具体 symbol 留给截图验证
  - `More` 内部顺序先写死为：`Show in Finder`、`Copy Path`、分隔线、`rolloutPath`、`DB rows`
  - `Actions` 列不再受旧 `110pt` 固定宽度假设约束
- 仍保留的小开放点：
  - provider display name 的短期 formatter 边界
  - section floor 的固定值或自适应策略

## Micro-Sync 6
- 基于 Round 8 再次收口。
- 新调整：
  - provider formatter 从开放问题降级为第一轮实现规则：白名单格式化，未知 ID 原样展示
  - `More` 菜单顺序视为已决，不再保留为开放问题
  - Actions 列宽的 `40/130/160` 只作为截图验证起始基线，不写死成正式规格
  - 第一轮实现可压缩为两段：语义层、视图层
- 当前仅剩的核心开放点：
  - section floor 采用固定值还是自适应

## Micro-Sync 7
- 新增 debate 文档：`docs-linhay/debate/20260414/codex-usage/20260414-intraday-usage-curve-v01.md`
- 主题：如何为现有 Usage 页添加“日内使用曲线”。
- 关键结论：
  - 日内曲线应作为现有“历史 Token 消耗”模块中的新粒度，不做独立 section。
  - 不直接污染现有 `ProviderTokenTrendSnapshot`，而是新增独立 intraday 模型。
  - Gemini 先做，Codex 后做；Codex 当前只有日级聚合链路，需先扩 scanner/fetcher。
  - 第一版只收敛为 `Today + 1h bucket + 本地时区`，且不新增第二排 intraday summary。
- 后续行动：
  - 先补 feature/spec
  - 再做 Gemini intraday
  - Codex 单独立项评估 hour bucket 方案

## Micro-Sync 8
- 根据补充信息修正 intraday debate 对 Codex 的判断。
- 新结论：
  - Codex 原始 session / rollout 事件本身已有 `timestamp`，`CodexSessionEventParser` 和 `CostUsageScanner` 当前也都能读到。
  - 真正限制不在“缺原始时间戳”，而在 scanner/cache/fetcher 这条产品链路最终只保留了 `dayKey` 聚合结果。
  - 因此 Codex intraday 属于“现有聚合链路增量扩展”，不是从零补数据源。
- 对应调整：
  - debate 文档把“Codex 当前只有日级聚合产物”的表述细化为“原始事件有 timestamp，但中间层已压平成 dayKey”
  - Codex Phase 2 的重点收敛为 hour bucket 的 cache/fetcher 设计，而不是原始数据可得性

## Micro-Sync 9
- 用户要求把 intraday 设计方案完整落到 debate 文档中，已补充“设计方案（定稿）”大段。
- 设计定稿内容包含：
  - 目标与非目标
  - 最终 UI 交互
  - intraday 独立状态模型
  - provider intraday 协议与 registry 方案
  - Gemini 实现方案
  - Codex scanner/cache/fetcher 扩展方案
  - engine/page 改造点
  - 调试文案、迁移计划与测试清单
- 当前推荐决策：
  - 同一趋势 section 内做 `历史 / Today` 模式切换
  - Gemini 先做
  - Codex 通过扩 scanner/cache/fetcher 支持 `hourKey`

## Micro-Sync 10
- 按用户要求开了 3 个 subagent，从交互、Codex 架构、性能测试三个角度反复复核 intraday 方案。
- 多 agent 复核后的共同结论：
  - 主方案没有被推翻，整体方向成立。
  - 但需要把 4 个边界写死：
    - `Today` 是模式，不是 range
    - intraday 保持独立状态模型
    - 第一版不做准实时 watcher / 高频刷新
    - Codex 只能扩 scanner/cache/fetcher，不能临时另起数据链路
- debate 文档已新增多 agent 复核轮次，并把这些约束写入最终推荐决策。

## Micro-Sync 11
- 按用户要求继续让外部 CLI 参与讨论。
- 结果：
  - `Gemini CLI` 成功给出外部评审意见，补充了 3 条工程边界：
    - Codex 小时级 cache 需要有限保留窗口
    - intraday 要显式处理午夜 rollover 与时区变更
    - 零桶补齐必须在 service/fetcher 层完成，不能下放给 UI
  - `Claude Code` 在本机 headless/plan 调用下长时间无有效输出，本轮未形成可用结论，因此没有纳入 debate 实质观点。
- 已把 `Gemini CLI` 的新增意见并入 debate 文档与最终决策。

## Micro-Sync 12
- 对 `docs-linhay/debate/20260414/codex-usage/20260414-intraday-usage-curve-v01.md` 再做了两轮收敛，目标从“继续辩论”切换为“修订后是否直接接受”。
- 本轮新增的修订版共识包把以下边界写死：
  - `Today` 显隐只能由 intraday capability 驱动
  - provider 不支持 intraday、能力变更或当天无数据时自动回退到 `历史`
  - `Today` 的日边界和 `sum(hours in today) == day(today)` 校验统一以同一份 snapshot 的 `timezoneIdentifier` 为准
  - DST / 时区变化日允许 `23 / 25` 个小时桶
  - 午夜 rollover / 时区变化时，当前 intraday snapshot 与 Codex 小时级 cache 同时失效
  - service/fetcher 负责补零失败 / 不完整桶 / fetch 失败的标记与日志，UI 不补洞
  - `Today` 视图必须展示 freshness 信息，避免被误解为准实时监控
- 最终确认结果：
  - 3 个 subagent 全部从“接受/有条件接受”收敛到“接受”
  - `Claude Code` 使用 `--bare -p` 轻量模式后给出“接受”
  - `Gemini CLI` 在 `gemini-3.1-pro-preview` 出现 `429 RESOURCE_EXHAUSTED` 后，切换 `gemini-2.5-flash` 给出“接受”
  - `Codex CLI` 最终确认也给出“接受”
- 当前状态：
  - intraday usage curve 的产品方案、交互方案、模型边界、刷新边界与测试边界已达成共识
  - 仅剩 `Codex` hour bucket 落入现有 cache 还是拆 `-intraday` 文件这一项 Phase 2 实现细节，已不阻塞方案结论

## Micro-Sync 13
- 用户新增了一个更强的交互场景：
  - 当前主图保持日级
  - 点击某一个具体日期后，再展开该天的分钟级曲线
  - 默认 `30min`
  - 可切换 `15min / 60min`
- 这使原先的 `Today-only + 1h bucket` 方案不再是最新共识，已在 debate 文档中被新版覆盖。
- 新版共识调整为：
  - intraday 入口从 `Today` 模式切换，改成“单日钻取”
  - `Today` 退化为日级图表中的一天，而不是独立模式
  - intraday bucket 从仅 `1h` 升级为 `15min / 30min / 60min`
  - 默认 bucket 为 `30min`
  - provider capability 从“支持 Today intraday”改为“支持日级图表的 intraday drilldown”
  - Codex cache 基准粒度不再适合只存 `hours`，需要改为 `15min` base buckets，再导出 `30/60min`
- 新增一个关键工程判断：
  - Gemini Phase 1 可先做“按选中日期即时聚合”，不必先把所有历史分钟桶长期缓存化
  - Codex 仍必须走 scanner/cache/fetcher 正式链路，不能在 App 层临时扫单天数据

## Micro-Sync 14
- 针对“点击某一天后进入 15/30/60 分钟单日钻取”的新场景，又补了一轮 agent 共识确认。
- 第一轮结果：
  - 3 个 subagent 都是“有条件接受”
  - `Gemini CLI`、`Codex CLI` 直接接受
  - `Claude Code` 也是“有条件接受”
- 所有条件项最终收敛为同一组边界：
  - intraday snapshot 必须补齐 `actualBucketCount`、`rangeStart`、`rangeEnd` 等 metadata
  - `dayKey` 必须明确是 `timezoneIdentifier` 下的本地自然日
  - cache 的唯一事实源必须是 `15min` base buckets；`30/60min` 只做派生展示
  - UI 需要显式展示“粒度 + 实际桶数”、不支持 drilldown 的禁用/原因提示、历史日的“静态快照 / 手动刷新”语义
  - Today 与历史日的失效策略需要明确分开
- 第二轮结果：
  - 3 个 subagent 全部转为“接受”
  - `Claude Code`、`Gemini CLI`、`Codex CLI` 也全部给出“接受”
- 结论：
  - 新版“单日钻取”方案已经完成 agent 共识，不再只是我个人修订
  - 当前 debate 文档中的最新正式方案应以“第 12 轮 / 第 13 轮 / 最新共识状态（经 agent 复核确认）”为准

## Micro-Sync 16
- 继续把单日钻取剩余的 3 个开放问题讨论到关闭：
  - `A`：内联展开 vs 详情态切换
  - `B`：钻取打开后保留主图交互 vs 冻结主图交互
  - `C`：Gemini Phase 1 历史日是否增加轻量文件级 cache
- 第一轮票型：
  - `A` 有分歧，但 `A1`（内联展开）票数略高
  - `B2`（冻结主图交互）被 3 个 subagent 一致否掉
  - `C1`（不做文件级 cache）明显占优
- 最终收口包定为：
  - `A1`：内联展开
  - `B1`：保留主图 range/hover，但锁定 `selected day`
  - `C1`：Gemini Phase 1 不做文件级 cache，只做按选中日即时聚合
- 最终确认结果：
  - 3 个 subagent 全部接受
  - `Claude Code`、`Gemini CLI`、`Codex CLI` 全部接受
- 当前状态：
  - 单日钻取相关的开放问题已经全部关闭
  - 最新正式方案应以 debate 文档中的“第 14 轮 / 第 15 轮 / 最新共识状态（全部歧义已关闭）”为准

## Micro-Sync 17
- 根据已关闭歧义的会议结论，新增了两份进入实现前的正式文档：
  - `docs-linhay/features/provider-usage-intraday-drilldown-2026-04-14.md`
  - `docs-linhay/plans/2026-04-14-provider-usage-intraday-drilldown-preflight.md`
- 这两份文档分别承担：
  - `features`：锁定最终需求口径、BDD、范围/非目标
  - `plans`：整理会议总结、分阶段执行顺序、代码落点、测试门禁、风险与防线
- 当前进入代码阶段前的基线已经明确：
  - 主图保持日级
  - 点击某天进入内联单日钻取
  - 默认 `30min`，支持 `15/30/60min`
  - `15min` 是唯一事实缓存
  - Gemini Phase 1 不做文件级 cache

## Micro-Sync 10
- 完成 `codex-sessions` tab UI/UX 完整执行计划，落地范围覆盖：
  - provider display name 主显 / raw id 次显
  - `Move + More` 两段式动作与显式诊断入口
  - section 三态语义、overview 决策指标、sticky grouping、section floor=`2`
  - `Localizable.xcstrings` 新增 key 补齐
- 关键修正：
  - mixed-provider 且仍有可编辑 session 的 section 不再误判为只读，而是归类为 `singleSessionOnly`
  - builder 测试不再硬编码英文文案，改为跟随真实本地化取值
  - 清理了本轮 builder / view model 上的 Swift 6.2 actor-isolation 警告
- 验证：
  - 2026-04-14 15:19:43（Asia/Shanghai）定向 `xcodebuild test` 通过
  - 覆盖 `CodexSessionsSectionDataBuilderTests`、`CodexSessionsTabViewModelTests`、`CodexSessionsCardSnapshotTests`
  - autoresearch metric：`failing_codex_sessions_uiux_regressions` 从 `18` 降到 `0`

## Micro-Sync 14
- 新增 debate 文档：`docs-linhay/debate/20260414/resource-center/20260414-skill-install-github-scale-v01.md`
- 主题：GitHub/local repo skill（以 `scale` 为例）安装卡在“安装中”后失败的根因与修复。
- 关键结论：
  - 安装主根因在 `SkillInstaller`，不是仓库扫描链路。
  - 当 provider 的 `skills` 根目录本身是指向 `~/.nolon/skills` 的符号链接时，App 端原安装器缺少 linked-root 保护，会把刚复制进去的实体 skill 目录错误地再处理成 skill 级符号链接。
  - Resource Center 的“安装中”状态机也有缺陷：安装失败不会即时回传 UI，只会在 45 秒 timeout 后才清掉 pending，导致用户误以为是“超时”。
- 已完成修复：

## Micro-Sync 15
- 完成 Codex 首次 preflight 双读迁移的最小实现，主题文档：
  - `docs-linhay/debate/20260414/codex-accounts/20260414-codex-auth-migration-dual-source-v01.md`
- 本轮最终落地的决策：
  - `auth.json + config.toml` 双读保留
  - `config.toml` 在迁移阶段继续保持只读证据源
  - 没有 provenance 时，`apikey` 账号不得自动升级为 `relayProfile`
  - 只有“现有受管 snapshot + active-provider-config managed state + 当前 detached auth 可匹配”同时成立时，才允许补 `nolon.relay`
- 代码落点：
  - `libs/Providers/Sources/ProviderUsage/CodexAuthManager+ProviderSync.swift`
  - `libs/Providers/Sources/ProviderUsage/CodexAuthManager+ActiveProviderConfig.swift`
  - `libs/Providers/Sources/ProviderUsage/CodexAuthManager.swift`
- 回归验证：
  - 无 provenance 仍保持 `officialAPIKey`
  - 有 managed provenance 才升级 `relayProfile`
  - dual-read preflight 后 `config.toml` 字节级不变
  - 既有 `relay -> oauth` patch/restore guard 继续通过
  - `SkillInstaller` 新增统一 `installSkillContent(...)`，对齐 `ProviderSkillMaintenanceService` 的 linked-root 保护。
  - `ResourceCatalogGridView` / `ResourceCenterView` / `MainSplitView` 改成 `async throws` 安装回调，失败时即时移除 pending 并显示真实错误。
  - 新增测试：
    - `nolonTests/SkillInstallerTests.swift`
    - `nolonTests/ResourceCatalogGridViewInstallStateTests.swift`
- 验证：
  - 2026-04-14 16:14（Asia/Shanghai）定向 `xcodebuild test` 通过。
  - 覆盖：
    - `nolonTests/SkillInstallerTests`
    - `nolonTests/ResourceCatalogGridViewInstallStateTests`

## Micro-Sync 15
- 按用户要求补做了一轮并行多 agent 复核，并让外部 CLI 参与讨论。
- 参与方：
  - 内部 agent：`Singer`、`Helmholtz`
  - 外部 CLI：`Gemini CLI`
  - `Claude Code` 已尝试调用，但在本轮可接受等待时间内没有返回有效审查结果，因此未纳入实质结论
- 多方共识：
  - 当前修复方向成立，没有新的 blocking finding。
  - 这次修改可以解决 `scale` 在 linked-root provider 布局下的安装失败，以及主列表“长时间安装中再报错”的问题。
- 新增风险结论：
  - Resource Center detail sheet 的 workflow / MCP 安装入口仍可能存在 `try? await` 吞错路径，后续值得补齐。
  - linked-root 判断目前只覆盖单层符号链接。
  - 实体化复制在超大资源场景下可能放大安装耗时与 timeout 风险。

## Micro-Sync 16
- 已继续推进讨论直到分歧关闭，并顺手补掉了唯一还未收敛的代码缺口：
  - 将 workflow detail sheet、MCP detail sheet、remote skill detail window 的安装入口统一改为走 `begin*Install(...)`
  - 移除了这些入口原有的 `Task { try? await ... }` 吞错路径
- 最终参与方共识：
  - 内部 agent `Singer`、`Helmholtz`
  - 外部 `Gemini CLI`
  - 外部 `Claude Code`（极简 prompt 最终返回）
- 最终结论：
  - 不再存在 blocking 分歧
  - 这次修复已能解决 `scale` 安装失败与“长时间安装中再报错”的主问题
- 剩余仅有 non-blocking 风险：
  - 多级 symlink 解析
  - 超大资源仓库的实体化复制性能/空间压力
  - 极慢安装场景下的 timeout hint 保守误报

## Micro-Sync 16
- `codex-sessions` tab debate 出现新的默认浏览口径：
  - 默认 grouping 改为 `project`
  - 组内按时间倒序
  - 每组默认展示 `5` 条，超出后 `展开 / 收起`
  - 主表格字段改为 `名称 / id / 时间 / provider / 用量 / 菜单`
  - tab 切换采用“保留旧数据 + 后台增量刷新”
- 当前判断：
  - 这不是局部 UI 微调，而是在重定整个 tab 的默认浏览模型。
  - 若采纳，需要重新评估已落地的 `provider` 默认分组、全局分页、section floor=`2`` 与 overview 信息架构。
  - 已把该口径写入 `docs-linhay/debate/20260414/codex-sessions/20260414-codex-sessions-tab-ui-ux-v01.md`，作为新的主方案候选。

## Micro-Sync 17
- 新增 debate 文档：
  - `docs-linhay/debate/20260414/codex-accounts/20260414-codex-active-auth-rematerialization-v01.md`
- 主题：
  - 为什么编辑 active Codex configured account 后，`config.toml` 更新了，但 provider `auth.json` 没同步。
- 多方共识：
  - 当前 source of truth 是 SQLite snapshot。
  - provider `auth.json` 只是激活时物化出的运行时视图。
  - 现有编辑链路只会做：
    - `updateConfiguredAccount(...)`
    - `refreshActiveProviderConfigIfNeeded(...)`
  - 但后者此前只同步 `config.toml`，不会重新 materialize active `auth.json`。
  - 因此这属于 active configured account 编辑场景下的实现缺口，不是单纯观察口径差异。
- 已完成修复：
  - 在 `refreshActiveProviderConfigIfNeeded(...)` 中，active account 命中后会先 `syncActiveProviderConfig(...)`，再 `activateAccount(account, for: provider)`，确保 provider `auth.json` 与最新 SQLite payload 同步。
- 回归验证：
  - 新增测试覆盖“编辑 active configured account 后 provider `auth.json` 被重新物化”。
  - 同时确认：
    - relay config rewrite 仍通过
    - relay -> oauth patch/restore guard 仍通过

## Micro-Sync 17
- 按用户要求继续让多方参与复核：
  - 内部 subagents：`Newton`、`Volta`、`Bohr`
  - 外部 CLI：`Claude Code`、`Gemini CLI`
- 多方稳定共识：
  - 新方案方向成立，但它是“新主方案”，不是旧 migration 方案上的顺滑续写。
  - 默认 `project` 分组只有在页面正式改为 `project 会话浏览优先` 时才合理。
  - 当前全局分页模型与“每组默认 5 条 + 展开/收起”不兼容，需要重写为 group 级 visible budget。
  - `usage` 列需要新的数据契约与行级异步缓存，不能只改表头。
  - tab 切换“保留旧数据 + 后台增量刷新”已有雏形，但仍需显式 cache / refresh contract 才能稳定实现。
- 已把这轮多方复核结果写回同一份 debate 文档，作为 Round 11。

## Micro-Sync 18
- 对 `codex-sessions` 新方案的最后残余分歧做了裁决，分歧已关闭。
- 裁决结果：
  - 在产品层面，最新用户口径正式覆盖旧定位；
  - `codex-sessions` tab 新主方案改写为 `project 会话浏览优先，迁移与诊断可达`
  - 在工程层面，仍要求先补新的 feature / plan 与状态约束，再进入第二轮实现
- 同步退休的旧结论：
  - `provider` 默认 grouping
  - 全局 `visibleSessionLimit + section floor=2 + Load More`
  - migration-first 的旧默认浏览模型
- debate 文档已新增 Round 12，作为主方案争议的正式关闭点。

## Micro-Sync 19
- 为 `codex-sessions` 第二轮执行准备正式文档：
  - 在 `docs-linhay/features/codex-sessions-tab-2026-04-10.md` 追加 `2026-04-14：Project-First 浏览模型重定`
  - 新增执行计划 `docs-linhay/plans/2026-04-14-codex-sessions-project-first-exec.md`
- 文档冻结内容：
  - 正式定位：`project 会话浏览优先，迁移与诊断可达`
  - 默认 `project` 分组
  - 每组默认 `5` 条 + `展开 / 收起`
  - 主列固定为 `名称 / id / 时间 / provider / 用量 / 菜单`
  - tab 采用“旧数据先显示 + 后台增量刷新”
- 下一步已明确：
  - 第二轮实现应按新 plan 走 BDD/TDD，不再沿用第一轮 provider-first 默认浏览模型。

## Micro-Sync 18
- 已完成这轮 `scale` GitHub skill 安装问题的最终验证收口。
- 最新定向测试：
  - `xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/SkillInstallerTests -only-testing:nolonTests/ResourceCatalogGridViewInstallStateTests`
  - 执行时间：2026-04-14 17:36（Asia/Shanghai）
  - 结果：`10` 个测试全部通过，`0` 失败
- 本轮确认点：
  - `SkillInstaller` / `ProviderSkillMaintenanceService` 的 linked-root 保护已升级为真实路径解析，新增“父目录 symlink”场景测试通过
  - `ResourceCatalogGridView` 安装失败状态机测试继续通过
  - 此前多 agent 与外部 CLI 的“无 blocking 分歧”结论保持不变
- 已把最终验证闭环写回：
  - `docs-linhay/debate/20260414/resource-center/20260414-skill-install-github-scale-v01.md`

## Micro-Sync 19
- 已补齐这轮 `scale` 安装修复的文档落位，避免 debate 收敛后缺少正式执行说明。
- 本次新增/更新：
  - 更新 feature：`docs-linhay/features/skill-install-nonstandard-symlink-layout-2026-03-03.md`
  - 新增执行说明：`docs-linhay/dev/resource-center-skill-install-github-scale-fix-2026-04-14.md`
  - 新增执行计划：`docs-linhay/plans/2026-04-14-resource-center-skill-install-github-scale-fix.md`
- 文档口径：
  - feature 已补到和最终实现一致，纳入“父目录 symlink 解析”与“安装失败即时回显”两条规则
  - dev 文档沉淀实现摘要、关键文件、BDD 映射、验证命令
  - plan 文档聚焦执行步骤、完成定义、手工 smoke 与上线收口

## Micro-Sync 20
- `provider-usage intraday drilldown` 的 foreground autoresearch 已继续推进，并完成两轮 kept iteration：
  - Iteration 1：提交 `c75cedb`
    - 新增 `ProviderIntradayBucket / ProviderIntradayUsageSnapshot`
    - `ProviderUsageEngine` 增加 drilldown skeleton state
    - `NolonUIFoundation` 增加 drilldown section data
  - Iteration 2：提交 `e841035`
    - 新增 `GeminiIntradayUsageService`
    - Codex `CostUsageScanner / CostUsageCache` 增加 `quarterHours` 事实桶入口
    - 补齐 Gemini intraday 与 Codex quarter-hour 聚合测试
- 当前 autoresearch run 状态：
  - run tag：`provider-usage-intraday-drilldown`
  - retained metric：`remaining_intraday_drilldown_gaps = 0`
  - keeps：`2`
  - guard：`xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/ProviderUsageRootViewModelStoreTests` 通过
- 已完成的验证：
  - `swift test --package-path libs/Providers --filter 'GeminiIntradayUsageServiceTests|CostUsageStoragePathTests/costUsageScannerAggregatesQuarterHours|ProviderIntradayUsageModelsTests'`
  - `xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/ProviderUsageRootViewModelStoreTests`
- 当前结论：
  - Phase 0 的通用模型 / engine skeleton 已落地
  - Phase 1 的 Gemini 单日聚合服务已具备基础实现
  - Phase 2 的 Codex `15min` 事实桶入口已进入 scanner/cache
  - 下一步重点应切到 App/UI 侧的 drilldown inline wiring，而不是继续补数据层 marker

## Micro-Sync 21
- 启动了一轮新的 `codex-autoresearch` foreground 监督，用于收口 `scale` GitHub skill 安装修复。
- 用户明确要求：
  - 不做 live smoke
  - 使用 foreground 方式监督
- 本轮监督范围：
  - `SkillInstaller` / `ProviderSkillMaintenanceService` 的 linked-root 与 parent-linked-root 保护
  - `ResourceCatalogGridView` 安装失败即时回显状态机
  - 对应测试、执行说明、执行计划、debate 收口文档
- 前台 run 基线结论：
  - `remaining_supervision_gaps = 0`
  - 在“跳过 live smoke”的前提下，本轮监督目标在启动时已经满足
  - 不需要再追加产品代码或执行文档改动，只保留这条监督记录
- 当前 run 标识：
  - run tag：`resource-center-scale-install-supervision`
  - baseline commit：`e841035`
  - guard：`xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/SkillInstallerTests -only-testing:nolonTests/ResourceCatalogGridViewInstallStateTests`

## Micro-Sync 22
- `resource-center-scale-install-supervision` 这轮 foreground autoresearch 最终按 `blocked` 收尾。
- 原因不是 `scale` 修复链路本身回退，而是 guard 命令在当前脏工作区里被无关测试编译错误卡住：
  - 文件：`nolonTests/CodexSessionsTabViewModelTests.swift`
  - 典型错误：
    - `Range<CGFloat>` 上调用 `map`
    - `XCTUnwrap` 泛型推断失败
    - `.success(.init(...))` 上下文推断失败
    - `.placeholder` 无法解析
- 当前判断：
  - `scale` 修复范围内的机械 verify 仍为 `remaining_supervision_gaps = 0`
  - 但基于 `xcodebuild` 的 guard 无法在现工作区完成，因此本轮监督不能宣称“完全无阻塞收口”
  - 该 blocker 属于当前 repo 里另一条 `CodexSessions` 变更链，不在本轮监督 scope 内

## Micro-Sync 23
- 已按用户要求再次重试 guard。
- 结论：
  - 上一轮 `CodexSessionsTabViewModelTests.swift` 编译失败属于旧状态；文件在失败后被再次修改。
  - 重新执行 guard 后，`SkillInstallerTests + ResourceCatalogGridViewInstallStateTests` 已全部通过。
- 最新验证：
  - `xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/SkillInstallerTests -only-testing:nolonTests/ResourceCatalogGridViewInstallStateTests`
  - 时间：2026-04-14 21:12（Asia/Shanghai）
  - 结果：`10` 个测试全部通过，`0` 失败
- 当前 foreground autoresearch 状态：
  - run tag：`resource-center-scale-install-supervision`
  - retained metric：`remaining_supervision_gaps = 0`
  - 最新 iteration：`no-op`
  - 说明：监督目标未新增实现改动，只是阻塞解除后重新确认 guard 通过

## Micro-Sync 23
- 已完成 `codex-sessions` 第二轮 `project-first` 执行收口。
- 关键落地：
  - `Sessions` 默认 grouping 改为 `project`
  - project 组内按时间倒序
  - 每组默认 `5` 条 + `展开 / 收起`
  - 固定主列：`名称 / id / 时间 / provider / 用量 / 菜单`
  - `usage` 走异步行级回填
  - refresh 保持“旧数据先显示 + 后台补新 + 展开状态不丢失”
- 测试回执：
  - `xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/CodexSessionsCardSnapshotTests -only-testing:nolonTests/CodexSessionsSectionDataBuilderTests -only-testing:nolonTests/CodexSessionsTabViewModelTests`
  - 执行时间：`2026-04-14 20:34 CST`
  - 结果：`12` 个测试全部通过，`0` 失败
- 执行过程顺手修复了两类编译门：
  - `PageMarkerRouteResolverTests` 适配 `ProviderTokenTrendSection` 新参数
  - `CodexSessions*Tests` 收紧 Swift 5 类型推断与模块导入
- 结论：
  - `Micro-Sync 22` 中提到的 `CodexSessions` 测试 blocker 已在本轮解决
  - `codex-sessions` 新主方案已从 debate / plan / feature 落到代码和测试，不再是候选状态

## Micro-Sync 24
- `provider-usage intraday drilldown` 已完成 App/UI 侧主链路收口。
- 本轮新增落地：
  - `CodexQuarterHourUsageFetcher` 与 `CodexIntradayUsageService`
  - `ProviderUsageEngine -> ProviderTokenTrendViewModel -> ProviderTokenTrendSection -> NolonUI` 的钻取状态链
  - 日级图点击具体某一天后，在同一 section 内联展开分钟级钻取
  - 默认 `30min`，支持 `15min / 30min / 60min`
  - `selected day` 改为外部受控，打开钻取后切换 `7D / 30D / ALL` 也不丢失当前选中日
- 当前实现口径：
  - `Gemini` 继续保持“按选中日即时聚合，不做文件级 cache”
  - `Codex` 以 `15min` 事实桶为基础，经正式链路派生 `30/60min`
  - 历史日 freshness 语义为“静态快照 / 手动刷新”
- 已完成的定向验证：
  - `swift test --package-path libs/Providers --filter 'CodexIntradayUsageServiceTests|GeminiIntradayUsageServiceTests|CostUsageStoragePathTests/costUsageScannerAggregatesQuarterHours|ProviderIntradayUsageModelsTests'`
- 进行中的收尾：
  - 扩大 app 级验证
  - 执行 `qmd update && qmd embed`
  - 整理并提交本轮 intraday 相关改动

## Micro-Sync 25
- `provider-usage intraday drilldown` 的 app 级定向验证已完成，时间：`2026-04-14 21:05 CST`。
- 验证通过：
  - `xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -derivedDataPath /tmp/nolon-intraday-targeted -destination 'platform=macOS' -only-testing:nolonTests/GeminiUsageTokenTrendViewModelTests -only-testing:nolonTests/ProviderTokenTrendViewModelParityTests -only-testing:nolonTests/ProviderUsageRootViewModelStoreTests -only-testing:nolonTests/PageMarkerRouteResolverTests -only-testing:nolonTests/CodexSessionsTabViewModelTests`
  - 结果：`25` 个 XCTest 全部通过，`0` 失败
  - `swift-testing` suite：
    - `ProviderTokenTrendViewModelParityTests`
    - `ProviderUsageRootViewModelStoreTests`
  - 结果：`2` 个 case 全部通过，`0` 失败
- 额外记录：
  - 曾尝试跑一轮完整 `xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS'`
  - 当时测试宿主 `nolon` 进程持续高 CPU，`xcodebuild` 本体空转，未在合理时间内收尾，因此主动中止，未将其作为本轮完成门禁
  - 当前以“providers 层 intraday 测试 + app 级定向验证”作为本轮可回归依据
