# 2026-03-17

- 决策：确认 `nolon` 内存暴涨的根因不是主进程泄漏，而是 `CodexStatusProbe` 在 `parseFailed/timedOut` 后触发 TTY fallback，持续拉起并遗留 `codex -s read-only -a untrusted` 子进程。
- 行动：新增 `CodexStatusProbe` 测试（先红后绿），并移除 `runAndParse` 中的 interactive fallback 分支，避免再次创建难回收的 TTY 会话进程。
- 结果：相关测试通过（`CodexStatusProbeTests`、`CodexUsageDescriptorTests`、`NolonCodexCLIServiceTests`）。
