# 2026-03-21

- DesignSystem 内聚推进：在 `libs/NolonUI` 新增并整理 `DesignSystem` 目录（`Core/Styles/Components/Layout`），将 Sidebar 所需样式能力放入 UI 包内部，支持组件级独立预览。
- 并发修复：`NolonUI` 的 `DesignSystem.CardShadow` 静态常量触发 Swift 6 并发安全检查；已改为计算属性形式（`static var`）消除共享静态存储告警。
- 回归验证：`swift test --package-path libs/NolonUIFoundation` 与 `swift test --package-path libs/NolonUI` 均通过。
- 主工程验证：`xcodebuild -project nolon.xcodeproj -scheme nolon-app -configuration Debug -destination 'platform=macOS,arch=arm64' -derivedDataPath /tmp/nolon-codex-dd7 build` 通过（`BUILD SUCCEEDED`）。
- 预览现状：Sidebar 组件拆分文件均保留独立 `#Preview`（Component / SectionHeader / ProviderRow / ToolRow），可在包内以假数据直接预览，不依赖 app 启动。
- HIG Sidebar 对齐：`nolonApp` 接入 `SidebarCommands()`，补齐系统侧边栏命令组（显示/隐藏 sidebar）入口。
- HIG Sidebar 对齐：`NolonUI.SidebarProviderRowView` 新增 `@Environment(\.sidebarRowSize)` 自适配，按 `small/medium/large` 调整行内 icon 尺寸、文本间距、垂直 padding，并在 `small` 尺寸隐藏副标题以提升导航扫读效率。
- 测试补充（TDD）：`NolonUITests` 新增 `SidebarProviderRowMetrics` 两个用例，覆盖 `small` 与 `large` 的关键行为断言。
- 验证：`swift test --package-path libs/NolonUIFoundation`、`swift test --package-path libs/NolonUI`、`xcodebuild -project nolon.xcodeproj -scheme nolon-app -configuration Debug -destination 'platform=macOS,arch=arm64' -derivedDataPath /tmp/nolon-codex-dd7 build` 均通过。
- 账号卡片迁移（Phase 1）：新增 `libs/NolonUIFoundation/AccountCard` 轻量展示模型（`AccountCardPresentation`、`AccountCardSelectionStyle`、`AccountSummaryCardHeaderModel` 等），并在 `libs/NolonUI/Components/AccountCard` 新增 `AccountSummaryCard`、`AccountSummaryContentCard` 及独立文件预览，主工程 `UnifiedAccountCard` 保留业务编排与 debug marker，仅回依赖 UI 包组件。
- 兼容策略：为避免 `NolonUI` 设计系统扩展与主工程同名 API 冲突（如 `dsCard`），主工程保留本地包装器 `AccountSummaryCard/AccountSummaryContentCard`，内部转发到 `NolonUI`；`ProviderUsageView` 仅引入 `NolonUIFoundation`。
- 迁移验证（红绿流程）：先新增失败测试 `AccountCardPresentationTests`、`NolonUITests.testAccountSummaryCardSelectionMetrics...`，后实现通过；最终验证 `swift test --package-path libs/NolonUIFoundation`、`swift test --package-path libs/NolonUI`、`xcodebuild -project nolon.xcodeproj -scheme nolon-app -destination 'platform=macOS' -derivedDataPath /tmp/nolon-codex-dd7 build` 全部通过。
- 资源中心关闭按钮组件化：新增 `NolonUI` 组件 `Components/ResourceCenter/ResourceCenterCloseButton`（含 cancel 快捷键与文件级预览），主工程通过 `Remote/ResourceCenterCloseButton.swift` 包装回接，在 `ResourceCatalogGridView` 的 search bar 使用该组件替换原内联 `xmark` 按钮实现。
- 测试与验证：新增 `NolonUITests.testResourceCenterCloseButtonMetrics_ExposeExpectedBaseline` 并通过；`swift test --package-path libs/NolonUIFoundation`、`swift test --package-path libs/NolonUI`、`xcodebuild -project nolon.xcodeproj -scheme nolon-app -destination 'platform=macOS' -derivedDataPath /tmp/nolon-codex-dd7 build` 均通过。
- 账号卡片预览扩展：`AccountSummaryContentCard` 从单场景预览升级为多场景集合（完整态、紧凑头部、长文案告警态、仅操作区），并新增通用 `previewScene/detailRow` 预览辅助，便于统一回归 header/body/details/actions 的边界表现。
- 验证：`xcodebuild -project nolon.xcodeproj -scheme nolon-app -configuration Release -destination 'platform=macOS' build -quiet` 通过（仅有既有 actor-isolation 警告，与本次改动无关）。
- 账号卡片预览状态矩阵增强：`AccountSummaryContentCard` 预览从结构示例升级为业务态集合，新增/覆盖额度健康、高危低额、拉取报错、加载骨架、空数据、企业无限额度等场景，便于统一回归阈值色彩、文案截断、section 组合与操作区布局。
- 测试补充：`AccountSummaryCardSnapshotTests` 新增错误态与骨架态两条快照用例（`error-body-state`、`loading-skeleton-state`）；当前仓库测试基线仍被既有 `NolonUIFoundation` import 缺失问题阻塞，非本次改动引入。
- 预览可用性增强：`AccountSummaryContentCard` 预览拆分为独立入口（All/Healthy/LowQuota/Error/Loading/Empty/Enterprise），便于在 Xcode 预览面板按状态单独回归，不再依赖滚动总览定位。
EOF && qmd update && qmd embed- 账号卡片预览新增“账号列表模式”：`AccountSummaryContentCard` 增加 `Scene.listMode` 与独立 `#Preview`，在同一列表容器下展示 active/low-quota/error/loading 四种账号卡片组合，用于回归列表密度与混合状态可读性。
EOF && qmd update && qmd embed- 预览扩展（网关+图表）：`AccountSummaryContentCard` 新增 `Scene.gatewayCard`、`Scene.gatewayList`、`Scene.usageChart`，并补独立 `#Preview` 入口；覆盖网关单卡、网关列表（混合状态）和用量图表模块（趋势柱状+指标+范围切换动作）。
- 验证：默认 DerivedData 受 build.db 锁影响，改用隔离路径执行 `xcodebuild -project nolon.xcodeproj -scheme nolon-app -configuration Release -destination 'platform=macOS' -derivedDataPath /tmp/nolon-codex-dd9 build -quiet` 通过。
- 同页预览落地：`AccountSummaryContentCard` 新增 `Scene.unifiedPage`，将账号列表、网关列表、用量图表整合到一个 `Unified Account Workspace` 预览页面，并提供独立 `#Preview` 入口。
- 验证补充：应用级 `xcodebuild` 在隔离 DerivedData 仍受磁盘空间不足（No space left on device）影响；改用 `swift build --package-path libs/NolonUI` 验证组件包编译通过。
EOF && qmd update && qmd embed- 预览入口收敛：将 `AccountSummaryContentCard` 的 `Scene.all` 默认改为渲染 `unifiedPageScene`，使 `All` 入口即统一页面（账号列表 + 网关列表 + 用量图表），降低预览选择成本。
EOF && qmd update && qmd embed- 资源中心侧边栏适配修正：`ResourceCenterSidebar` 的 tab 回写从 `init(foundationID:)` 闭包引用改为 `fromFoundationID` 显式映射，消除 `main actor-isolated initializer` 并发告警；并新增 `nolonTests/ResourceCenterTabTypeMappingTests.swift` 锁定双向映射行为。
- 测试基线补齐：`nolonTests` 中引用 `NolonUIFoundation` 类型的用例补充模块导入（`AccountSummaryCardSnapshotTests`、`AccountCardPresentationTests`、`CodexUsageTabPresentationTests`、`GatewayCardVisualRegressionSnapshotTests`、`NolonAccountsViewModelTests`、`ProviderSidebarAdapterTests`）。
- 环境风险记录：执行 `xcodebuild test` 时出现磁盘不足（`Macintosh HD is out of space`），已清理临时构建目录 `/tmp/nolon-codex-dd*`，可用空间恢复到约 22Gi。
- 验证结果：`swift test --package-path libs/NolonUIFoundation`、`swift test --package-path libs/NolonUI` 通过；`xcodebuild ... -only-testing:nolonTests/ResourceCenterTabTypeMappingTests test` 通过；`xcodebuild -project nolon.xcodeproj -scheme nolon-app -destination 'platform=macOS' -derivedDataPath /tmp/nolon-codex-dd11 build` 通过。
- 预览可视范围修复：新增 `AccountSummaryContentCardPreviewScene` 作为统一预览宿主，按场景分配画布高度（All/Unified/List/GatewayList 等），解决默认预览尺寸导致的内容截断与“看不全”问题。
EOF && qmd update && qmd embed- 预览横向空间调整：`AccountSummaryContentCardPreviewScene` 增加按场景可变宽度（Unified/All 1360，列表类 1240，图表 1180，单卡 1080/1100），修复“左右宽度不够”导致的内容拥挤与截断。
EOF && qmd update && qmd embed- 小屏适配：预览宿主宽度整体下调（Unified/All 1080，列表 980，图表 940，网关单卡 920，其余 900），以适配较小屏幕下的 Xcode 预览视口。
EOF && qmd update && qmd embed- 真实模拟账号卡片：`AccountSummaryContentCard` 新增 `Scene.codexGeminiAccounts`，包含更接近线上信息结构的 Codex/Gemini 两张账号卡（计划、区域、额度、auth 来源、同步状态、操作按钮），并加入独立 `#Preview` 入口与 unified page 组合展示。
EOF && qmd update && qmd embed- 账号卡交互口径统一：全部账号卡预览移除来源/runtime 展示，并去除“刷新/激活”内联按钮（操作默认走右键菜单），已同步到 Healthy/Codex/Gemini/LowQuota/Empty 等账号场景。
EOF && qmd update && qmd embed- 账号卡片交互最终统一：所有账号卡预览场景均关闭 actions section（移除 TopUp/Connect/Retry/Details 等内联按钮），账号操作统一走右键菜单。
EOF && qmd update && qmd embed- 按 SwiftUI 布局指南整理预览间距：参考 xcdocs（`Building layouts with stack views`、`ViewSpacing`）将 `AccountSummaryContentCard` 预览页改为分层节奏 token（16/12/8），统一页面/分组/行内间距并清理散落魔法值。
EOF && qmd update && qmd embed- 预览间距 token 复用化：新增 `DesignSystem/Core/PreviewLayoutTokens.swift`（16/12/8），并接入 `AccountSummaryContentCard`、`AccountSummaryCard` 两个预览容器，形成可复用基线。
EOF && qmd update && qmd embed- 预览可浏览性增强：`AccountSummaryContentCardPreviewScene` 外层改为 `ScrollView([.vertical, .horizontal])`，小屏预览可双向滚动查看完整内容。
EOF && qmd update && qmd embed- 用量模块组件化：将 `AccountSummaryContentCard` 里的用量图表与指标行抽离到新文件 `Components/AccountCard/AccountUsageChartModule.swift`（`AccountUsageChartModule`、`AccountUsageMetricRow`），原场景仅负责数据组装。
EOF && qmd update && qmd embed- 账号列表进度条组件化：新增 `Components/AccountCard/AccountQuotaModule.swift`（`AccountQuotaModule`、`AccountInlineQuotaProgress`、`AccountQuotaRow`、`AccountQuotaStyle`），并移除 `AccountSummaryContentCard` 内部重复的 quota/list 进度绘制实现。
EOF && qmd update && qmd embed- 状态模块组件化：新增 `Components/AccountCard/AccountStateModule.swift`（`AccountErrorStateModule`、`AccountLoadingStateModule`、`AccountEmptyStateModule`），并在 `AccountSummaryContentCard` 中替换原内联 error/loading/empty 视图实现。
EOF && qmd update && qmd embed- 账号列表模式组件化：新增 `Components/AccountCard/AccountListModeModule.swift`（`AccountListModeModule`、`AccountListModeItem`），`AccountSummaryContentCard` 的 `listModeScene` 改为仅注入 mock 数据并调用新模块。
EOF && qmd update && qmd embed- 列表模式样式升级：`AccountListModeModule` 从“卡片式 list”改为“table-like 行式列表”，新增表头（Account/Plan/Status/Usage）、行分隔与列宽约束，保留行内用量进度条。
EOF && qmd update && qmd embed- 列表状态展示调整：`AccountListModeModule` 去掉 Status 文字列，状态仅用颜色圆点标识并前置到第一列账号标题前；Usage 列宽同步扩到 220 以容纳多窗口额度。
EOF && qmd update && qmd embed- ProviderUsage 主工程已接回列表模式改造：`codexAccountLayoutMode == .list` 改为 table-like 展示（Account/Plan/Usage 三列 + 第一列状态色点），并保留原有账号激活、刷新、re-login 与右键菜单能力。
- 列表用量改为按 quota windows 分行展示（最多 3 个窗口），避免聚合成单行文本；Plan 列仅显示套餐字段，避免重复信息。
- 2026-03-21 验证：`xcodebuild -project nolon.xcodeproj -scheme nolon -configuration Debug -destination 'platform=macOS' build` 通过；`xcodebuild ... test -only-testing:nolonTests/CodexUsageTabPresentationTests` 因 scheme 未配置 test action 无法执行。
- 卡片模式已接回主工程：`UnifiedAccountCard` 的 quota body 从 `ProviderQuotaSection` 切换为 `NolonUI` 模块化渲染（`AccountQuotaModule` / `AccountErrorStateModule` / `AccountLoadingStateModule` / `AccountEmptyStateModule`），与预览方案保持一致。
- 为主工程复用，`libs/NolonUI` 中额度与状态模块已提升为 `public`（含初始化器），并通过 `swift build --package-path libs/NolonUI` 与主工程 `xcodebuild ... build` 验证通过。
- 网关 UI 开始抽离到 UI 库：新增 `NolonUI.GatewayCardModule` 与 `GatewayCardMemberItem`（`libs/NolonUI/.../GatewayCardModule.swift`），主工程 `ProviderUsageView.gatewayCardView` 改为调用该组件，仅保留业务交互（激活、拖拽、菜单、增删成员）。
- 验证通过：`swift build --package-path libs/NolonUI` 与 `xcodebuild -project nolon.xcodeproj -scheme nolon -configuration Debug -destination 'platform=macOS' build`。
- 继续网关抽离：新增 `NolonUI.GatewayCardListModule`（list/cards 双布局容器），主工程 `ProviderUsageView.gatewayCardsContainer` 已切换调用该模块，`gatewayCardView` 继续承载业务交互。
- 为网关抽离组件补齐独立预览：`GatewayCardModule` 与 `GatewayCardListModule` 新增 `#Preview`，覆盖 list/cards 与 compact/normal 场景。
- 网关成员标签细节打磨：`GatewayCardModule` 移除成员标签内删除按钮；无套餐时不显示首字母图标但保留等宽占位（透明圆形），收敛与有套餐标签的宽度/对齐差异；并统一 chip 前导与间距参数。
- 调用清理：同步移除 `GatewayCardListModule` 预览与主工程 `ProviderUsageView.gatewayCardView` 中 `onRemoveMember` 传参，避免签名漂移。
- 构建验证：`swift build --package-path libs/NolonUI` 与 `xcodebuild -project nolon.xcodeproj -scheme nolon -configuration Debug -destination 'platform=macOS' build` 均通过。
- 发版策略调整：后续改为 GitHub Actions 手动触发发版（`.github/workflows/release.yml`），不再依赖本地手动执行全流程。
- Sparkle CI 方案：`scripts/release.sh` 新增 `SPARKLE_PRIVATE_KEY` 环境变量入口；CI 使用 `sign_update --ed-key-file -`，本地默认仍走 Keychain（`--account ed25519`）。
- 文档补齐：新增 `docs-linhay/spaces/github-actions-release/README.md` 与 `docs-linhay/dev/ops/github-actions-release-runbook-2026-03-21.md`。
- GitHub 发版流程新增 tag 自动触发：`.github/workflows/release.yml` 支持 `push.tags = v*`，并根据事件自动切换 `CI_TAG_MODE`。
- release 脚本新增 `CI_TAG_MODE`/`RELEASE_PUSH_BRANCH`：tag 模式跳过二次打 tag，改为仅同步 appcast commit 到 `main`，避免 tag 冲突。
- 文档同步：更新 `docs-linhay/spaces/github-actions-release/README.md` 与 `docs-linhay/dev/ops/github-actions-release-runbook-2026-03-21.md` 说明双触发模式。
