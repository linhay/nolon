# 2026-04-08

- 决策：先处理超长 UI 文件而非服务层巨石，优先选择可机械拆分且行为风险低的 section 迁移。
- 行动：将 `ProviderUsageView.swift` 的网关与 Claude 编辑器段落拆到 `ProviderUsageView+Gateway.swift`。
- 行动：将 `UnifiedDomainCardViews.swift` 的资源卡片与配套 support views 拆到 `UnifiedResourceCardViews.swift`。
- 行动：将 `UnifiedAccountCardComponents.swift` 的 Codex 紧凑列表与 panorama scaffold 拆到 `UnifiedCodexCompactAccountComponents.swift`。
- 行动：继续拆分 `UnifiedRemoteComponents.swift`、`UnifiedProviderSharedViews.swift`、`UnifiedCodexSharedViews.swift`、`CodexAdvancedConfigView.swift`，分别迁出 remote catalog/install state、provider usage support、codex runtime、config support 到 companion files。
- 行动：将 `NolonCodexCLI.swift` 进一步拆成 `NolonCodexCLIPayloads.swift`、`NolonCLIEntrypoint.swift`、`NolonCodexCLIRuntimeSupport.swift`，把 payload、CLI 帮助路由、runtime helper 从主服务实现中分离。
- 验证：门禁范围扩展到 15 个已拆分文件；脚本复核全部目标文件当前都低于 2000 行。
- 验证：`NolonCodexCLI.swift` 当前降到 1962 行，本轮 CLI companion files 也都低于 2000 行，并已补进门禁测试。
- 验证：新增 `nolonTests/OversizedViewFileGuardTests.swift`，并用脚本确认 6 个目标文件行数均低于 2000。
- 风险：`xcodebuild test -only-testing:nolonTests/OversizedViewFileGuardTests` 首轮依赖编译过重，未等到测试执行结果，需要后续在完整构建窗口内补跑。
