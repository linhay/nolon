# 2026-04-01

## Codex 账号体系 SQLite 迁移（进行中）

- 激活链路改为：账号激活时先在 `~/.nolon/codex/active-auth/<provider-id>/<account-id>.json` 产出 auth 文件，再将 provider 目录下 `auth.json` 建立软链接到该文件。
- 保留轮询回写：轮询仍监听 provider `auth.json`，通过软链接目标变化可持续回写 SQLite。
- `codex_accounts` 主表在新建场景不再包含 `relative_auth_path/auth_json`，并兼容旧库（旧库存在该列时按旧列写入，避免迁移中断）。
- 新增 `codex_account_metadata`，与 `codex_account_credentials` 一起拼装运行时 auth JSON。
- 删除账号时同步删除 `codex_account_credentials/codex_account_metadata`。
- 迁移入口会清理 `~/.nolon/codex/auth/*.json`，逐步替代 managed auth 文件为 SQLite 数据源。
- 导入面板新增只读补全信息展示：`auth_mode / account_id / plan / base_url / api key 尾号`，不提供编辑入口。
- 自定义 SQLite 分组导入改为“直接入主账号库 accounts.sqlite3”：导入时创建 codex 账号并写入 `codex_account_metadata.custom_group_name`。
- 账号列表分组新增“按自定义分组”，数据来自 `codex_account_metadata.custom_group_name`。
- 导入完成后无论目标类型都会触发 `load()`，确保导入结果即时可见。

- 2026-04-01：将 `CodexAuthManager.swift` 的 SQLite 存储与 schema 逻辑拆分到 `CodexAuthManager+SQLite.swift`，主文件保留账号业务与轮询逻辑；行为不变，编译通过。
- 2026-04-01：继续拆分 CodexAuthManager：新增 `CodexAuthManager+Import.swift`（导入校验/凭证补全/OAuth 资源换取）与 `CodexAuthManager+ProviderAuthPolling.swift`（auth.json 轮询回写）；主文件进一步收敛。
- 2026-04-01：尝试将 Import/Activation/Polling 与 SQLite 拆到独立文件时，完整 App 构建暴露 Swift private 跨文件可见性问题。已回收至单文件确保 BUILD SUCCEEDED；后续若继续拆分需先设计访问级别（internal/fileprivate）与 helper 下沉策略。
- 2026-04-01：新增 `CodexAuthManagerSupport.swift`，下沉 OAuth token refresh / userinfo 获取与 ditto 执行；`CodexAuthManager` 改为薄代理调用，主工程 BUILD SUCCEEDED。
- 2026-04-01：继续下沉 JSON 工具函数到 `CodexAuthManagerSupport`（decode/encode/get/set/remove/encodeJSONObjectObject/firstNonEmptyString），`CodexAuthManager` 保留薄封装；主工程 BUILD SUCCEEDED。
- 2026-04-01：继续下沉导入候选解析（zip 解包 + JSON array 展开）到 `CodexAuthManagerSupport`，`CodexAuthManager.importCandidates(for:)` 仅保留薄封装映射；主工程 BUILD SUCCEEDED。

## Sub2API 下线（2026-04-01）
- 按产品决策移除 Codex `Sub2API` 全链路支持：底层导出实现、CLI `codex auth export`、账号页/导入面板相关 UI 入口与测试用例。
- `Sub2APIExport.swift` 已删除；`CodexAuthManager` 中 `exportAccountsAsSub2API` / `exportValidatedAuthFilesAsSub2API` 与映射逻辑已移除。
- `NolonCoreCLIKit` 中 `auth export` 命令与对应 help/formatter/path 已移除。
- 构建验证：`xcodebuild build -project nolon.xcodeproj -scheme nolon -destination 'platform=macOS'` 通过。

## Codex provider 固定 ID 与 active map 清理（2026-04-01）
- 决策：原始厂商 provider 采用模板稳定 UUID（不再每次冷启动随机生成）。
- 决策：`codex_active_accounts` 的键改为“规范 provider key”（原始 Codex/CodexXcode 使用 `codex` / `codex-xcode`），并兼容读取旧 key。
- 决策：保存 active map 时按当前 `providers.json` 进行清理，移除已不存在 provider 的历史残留映射，避免表持续膨胀。
- 测试：新增 `ProviderSettings` 与 `CodexAuthManager` 对应回归测试，覆盖稳定 ID、旧 ID 迁移、active map 残留清理。
- 2026-04-01：为 `CodexAuthManager` 增加“启动即迁移”步骤：`migrateAccountsStoreToSQLiteIfNeeded()` 内新增 `migrateActiveAccountProviderKeysIfNeeded()`，会将 original vendor 旧 provider UUID key 迁移到 canonical key（`codex` / `codex-xcode`），并按当前 providers 集合清理 orphan 映射；同时同步回写 `active-accounts.json` 镜像。
- 2026-04-01：新增回归测试 `loadAccountsMigratesLegacyOriginalVendorActiveKeyToCanonical`，覆盖“仅加载账号也会自动迁移 active key”的场景。
- 2026-04-01：本机现网库检查：`~/.nolon/nolon.sqlite3` 的 `codex_active_accounts` 当前为 1 行，键为 `provider_id=codex`，历史重复/残留已收敛。

## runtime-home 启动后台清理（2026-04-01）
- 决策：`~/.nolon/codex/runtime-home` 采用“每次 App 启动后后台清理”策略，避免长期堆积。
- 规则：保留 active account 对应目录；非 active 目录按目录修改时间判断，超过 24 小时自动删除；保留最近目录以降低误删风险。
- 实现：
  - `CodexAuthManager` 新增 `cleanupRuntimeHomesOnAppLaunch(maxAge:now:)`，返回扫描/删除/保留统计。
  - `nolonApp.swift` 新增 `CodexRuntimeHomeCleanupService`，在 `ContentView.task` 中与后台轮询并行启动，异步执行清理并记录日志。
- 测试：`CodexAuthManagerTests` 新增用例，覆盖“保留 active + 删除过期 inactive”与“保留近期 inactive”场景。

## ProviderUsageEngine Codex 结构拆分（2026-04-01）
- 决策：将 `ProviderUsageEngine.swift` 内高密度的 Codex 类型定义与派生状态计算迁移到新文件 `nolon/Skills/Domain/Providers/Usage/Engine/CodexUsageEngine.swift`（`extension ProviderUsageEngine`），降低主文件体积与职责耦合。
- 迁移内容：`CodexAccountDisplayState/CodexAccountSortOption/CodexConfigEditorDraft/CodexImportCandidate` 等 Codex 相关嵌套类型、`CodexAddSource`、以及 `codexAccountDisplaySections/codexPrimaryHeaderActions/codexImportCandidateSections` 等派生属性。
- 兼容调整：`uniqueCodexAccountsInDisplayOrder()` 从 `private` 调整为默认内部可见，以支持跨文件 extension 复用，行为不变。
- 验证：
  - 定向回归通过：`CodexAccountDisplaySectionsTests`、`CodexGatewayCardsViewModelParityTests`、`CodexImportExportViewModelParityTests`、`ProviderUsageSubViewModelWiringTests`、`ProviderTokenTrendViewModelParityTests`。
  - 全量 `nolon-tests` 仍存在历史失败项（与本次拆分无直接关系），本次改动相关测试均通过。
- 2026-04-01：`ProviderUsageEngine` 进一步重构 action 注入：新增 `ActionDependencies`（聚合 codex/gemini 闭包依赖），引擎改为单一 `actions` 存储；原有 `codexActivateAction` 等名称保留为计算属性别名，保持调用点与测试注入签名兼容。
- 2026-04-01：新增 `ProviderUsageEngine+ActionAliases.swift`，集中维护 `typealias` 与 `ActionDependencies`，主文件去除别名定义与散落存储属性，降低顶部噪音。
- 验证：`CodexAccountDisplaySectionsTests` + `ProviderUsageSubViewModelWiringTests` 通过。
- 2026-04-01：尝试将 `ProviderUsageEngine` 中 2000+ 行 Codex 行为整体迁移到独立扩展文件时，触发大量 Swift 访问控制边界（跨文件 extension 无法访问 `private` 成员），涉及状态字段/静态 helper/任务句柄等；已回滚该次大迁移，保留“已验证抽象”版本（`CodexUsageEngine.swift` + `ProviderUsageEngine+ActionAliases.swift` + `ActionDependencies`）。
- 结论：后续要完成“Codex 行为全外提”，需先确定访问控制策略（放宽为 internal 或引入 dedicated Codex 子引擎对象并通过显式接口访问状态）。
- 2026-04-01：落地 `AnyUsageEngine` 抽象层（`nolon/Skills/Domain/Providers/Usage/Engine/AnyUsageEngine.swift`），`ProviderUsageStateStore` 由持有具体 `ProviderUsageEngine` 调整为持有 `any AnyUsageEngine`（并保留 `engine/providerEngine` 兼容桥接），Root 层新增 `init(provider:engine:)`，上层编排可面向接口注入。
- 2026-04-01：将 Usage Engine 相关 action/typealias 去 `ProviderUsageEngine` 命名空间化：新增全局别名（`UsageEngineCodexActivateAction` 等）并保留旧嵌套别名兼容，Root 注入签名改为全局别名。
- 2026-04-01：新增 `UsageEngineTypeAliases.swift`，将 `ProviderUsageEngine.Codex*` 与 `TokenTrendRange` 提供为模块级别别名；`ProviderUsageSubViewModels.swift` 已改用别名，减少对 `ProviderUsageEngine` 具体类型命名空间的耦合。
- 验证：`xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/ProviderUsageSubViewModelWiringTests -only-testing:nolonTests/ProviderUsageRootViewModelStoreTests` 通过（5 tests）。
- 2026-04-01（继续）：去除 `ProviderUsageStateStore` 的 `providerEngine` 桥接，改为直接持有 `any ProviderUsageEngineProtocol`；新增 `ProviderUsageEngineProtocol.swift` 定义 Root/SubViewModel 所需能力面，`ProviderUsageEngine` 通过 extension conform。
- 2026-04-01（继续）：`ProviderUsageRootViewModel.init(provider:engine:)` 参数提升为 `any ProviderUsageEngineProtocol`，`ProviderUsageSubViewModels` 全量改为依赖 `state.engine`（协议类型），不再依赖具体 `ProviderUsageEngine`。
- 2026-04-01（继续）：为兼容 unified pipeline 等调用，协议补充 `activeClaudeAccountId/activeGeminiAccountId`。
- 验证：`ProviderUsageSubViewModelWiringTests` + `ProviderUsageRootViewModelStoreTests` 再次通过（5 tests）。

## 2026-04-01 ProviderUsageEngine 分域抽象（一步到位）
- 将 `ProviderUsageEngineProtocol` 超大协议拆分为四个实质能力协议：`ProviderUsageCommonEngineProtocol` / `ProviderUsageCodexEngineProtocol` / `ProviderUsageClaudeEngineProtocol` / `ProviderUsageGeminiEngineProtocol`。
- 将 `AnyUsageEngine` 升级为组合根协议，直接暴露 `common/codex/claude/gemini` 四个子引擎，`ProviderUsageEngine` 通过 extension 实现组合映射。
- `ProviderUsageStateStore` 改为持有 `any AnyUsageEngine`，移除 capability 强转和 `preconditionFailure`。
- `ProviderUsageRootViewModel` 依赖注入类型改为 `any AnyUsageEngine`。
- `ProviderUsageSubViewModels` 清理剩余 `state.engine` 访问，按能力面访问 `state.commonEngine` / `state.codexEngine`。
- 同步修复 Provider Usage 相关 parity/wiring/pipeline 测试中的引擎访问路径。
- 回归验证：`xcodebuild test ...` 指定 8 个 Provider Usage 相关测试套件，14 tests 全通过。
- 2026-04-01（继续）：将公共能力面中的 `codexAccountLayoutMode/setCodexAccountLayoutMode` 重命名为中性语义 `accountLayoutMode/setAccountLayoutMode`，并从 `ProviderUsageCodexEngineProtocol` 移除此职责；`ProviderUsageEngine` 通过桥接实现保证行为不变。
- 影响：`ProviderUsageSubViewModels` 的公共与 Codex 状态均改为通过 `commonEngine` 读写布局模式，进一步减少 codex 命名泄漏到通用层。
- 验证：Provider Usage 8 个测试套件共 14 tests 通过。
- 2026-04-01（硬切）：移除 `accountLayoutMode` 兼容桥接。`ProviderUsageEngine` 内部字段与方法已从 `codexAccountLayoutMode/setCodexAccountLayoutMode` 正式重命名为 `accountLayoutMode/setAccountLayoutMode`，协议扩展中的旧名转发逻辑已删除。
- 结果：布局模式在领域层完全使用中性命名，不再保留旧 codex 命名入口。
- 验证：Provider Usage 8 套件 14 tests 通过。
- 2026-04-01（继续硬切）：将布局模式类型从 `CodexAccountLayoutMode` 彻底中性化为 `UsageAccountLayoutMode`（含引擎内部枚举命名），删除旧别名 `typealias CodexAccountLayoutMode`。
- 影响：公共协议/Root/View 层不再出现 `CodexAccountLayoutMode` 或 `ProviderUsageEngine.CodexAccountLayoutMode`。
- 验证：Provider Usage 8 套件 14 tests 通过。

- 2026-04-01: Codex 激活物化文件路径从 `~/.nolon/codex/active-auth/<provider-id>/<account-id>.json` 收敛为 `~/.nolon/codex/active-auth/<provider-id>/auth.json`；激活写入后清理同目录下历史 `<account-id>.json` 旧文件，保持每 provider 单一激活文件。
