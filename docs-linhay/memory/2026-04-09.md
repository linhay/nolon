## 代码巨石收敛

- 完成 `NolonCoreCLIRunner.swift`、`ProviderUsageEngine.swift`、`CodexAuthManager.swift` 的 companion file 拆分，主文件保留核心编排，按 Gemini、Plugin、Resource、Codex 登录/刷新/管理、SQLite/导入校验/Provider 同步等职责分离。
- 继续修复 companion file 带来的可见性问题，统一将跨文件同类型扩展必须访问的 helper 从 `private/fileprivate` 收敛到模块内可见，避免重新耦回超长单文件。
- 修复前一轮 UI 拆分遗留的 `CodexAdvancedConfigView`、`ProviderUsageView+Gateway` 编译问题，补齐缺失 imports，并让 `ClaudeAccountEditorSheet`、`nolonLabel`、`compactUsageWindows`、`CodexListUsageWindow` 等跨文件符号可见。

## 超长文件门禁

- `nolonTests/OversizedViewFileGuardTests.swift` 扩充覆盖服务层与测试层 companion files，并修复 `split` 参数顺序错误。
- 通过全项目扫描确认当前项目内已无超过 2000 行的 Swift 文件；最后清零项来自测试文件拆分：
  - `nolonTests/CodexAuthServiceTests.swift` 按 test class 拆成多个独立 XCTest 文件。
  - `libs/Providers/Tests/ProvidersTests/NolonCodexCLIEntrypointTests.swift` 按入口场景拆出 companion test file。
  - `libs/Providers/Tests/ProvidersTests/CodexTests/CodexAuthManagerTests.swift` 按 suite extension 拆出 companion test file。
  - `libs/Providers/Tests/ProvidersTests/NolonCoreCLIKitTests.swift` 进一步拆为主 suite、多个 `Part` 文件和两个 `Support` 文件，分离 mock/service helper 与测试断言。

## 验证结论

- `xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/OversizedViewFileGuardTests` 已通过。
- `swift test --package-path libs/Providers --filter 'NolonCoreCLIKitTests|NolonCodexCLIEntrypointTests|CodexAuthManagerTests'` 未能完成，阻塞于上游依赖 `swift-collections` 的 `ContainersPreview/TemporaryAllocation.swift` 编译错误：`thrown expression type 'any Error' cannot be converted to error type 'E'`。

## 拆分后收敛

- 将 `ProviderUsageEngine` 的 Codex 账号校验逻辑从同类型跨文件静态 helper 收敛为独立的 `CodexAccountValidationService`，减少 `ProviderUsageEngine` 表面积，并去掉对主类型静态常量的 `nonisolated` 访问耦合。
- 为拆分后的 `CodexAuthService` 相关 XCTest 文件补建共享 support file：`CodexAuthServiceTestSupport.swift`，集中承载 `AsyncFlagBox`、`LockedBox`、`AsyncIntBox`、`UsageViewModelTestError` 等 fixture，避免 companion test files 彼此复制 helper。
- 修复测试拆分引入的共享 helper 重复定义问题（`AsyncGate`），确认新的 support 布局仍能通过现有超长文件门禁测试。

## Companion 命名与测试支撑层收口

- 将 `libs/Providers/Tests/ProvidersTests` 下自动生成的 `Part1/Part2/SupportA/SupportB` companion files 重命名为语义化主题文件，例如 `NolonCoreCLIKitTests+ParsingAndGemini.swift`、`NolonCodexCLIEntrypointTests+AuthUsageRouting.swift`、`CodexAuthManagerTests+PreflightAndHealing.swift`，降低后续定位测试场景的认知成本。
- 在 `NolonCoreCLIKitTests+MockRepositoryCoreSupport.swift` 中补建 `DelegatingMockSkillsRepositoryService`，把 `NolonSkillsRepositoryServing` 的公共转发实现集中到一个默认扩展中，后续 mock 只需要声明差异行为。
- 基于该抽象，重写 `NolonCoreCLIKitTests+MockRepositoryRemoteSearchSupport.swift`，删除原来大量重复的仓储接口样板实现，保留每个远端搜索 mock 的差异化返回或错误注入逻辑。
- 再次验证：`OversizedViewFileGuardTests` 通过，且全项目 Swift 行数扫描仍为 `0` 个文件超过 2000 行；`swift test --package-path libs/Providers --filter NolonCoreCLIKitTests` 依旧阻塞于外部依赖 `swift-collections` 的 `TemporaryAllocation.swift` 编译错误，没有新增到本地测试支撑层的编译回归。

## 测试契约与编译告警清理

- 调整 `NolonAccountsViewModelTests.swift` 中 Codex auth 路径/JSON/编辑相关断言，使其匹配当前实现契约：复制路径返回 `relativeAuthPath`，编辑 auth.json 通过临时 `nolon-auth-inspect` 文件打开，复制 JSON 走非 materialized auth 数据读取。
- 用引用型 sink 替代测试里在可逃逸闭包中直接修改捕获变量的写法，清理 `NolonAccountsViewModelTests.swift` 与 `ProviderUsageUnifiedAccountsPipelineTests.swift` 中的 Swift 6 并发告警。
- 顺手移除 `NolonAccountsViewModel.swift` 对 `CodexAuthManager` 非隔离读方法的多余 `await`，避免继续产生“no async operations occur within await expression”告警。
- 修复两个额外编译 warning：`NolonAgentsManagementViewModel` 改为显式 `init(service:)` 与 `init()`，避免默认参数触发 main-actor 初始化告警；`ProviderUsageAccountsViewModel+CardModels.swift` 明确处理可选 `cardKind` 的字符串化，避免 optional debug description warning。
- 定向验证通过：`xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/NolonAccountsViewModelTests -only-testing:nolonTests/ProviderUsageUnifiedAccountsPipelineTests` 已通过；剩余 warning 仅为工具链层 `appintentsmetadataprocessor` 的 metadata 提示，不来自本地源码。

## Auth Inspect 共享抽象

- 新增 `CodexAuthInspectionSupport.swift`，集中承载 Codex auth 原始 JSON 非空校验与临时 inspect 文件写入规则，避免账户页 `NolonAccountsViewModel` 和 usage engine `ProviderUsageEngine+CodexLogin` 各自维护一份 `nolon-auth-inspect` 临时文件逻辑。
- `NolonAccountsViewModel.swift` 与 `ProviderUsageEngine+CodexLogin.swift` 现已统一通过该 helper 处理 `copyCodexAccountAuthJSON` 和 `editCodexAccountAuthJSON` 流程，收敛了重复实现，降低后续契约漂移风险。
- 再次验证通过：`xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/NolonAccountsViewModelTests -only-testing:nolonTests/ProviderUsageEngineCLILoginTests` 已通过；剩余 warning 仍仅为工具链层 `appintentsmetadataprocessor` 提示。

## Copy Toast 行为统一

- 新增 `CopyToastSupport.swift`，定义共享的 `CopyToastPresenting` 协议、默认“Copied”文案与统一的 toast 自动消失时序。
- `NolonAccountsViewModel.swift` 改为实现 `CopyToastPresenting`，移除原有基于 generation 计数器的本地 toast 收起逻辑。
- `ProviderUsageEngine.swift` / `ProviderUsageEngine+CodexLogin.swift` 也改为走同一套 `CopyToastPresenting` 默认实现，删除 usage engine 侧重复的 `showCopyToast()` 生命周期管理。
- 定向验证通过：`xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/NolonAccountsViewModelTests -only-testing:nolonTests/ProviderUsageEngineCLILoginTests` 再次通过，说明 toast 协议化抽象没有影响账户页和 usage engine 的交互行为。

## Codex Gateway ProviderID 解析统一

- 新增 `CodexGatewayProviderIDResolver.swift`，把 Codex gateway provider id 的解析规则统一收敛为单一真源，按 `templateId` 与稳定 provider id 共同解析 `codex` / `codex-xcode`。
- `NolonAccountsViewModel.swift` 的账户激活路径和 `ProviderUsageEngine+CodexLogin.swift` 的 CLI login 准备路径现已共用该 resolver，避免账户页和 usage engine 对 gateway stop 目标各自维护一套映射逻辑。
- 新增回归覆盖：`NolonAccountsViewModelTests.swift` 验证 `codex-xcode` provider 激活时会停止 `codex-xcode` gateway；`ProviderUsageEngineCLILoginTests.swift` 验证 `codex-xcode` CLI login 准备阶段同样会停止 `codex-xcode` gateway。
- 定向验证通过：`xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/NolonAccountsViewModelTests -only-testing:nolonTests/ProviderUsageEngineCLILoginTests` 已通过；剩余 warning 依旧仅为工具链层 `appintentsmetadataprocessor` 提示。

## 账户展示与 Quota Builder 收敛

- 新增 `ProviderUsageErrorPresentationSupport.swift` 与 `CodexAccountFailurePresentationBuilder`，统一 Codex 账户失败态的 raw detail / display detail / summary / auth failure 判定逻辑；`AccountCardModels.swift` 与 `ProviderUsageAccountsViewModel+CardModels.swift` 不再各自维护一份失败文案推导。
- 新增 `CodexAccountActionFactory.swift`，把 Codex 账户卡片和 usage 卡片里重复的 `activate / relogin / copy auth / edit auth / reveal / delete / refresh` action 定义集中到一处，稳定 action id、title 和 systemImage。
- 新增 `ProviderQuotaSectionDataBuilder.swift`，统一 `ProviderQuotaSection.swift` 与 `ProviderUsageView.swift` 对 `ProviderQuotaSectionData` 的组装，包括 account title、rows、creditsText、percentText、window label 和 legacy windows fallback。
- `ProviderUsageEngine+CodexLogin.swift` 也改为复用 `ProviderUsageErrorPresentationSupport`，避免 engine 层和展示层各自维护相同的 detail/summary 错误格式化逻辑。
- 新增回归测试：`CodexAccountPresentationSupportTests.swift`、`ProviderQuotaSectionDataBuilderTests.swift`，并结合既有的 `NolonAccountsViewModelTests.swift`、`ProviderUsageUnifiedAccountsPipelineTests.swift`、`ProviderQuotaSectionTests.swift`、`ProviderUsageEngineCLILoginTests.swift` 验证共享 builder 落地后行为未变。
- 定向验证通过：`xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/NolonAccountsViewModelTests -only-testing:nolonTests/ProviderUsageEngineCLILoginTests -only-testing:nolonTests/ProviderQuotaSectionTests -only-testing:nolonTests/ProviderQuotaSectionDataBuilderTests -only-testing:nolonTests/CodexAccountPresentationSupportTests -only-testing:nolonTests/ProviderUsageUnifiedAccountsPipelineTests` 已通过，共 45 个测试，0 失败。

## Codex ProviderID 规范化继续收口

- `ProviderUsageEngine+CodexRefresh.swift` 的 CLI login home 路径构建不再维护独立的 `canonicalCodexProviderID(for:)` 逻辑，改为复用 `CodexGatewayProviderIDResolver.resolveOrDefault(provider:)`，让 gateway stop 与 CLI home folder 共享同一套 `codex` / `codex-xcode` 解析规则。
- `ProviderUsageEngineCLILoginTests.swift` 新增 `codex-xcode` 场景，验证 `prepareCLILoginHomeDirectory()` 会落到 `.../cli-login-home/codex-xcode`。
- 定向验证通过：`xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/ProviderUsageEngineCLILoginTests -only-testing:nolonTests/NolonAccountsViewModelTests` 已通过，共 39 个测试，0 失败。

## 文本规范真源收口

- 新增 `TextNormalizationSupport.swift`，把空白裁剪、首个非空值选择、非空文本拼接统一收敛为单一 helper，避免 `Accounts` / `ProviderUsage` 在 `trimmingCharacters(in: .whitespacesAndNewlines)`、空字符串过滤和 `" • "` 拼接规则上继续各自漂移。
- `AccountCardModels.swift` 的 `codexSubtitleText`、`ProviderUsageView.swift` 的 generic outcome identity/account/plan 展示、`ProviderQuotaSectionDataBuilder.swift` 的账号标题回退、`NolonAccountsViewModel.swift` 的 usage summary 邮箱归一化、`ProviderUsageErrorPresentationSupport.swift` 的 persisted failure 文本清洗、`CodexGatewayProviderIDResolver.swift` 的 provider id/template id 归一化，现已统一改走这套 helper。
- 保留 `ProviderUsageEngine+TextSupport.swift` 作为 engine 域内的局部静态文本 helper，用于 Codex management/refresh 路径；并额外验证这次抽出的 engine 文本 support 没有引入回归。
- 新增 `TextNormalizationSupportTests.swift`，锁定 `trimmed / firstNonEmpty / joinedNonEmpty` 的基础契约。
- 定向验证通过：
  - `xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/ProviderUsageEngineCLILoginTests -only-testing:nolonTests/ProviderUsageEngineActivationTests` 已通过，共 18 个测试，0 失败。
  - `xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/TextNormalizationSupportTests -only-testing:nolonTests/NolonAccountsViewModelTests -only-testing:nolonTests/ProviderQuotaSectionDataBuilderTests -only-testing:nolonTests/ProviderUsageEngineCLILoginTests` 已通过，共 45 个测试，0 失败。
  - 自有源码目录（`nolon`、`libs/NolonUI`、`libs/NolonUIFoundation`、`libs/Providers/Sources`、`nolonTests`、`nolonUITests`）的 Swift 超长文件扫描仍为 `TOTAL 0`。

## 账号标题与快照显示名收口

- 新增 `AccountDisplayTextSupport.swift`，把账户标题回退、邮箱/项目副标题拼接、Codex snapshot display name 解析统一收敛到一处。
- `AccountCardModels.swift` 中 Gemini 卡片副标题和 summary-backed 账户卡片标题/Quota 标题不再手写 `email ?? label` 或 `" • "` 拼接，改为统一走 `AccountDisplayTextSupport`。
- `NolonAccountsViewModel.swift` 的 `mergeCodexSnapshotAccounts` 不再直接调用 `preferredDisplayName(...) ?? account.name`，而是改为复用 `ProviderUsageAccountDisplayNameResolver` 这套已有真源；provider 级 fallback summary 也改走同一套 resolver，确保账户页与 usage 卡片页在 Codex API key / relay / chatgptAccount 等场景下显示名一致。
- `ProviderUsageAccountsViewModel+CardModels.swift` 的 Gemini 活跃卡片 quota 账号标题也统一改走 `AccountDisplayTextSupport.title(...)`，避免 `account.email ?? account.name` 在多处散落。
- 新增回归测试：`AccountDisplayTextSupportTests.swift` 锁定标题回退、空白副标题过滤、API key snapshot 命名；`NolonAccountsViewModelTests.swift` 增加 API key snapshot merge 场景，验证显示名稳定落为 `key-<suffix>`。
- 定向验证通过：
  - `xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/AccountDisplayTextSupportTests -only-testing:nolonTests/NolonAccountsViewModelTests -only-testing:nolonTests/ProviderQuotaSectionDataBuilderTests -only-testing:nolonTests/ProviderUsageUnifiedAccountsPipelineTests` 已通过，其中 XCTest 30 个、Swift Testing 7 个，0 失败。
  - 自有源码目录 Swift 超长文件扫描仍为 `TOTAL 0`。

## 共享账户动作与错误展示入口继续收口

- 新增 `AccountCardActionFactory.swift`，把 Claude / Gemini / Codex 之间重复的共享卡片动作定义抽到单一真源，统一覆盖 `primary activate`、`menu edit`、`menu refresh`、`menu delete` 这几类通用 action。
- `CodexAccountActionFactory.swift` 改为复用 `AccountCardActionFactory` 的共享部分；`ProviderUsageAccountsViewModel+CardModels.swift` 中 Claude 和 Gemini 卡片也不再手写对应 action 结构，避免后续 action id、icon 或禁用态在不同 provider 卡片上继续漂移。
- `ProviderUsageErrorPresentationSupport.swift` 新增面向 UI 的 `displayText(error:)` 入口，Gemini unified account 卡片的 live failure 错误展示不再直接手写 `detailText + displayText` 组合，统一走共享 support。
- `UnifiedAccountCard.swift` 的 page marker footer / row 标题文本也改为复用 `TextNormalizationSupport`，把 debug locator 的空白过滤和 `" • "` 拼接收回同一套文本规范。
- 新增回归覆盖：
  - `CodexAccountPresentationSupportTests.swift` 增加共享 `AccountCardActionFactory` action id 稳定性测试，以及 `ProviderUsageErrorPresentationSupport.displayText(error:)` 的展示长度测试。
  - `ProviderUsageUnifiedAccountsPipelineTests.swift` 增加 Gemini live failure unified card 用例，验证共享错误展示入口和共享 action factory 落地后行为稳定。
- 定向验证通过：
  - `xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/CodexAccountPresentationSupportTests -only-testing:nolonTests/ProviderUsageUnifiedAccountsPipelineTests -only-testing:nolonTests/GatewayCardVisualRegressionSnapshotTests` 已通过，其中 XCTest 4 个、Swift Testing 9 个，0 失败。
  - 自有源码目录 Swift 超长文件扫描仍为 `TOTAL 0`。

## Gate 仓库安装 skill 避免落地为全局软链接

- 场景：当 `codex/skills` 已整体软链接到 `~/.nolon/skills` 时，从本地仓库（如 gate）安装 `scale`，旧逻辑会在 `~/.nolon/skills/scale` 再创建一个指回仓库源目录的 skill 级软链接，不符合“全局 skills 目录应保存实体内容”的预期。
- 决策：在 `ProviderSkillMaintenanceService.installSkillInternal` 中新增 linked-root 分支；若 provider skills 根目录本身是软链接，则安装 skill 时直接把内容复制到链接目标目录，并返回 `.copy` 安装结果，不再在全局 skills 根目录下创建嵌套软链接。
- 补充收口：普通 `.copy` 安装也统一改走 `SkillContentMaterializer.copyMaterializingSymlinks(...)`，保证复制后的 skill 目录继续保持自包含。
- 回归覆盖：新增 `NolonSkillsRepositoryServiceTests.installSkillCopiesIntoLinkedGlobalSkillsRoot`，锁定“从 gate 仓库安装到已链接的 codex skills 根目录时，`~/.nolon/skills/scale` 为实体目录且不是软链接”。
- 回归覆盖补充：新增 `NolonResourceKitTests.providerSkillMaintenanceServiceInstallsCopiesIntoLinkedGlobalRoot`，直接在基础设施层锁定 linked-root 安装行为，避免只有 CLI facade 被覆盖。
- 验证阻塞：
  - `swift test --package-path libs/Providers --filter NolonSkillsRepositoryServiceTests` 失败，阻塞于上游依赖 `swift-collections` 的 `ContainersPreview/TemporaryAllocation.swift` 在当前工具链下报错：`thrown expression type 'any Error' cannot be converted to error type 'E'`。
  - `xcodebuild test -project nolon.xcodeproj -scheme NolonCoreCLIKit -destination 'platform=macOS' -only-testing:ProvidersTests/NolonSkillsRepositoryServiceTests` 无法执行，因为 `NolonCoreCLIKit` scheme 未配置 test action。
  - `xcodebuild build -project nolon.xcodeproj -scheme NolonCoreCLIKit -destination 'platform=macOS'` 已通过，`BUILD SUCCEEDED`；说明改动在工程工具链下可编译，但 package tests 入口仍未打通。
