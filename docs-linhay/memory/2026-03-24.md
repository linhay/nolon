## 2026-03-24

- 里程碑：发布 `v2.1.6`。
- 关键修复：Provider 资源告警中点击 `orphaned skills` 可滚动到对应技能卡片。
- 关键修复：技能卡片 `关联工作流` 操作区域左对齐，统一卡片 action 区 spacing 与按钮样式。
- 回归：`ProviderDetailGridViewIssueNavigationTests`、`ResourceCardCopySupportTests`、`SkillDetailViewCloseActionTests` 通过。
- 关键修复：Codex 账号列表 `table/list` 模式下，关闭行级文本选择并补齐整行点击命中区（`.contentShape(Rectangle())`），恢复点击账号触发切换确认。
- 回归：`CodexUsageTabPresentationTests` 通过（新增 `testBDD_GivenCodexLayoutMode_WhenResolvingTextSelectionPolicy_ThenListDisablesSelectionToKeepTapSwitching`）。
- 关键修复：Git 仓库 URL 解析新增 GitLab 多级 group 支持，`https://gitlab.../<group>/<subgroup>/<repo>` 不再误判为 `owner/repo + subpath`；并支持 `/-/tree/<branch>/<path>` 提取子路径。
- 关键修复：Git 同步在 `automatic` 策略下，若 `https anonymous` clone 失败会自动回退 SSH clone（前提 SSH 可用），用于覆盖 GitLab 私仓 HTTPS 无交互认证失败场景。
- 回归：`xcrun swift test --package-path libs/Providers --filter RemoteGitRepositorySupportTests --filter SkillsRepositoryFacadeTests --filter repositoryDraftServiceParsesImportIntent` 与 `xcodebuild ... -only-testing:nolonTests/AddRepositoryViewModelTests -only-testing:nolonTests/RemoteRepositoryTests` 通过。
- 里程碑：完成本地发版 `v2.1.8`（双架构 DMG 构建、签名与公证、Sparkle appcast 发布、GitHub Release 发布）。
- 关键修复：资源中心仓库视图不再注入内置 MCP `XcodeMCPKit`，MCP 列表严格按仓库返回资源展示（避免“仓库外资源”混入）。
- 关键修复：本地/仓库 workflow 扫描忽略 `SKILL.md`，避免将技能规范文件误识别为 workflow（如 `workflow - axure-skill` 误报）。
- 回归：`xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/ResourceCatalogGridViewModelTests` 通过（新增 `testLoadContentMCPs_UsesRepositoryResultOnly`）。
- 测试策略补充：`ProvidersTests` 属于 SwiftPM 独立测试目标，不能直接纳入 `nolon.xcodeproj` 的 `nolon-tests` test plan；需使用 `swift test --package-path libs/Providers --filter ...` 执行。
- 回归：`swift test --package-path libs/Providers --filter ProvidersTests.NolonResourceKitTests/remoteCatalogQueryServiceWorkflowQuerySkipsSkillSpec` 通过。
- 关键修复：新增/编辑仓库保存后立即选中对应仓库；包含 Git 目录候选二次确认路径（`confirmDirectorySelection` 返回保存后的仓库并回写 `selectedRepository`）。
- 回归：`xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/AddRepositoryViewModelTests -only-testing:nolonTests/RemoteRepositorySidebarViewModelTests` 通过（新增 `testSaveRepository_LocalFolder_TriggersOnRepositorySavedCallback`）。
- 关键修复：删除当前选中仓库时立即清空 `selectedRepository`，右侧详情面板同步消失；覆盖右键删除与列表批量删除路径。
- 回归：`xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/RemoteRepositorySidebarViewModelTests` 通过（新增 `selectedRepositoryAfterRemoving` 两个用例）。
