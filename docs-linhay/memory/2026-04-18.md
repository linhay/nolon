## Micro-Sync 00:25
- 完成 `Codex Sessions` 的 `usage` 独立索引落地，优先级按 2026-04-17 评估结论执行为 `usage > search > state projection`。
- 本轮实现范围：
  - 新增 `libs/Providers/Sources/Providers/Codex/CodexSessionUsageIndex.swift`
  - `CodexSessionStore.loadSessionUsage(...)` 改为优先读取 `Application Support/Nolon/codex-sessions/usage-index-v1.sqlite`
  - 索引支持 `cache hit / delta append / full rebuild / file missing` 四条路径
  - 用 `file_id`（system file number）区分“原文件 append”与“同路径文件替换”，避免误走 tail 增量
- 设计边界保持不变：
  - 索引是可丢弃缓存，不是真源
  - 真源仍是 rollout 文件
  - 上层 `CodexSessionsTabServicing` 协议未改，ViewModel/UI 无需感知索引来源
- 验证结果：
  - `swift test --package-path libs/Providers --filter CodexSessionStoreTests` 通过
  - `xcodebuild test -skipPackageUpdates -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/CodexSessionsTabViewModelTests` 通过
- 环境备注：
  - 直接跑 `xcodebuild test` 会在依赖更新阶段受 `github.com` DNS 解析失败影响，因此本轮继续使用 `-skipPackageUpdates`

## Micro-Sync 01:28
- 修复 `Codex Sessions` 在 `按用量` 排序下“组和组之间看起来没有排序”的口径错位问题。
- 根因不是 section 排序函数失效，而是“组头展示口径”和“组排序口径”不一致：
  - `CodexSessionsTabViewModel.sortSections(...)` 一直按整组聚合总 token 排序。
  - `CodexSessionsSectionDataBuilder` 之前只聚合 `section.sessions`，而折叠态下这里通常只包含当前可见的前 5 条 row。
  - 结果是 UI 组头显示的 usage 只代表“可见子集”，用户看到的数字与实际排序依据不一致，因此误判为组间未按 usage 排序。
- 本轮修复：
  - `SessionSection` 新增 `usageSessionIDs`
  - 组头 usage 改为按整组全部 session 聚合，不再依赖折叠态可见 rows
  - 保持“组头聚合来源”和“列表当前可见 rows”两条数据链路分离
- 验证结果：
  - 新增 `testBDD_GivenCollapsedSectionWithHiddenSessionUsage_WhenBuildingSectionData_ThenGroupUsageIncludesHiddenSessions`
  - `xcodebuild test -skipPackageUpdates -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/CodexSessionsSectionDataBuilderTests` 通过
  - `xcodebuild test -skipPackageUpdates -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/CodexSessionsTabViewModelTests` 通过
  - `xcodebuild test -skipPackageUpdates -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/CodexSessionsOverviewDataBuilderTests -only-testing:nolonTests/CodexSessionsCardSnapshotTests` 通过
- 额外注意：
  - 并发触发两条 `xcodebuild test` 会命中 `build.db locked`，后续相关回归必须串行执行

## Micro-Sync 01:51
- 排查“编译 app 有问题”后确认，主 app 代码本身可通过：
  - `xcodebuild build -skipPackageUpdates -project nolon.xcodeproj -scheme nolon-app -configuration Release -destination 'platform=macOS'` 通过
  - `RUN_TESTS=0 ./build.sh` 通过
- 真正故障点在 `build.sh` 的包解析策略，而不是 app target：
  - 旧逻辑默认会让 `xcodebuild` 尝试更新 SwiftPM 远端依赖
  - 当前环境里 `github.com` DNS 解析失败时，会直接报 `Could not resolve package dependencies`
- 本轮调整：
  - `build.sh` 默认改为 `NO_SPM_UPDATE=1`
  - 构建与测试统一复用同一组 `xcodebuild` 公共参数
  - 默认只注入 `-skipPackageUpdates`，不再使用 `-disableAutomaticPackageResolution`
  - 原因：仓库当前 `Package.resolved` 处于 out-of-date 状态，禁用自动解析会把本地构建也一并拦死
  - 新增 `XCODEBUILD_BIN` 注入点，方便脚本级 smoke test
  - 新增 `scripts/tests/build-smoke.sh`
- 验证结果：
  - `bash scripts/tests/build-smoke.sh` 通过
  - `RUN_TESTS=0 ./build.sh` 通过
  - `SKIP_CLEAN=1 ./build.sh` 已可稳定进入 unit tests，不再因 SwiftPM 依赖更新失败提前退出
  - 当前完整门禁剩余 1 个既有失败：
    - `SkillInstallerTests/testBDD_GivenDirectLinkedProviderRoot_WhenScanningInstalledSkill_ThenMarkInstalled`
    - 失败断言：`XCTAssertEqual failed: ("nil") is not equal to ("Optional(NolonResourceKit.SkillInstallationState.installed)")`
    - 判定：这是测试/业务逻辑问题，不是编译或包解析问题

## Micro-Sync 09:08
- 修复 `SkillInstaller.scanProvider(...)` 在“provider 的 `skills` 根目录本身是 symlink，且直接指向全局 skills 根”场景下漏扫已安装技能的问题。
- 根因定位：
  - `SkillInstallRootResolver.resolvedScannableFolder(...)` 之前先判断 `isFolderExists`，会在 direct-linked root 场景直接返回原始 symlink 路径。
  - `SkillInstaller.scanDirectory(...)` 依赖这个返回值继续枚举，导致 `testBDD_GivenDirectLinkedProviderRoot_WhenScanningInstalledSkill_ThenMarkInstalled` 场景下扫描结果缺失 `karpathy-guidelines`。
  - 同类逻辑的 ancestor-linked root 场景本身不受影响，因为最终节点不是 symlink。
- 本轮实现：
  - 调整 `libs/Providers/Sources/NolonResourceKit/Infrastructure/ProviderSkillsLinkService.swift`
  - `resolvedScannableFolder(...)` 改为优先解析最终节点 symlink 的真实目录，再回退普通目录判断
  - 保持外部 reported path 不变，只修正扫描入口
- 验证结果：
  - `xcodebuild test -skipPackageUpdates -project nolon.xcodeproj -scheme nolon-app -destination 'platform=macOS' -only-testing:nolonTests/SkillInstallerTests/testBDD_GivenDirectLinkedProviderRoot_WhenScanningInstalledSkill_ThenMarkInstalled -only-testing:nolonTests/SkillInstallerTests/testBDD_GivenParentLinkedProviderRoot_WhenScanningInstalledSkill_ThenMarkInstalled` 通过
  - `xcodebuild test -skipPackageUpdates -project nolon.xcodeproj -scheme nolon-app -destination 'platform=macOS' -only-testing:nolonTests/SkillInstallerTests` 通过
  - `xcodebuild build -skipPackageUpdates -project nolon.xcodeproj -scheme nolon-app -destination 'platform=macOS'` 通过

## Micro-Sync 10:21
- 计划执行新版本发布与 DMG 打包，目标版本先按 `v2.1.15` 预估：
  - 当前工程 `MARKETING_VERSION = 2.1.14`
  - `HEAD` 相对 `v2.1.14` 仍有未发布提交，包含 codex sessions、provider usage、resource 扫描修复等内容
- 发布前置校验结果：
  - `./build.sh` 通过，包含 `Release` 构建与 unit tests，最终输出 `** TEST SUCCEEDED **`
  - 本机存在 `Developer ID Application: HAN LIN (3L8RM3MDLS)`，具备 DMG 签名基础
  - Sparkle 默认 keychain 账户 `ed25519` 可正常签名，`temp_sparkle/bin/sign_update --account ed25519 temp_sparkle/CHANGELOG` 成功返回 `sparkle:edSignature`
- 当前明确阻塞：
  - `curl -I --max-time 10 https://github.com`
  - `curl -I --max-time 10 https://api.github.com`
  - 两者均稳定返回 `curl: (28) Resolving timed out after 10005 milliseconds`
  - 这意味着 `git push`、`gh release`、GitHub assets 上传与 appcast 在线校验当前都无法可靠执行
- 发布决策：
  - 暂不运行 `scripts/release.sh`，避免在无外网解析能力时产生半完成的本地 commit/tag/release 状态
  - 待 GitHub DNS/网络恢复后，再从当前干净工作树直接执行正式发布

## Micro-Sync 10:40
- 正式发布 `v2.1.15` 完成，发布流程使用：
  - `NO_SPM_UPDATE=1 ./scripts/release.sh 2.1.15`
  - 发布脚本自动更新版本号、生成双架构 DMG、Sparkle 签名、更新 appcast、提交/tag/push、创建并发布 GitHub Release
- 构建与分发结果：
  - `arm64` DMG：`release/nolon-arm64.dmg`
  - `x86_64` DMG：`release/nolon-x86_64.dmg`
  - 两个 DMG 均完成 Apple notarization 并 `staple` 成功
  - Sparkle appcast 已更新到 `docs/appcast.xml` 与根目录 `appcast.xml`
- 发布产物与版本标识：
  - release commit：`7320450` (`chore(release): v2.1.15`)
  - git tag：`v2.1.15`
  - GitHub Release：`https://github.com/linhay/nolon/releases/tag/v2.1.15`
  - build number：`202604181029`
- 在线校验结果：
  - GitHub Pages build 完成
  - 在线 appcast 已确认包含 `2.1.15`
- 过程性记录：
  - 发布前为保持工作区干净，先提交记忆预检记录：`66aeb01` (`docs(memory): record release preflight status`)
  - 线上上一版为 `v2.1.14`，本次顺延发布为 `v2.1.15`

## Micro-Sync 10:52
- 重新收敛 `Codex Sessions` 点击会话后的内联详情卡片，目标是解决“展开后过高、信息堆叠、浏览节奏被打断”。
- 这轮只调整展示层，不改 ViewModel 和详情数据结构：
  - `nolon/Skills/Domain/Providers/Views/CodexSessionsTabView.swift`
  - `CodexSessionsDetailPanelView` 从“header + summary + command strip + action chips + metadata grid”改为更紧凑的内联信息面板
  - 保留标题、状态、摘要、resume 命令、项目/rollout 路径、元数据，但改成指标胶囊、单行路径条、metadata chips，减少纵向高度
  - 主操作收敛为 `Resume` 与 `Show in Finder`，复制类操作直接挂到命令条/路径条上
- 验证结果：
  - `xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/CodexSessionsCardSnapshotTests -skipPackageUpdates` 通过
  - 快照用例 `detail-panel-quick-command` 与 `inline-detail-selected-row` 已按新布局刷新

## Micro-Sync 11:22
- 对 `Codex Sessions` 详情卡完成第二轮重设计，这次补齐了用户明确要求的结构化用量、可复制会话标识、以及时间线口径。
- 文档先行已落地：
  - `docs-linhay/debate/20260418/codex-sessions/20260418-codex-sessions-detail-card-v01.md`
  - `docs-linhay/plans/2026-04-18-codex-sessions-detail-card-redesign-exec.md`
- 本轮实现范围：
  - `libs/Providers/Sources/Providers/Codex/CodexSessionStore.swift`
  - `nolon/Skills/Domain/Providers/Views/CodexSessionsTabView.swift`
  - `nolon/Skills/Domain/Providers/Views/CodexSessionsTabViewModel.swift`
  - `nolonTests/CodexSessionsCardSnapshotTests.swift`
  - `nolonTests/CodexSessionsTabViewModelTests.swift`
  - `libs/Providers/Tests/ProvidersTests/CodexTests/CodexSessionStoreTests.swift`
- 达成并落实的关键口径：
  - 时间只展示 `Started / Last activity`，不再伪装为“结束时间”
  - `Session ID` 与 `Thread ID` 分离展示与复制，不再复用 `displayID`
  - `Updated` 退回上下文辅助信息，与 `Last activity` 明确区分
  - 默认不再展示整条 resume command，保留 `Resume / Copy Command / Show in Finder`
  - `DB Rows / paths / forked-from / source / originator` 降级到 diagnostics
- 验证结果：
  - `xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -skipPackageUpdates -only-testing:nolonTests/CodexSessionsCardSnapshotTests -only-testing:nolonTests/CodexSessionsTabViewModelTests` 通过
  - `swift test --package-path libs/Providers --filter CodexSessionStoreTests` 通过

## Micro-Sync 11:36
- 按 reviewer 收口 `Codex Sessions` 详情卡与 timeline 语义，避免“看起来能用，但口径不一致”。
- 本轮补强：
  - `CodexSessionsTabView.swift`
    - `Last activity` 在 timeline loading / failed 态不再回退为 `Updated`
    - 去掉重复的 `Show in Finder` action chip
  - `CodexSessionsTabViewModel.swift`
    - 同一会话在 timeline 失败后，允许再次点击触发重试
    - 为 timeline 请求增加 request id 防止旧请求覆盖新状态
  - `CodexSessionStore.swift`
    - 空白 `threadID` 统一归一化为 `nil`
  - 测试：
    - store 增加 blank thread id 与 file metadata fallback 精确断言
    - 详情卡快照增加 loading / failed timeline 场景
    - ViewModel 增加 same-session retry 场景
- 验证结果：
  - `xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -skipPackageUpdates -only-testing:nolonTests/CodexSessionsCardSnapshotTests -only-testing:nolonTests/CodexSessionsTabViewModelTests` 通过
  - `swift test --package-path libs/Providers --filter CodexSessionStoreTests` 通过
- 当前保留风险：
  - timeline 旧请求防护已在实现中补齐，但基于现有 mock 的“删除 session 后旧请求回写”场景仍缺可靠自动化测试，后续需要单独改造 mock 或增加集成测试覆盖

## Micro-Sync 12:40
- 排查“会话列表加载不出来”时先验证了等价 CLI：
  - 已安装的 `~/.nolon/bin/nolon` 不包含 `codex session` 子命令，属于本机 CLI 二进制滞后
  - 源码版 `swift run --package-path libs/Providers nolon codex session list --provider codex` 可正常列出 `2648` 条会话，说明底层 session 扫描链路正常
- 根因收敛：
  - `CodexSessionsTabViewModel` 初次加载优先走 `snapshotStream`
  - 若流式加载失败，旧实现会直接清空 `sections` 并把 `didStartInitialLoad` 保持为 `true`
  - 由于页面通过 `CodexSessionsTabViewModelStore.shared` 复用同一个 ViewModel，再次进入页面只会调用 `loadIfNeeded()`，从而跳过重试，形成“CLI 有数据、UI 空白”的卡死状态
- 本轮修复：
  - `CodexSessionsTabViewModel.swift`
    - 初次 `snapshotStream` 失败时，自动回退到 `loadSnapshot`
    - 加载彻底失败时，将 `didStartInitialLoad` 复位，允许同一 ViewModel 后续重试
    - 新增 `load_stream_failed_fallback` 性能日志，区分流式失败与整体失败
  - `nolonTests/CodexSessionsTabViewModelTests.swift`
    - 新增 `snapshotStream` 失败但 `loadSnapshot` 成功时的回退测试
    - 新增首轮失败后二次 `loadIfNeeded()` 可恢复的回归测试
- 验证结果：
  - `xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/CodexSessionsTabViewModelTests/testBDD_GivenInitialSnapshotStreamFailsButSnapshotLoadSucceeds_WhenLoading_ThenViewModelFallsBackToSnapshotRows -only-testing:nolonTests/CodexSessionsTabViewModelTests/testBDD_GivenInitialLoadFails_WhenLoadIfNeededRunsAgain_ThenViewModelRetriesLoading` 通过

## Micro-Sync 13:20
- 收口 `nolon` 安装链与会话 CLI 漂移问题，避免再次出现“源码已支持 `codex session`，安装版却缺子命令”的误判。
- 本轮修复：
  - `scripts/tests/install-nolon-cli-smoke.sh`
    - 将 root/codex/probe help 断言同步为当前真实输出格式（`Usage: ...`）
    - 新增安装后二进制 `codex session --help`
    - 新增安装后二进制 `codex session list --help`
  - `docs-linhay/dev/nolon-cli-install-script.md`
    - 同步默认覆盖安装的真实行为
    - 补充 `--no-force` 语义与安装后二进制 help 验证覆盖面
- 本机环境同步：
  - 已执行 `bash scripts/install-nolon-cli.sh --configuration debug`
  - 复验 `~/.nolon/bin/nolon codex session --help` 成功
  - 复验 `~/.nolon/bin/nolon codex session list --provider codex` 成功，输出 `total: 2648 / live: 2250 / archived: 398`
- 验证结果：
  - `bash scripts/tests/install-nolon-cli-smoke.sh` 通过

## Micro-Sync 13:55
- 收口 `Codex Sessions` 的自动刷新、诊断桥接和 ViewModel 缓存策略，目标是修掉“重进页面不刷新”“流式失败后 UI 空白”“诊断只在日志里不可见”这三条链路缺陷。
- 最终行为口径：
  - 页面首次进入统一走 `handleViewAppearance()`，已加载页面按 staleness 决定是否 refresh
  - App 回到前台时按 staleness 检查；页面停留期间只做 60 秒一次的前台轮询检查
  - `CodexSessionStore.warning/performance` 透传 `codex_home_path`，overview footer 直接展示匹配 provider 的 warning / slow / failed 诊断
- 缓存取舍：
  - `CodexSessionsTabViewModelStore` 最终只保留“按 provider 复用 ViewModel”的身份缓存
  - 放弃 store 层 stale recreate / eviction
  - 原因是 `CodexSessionsTabViewModel` 析构路径存在稳定 `malloc: pointer being freed was not allocated` 崩溃，freshness 改为在 ViewModel 内部按时间戳刷新
- 失败回退：
  - 初次 `snapshotStream` 失败时自动回退 `loadSnapshot`
  - 初次加载整体失败时复位 `didStartInitialLoad`，允许同一 ViewModel 再次进入时重试
- 验证结果：
  - `xcodebuild test -skipPackageUpdates -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/CodexSessionsOverviewDataBuilderTests` 通过
  - `xcodebuild test -skipPackageUpdates -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/CodexSessionsTabViewModelTests` 37 项通过
  - `swift test --package-path libs/Providers --filter CodexSessionStoreTests` 25 项通过
  - 安装版 `~/.nolon/bin/nolon codex session list --provider codex` 成功返回 `total: 2648 / live: 2250 / archived: 398`

## Micro-Sync 14:15
- 对 `Codex Sessions` 行内详情卡做第三轮 IA 收口，这轮只重排布局，不改数据口径和 ViewModel 行为。
- 设计评审结论与落地一致：
  - 问题不是字段缺失，而是主次层级太平均、容器过多、复制动作分散
  - 旧版把 usage / identity / path / diagnostics 都做成并列卡片，inline 展开时视觉过厚
- 本轮新结构：
  - 抬头区：标题、状态、provider/group/updated 摘要、`Resume / Copy Command / Show in Finder`
  - 主面板：`Usage`、`Timeline`、`Identity`
  - 次级面板：`Context`
  - 低频信息：`Diagnostics` 折叠，默认收起
- 交互口径：
  - copy 动作回到对应字段右侧
  - 诊断信息不再默认铺开，不再和主阅读流抢注意力
- 验证结果：
  - `xcodebuild test -skipPackageUpdates -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/CodexSessionsCardSnapshotTests` 13 项通过

## Micro-Sync 15:18
- 现象：账户页 dashboard 的 “Aggregated Usage Trend” 中，今天和昨天的柱子经常看起来接近，但并非真实统计结果。
- 根因：
  - `NolonAccountsView.trendSamples()` 之前没有读取任何 daily usage 数据。
  - 它把 `rankingItems().map(\\.value)` 的总和当作 “today”，再人工拆成 `[today/3, today/2, today*2/3, today]` 四个占位柱。
  - `rankingItems()` 本身取的是 `UsageSummary.totalCount`，语义是账号/结果数量，不是 token 用量。
  - 因此账户页 trend 属于假数据展示，且 `7d/14d/30d/all` 切换此前不改变统计样本。
- 修复：
  - `NolonAccountsViewModel` 新增账户页 dashboard trend 聚合逻辑，按 provider 拉取 `ProviderTokenTrendSnapshot.points`，按自然日汇总 token。
  - 按窗口生成连续日期序列，缺失天补 0；超过 7 个柱时按连续区间分桶，避免面板溢出。
  - `NolonAccountsView` 改为直接使用 `viewModel.trendPanelSamples(for:)`，移除占位趋势算法。
- 验证结果：
  - `xcodebuild test -quiet -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/NolonAccountsViewModelTests/testBDD_GivenProviderTokenTrendPoints_WhenBuildingDashboardTrendSamples_ThenAggregatesActualTodayAndYesterdayValues -only-testing:nolonTests/NolonAccountsViewModelTests/testBDD_GivenFourteenDayWindow_WhenBuildingDashboardTrendSamples_ThenUsesRequestedWindowBuckets` 通过
