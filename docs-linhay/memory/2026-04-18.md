## Micro-Sync 00:25
- 完成 `Codex Sessions` 的 `usage` 独立索引落地，优先级按 2026-04-17 评估结论执行为 `usage > search > state projection`。
- 本轮实现范围：
  - 新增 `libs/Providers/Sources/Providers/Codex/CodexSessionUsageIndex.swift`
  - `CodexSessionStore.loadSessionUsage(...)` 改为优先读取 `Application Support/Nolon/codex-sessions/usage-index-v1.sqlite`
  - 索引支持 `cache hit / delta append / full rebuild / file missing` 四条路径
  - 用 `file_id`（system file number）区分“原文件 append”与“同路径文件替换”，避免误走 tail 增量
- 设计边界保持不变：
  - 索引是可丢弃缓存，不是真源
  - 真源仍是 rollout 文件
  - 上层 `CodexSessionsTabServicing` 协议未改，ViewModel/UI 无需感知索引来源
- 验证结果：
  - `swift test --package-path libs/Providers --filter CodexSessionStoreTests` 通过
  - `xcodebuild test -skipPackageUpdates -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/CodexSessionsTabViewModelTests` 通过
- 环境备注：
  - 直接跑 `xcodebuild test` 会在依赖更新阶段受 `github.com` DNS 解析失败影响，因此本轮继续使用 `-skipPackageUpdates`
