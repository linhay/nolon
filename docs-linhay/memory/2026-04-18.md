## Micro-Sync 00:25
- 完成 `Codex Sessions` 的 `usage` 独立索引落地，优先级按 2026-04-17 评估结论执行为 `usage > search > state projection`。
- 本轮实现范围：
  - 新增 `libs/Providers/Sources/Providers/Codex/CodexSessionUsageIndex.swift`
  - `CodexSessionStore.loadSessionUsage(...)` 改为优先读取 `Application Support/Nolon/codex-sessions/usage-index-v1.sqlite`
  - 索引支持 `cache hit / delta append / full rebuild / file missing` 四条路径
  - 用 `file_id`（system file number）区分“原文件 append”与“同路径文件替换”，避免误走 tail 增量
- 设计边界保持不变：
  - 索引是可丢弃缓存，不是真源
  - 真源仍是 rollout 文件
  - 上层 `CodexSessionsTabServicing` 协议未改，ViewModel/UI 无需感知索引来源
- 验证结果：
  - `swift test --package-path libs/Providers --filter CodexSessionStoreTests` 通过
  - `xcodebuild test -skipPackageUpdates -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/CodexSessionsTabViewModelTests` 通过
- 环境备注：
  - 直接跑 `xcodebuild test` 会在依赖更新阶段受 `github.com` DNS 解析失败影响，因此本轮继续使用 `-skipPackageUpdates`

## Micro-Sync 01:28
- 修复 `Codex Sessions` 在 `按用量` 排序下“组和组之间看起来没有排序”的口径错位问题。
- 根因不是 section 排序函数失效，而是“组头展示口径”和“组排序口径”不一致：
  - `CodexSessionsTabViewModel.sortSections(...)` 一直按整组聚合总 token 排序。
  - `CodexSessionsSectionDataBuilder` 之前只聚合 `section.sessions`，而折叠态下这里通常只包含当前可见的前 5 条 row。
  - 结果是 UI 组头显示的 usage 只代表“可见子集”，用户看到的数字与实际排序依据不一致，因此误判为组间未按 usage 排序。
- 本轮修复：
  - `SessionSection` 新增 `usageSessionIDs`
  - 组头 usage 改为按整组全部 session 聚合，不再依赖折叠态可见 rows
  - 保持“组头聚合来源”和“列表当前可见 rows”两条数据链路分离
- 验证结果：
  - 新增 `testBDD_GivenCollapsedSectionWithHiddenSessionUsage_WhenBuildingSectionData_ThenGroupUsageIncludesHiddenSessions`
  - `xcodebuild test -skipPackageUpdates -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/CodexSessionsSectionDataBuilderTests` 通过
  - `xcodebuild test -skipPackageUpdates -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/CodexSessionsTabViewModelTests` 通过
  - `xcodebuild test -skipPackageUpdates -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/CodexSessionsOverviewDataBuilderTests -only-testing:nolonTests/CodexSessionsCardSnapshotTests` 通过
- 额外注意：
  - 并发触发两条 `xcodebuild test` 会命中 `build.db locked`，后续相关回归必须串行执行

## Micro-Sync 01:51
- 排查“编译 app 有问题”后确认，主 app 代码本身可通过：
  - `xcodebuild build -skipPackageUpdates -project nolon.xcodeproj -scheme nolon-app -configuration Release -destination 'platform=macOS'` 通过
  - `RUN_TESTS=0 ./build.sh` 通过
- 真正故障点在 `build.sh` 的包解析策略，而不是 app target：
  - 旧逻辑默认会让 `xcodebuild` 尝试更新 SwiftPM 远端依赖
  - 当前环境里 `github.com` DNS 解析失败时，会直接报 `Could not resolve package dependencies`
- 本轮调整：
  - `build.sh` 默认改为 `NO_SPM_UPDATE=1`
  - 构建与测试统一复用同一组 `xcodebuild` 公共参数
  - 默认只注入 `-skipPackageUpdates`，不再使用 `-disableAutomaticPackageResolution`
  - 原因：仓库当前 `Package.resolved` 处于 out-of-date 状态，禁用自动解析会把本地构建也一并拦死
  - 新增 `XCODEBUILD_BIN` 注入点，方便脚本级 smoke test
  - 新增 `scripts/tests/build-smoke.sh`
- 验证结果：
  - `bash scripts/tests/build-smoke.sh` 通过
  - `RUN_TESTS=0 ./build.sh` 通过
  - `SKIP_CLEAN=1 ./build.sh` 已可稳定进入 unit tests，不再因 SwiftPM 依赖更新失败提前退出
  - 当前完整门禁剩余 1 个既有失败：
    - `SkillInstallerTests/testBDD_GivenDirectLinkedProviderRoot_WhenScanningInstalledSkill_ThenMarkInstalled`
    - 失败断言：`XCTAssertEqual failed: ("nil") is not equal to ("Optional(NolonResourceKit.SkillInstallationState.installed)")`
    - 判定：这是测试/业务逻辑问题，不是编译或包解析问题

## Micro-Sync 09:08
- 修复 `SkillInstaller.scanProvider(...)` 在“provider 的 `skills` 根目录本身是 symlink，且直接指向全局 skills 根”场景下漏扫已安装技能的问题。
- 根因定位：
  - `SkillInstallRootResolver.resolvedScannableFolder(...)` 之前先判断 `isFolderExists`，会在 direct-linked root 场景直接返回原始 symlink 路径。
  - `SkillInstaller.scanDirectory(...)` 依赖这个返回值继续枚举，导致 `testBDD_GivenDirectLinkedProviderRoot_WhenScanningInstalledSkill_ThenMarkInstalled` 场景下扫描结果缺失 `karpathy-guidelines`。
  - 同类逻辑的 ancestor-linked root 场景本身不受影响，因为最终节点不是 symlink。
- 本轮实现：
  - 调整 `libs/Providers/Sources/NolonResourceKit/Infrastructure/ProviderSkillsLinkService.swift`
  - `resolvedScannableFolder(...)` 改为优先解析最终节点 symlink 的真实目录，再回退普通目录判断
  - 保持外部 reported path 不变，只修正扫描入口
- 验证结果：
  - `xcodebuild test -skipPackageUpdates -project nolon.xcodeproj -scheme nolon-app -destination 'platform=macOS' -only-testing:nolonTests/SkillInstallerTests/testBDD_GivenDirectLinkedProviderRoot_WhenScanningInstalledSkill_ThenMarkInstalled -only-testing:nolonTests/SkillInstallerTests/testBDD_GivenParentLinkedProviderRoot_WhenScanningInstalledSkill_ThenMarkInstalled` 通过
  - `xcodebuild test -skipPackageUpdates -project nolon.xcodeproj -scheme nolon-app -destination 'platform=macOS' -only-testing:nolonTests/SkillInstallerTests` 通过
  - `xcodebuild build -skipPackageUpdates -project nolon.xcodeproj -scheme nolon-app -destination 'platform=macOS'` 通过
