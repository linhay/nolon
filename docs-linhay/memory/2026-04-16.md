## Micro-Sync 00
- 修复 `ProviderUsageMonitor.swift` 中 `Cannot find 'GitHubCLITokenResolver' in scope` 的 Xcode 编译问题。
- 根因：
  - `GitHubCLITokenResolver` 作为新增 `ProviderUsage` source file 时，SwiftPM 可识别，但 Xcode 对本地 package 新增文件的感知不稳定。
- 修复：
  - 将 `GitHubCLITokenResolver` 并回现有文件 `TokenAccounts.swift`
  - 删除独立 `GitHubCLITokenResolver.swift`，避免继续依赖 “新增 package 文件被 Xcode 正确纳入” 这一前提
- 验证：
  - `swift build --package-path libs/Providers --target ProviderUsage`
  - `swift build --package-path libs/Providers --target NolonCoreCLIKit`

## Micro-Sync 01
- 修复 Copilot usage 在 app UI 仍显示 `Missing token for copilot.` 的问题。
- 根因：
  - Copilot 官方 CLI 当前登录态来自 `gh auth token`
  - app 的 GUI 进程环境中，`PATH` 可能不包含 Homebrew 路径，导致 `GitHubCLITokenResolver` 原先仅靠普通 PATH 查找时解析不到 `gh`
- 修复：
  - `GitHubCLITokenResolver` 改为优先使用 `SKProcessRunner.resolveExecutableInUserShellSync` 解析 `gh`
  - 普通 PATH 查找改为 fallback
  - `PATH` 处理改为补强常见路径，而不是仅在空值时覆盖
  - 进一步对齐 Codex 的环境加载口径：执行 `gh auth token` 前先合并用户 shell 环境，再应用显式环境覆盖
  - Copilot usage 拉取时补充 GitHub `/user` viewer profile，用于展示 login / name，避免 UI 落到 `Unknown Account`
  - Copilot plan 展示改为 humanize，例如 `individual -> Individual`
  - 修复 viewer profile 一直拿不到的问题：`CopilotUsageFetcher` 原先对 `/user` 也带了无效的 `X-Github-Api-Version: 2025-04-01`，GitHub REST 实际返回 400；已改为稳定版本 `2022-11-28`
- 测试：
  - 新增 `ProviderUsageTokenAccountsTests`
    - `GitHub CLI resolver prefers shell lookup when app PATH is incomplete`
    - `GitHub CLI resolver supplements PATH with common binary locations`
    - `GitHub CLI resolver merges user shell environment before explicit overrides`
  - 新增/更新 `CopilotUsageDescriptorTests`
  - 更新 `NolonCoreCLIKitTests+Copilot`
- 验证：
  - `swift build --package-path libs/Providers --target ProviderUsage`
  - `swift build --package-path libs/Providers --target NolonCoreCLIKit`
  - `swift test --package-path libs/Providers --filter ProviderUsageTokenAccountsTests`
  - `swift test --package-path libs/Providers --filter ProviderUsageMonitorServiceTests`
  - `swift run --package-path libs/Providers nolon copilot auth status --provider copilot`
  - `swift run --package-path libs/Providers nolon copilot auth usage --provider copilot`
  - 现场确认 `copilot auth usage` 输出从 `label: GitHub CLI / plan: individual` 变为 `label: linhay / plan: Individual`

## Micro-Sync 02
- 修复 Codex 启动时的 MCP 警告收口问题。
- 根因：
  - 历史测试曾把 `alpha`、`zeta` 及 `reg-*` 测试 server 名泄漏进真实 `~/.codex/config.toml`
  - 当前剩余 warning 分别来自未登录的 `figma` 和超时的 `xcode`
- 修复：
  - 清理真实 `~/.codex/config.toml` 中遗留的 `alpha` / `zeta` / `reg-*` MCP 段落
  - 将 `NolonResourceKitTests.swift` 中 linked MCP 用例的通用测试名改为 `reg-linked-alpha` / `reg-linked-beta`
  - 在 `~/.nolon/mcps/figma.json` 源配置中写入 `enabled: false`
  - 在真实 `~/.codex/config.toml` 中将 `figma` 与 `xcode` 显式设为 `enabled = false`
- 验证：
  - `swift test --package-path libs/Providers --filter NolonResourceKitTests`
  - 启动 `codex` 复核，确认不再出现 `alpha` / `zeta` / `reg-*` / `figma` / `xcode` 相关 warning

## Micro-Sync 03
- 对 `Codex Sessions` tab 上方 `overview card` 发起一轮 `$debate`，范围只限定在 overview card，不扩展到 section rows。
- 关键代码事实：
  - `overview card` 当前固定为 `header -> segmented picker -> statusBanners -> metrics` 四层纵向堆叠
  - 外层常量偏大：`spacing: 18`、`.padding(22)`、refresh `.controlSize(.large)`、metric `minHeight: 82`
  - subtitle 为长说明句，当前完整展开
  - status 区允许 `statusMessage` 与 `backgroundScanningMessage` 双 banner 叠加
- 有效结论：
  - 不建议只做纯常量压缩
  - 最小正确方案应为：
    1. 把 `segmented picker` 并入 header 控制区
    2. 同时收紧 `spacing/padding/controlSize/metric minHeight`
    3. 如窄宽仍偏高，再把 subtitle 限制为最多 2 行
    4. 若状态区仍压首屏，再考虑把双 banner 合并为更轻的单条状态展示
- 辩论文档：
  - `docs-linhay/debate/20260416/codex-sessions-overview-card/20260416-codex-sessions-overview-card-v01.md`
- 工具阻塞：
  - Claude Code CLI 本轮未能参与有效发言，`claude -p` / `claude --model sonnet -p` / `claude --bare --model sonnet -p` 均被本机错误模型配置 `gpt-5(medium)` 阻塞

## Micro-Sync 04
- 已按 `Micro-Sync 03` 的结论直接实现 `Codex Sessions` overview card 压缩版。
- 实现：
  - `libs/NolonUI/Sources/NolonUI/Components/Shared/UnifiedCodexSessionViews.swift`
    - 外层压缩为 `spacing: 12`、`padding: 16`
    - 把 `segmented picker` 并入 header controls，与 refresh 同组
    - header 图标、标题、副标题、refresh control size 全部缩紧
    - subtitle 限为最多 2 行，并补 `help`
    - status 区从全宽 banner 改为 `FlowLayout` 下的轻量 capsule 状态条
    - metrics grid 压缩为更小的 `minimum/spacing/minHeight`
  - `nolonTests/CodexSessionsCardSnapshotTests.swift`
    - 新增独立 overview 快照用例 `overview card keeps compact controls under status pressure`
    - 复用 `makeOverviewCard()` 统一 overview 快照数据
- 验证：
  - `xcodebuild build -quiet -project nolon.xcodeproj -scheme nolon -destination 'platform=macOS'` 通过
  - `xcodebuild test -quiet -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/CodexSessionsCardSnapshotTests` 未能执行完成
- 当前测试阻塞：
  - `nolonTests/ProviderUsageUnifiedAccountsPipelineTests.swift` 存在现有编译错误：
    - `ClaudeAccountEditorPreviewBuilder.applyingSettingsPreviewJSON` 不存在
    - `.edit` / `.apiKey` 上下文推断失败
  - 因为 `nolon-tests` 目标需先完成测试编译，所以即使只跑 `CodexSessionsCardSnapshotTests` 也会被该错误拦住

## Micro-Sync 05
- 完成 Claude 账号编辑页 JSON 区双向联动改造，替换此前“只读预览”。
- 关键决策：
  - JSON 区与表单草稿共用同一套映射规则，避免“预览看到的 JSON”和“实际保存写回的 JSON”分叉。
  - 只接受当前表单能完整表达的 `env` 片段；未知根字段、未知 env 键、同时出现 `ANTHROPIC_AUTH_TOKEN` 与 `ANTHROPIC_API_KEY` 时直接报错，不做静默降级。
  - `Format JSON` 会按真实写回语义格式化，空模型字段仍按官方空值语义被省略。
- 实现：
  - `ProviderUsageSubViewModels.swift`
    - 为 `AccountEditorDraft` 新增 JSON 解析与回写能力
    - 新增 `ClaudeAccountEditorPreviewBuilder.applyingSettingsPreviewJSON`
    - 新增 JSON 同步错误类型与本地化文案
  - `ClaudeAccountEditorSheet.swift`
    - 将只读 `Text` 预览替换为基于 `WebCodeEditorView` 的可编辑 JSON 区
    - 新增表单 -> JSON、JSON -> 表单双向同步
    - 新增 `Format JSON` 动作与联动错误提示
  - `UnifiedEditorSharedViews.swift`
    - 为公共 Web 编辑器补充可选 `onTextChanged` 回调，供 Claude 编辑页实时消费
  - `ClaudeAccountEditorSheetSnapshotTests.swift`
    - 因 `WKWebView` 图像快照在测试环境下不稳定，改为更稳的渲染 smoke test：验证 JSON 编辑器已嵌入、编辑态字段已预填充
- 验证：
  - `xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO CODE_SIGN_IDENTITY='' -only-testing:nolonTests/ProviderUsageUnifiedAccountsPipelineTests -only-testing:nolonTests/ClaudeAccountEditorSheetSnapshotTests -quiet`

## Micro-Sync 06
- 修正 `Codex Sessions` 在 section 折叠态下的预览策略。
- 根因：
  - 当前 session row 还没有 inline detail 展开能力，折叠态本质上只是“预览态”。
  - 原实现按全量排序后直接截前 5 条，导致 archived 会话可能挤占预览位，和“优先看 live 会话”的使用预期不一致。
- 修复：
  - `CodexSessionsTabViewModel` 新增 `visibleSessions(for:isExpanded:)`
  - section 展开时仍显示全量 session
  - section 折叠时优先显示前 5 条 live session
  - 若当前 section 没有 live session，则 fallback 为前 5 条全量 session，避免空白卡片
  - 可见 rows 重建后同步修复 `selectedSessionID`，保证默认选中仍落在当前可见列表内
- 测试：
  - `CodexSessionsTabViewModelTests`
    - `testBDD_GivenMixedLiveAndArchivedSessions_WhenSectionIsCollapsed_ThenOnlyTopFiveLiveSessionsAreVisible`
- 验证：
  - `xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/CodexSessionsTabViewModelTests`

## Micro-Sync 07
- 修复 `Codex Sessions` tab 切走再返回时反复重新加载数据的问题。
- 根因：
  - `MainSplitViewModel.selectedTab` 本身已持久化，但 `CodexSessionsTabView` 每次出现都会新建页面级 `CodexSessionsTabViewModel`
  - 页面进入时固定执行 `load()`，导致切换到其他 tab 再回来会重新扫描 session snapshot
- 修复：
  - 新增 `CodexSessionsTabViewModelStore.shared`，按 `provider.id` 复用 `CodexSessionsTabViewModel`
  - `CodexSessionsTabView` 初始化改为优先从 store 取 VM
  - 页面入口加载从 `load()` 改为 `loadIfNeeded()`，仅首次进入自动扫描；显式 refresh 仍走 `refresh()`
- 测试：
  - `CodexSessionsTabViewModelTests`
    - 新增 `GivenSessionsViewModel_WhenLoadIfNeededRunsTwice_ThenSnapshotStreamLoadsOnlyOnce`
    - mock service 增加 `snapshotStreamCallCount` 计数，验证首次加载后不会重复触发 stream 扫描
- 验证：
  - `xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/CodexSessionsTabViewModelTests`

## Micro-Sync 08
- 修复 Codex usage 中 `ALL` 总用量卡片错误复用 30 天历史的问题。
- 根因：
  - `ProviderUsage.CodexTokenTrendService.fetchGlobalSnapshot(trailingDays:)` 之前把 `trailingDays` 直接传给底层 `CostUsageFetcher`
  - 当页面默认处于 `30D` 范围时，底层返回的 `daily` 仅包含近 30 天数据，导致 UI 虽然有 `allDaysTokens` 字段，但实际只能从 30 天切片里求和
  - 结果就是 `ALL` 卡片数值和 `30 Days` 卡片长期一致
- 修复：
  - `CodexTokenTrendService` 改为始终加载全历史快照，再在 service 内按当前范围裁剪图表 `points`
  - 保持 `today / 7d / 30d / all` 四张 summary card 都基于完整历史独立计算
- 测试：
  - 更新 `CodexTokenTrendServiceTests.keepsSummaryMetricsStableWhileSlicingChartRange`
  - 新断言要求 loader 在 `7d/30d` 图表范围下也必须请求全历史（`trailingDays == nil`）
- 验证：
  - `swift test --package-path libs/Providers --filter CodexTokenTrendServiceTests`

## Micro-Sync 09
- 对 `Codex Sessions overview card` 的上一轮 UI debate 做了引申讨论，聚焦后续产品功能与 `$debate` 能力建设。
- 有效结论：
  - 产品侧：
    - `overview card` 不应停留在本轮“压缩版静态卡片”，后续应考虑 `Compact / Diagnostic` 两档总览模式
    - 当前 `statusMessage/backgroundScanningMessage` 不宜继续扩展为多字符串 banner，后续更适合升级为带优先级、动作位和进度的状态中心
    - 当前单一 `Refresh` 按钮和后台扫描提示存在语义断层，后续应评估“全量刷新 / 后台增量扫描 / 重试 / 取消”等后台同步任务入口
  - 工程侧：
    - `CodexSessionsCardSnapshotTests` 对视觉回归有价值，但对运行时状态组合覆盖不足
    - 后续要补 `overview` builder / mapper 的状态矩阵测试，不再只依赖快照
  - debate 能力侧：
    - 优先建设 `证据校验器`
    - 增加参与者 preflight 与自动降级到备用席位
    - 主持人需要维护“代码证据 x 议题维度”覆盖矩阵，至少包含 UI / 状态模型 / 测试 / 主持风险
- 文档：
  - `docs-linhay/debate/20260416/codex-sessions/20260416-codex-sessions-debate-followups-v01.md`
- 工具事实：
  - 本轮 Gemini CLI 提供了有效观点
  - Claude Code 再次因模型访问错误未能形成有效论点

## Micro-Sync 10
- `Provider Usage` Phase 3 首批双 tab 已落地到 provider 详情页：
  - `codex`、`gemini`、`antigravity` 现在会显示独立的 `Accounts` 与 `Usage` tab
  - `Accounts` 只渲染账号管理区
  - `Usage` 只渲染 token trend / intraday drilldown
  - 非拆分 provider 的 `usage` 继续保留 `combined` 兼容模式
- 实现要点：
  - `ProviderContentTabType.availableTabs(for:)` 增加“首批双 tab provider”自动补 `accounts` 的规则
  - `ProviderUsageRootViewModel` 新增 `ProviderUsagePageMode`，按页面模式分发标题、page marker 与 `loadIfNeeded`
  - `ProviderDetailGridView` 的 `.accounts` / `.usage` 已改为不同页面模式
  - `ProviderUsageView` 已按 `accounts / usage / combined` 三种模式拆内容与加载入口
- 范围限制：
  - `claude` 暂未进入本轮，因为当前 provider 详情页还没有稳定的 token trend 独立页语义
  - `codexXcode` 继续不暴露 `accounts`，账号与用量仍由 Xcode 管理
- 测试：
  - 新增 `ProviderAccountUsageTabConfigurationTests`
  - 更新 `ProviderUsageRootLoadBoundaryTests`
  - 定向验证通过：
    - `xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/ProviderAccountUsageTabConfigurationTests -only-testing:nolonTests/ProviderUsageRootLoadBoundaryTests -only-testing:nolonTests/CodexSessionsTabConfigurationTests -only-testing:nolonTests/GeminiUsageTabConfigurationTests -only-testing:nolonTests/ProviderUsageSubViewModelWiringTests -only-testing:nolonTests/ProviderTokenTrendViewModelParityTests -only-testing:nolonTests/ProviderUsageAccountsViewModelParityTests`
  - 结果：`20` 个定向测试通过，`0` 失败
- 额外阻塞修复：
  - `CodexConfigEditorSheet.swift` 存在工作区现有括号不配对，导致 `xcodebuild` 无法编译
  - 已做最小语法修复，只补结构闭合，不调整该文件其它逻辑

## Micro-Sync 11
- 参考 `cc-switch` 重新收敛了 Codex relay 配置边界：
  - 保留 `query_params`
  - 移除 app 层对 relay `headers` 的结构化支持
- 结论依据：
  - `cc-switch` 的 Codex provider preset 确实使用 `query_params`，典型场景是 Azure OpenAI 的 `api-version`
  - `cc-switch` 的 Codex account/provider config 主编辑路径没有把 `headers/http_headers` 当成常规账号字段
- 本次实现：
  - `CodexConfigEditorDraft` 删除 `headersText`
  - `auth.json` 预览和双向同步不再生成/接受 `nolon.relay.headers`
  - `config.toml` 预览不再生成 `http_headers`
  - 保存和验证链路只消费 `base_url + model_provider + query_params`
- 测试与验证：
  - `xcodebuild build -project nolon.xcodeproj -scheme nolon -destination 'platform=macOS' CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO CODE_SIGN_IDENTITY='' -quiet` 通过
  - 定向逻辑测试继续验证 `ProviderUsageUnifiedAccountsPipelineTests` 与 `ProviderUsageEngineValidateConfiguredAccountTests`
  - `CodexConfigEditorSheetSnapshotTests` 仍有快照/宿主环境问题，未作为本轮逻辑回归通过条件

## Micro-Sync 12
- 针对用户追问“1-4 哪些应该先实现、能不能实现”补了一轮收口型 `$debate`，目标不再是发散产品想法，而是裁定可实现性和优先级。
- 结论：
  - 可以先做：`1. overview card 的 Compact / Diagnostic 两档模式`、`4. overview 状态矩阵测试`
  - 能做但不建议现在先做：`2. 状态中心`
  - 能做但必须单独设计：`3. 后台同步任务模型`
- 关键依据：
  - `1` 主要是 `CodexSessionsOverviewCardView` 和 overview 展示策略调整，风险最低
  - `4` 当前最缺，能先给后续状态重构提供测试护栏
  - `2` 受限于 `CodexSessionsOverviewData` 仍是平铺字段，以及 `CodexSessionsTabViewModel` 只有散装布尔状态
  - `3` 成本最高，因为当前没有后台任务实体、取消/重试模型、分页状态或刷新任务状态机
- 文档：
  - `docs-linhay/debate/20260416/codex-sessions/20260416-codex-sessions-implementation-feasibility-v01.md`
- 工具事实：
  - Gemini CLI 提供了有效观点并同意 `4` 应优先
  - Claude Code 因模型访问错误阻塞
  - Codex subagent 因 `auth_unavailable` 阻塞

## Micro-Sync 13
- 已按 `4 -> 1` 完成 `Codex Sessions overview` 落地：
  - 新增 `CodexSessionsOverviewDataBuilder`，把 overview 状态拼装从 `CodexSessionsTabView` 里抽出
  - `CodexSessionsOverviewData` 新增 `displayMode`，支持 `compact / diagnostic`
  - `CodexSessionsOverviewCardView` 按模式切换 subtitle 长度、metrics 集合和整体密度
- 关键契约：
  - idle 浏览态默认 `compact`
  - `isLoading && hasVisibleSections`、`isPreparingRewrite`、`isApplyingRewrite` 自动切到 `diagnostic`
  - `compact` 只保留 `sessions / groups / attention`
  - `diagnostic` 增加 `rewritable`
- 回归验证：
  - `xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/CodexSessionsOverviewDataBuilderTests`
  - `xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/CodexSessionsCardSnapshotTests`
  - `xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/CodexSessionsTabViewModelTests`
  - 结果：`25` 个定向测试通过，`0` 失败
- 额外收口：
  - `ProviderUsage` 中 `ClaudeSessionUsageSupport.defaultProjectsRoots` 的默认参数写法触发 Swift 编译器崩溃，导致测试目标无法编译
  - 已拆成显式零参入口和显式 `environment` 入口，行为不变，只消除编译器歧义

## Micro-Sync 14
- 已把 `claude` 纳入 provider 详情页双 tab：
  - `ProviderContentTabType.availableTabs(for:)` 现在会对 `claude` 自动补出 `Accounts`，并排在 `Usage` 之前
  - `ProviderDetailGridView` 与 `ProviderUsageView` 继续复用既有 `accounts / usage / combined` 页面模式，不额外引入新页面事实源
- 已补齐 `claude` token trend / intraday 逻辑：
  - 新增 `ClaudeSessionUsageSupport`、`ClaudeTokenTrendService`、`ClaudeIntradayUsageService`
  - 数据来源不是 Claude web usage API，而是本地 Claude session `jsonl` 日志聚合
  - 解析时只保留带 usage 的 assistant 事件，并按 `sessionId|messageId|requestId|isSidechain` 做 cumulative chunk 去重
  - `input_tokens + cache_creation_input_tokens` 合并记入 input，`total = adjustedInput + cacheRead + output`
  - 会排除明显的 Vertex AI 记录，避免把非 Claude native session 混进 trend
- app 接线同步完成：
  - `ProviderUsageEngine` 已支持 `claude` 的 token trend fetch、intraday fetch、loading skeleton 与 capability
  - `ProviderUsageAccountsViewModel+CardModels` 已让 `claude` 账号页显示 token trend 入口语义
  - 顺手把 `antigravity` 的 token trend / intraday capability guard 对齐，避免已拆 tab 但 usage 页空白
- 测试与验证：
  - `xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/ProviderAccountUsageTabConfigurationTests -only-testing:nolonTests/ClaudeUsageTokenTrendViewModelTests`
  - 结果：`8` 个测试通过，`0` 失败
  - `swift build --package-path libs/Providers --target ProviderUsage` 通过，确认 Claude usage service 可独立编译
  - `swift test --package-path libs/Providers --filter 'ClaudeTokenTrendServiceTests|ClaudeIntradayUsageServiceTests'` 仍被仓库既有失败阻塞：
    - `NolonSkillsRepositoryServiceTests.swift:355`
    - `NolonCoreCLIKitTests+SearchInstallAndRemoteErrors.swift:498`
    - 错误均为 `STPath` 缺少 `subpath`，与本轮 Claude 改动无关

## Micro-Sync 14:17
- 已修复 `ProviderUsageEngine` 对 Claude token trend / intraday 的编译入口问题：
  - 不再在 app 层引用不存在的统一 service 类型
  - 改为由 `ProviderUsageRegistry` 暴露公开 facade，集中分发 `codex / claude / gemini / antigravity` 的 trend 与 intraday 查询
- app 侧现状：
  - `ProviderUsageEngine` 已接入 `claudeTokenTrendFetchAction`
  - `claude` / `gemini` / `antigravity` 均纳入 usage curve capability 与 loading skeleton 判定
  - `codex` / `claude` / `gemini` / `antigravity` 的 intraday 默认入口都已改走 `ProviderUsageRegistry`
- 验证结果：
  - `swift build --package-path libs/Providers --target ProviderUsage` 通过
  - `xcodebuild build -project nolon.xcodeproj -scheme nolon -destination 'platform=macOS' -derivedDataPath /tmp/nolon-app-launch-fix` 通过
  - `xcodebuild build -project nolon.xcodeproj -scheme nolon-app -destination 'platform=macOS' -derivedDataPath /tmp/nolon-gui-launch` 通过
  - 已实际拉起 `/tmp/nolon-gui-launch/Build/Products/Debug/nolon.app`，进程存活：`/private/tmp/nolon-gui-launch/Build/Products/Debug/nolon.app/Contents/MacOS/nolon`
- 当前残余风险：
  - `swift test --package-path libs/Providers --filter ClaudeTokenTrendServiceTests` 仍被仓库既有测试失败阻塞，不是本次改动引入
  - 失败点在 `NolonCoreCLIKitTests+SearchInstallAndRemoteErrors.swift:498` 与 `NolonSkillsRepositoryServiceTests.swift:355`，错误均为 `STPath` 无 `subpath`

## Micro-Sync 14:35
- 已完成一轮 `debate` 调查 Claude “今日用量”与上游统计不一致的问题，结论分三层：
  - 预期口径差异：当前 Claude Usage 页明确扫描本地 session 日志，不走官方 usage API
  - 日界线差异：当前 `todayTokens` 按 `TimeZone.current` 切天，而官方 daily usage / analytics 示例按 UTC 边界返回
  - 真实实现 bug：当前 `ClaudeSessionUsageSupport` 的 dedupeKey 包含 `isSidechain`，可能把同一 request 在 parent / subagent 两份日志里双计
- 当前 Claude 本地统计规则：
  - 只解析 `type == assistant` 且带 `message.usage` 的行
  - 总量公式为 `input + cache_creation + cache_read + output`
  - `inputTokens` 字段实际包含 `input + cache_creation`
  - intraday 展示层会隐藏未来时间桶和无 usage 的 bucket，但这不影响 `todayTokens`
- 已产出辩论文档：
  - `docs-linhay/debate/20260416/claude-usage-rule-mismatch/20260416-claude-usage-rule-mismatch-v01.md`
- 后续如果要修：
  - 先把 Claude 去重改成 canonical key = `sessionId + messageId + requestId`
  - 补 parent/subagent 双份日志回归测试
  - 再决定产品是否要把“今日”统一到 UTC，以对齐上游 daily usage

## Micro-Sync 15:40
- 已完成一轮 `codex` Usage 规则核查，结论与 Claude 不同，当前最关键的是口径差异而不是 cached 算式错误：
  - `codex` Usage tab 当前事实源是本地 `sessions/*.jsonl` 扫描，不是上游 Usage API / Dashboard
  - `CodexTokenTrendService` / `CodexIntradayUsageService` 都会主动移除 `CODEX_HOME`，因此 Usage tab 当前是 `global ~/.codex` 口径，而不是账号隔离口径
  - 本地“今日”按 `TimeZone.current` 切天；OpenAI 官方 `API Usage Dashboard` 文档明确写明使用 `UTC`
  - `cached_input_tokens` 不是 total 之外额外再加的一份输入；本地测试样例 `input=100 cached=80 output=30 total=130` 已证明 total 仍是 `input + output`
- 已产出辩论文档：
  - `docs-linhay/debate/20260416/codex-usage-rule-mismatch/20260416-codex-usage-rule-mismatch-v01.md`
- 当前判断：
  - 如果用户对比的是上游某个账号/项目的官方统计，本地 Codex Usage 页与其不一致是可预期的，主因是 `scope + timezone + data source` 三重口径差异
  - 当前实现层面的明确缺口是：Usage tab 没有暴露账号级来源和时区口径，容易让用户误认为它与上游统计完全同源

## Micro-Sync 16:05
- 已用本机 `~/.codex/sessions/2026/04/16/*.jsonl` 做实测回算，新的高优先级结论是：
  - 今天共扫到 `40` 个 Codex session 文件
  - 按当前本地 raw 口径汇总，today raw total 约 `455.3M`
  - 如果把 cached input 只按 `50%` 权重折算，有效用量约 `248.9M`，是 raw 的 `54.7%`
  - 这个比例与用户反馈“上游只有我们的一半”高度吻合
- 关键推断：
  - 本地 Usage 页当前更像“raw tokens trend”
  - 上游用户正在看的那套数，更像“对 cached input 折价后的有效用量 / 计费用量”，而不是 raw total
- 重要补充：
  - 这不是“我们把 cached 漏算了”，而是“我们把 cached 按 1x 展示了”
  - 多个大 session 的 cached 占 raw 比例超过 `90%`，所以一旦上游对 cached 折价，差距会被迅速放大

## Micro-Sync 16:25
- 已按 `debate` 协作模式把 `nolon` 与参考项目 `cc-switch` 做代码级对比，主持裁定如下：
  - 时间口径：双方都按本地时区切日，不是 UTC；只有“本地自然日窗口” vs “秒级滑动窗口”的边界差，不足以解释“上游只有本地一半”
  - token 口径：双方 `totalTokens` 都是 `input + output`，缓存读写单列展示，不会再额外叠加进 total
  - cost 口径：`cc-switch` 与 `nolon` 都对 cached input 做独立折价计费；`nolon` 的 `CostUsagePricing.codexCostUSD` 已确认与 `cc-switch` 同方向
  - 因此：如果继续存在巨大差异，优先怀疑“数据源 / scope / 重复来源”，而不是“时间”或“cached 成本公式”
- 已产出辩论文档：
  - `docs-linhay/debate/20260416/codex-vs-cc-switch-usage-boundary/20260416-codex-vs-cc-switch-usage-boundary-v01.md`

## Micro-Sync 16:52
- 已继续完成一轮 `debate` 深挖，当前 `codex` 异常的主因进一步收敛：
  - `CodexTokenTrendService.fetchGlobalSnapshot(...)` 与 `CodexIntradayUsageService.fetchGlobalSnapshot(...)` 都会主动移除 `CODEX_HOME`，因此当前 Usage 页明确走 `global ~/.codex`，不是当前账号 / 当前项目口径
  - `ProviderUsageRegistry` 与 `ProviderUsageEngine.refreshTokenTrend()` 也都固定把 Codex 分发到这条 `global` 链路
  - scanner 去重只看 `fileIdentity` 与 `session_meta.payload.id`；不会折叠不同 `id` 的 `subagent / explorer / forked` rollout
- 本机抽样观察（2026-04-16 16:49 左右，目录仍在持续新增文件）：
  - `~/.codex/sessions/2026/04/16` 抽样时共有 `50` 个 `.jsonl`
  - 至少混有 `nolon`、`dxyer`、`~/.nolon/skills/debate`、`ohos-clinmaster`、`Overloaded-org`、`linhey.wiki` 六类 `cwd`
  - 因此“本地比上游显著偏高”最有解释力的原因已从“时间差异”转为“global scope 混算 + 派生会话全部独立计入”
- 对 16:05 那条“cached 折价后接近上游”的推断做修正：
  - 结合 `cc-switch` 参考实现后，这条解释不能作为主结论保留
  - 当前更可靠的结论是：主因在 `scope / source / duplicated-or-derived sessions`
- 已新增辩论文档：
  - `docs-linhay/debate/20260416/codex-global-scope-and-subagent-counting/20260416-codex-global-scope-and-subagent-counting-v01.md`

## Micro-Sync 17:02
- 又补了一层和“怎么修”直接相关的约束：
  - 代码里确实有按账号隔离的 Codex `runtime-home` 机制：`CodexAuthManager.runtimeHomeFolder(accountID:)`
  - `NolonCodexCLI` 在 app 驱动的账户运行时也会把 `CODEX_HOME` 指到该 `runtime-home`
  - 但本机当前 `~/.nolon/codex/runtime-home/` 目录本身是空的，没有可直接拿来回放的本地 sessions 历史
- 这意味着：
  - “把 Usage 从 global 改成 active account runtime-home”在产品语义上是对的，但**仅改查询环境变量并不足够**，否则很可能直接变成无历史数据
  - 如果要真正做成账号级本地 trend，需要同时解决“Codex 会话历史实际写到哪里”的问题

## Micro-Sync 17:05
- 用户已明确裁定 Codex Usage 产品语义为 `global local usage`
- 已按该裁定落地最小实现：
  - `CodexTokenTrendService` / `CodexIntradayUsageService` / `CodexQuarterHourUsageFetcher` 的 `sourceLabel` 统一改为 `global local usage`
  - CLI 侧 `shouldShowGlobalFallbackHint(...)` 已兼容 `global local usage` 新标签
  - 相关 Codex trend / intraday / CLI contract 测试 fixture 已同步更新
- 文档同步：
  - `docs-linhay/features/provider-account-usage-tab-split-2026-04-16.md` 已补充 `codex` Usage 页语义说明
- 验证：
  - `xcodebuild build -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS'` 通过
  - `xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -only-testing:ProvidersTests/...` 无法执行：当前 `nolon-tests` scheme 未包含 `ProvidersTests`
