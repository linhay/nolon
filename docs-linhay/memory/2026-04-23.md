# 2026-04-23

## Codex Usage Token Trend Workspace UI 收口
- 用户继续收敛 `Codex -> Usage` 页下半区 UI，明确要求：
  - 日级柱体更粗；
  - `Daily Trend / Intraday Drilldown` 合并成同一个 workspace，并用 segmented 切换；
  - `Daily Trend` 支持 `Bars / Line`，`Line` 只显示 `total`，且记住显示选择；
  - 点击单日后自动切到 intraday；
  - 图区 sticky，表格取消内部滚动，改为跟整页一起滚动。
- 本次落地：
  - `NolonUIFoundation` 新增 `ProviderTokenTrendChartStyle` / `ProviderTokenTrendContentTab`，并把 `supportsIntradayDrilldown / chartStyle / activeTab` 收进 `ProviderTokenTrendSectionData`。
  - `ProviderTokenTrendViewModel` 新增 provider 级 `chartStyle` 偏好持久化；选中单日时自动切 `intraday`，取消选中时回退 `daily`。
  - `UnifiedProviderUsageSupportViews` 重构为单一 `Trend Workspace`：
    - 顶部 segmented 在 `Daily Trend / Intraday Drilldown` 间切换；
    - `Daily Trend` 在 header 内支持 `Bars / Line`；
    - `Line` 模式仅展示 `total` 折线；
    - workspace 使用 sticky header，图区固定，`Daily Breakdown / Intraday Breakdown` 表格跟随整页滚动；
    - 调整 chart slot / bar 宽度，提升柱体可读性。
  - `docs-linhay/features/provider-usage-intraday-drilldown-2026-04-14.md` 与 `docs-linhay/dev/codex-usage-token-trend-workspace-ui-2026-04-23.md` 已同步收敛后的交互语义。
- 验证结果：
  - `xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/ProviderTokenTrendViewModelParityTests -only-testing:nolonTests/ProviderTokenTrendSectionPresentationSupportTests -only-testing:nolonTests/PageMarkerRouteResolverTests` 通过。
  - `swift test --package-path libs/NolonUIFoundation --filter ProviderTokenTrendModelsTests` 通过。
  - `git diff --check` 通过。
  - `swift test --package-path libs/NolonUI --filter ProviderTokenTrendSectionViewTests` 仍被历史问题阻塞：`libs/NolonUI/Tests/NolonUITests/NolonUITests.swift:126` 的 `ProviderCardTemplate` 不在 scope；与本次 token trend UI 改动无关。

## Release Gate 收尾
- 为通过 `OversizedViewFileGuardTests`，本次额外做了两处“仅拆文件、不改语义”的收尾：
  - 将 `ProviderTokenTrendSectionView` 的布局辅助类型与 helper 拆到 `libs/NolonUI/Sources/NolonUI/Components/Shared/ProviderTokenTrendSectionSupport.swift`，把 `UnifiedProviderUsageSupportViews.swift` 压回 2000 行以内。
  - 将 `ProviderUsageEngine` 的状态/缓存辅助逻辑拆到 `nolon/Skills/Domain/Providers/Usage/Engine/ProviderUsageEngine+StateSupport.swift`，把 `ProviderUsageEngine.swift` 压回 2000 行以内。
- 全量门禁结果：
  - `xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/OversizedViewFileGuardTests -only-testing:nolonTests/ProviderTokenTrendViewModelParityTests -only-testing:nolonTests/ProviderTokenTrendSectionPresentationSupportTests -only-testing:nolonTests/PageMarkerRouteResolverTests` 通过。
  - `NO_SPM_UPDATE=1 ./build.sh` 通过，确认 `Release build + unit tests` 均已恢复为绿色。
