# 2026-03-26

## NolonUI 组件范式整改（全量推进）

- 决策：将 `libs/NolonUI` 从“仅挂载空 ViewModel”升级为“组件内部 UI 状态迁移到对应 `@Observable` ViewModel”的实质落地模式，确保组件与 ViewModel 关系不止命名层面。
- 变更范围：`ComponentViewModels.swift` 与 7 个组件文件，覆盖删除确认弹窗状态、卸载确认状态、hover 状态、分栏可见性状态。
- 关键动作：
  - 在 `ComponentViewModels.swift` 新增可观测状态字段：`columnVisibility`、`showingDeleteConfirmation`、`showingUninstallConfirmation`、`isHovered`。
  - 组件侧将本地 `@State` 迁移为 `viewModel` 状态绑定（例如 `isPresented: $viewModel.showingDeleteConfirmation`）。
- 验证结果：执行 `xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS'`，结果通过（`532` tests，`0` failures，`1` skipped）。
- 风险记录：其余无本地行为状态的组件当前仍为“轻量 VM 持有”模式，后续若新增行为状态需继续遵循“优先入 ViewModel”。

## 主工程视图范式门禁强化（继续整改）

- 决策：将“主工程 View 与 ViewModel 一对一”从命名约定升级为显式绑定约束，新增 `ViewComponentBindable` 协议并要求每个主工程 `View` 提供 `ComponentViewModel` 类型绑定。
- 变更范围：新增 `nolon/Skills/Shared/UI/ViewComponentBindings.swift`；增强 `nolonTests/ViewComponentParadigmTests.swift`。
- 关键动作：
  - 为主工程视图补齐 `extension Xxx: ViewComponentBindable { typealias ComponentViewModel = XxxViewModel }`。
  - 测试新增绑定检查：除 `@Observable` ViewModel 存在性外，强制存在 `ViewComponentBindable` 扩展。
- 验证结果：
  - `xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/ViewComponentParadigmTests` 通过。
  - `xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS'` 全量通过（日志 `/tmp/nolon-fulltest.log`，`TEST SUCCEEDED`）。
- 风险记录：当前绑定采用类型别名约束，尚未强制注入实例；后续若需要可进一步升级为“View 持有对应 ViewModel 实例/依赖注入”级别的静态检查。

## 账号视图/VM 合并（主工程）

- 决策：按低风险路径合并两类重复点：
  - 将 `AccountProviderSectionHeader` 与 `AccountVendorGroupHeader` 合并为通用 `AccountSectionHeader`（通过 `Style` 区分 section/provider）。
  - 将主工程中重复定义的 `AccountSummaryCardViewModel`、`AccountSummaryContentCardViewModel` 改为复用 `NolonUI` 的同名 VM（`typealias`）。
- 测试策略调整：`ViewComponentParadigmTests` 增加对 `typealias XxxViewModel = ...` 的识别，确保跨模块复用 VM 仍满足门禁。
- 验证结果：
  - 范式门禁：`-only-testing:nolonTests/ViewComponentParadigmTests` 通过。
  - 全量：`xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS'` 通过（`TEST SUCCEEDED`）。
- 过程说明：一次 `EXIT:65` 为并发触发的 `build.db` 锁冲突，串行重跑后通过，非代码缺陷。

## 通用单选/多选器落地（主工程视图复用）

- 决策：新增范型选择器 `GenericSelectionControl<Value, Content>`，同一组件支持三种绑定形态：
  - `Binding<Value>`（单选，非空）
  - `Binding<Value?>`（单选，可清空）
  - `Binding<Set<Value>>`（多选）
- 设计：将状态切换逻辑下沉到 `GenericSelectionStateResolver`，视图只负责渲染与交互包装。
- 接入范围：
  - `OnboardingProviderSelectionView`（多选 provider）
  - `NolonAccountsView`（单选时间窗）
  - `AppSettingsView`（单选分类）
  - `AddRepositorySheet`（单选仓库模板）
- 范式对齐：新增 `GenericSelectionControlViewModel` 并补齐 `ViewComponentBindable` 绑定。
- 测试：新增 `GenericSelectionControlTests`（5 个用例）验证单选/多选状态机行为；`ViewComponentParadigmTests` 与 `nolon-tests` 全量通过。

## 账号多选接入通用选择器

- 变更：将账号多选核心入口切换为 `GenericSelectionControl`：
  - `ProviderUsageView+CodexCompactList`：多选模式下账号卡片点击改为 `GenericSelectionControl<UUID>` 驱动 `selectedAccountIDs`。
  - `ProviderUsageView+CodexGatewaySection`：网关账号选择弹窗行选择改为 `GenericSelectionControl<UUID>`。
- 兼容：为通用选择器新增 `onToggle` 钩子，保留多选点击时的附加副作用（如清理激活网关卡片选择）。
- 清理：删除旧的 `toggleGatewayAccountPickerSelection` 手工切换函数。
- 验证：`xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS'` 全量通过。

## 泛型 IDBox 引入（选择器类型抹平）

- 决策：新增 `IDBox<ID: Hashable>`，用于在选择器层统一处理不同 ID 原生类型（例如 `UUID` / `String`），降低选择状态绑定对业务 ID 类型的耦合。
- 接入：
  - `ProviderUsageView+CodexCompactList` 多选绑定改为 `Binding<Set<IDBox<UUID>>>` 与 `selectedAccountIDs` 双向映射。
  - `ProviderUsageView+CodexGatewaySection` 网关账号选择绑定改为 `Binding<Set<IDBox<UUID>>>`。
- 测试：`GenericSelectionControlTests` 新增 IDBox 场景用例；全量 `nolon-tests` 通过。

## 2026-03-26 账号类型协议化收敛（provider account protocol）
- 决策：在 Accounts 展示域引入 `ProviderAccountRecordConvertible` 协议，用 protocol 抹平 `ClaudeAccount` 与 `GeminiAuthAccount` 的展示差异。
- 实施：
  - 在 `AccountCardModels.swift` 增加协议与两种账号类型扩展适配。
  - 新增 `AccountRecordBuilder.providerAccount(...)` 泛型入口，`claude/gemini` 构建函数改为委托该入口。
  - 在 `NolonAccountsViewModel` 中新增泛型排序/激活ID提取/卡片映射方法，替换 Claude/Gemini 分支重复逻辑。
- 测试：新增 `testBDD_GivenProtocolConvertibleAccounts_WhenUsingGenericBuilder_ThenClaudeAndGeminiProduceStableCards`；`xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS'` 全量通过。
- 风险说明：Codex 账号路径未改协议化（仍保留独立摘要/快照逻辑），后续若要统一可在不影响现有行为前提下继续抽象。

## 2026-03-26 Codex 账号协议化适配补齐
- 需求：将 Codex 账号展示路径也纳入 provider account protocol，避免继续作为特例构建。
- 实施：
  - `ProviderAccountRecordConvertible` 去除 `Identifiable/UUID` 强约束，改为 `accountRecordRawID` 抽象记录 ID。
  - 新增 `AccountRecordBuilder.CodexSummaryAccountAdapter`，将 `AccountUsageSummary` 适配到统一协议。
  - `AccountRecordBuilder.codexAccounts(...)` 改为通过 `providerAccount(...)` 泛型入口构建。
  - `NolonAccountsViewModel` 中与排序/激活绑定相关的泛型工具补充 `Identifiable & ID == UUID` 约束，确保 Claude/Gemini 语义不变。
- 测试：新增 `testBDD_GivenCodexSummary_WhenBuildingProtocolBackedRecord_ThenPreservesReadonlyDetailAndQuota`；定向与全量 `nolon-tests` 均通过。

## 09:02 Accounts/UI 最佳范式收敛
- 将 `DirectoryPickerSheet` 的多选交互从手写 `contains/remove/insert` 统一为 `GenericSelectionControl(value:selections:)`，单一来源维护选择状态。
- 将 `NolonAccountsViewModel.accountCards(for:)` 中默认分支的大段摘要卡片映射抽取为 `makeSummaryUsageCards(provider:usageProvider:summaries:)`，降低主流程复杂度并统一摘要卡片构建入口。
- 验证结果：
  - `xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/GenericSelectionControlTests` 通过
  - `xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/NolonAccountsViewModelTests` 通过
  - `xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS'` 全量通过

## 09:20 Accounts 命名语义收敛（去 Codex 特例命名）
- 决策：已泛化的摘要账号构建路径不再保留 `codex*` 命名，避免误导后续扩展。
- 实施：
  - `AccountRecordBuilder.CodexSummaryAccountAdapter` 重命名为 `SummaryUsageAccountAdapter`。
  - `AccountRecordBuilder.codexAccounts(...)` 重命名为 `summaryUsageAccount(...)`。
  - 同步更新 `NolonAccountsViewModel` 与 `NolonAccountsViewModelTests` 调用点。
- 验证：
  - `xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/NolonAccountsViewModelTests` 通过（22/22）。
  - `xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS'` 全量通过（541 tests, 1 skipped, 0 failures）。

## 10:06 选择状态机统一（Engine 与 UI 复用同一 resolver）
- 决策：将 `GenericSelectionStateResolver` 从 `GenericSelectionControl.swift` 抽离到 `Skills/Shared/Selection`，让 UI 与 `ProviderUsageEngine` 共用同一选择状态机，避免账号多选逻辑继续在 Engine 里手写分叉。
- 实施：
  - 新增 `resolveBatchMultiSelection(current:toggledValues:)`，统一“section 全选/反选”行为。
  - `ProviderUsageEngine.toggleCodexAccountSelection` 改为调用 `resolveMultiSelection`。
  - `ProviderUsageEngine.toggleCodexSectionSelection` 改为调用 `resolveBatchMultiSelection`。
  - `GenericSelectionControlTests` 新增 batch 用例（全选后反选、缺失值并集、空批次不变）。
- 验证：
  - `xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/GenericSelectionControlTests` 通过（9/9）。
  - `xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/CodexAccountDisplaySectionsTests` 通过（51/51）。
  - `xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS'` 全量通过（544 tests, 1 skipped, 0 failures）。
- 过程说明：一次并行测试触发 `build.db is locked`，串行重跑后通过，非功能缺陷。
- 追加收敛：
  - `ProviderUsageEngine.toggleCodexSection` 改为复用 `resolveMultiSelection`。
  - `RemoteRepositorySidebarView.toggleSection` 改为复用 `resolveMultiSelection`。
- 补充验证：`xcodebuild test -quiet -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/CodexAccountDisplaySectionsTests` 通过（带 1 条既有 warning）。

## 10:19 折叠状态切换继续收敛
- 实施：
  - `ProviderUsageEngine.toggleCodexSection` 改为复用 `GenericSelectionStateResolver.resolveMultiSelection`。
  - `RemoteRepositorySidebarView.toggleSection` 改为复用 `GenericSelectionStateResolver.resolveMultiSelection`。
- 验证：
  - `xcodebuild test -quiet -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/CodexAccountDisplaySectionsTests` 通过。
  - `xcodebuild test -quiet -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/RemoteRepositorySidebarViewModelTests` 通过。

## 10:23 网关账号选择状态 IDBox 原生化
- 实施：
  - `ProviderUsageView.gatewayAccountPickerSelection` 从 `Set<UUID>` 升级为 `Set<IDBox<UUID>>`。
  - `ProviderUsageView+CodexGatewaySection` 移除每次渲染时的 `Set<UUID> <-> Set<IDBox<UUID>>` 绑定映射，直接传 `$gatewayAccountPickerSelection` 给 `GenericSelectionControl`。
  - 选择确认时按 `IDBox` 进行成员判断（`contains(IDBox(id))`）。
- 验证：
  - `xcodebuild test -quiet -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/CodexAccountDisplaySectionsTests` 通过。
  - `xcodebuild test -quiet -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/RemoteRepositorySidebarViewModelTests` 通过。

## 10:25 Codex 紧凑列表 IDBox 映射下沉到子 VM
- 实施：
  - 在 `ProviderUsageSubViewModels.CodexState` 新增 `selectedAccountIDBoxes: Set<IDBox<UUID>>`，集中处理与 `engine.selectedCodexAccountIDs` 的映射。
  - `ProviderUsageView+CodexCompactList` 改为直接绑定 `selectedAccountIDBoxes`，移除视图层的 `UUID <-> IDBox` 手工映射。
- 验证：
  - `xcodebuild test -quiet -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/CodexAccountDisplaySectionsTests` 通过。
  - `xcodebuild test -quiet -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/ProviderUsageSubViewModelWiringTests` 通过。
- 过程说明：一次并发触发 `build.db is locked`，串行重跑后通过。

## 10:30 选择状态行为下沉与顺序稳定化
- 实施：
  - `ProviderUsageEngine` 新增 `selectedCodexAccountIDsInCurrentDisplayOrder()`，对外暴露按当前账号展示顺序的已选 ID 列表。
  - `ProviderUsageSubViewModels.CodexState` 新增：
    - `hasSelectedAccounts`
    - `selectedAccountIDsInDisplayOrder`
    - `clearSelectedAccounts()`
  - `ProviderUsageView` 批量操作区不再直接操作 `selectedAccountIDs`，改为调用 `clearSelectedAccounts()` / `hasSelectedAccounts`。
  - `ProviderUsageView+CodexGatewaySection` 拖拽多选加入网关时改用 `selectedAccountIDsInDisplayOrder`，避免 `Array(Set)` 顺序不稳定。
- 测试：
  - 新增 `CodexAccountDisplaySectionsTests` 用例：`testBDD_GivenMultiSelectedAccountsOutOfOrder_WhenResolvingDisplayOrderIDs_ThenResultFollowsAccountOrder`。
  - 定向通过：`CodexAccountDisplaySectionsTests`、`ProviderUsageSubViewModelWiringTests`。

## 10:37 Token Trend 单选状态机收敛
- 决策：`ProviderTokenTrendSection` 的日期选中/反选（可清空单选）统一走 `GenericSelectionStateResolver.resolveSingleSelection`，消除图表与表格两处手写 toggle 分叉。
- 测试先行：
  - 在 `CodexUsageTabPresentationTests` 新增：
    - `testBDD_GivenSelectedDate_WhenTappingSameDate_ThenTokenTrendSelectionClears`
    - `testBDD_GivenSelectedDate_WhenTappingDifferentDate_ThenTokenTrendSelectionSwitches`
  - 先跑定向测试得到编译红灯（缺少 `ProviderTokenTrendSection.resolveSelectedDate`）。
- 实施：
  - `ProviderTokenTrendSection` 新增 `resolveSelectedDate(current:tapped:)`，内部复用 `GenericSelectionStateResolver.resolveSingleSelection(..., allowsEmptySelection: true)`。
  - `stackedBar` 与 `dataRow` 的点击逻辑统一改为调用 `resolveSelectedDate`。
- 验证：
  - `xcodebuild test -quiet -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/CodexUsageTabPresentationTests` 通过。
  - `xcodebuild test -quiet -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/GenericSelectionControlTests` 通过。
  - `xcodebuild test -quiet -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS'` 全量通过。
- 过程说明：一次并行触发 `build.db is locked`，串行重跑通过，非功能问题。

## 10:46 网关拖拽 Hover 目标状态机通用化
- 决策：将“拖拽 hover 进入/离开”这类可空单值状态切换沉淀到 `GenericSelectionStateResolver`，避免 `ProviderUsageView+CodexGatewaySection` 继续保留三元表达式手写逻辑。
- 测试先行：
  - 在 `GenericSelectionControlTests` 新增：
    - `testBDD_GivenOptionalHoverSelection_WhenHoverBegins_ThenCurrentBecomesHoveredValue`
    - `testBDD_GivenOptionalHoverSelection_WhenHoverEndsOnCurrentValue_ThenSelectionClears`
  - 先跑定向测试得到编译红灯（缺少 `resolveHoverSelection`）。
- 实施：
  - `GenericSelectionStateResolver` 新增 `resolveHoverSelection(current:hovered:isHovering:)`。
  - `ProviderUsageView+CodexGatewaySection` 的 `dropDestination(... isTargeted:)` 改为调用该 resolver，替换原有手写三元切换。
- 验证：
  - `xcodebuild test -quiet -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/GenericSelectionControlTests` 通过。
  - `xcodebuild test -quiet -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/CodexAccountDisplaySectionsTests` 通过。
  - `xcodebuild test -quiet -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS'` 全量通过。
- 过程说明：并发测试仍会触发 `build.db is locked`，统一改为串行验证。

## 10:59 布尔/排序切换状态机继续统一
- 决策：在 `GenericSelectionStateResolver` 继续补齐常见状态迁移（布尔开关、排序键+方向），减少 View/Engine 内分支切换散落。
- 测试先行：
  - `GenericSelectionControlTests` 新增：
    - `testBDD_GivenBooleanState_WhenToggling_ThenReturnsInvertedValue`
    - `testBDD_GivenSortState_WhenTappingCurrentKey_ThenDirectionToggles`
    - `testBDD_GivenSortState_WhenTappingNewKey_ThenKeyChangesAndUsesProvidedDefaultDirection`
  - 先跑定向测试得到编译红灯（缺少 `resolveBooleanToggle` / `resolveSortSelection`）。
- 实施：
  - `GenericSelectionStateResolver` 新增：
    - `resolveBooleanToggle(current:)`
    - `resolveSortSelection(currentKey:currentAscending:tappedKey:defaultAscendingForTappedKey:)`
  - `ProviderUsageEngine.toggleGatewayCardsSectionCollapsed()` 改为复用 `resolveBooleanToggle`。
  - `ProviderTokenTrendSection.sortableHeader` 的排序切换改为复用 `resolveSortSelection`。
  - `ProviderUsageEngine.selectCodexSortOption(_:)` 的排序切换改为复用 `resolveSortSelection`。
- 验证：
  - `xcodebuild test -quiet -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/GenericSelectionControlTests` 通过。
  - `xcodebuild test -quiet -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/CodexUsageTabPresentationTests` 通过。
  - `xcodebuild test -quiet -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/CodexAccountDisplaySectionsTests -only-testing:nolonTests/CodexUsageTabPresentationTests` 通过。
  - `xcodebuild test -quiet -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS'` 全量最终通过。
- 过程说明：一次全量出现测试进程早退（`signal kill`），重跑后通过；并发执行时仍可能触发 `build.db is locked`，后续继续串行执行测试。

## 11:04 Usage Card 枚举通用化（分层泛化）
- 决策：按“分层通用”落地，先通用 UI 层枚举，保留 provider 专属 policy 映射。
- 实施：
  - `CodexUsageCardStatusKind` 重命名为 `UsageCardStatusKind`。
  - `CodexUsageCardActionLayout` 重命名为 `UsageCardActionLayout`。
  - `CodexUsageCardPresentationPolicy` 保持不变，仅将返回类型切换为通用枚举（仍负责 Codex 状态到 UI 通用语义的映射）。
- 验证：
  - `xcodebuild test -quiet -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/CodexAuthServiceTests -only-testing:nolonTests/CodexUsageTabPresentationTests` 通过。
  - `xcodebuild test -quiet -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS'` 全量通过。

## 11:10 ProviderUsageView 计算属性下沉（第一批）
- 目标：减少 `ProviderUsageView` 内业务派生逻辑，优先下沉到 `ProviderUsageRootViewModel`。
- 实施：
  - 在 `ProviderUsageRootViewModel` 新增：
    - `usageNavigationTitle`
    - `debugPageMarkerItems`
    - `tokenTrendDebugPageMarkerItems`
    - `gatewayCardsDebugPageMarkerItems`
    - `NonCodexHeaderAction`（枚举）
    - `nonCodexHeaderActions`
  - `ProviderUsageView` 改为直接消费 `rootViewModel` 上述属性，删除本地重复计算（包括 `usageNavigationTitle`、`NonCodexHeaderAction`、`nonCodexHeaderActions`）。
- 验证：
  - `xcodebuild test -quiet -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/CodexUsageTabPresentationTests -only-testing:nolonTests/ProviderUsageSubViewModelWiringTests` 通过。
  - `xcodebuild test -quiet -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/ViewComponentParadigmTests` 通过。
  - `xcodebuild test -quiet -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS'` 全量通过。
- 过程说明：一次并发触发 `build.db is locked`，串行重跑通过。

## 11:15 Header Action 通过 Protocol + Enum 抹平
- 背景：`ProviderUsageView` 中 non-codex 与 codex 头部按钮分别走两套 `switch`，渲染和触发逻辑重复。
- 实施：
  - 新增 `ProviderUsageHeaderActionRepresentable` 协议，统一抽象：
    - `headerActionTitle`
    - `headerActionSystemImage`
    - `isHeaderActionEnabled(in:)`
    - `performHeaderAction(in:)`
  - 让以下枚举实现该协议：
    - `ProviderUsageRootViewModel.NonCodexHeaderAction`
    - `ProviderUsageEngine.CodexPrimaryHeaderAction`
  - `ProviderUsageView` 中 `nonCodexHeaderActionButton` 与 `codexPrimaryActionButton` 收敛为统一的 `headerActionButton(_:)` 渲染入口。
- 结果：保留“枚举表达业务意图”，通过 protocol 抹平按钮渲染/触发模型，减少 `switch` 分叉与重复逻辑。
- 验证：
  - `xcodebuild test -quiet -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/CodexAuthServiceTests -only-testing:nolonTests/CodexUsageTabPresentationTests` 通过。
  - `xcodebuild test -quiet -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/ProviderUsageSubViewModelWiringTests` 通过。
  - `xcodebuild test -quiet -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS'` 全量通过。
- 过程说明：一次并发触发 `build.db is locked`，串行重跑后通过。

## 11:20 去除 ProviderUsageView 中 AnyView
- 需求：明确不使用 `AnyView`。
- 实施：将 `ProviderUsageView.body` 从 `baseView / withSheets / withAlerts` 的 `AnyView` 级联，改为原生 SwiftUI 修饰器链（`VStack` 直接串联 sheet/alert/task/overlay/animation/debugPageLocator）。
- 验证：
  - `rg -n "AnyView" nolon/Skills/Domain/Providers/Usage/Views/Common/ProviderUsageView.swift` 无结果。
  - `xcodebuild test -quiet -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/CodexUsageTabPresentationTests -only-testing:nolonTests/ProviderUsageSubViewModelWiringTests` 通过。
  - `xcodebuild test -quiet -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS'` 全量通过。

## 11:40 全仓禁用 AnyView（主工程+组件库+测试）
- 需求：除已处理的 `ProviderUsageView` 外，其它位置也不允许使用 `AnyView`。
- 扫描范围：`nolon`、`libs`、`nolonTests`、`nolonUITests`。
- 实施：
  - `libs/NolonUI/Sources/NolonUI/Components/Shared/SheetHeaderView.swift`
    - 由 `AnyView` 存储 trailing，改为泛型 `SheetHeaderView<Trailing: View>`。
    - close 按钮走 `Trailing == EmptyView` 的初始化路径，custom trailing 走泛型构造。
  - `libs/NolonUI/Sources/NolonUI/Components/Shared/ThreeColumnScaffold.swift`
    - `configuredSidebar() -> AnyView` 改为 `@ViewBuilder -> some View`。
  - `nolon/Skills/Domain/Providers/Views/ProviderDetailGridView.swift`
    - `codexLinkedHint` 去掉 `AnyView(EmptyView())` / `AnyView(...)` 分支，改为 `if` 组合。
    - 新增 `codexLinkFolder(for:)` 辅助函数（类型使用 `ProviderContentTabType`）。
  - `nolon/Skills/Shared/Adapters/NolonUIBridgeAliases.swift`
    - `UISheetHeaderView` 改为泛型，移除 `.custom(() -> AnyView)`。
  - 测试文件中 Host 构造移除 `AnyView`：
    - `nolonTests/AccountSummaryCardSnapshotTests.swift`
    - `nolonTests/GatewayCardVisualRegressionSnapshotTests.swift`
    - `nolonTests/ProviderUsageEmptyStateCardSnapshotTests.swift`
    - `nolonTests/ToastViewSnapshotTests.swift`
    - `NSHostingController<AnyView>` 改为 `NSHostingController<some View>`。
- 验证：
  - `rg -n "\\bAnyView\\b" nolon libs nolonTests nolonUITests` 无结果。
  - 定向 tests（usage/wiring/view-paradigm/snapshot 相关）通过。
  - `xcodebuild test -quiet -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS'` 全量通过。
- 过程说明：一次并行测试触发 `build.db is locked`，切换串行后通过。

## 12:22 Actions Menu 继续抹平（Protocol + Enum）
- 背景：`ProviderUsageView` 的 `codexActionsMenuContent` 与 `nonCodexActionsMenuContent` 仍存在按钮渲染/触发重复。
- 实施：
  - 新增 `ProviderUsageMenuButtonActionRepresentable` 协议，统一按钮菜单动作能力：
    - `menuActionTitle`
    - `menuActionSystemImage`
    - `isMenuActionVisible(in:)`
    - `isMenuActionEnabled(in:)`
    - `performMenuAction(in:createGatewayCard:)`
  - 新增 `ProviderUsageMenuButtonAction` 枚举，覆盖现有按钮动作：
    - `refresh`
    - `claudeMigrateCurrent`
    - `claudeImportFromCCSwitch`
    - `codexEnterMultiSelection`
    - `codexNewAPIKey`
    - `codexNewRelay`
    - `codexCreateGatewayCard`
  - `ProviderUsageView` 新增统一渲染入口 `menuActionButton(_:)`，并将以下内容改为通过枚举驱动：
    - codex 视图 section 的“进入多选”
    - codex 账号管理 section 的三个按钮
    - non-codex 的刷新按钮
    - claude 账号管理 section 的两个按钮
  - 抽取 `accountLayoutMenuPicker()`，去除 codex/non-codex 中重复的“布局” picker 定义。
- 约束：全程未引入 `AnyView`。
- 验证：
  - `xcodebuild -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/CodexUsageTabPresentationTests -only-testing:nolonTests/ProviderUsageSubViewModelWiringTests -only-testing:nolonTests/CodexAccountDisplaySectionsTests test` 通过（115 tests, 0 failures）。

## 12:30 去 nonCodex 语义化命名（代码与测试层）
- 背景：用户反馈仍存在大量 `nonCodex` 结构命名，影响语义一致性。
- 实施（仅重命名，不改行为）：
  - `ProviderUsageRootViewModel`：
    - `NonCodexHeaderAction` -> `GenericHeaderAction`
    - `nonCodexHeaderActions` -> `genericHeaderActions`
  - `ProviderUsageView`：
    - `nonCodexHeaderActionButton` -> `genericHeaderActionButton`
    - `nonCodexMenuActions` -> `genericMenuActions`
    - `nonCodexActionsMenuContent` -> `genericActionsMenuContent`
    - 对应调用点全部改为 generic 命名。
  - `ProviderUsageView+ClaudeGeminiSection`：
    - `nonCodexAccountsSection` -> `genericAccountsSection`
    - `nonCodexLoadingContent` -> `genericLoadingContent`
    - `nonCodexSectionState` -> `genericSectionState`
  - 测试命名同步：
    - `nolonTests/ProviderUsageSubViewModelWiringTests.swift`
    - `nolonTests/CodexUsageTabPresentationTests.swift`
    - `nolonTests/CodexBinaryTabConfigurationTests.swift`
    - `nolonTests/CodexRulesTabTests.swift`
    - `libs/Providers/Tests/ProvidersTests/ProviderUsageMonitorServiceTests.swift`
    - `libs/Providers/Tests/ProvidersTests/CodexTests/CodexAuthManagerTests.swift`
- 扫描结果：
  - `rg -n "nonCodex|NonCodex|non-codex|Non-codex" nolon libs nolonTests nolonUITests` 无结果。
- 验证：
  - `xcodebuild -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/CodexUsageTabPresentationTests -only-testing:nolonTests/ProviderUsageSubViewModelWiringTests -only-testing:nolonTests/CodexBinaryTabConfigurationTests -only-testing:nolonTests/CodexRulesTabTests test` 通过（76 tests, 0 failures）。

## 12:34 CodexGateway/CodexCompactList 结构收敛
- 文件：
  - `nolon/Skills/Domain/Providers/Usage/Views/Common/ProviderUsageView+CodexGatewaySection.swift`
  - `nolon/Skills/Domain/Providers/Usage/Views/Common/ProviderUsageView+CodexCompactList.swift`
- 实施：
  - `CodexGatewaySection`：抽取 `GatewayCandidateSectionKind`，统一分组 icon/前景色策略，去除三处重复字符串判定逻辑。
  - `CodexCompactList`：抽取 `selectedCodexAccountIDBoxesBinding` 与 `handleCodexAccountCardTap(...)`，减少多选/单选分支中的重复交互代码。
- 约束：仅做结构与可维护性收敛，不改行为。
- 验证：
  - `xcodebuild -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/CodexUsageTabPresentationTests -only-testing:nolonTests/CodexAccountDisplaySectionsTests -only-testing:nolonTests/ProviderUsageSubViewModelWiringTests test` 通过（115 tests, 0 failures）。

## 12:39 两文件继续重构（行为不变）
- 目标文件：
  - `ProviderUsageView+CodexCompactList.swift`
  - `ProviderUsageView+CodexGatewaySection.swift`
- `CodexCompactList`：
  - 新增 `CodexCompactListPolicy.maxUsageWindowCount`，消除魔法数字 `3`。
  - 新增 `usesCompactCodexListRows`，减少重复 layout 判定调用。
  - 新增 `applyCodexAccountDraggable(accountID:)` View 扩展，统一可拖拽包装，去掉分支内重复 `if let accountId`。
  - 新增 `handleCodexAccountCardTap(...)`，收口卡片点击激活流程。
  - `compactPlanText` 去掉强制解包，改为安全返回。
- `CodexGatewaySection`：
  - 新增 `codexAccountsLoadingSkeleton`，收口 loading 骨架布局重复分支。
  - 新增 `gatewayCardsHeader`，收口网关区 header 结构，修复原始嵌套可读性问题。
  - `handleGatewayCardDrop(accountIDs:cardID:)`：多选场景由“首个拖拽项是否选中”改为“拖拽项中任一被选中即按当前多选集合处理”，避免漏判。
- 验证：
  - `xcodebuild -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/CodexUsageTabPresentationTests -only-testing:nolonTests/CodexAccountDisplaySectionsTests -only-testing:nolonTests/ProviderUsageSubViewModelWiringTests test` 通过（115 tests, 0 failures）。

## 12:57 ProviderUsageView 视图拆分（主文件瘦身）
- 背景：`ProviderUsageView.swift` 承担了过多动作协议/菜单视图渲染职责（>1100 行）。
- 拆分方案：
  - 新增 `ProviderUsageView+Actions.swift`
    - 承载 `ProviderUsageHeaderActionRepresentable`
    - `ProviderUsageMenuButtonActionRepresentable`
    - `ProviderUsageMenuButtonAction`
    - Header action 的两组枚举映射扩展。
  - 新增 `ProviderUsageView+HeaderMenu.swift`
    - 承载 `content/header/actionsMenu` 及相关渲染 helper（layout picker、menu sections、menu actions）。
  - `ProviderUsageView.swift` 保留主容器、生命周期、sheet/alert/overlay 编排与核心状态定义。
- 结构结果：
  - `ProviderUsageView.swift` 从 1168 行降至 425 行。
- 兼容性处理：
  - 由于拆分文件访问边界，新增只读桥接属性：
    - `currentRootViewModel`
    - `codexAutoSwitchThresholdMenuOptions`
    - `codexAutoSwitchCandidateMenuOptions`
- 验证：
  - `xcodebuild -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/CodexUsageTabPresentationTests -only-testing:nolonTests/ProviderUsageSubViewModelWiringTests -only-testing:nolonTests/ViewComponentParadigmTests test` 通过（64 tests, 0 failures）。

## 13:07 CodexImportSheet 专属 VM 收敛
- 决策：`CodexImportSheet` 改为使用域内专属 VM，避免将大量状态与 action 闭包继续透传在 `ProviderUsageView`。
- 实现：新增 `ProviderUsageCodexImportSheetViewModel`（`ProviderUsageSubViewModels.swift`），`CodexImportSheet` 改为接收 `@Bindable var viewModel`。
- 冲突修复：与共享占位类 `CodexImportSheetViewModel` 同名冲突，已通过重命名到域内前缀化类型解决。
- 结构收口：`CodexImportExportViewModel` 仅保留 sheet 呈现与 `sheetViewModel` 出口；旧的导入候选透传属性与导入动作透传方法移除。
- 进一步优化：`ProviderUsageView` 中 `isShowingLoginBinding / isShowingLoginURLSheetBinding / isShowingCodexImportSheetBinding` 下沉到对应 VM，主视图移除重复 Binding 组装。
- 测试：`xcodebuild test -scheme nolon-tests -only-testing:CodexUsageTabPresentationTests -only-testing:ProviderUsageSubViewModelWiringTests -only-testing:ViewComponentParadigmTests -only-testing:CodexImportExportViewModelParityTests` 通过。

## 13:19 展示策略下沉到 Provider ViewModel
- 决策：`ProviderUsageView` 内部静态展示策略（布局/上下文动作/header action 可见性）全部下沉到 `ProviderUsageAccountsViewModel` 及 `CodexState` / `GeminiState`。
- 变更：
  - `CodexState` 新增策略能力：compact/list、text selection、gateway 成员展示上限、activate action 可见性、header action 可见性。
  - `GeminiState` 新增 `shouldUseFullWidthCardLayout`。
  - `ProviderUsageAccountsViewModel` 新增 `shouldUseCompactUnifiedListRows`。
  - `ProviderUsageView` 删除对应静态策略函数，调用方全部改为 `viewModel`。
- 测试调整：`CodexUsageTabPresentationTests` 改为断言 ViewModel 类型上的策略 API（避免实例化 root 带来的测试进程重启副作用）。
- 验证：`CodexUsageTabPresentationTests`、`ProviderUsageSubViewModelWiringTests`、`ViewComponentParadigmTests` 全通过。

## 13:21 ProviderUsageView 配置常量继续下沉
- 决策：`ProviderUsageView` 中以下配置从 View 层移除，统一归到 `ProviderUsageAccountsViewModel.CodexState`：
  - 自动切号阈值选项：`[5,10,15,20,30]`
  - 候选余量阈值选项：`[10,20,30,40,50]`
  - 紧凑列表列宽：plan=96、usage=232
  - 紧凑列表使用窗口类型：`ListUsageWindow`
- 实现：`HeaderMenu` 与 `CodexCompactList` 调用全部改为 `viewModel.codex` 出口，不再通过 `ProviderUsageView` 桥接属性/内部类型。
- 验证：`CodexUsageTabPresentationTests`、`ProviderUsageSubViewModelWiringTests`、`ViewComponentParadigmTests` 全通过。

## 13:26 Usage 策略类型拆文件
- 决策：将 `ProviderUsageView.swift` 顶部的通用策略定义拆分到独立文件，降低主视图职责密度并保持可复用策略入口稳定。
- 拆分结果：
  - `UsageCardStatusKind` / `UsageCardActionLayout` -> `UsageCardPresentationTypes.swift`
  - `ProviderUsageLoginPolicy` -> `ProviderUsageLoginPolicy.swift`
  - `CodexUsageCardPresentationPolicy` -> `CodexUsageCardPresentationPolicy.swift`
- 修复：拆分后 `ProviderUsageLoginPolicy` 漏掉 `Foundation` 导入，已补充 `import Foundation`（用于 `URL`）。
- 验证：`xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/CodexUsageTabPresentationTests -only-testing:nolonTests/ViewComponentParadigmTests` 通过（64 tests, 0 failures）。

## 13:33 Codex Config Editor Sheet 状态枚举化
- 决策：`ProviderUsageView` 中 `isPresented: Binding(get/set)` 的配置编辑弹层改为 VM 枚举态聚合，避免继续扩散布尔展示状态。
- 实现：
  - 在 `ProviderUsageAccountsViewModel.CodexState` 新增 `ActiveSheet` 枚举（当前包含 `.configEditor`）。
  - 新增 `activeSheet` 与 `activeSheetBinding`，将 `engine.isShowingCodexConfigEditor` 映射为单一 sheet item 状态。
  - `ProviderUsageView` 由 `.sheet(isPresented:)` 改为 `.sheet(item:)`，按 `ActiveSheet` 分支渲染 `CodexConfigEditorSheet`。
- 验证：`CodexUsageTabPresentationTests` + `ViewComponentParadigmTests` 通过（64 tests, 0 failures）。

## 14:19 ProviderUsageView 文件聚合（回并）
- 决策：按需求将 `ProviderUsageView` 相关 5 个拆分文件回并到单文件，保留内部分区（MARK）以维持可维护性。
- 聚合结果：
  - `ProviderUsageView+Actions.swift`
  - `ProviderUsageView+ClaudeGeminiSection.swift`
  - `ProviderUsageView+HeaderMenu.swift`
  - `ProviderUsageView+CodexCompactList.swift`
  - `ProviderUsageView+CodexGatewaySection.swift`
  以上内容均并入 `ProviderUsageView.swift` 并删除原文件。
- 范式约束：
  - 视图仍只负责渲染编排；策略与状态读取继续走 `ProviderUsageRootViewModel / ProviderUsageAccountsViewModel`。
  - `sheet` 状态保持枚举化绑定（`CodexState.activeSheet`）。
- 验证：`CodexUsageTabPresentationTests` + `ViewComponentParadigmTests` 通过（64 tests, 0 failures）。

## 14:44 账号列表收敛到 NolonUI AccountListModeModule
- 目标：将主工程账号列表渲染收敛到 `libs/NolonUI` 统一组件，减少业务侧重复 list-row 实现。
- NolonUI 改动：
  - `AccountListModeModule` / `AccountListModeItem` / `AccountListModeSection` / `AccountListModeUsageWindow` 升级为 `public`，可供主工程复用。
  - 新增 `AccountListModeRowAction`，并为 `AccountListModeModule` 增加行点击与菜单动作回调（`onTap` / `onMenuAction`）。
  - 增加列表头文案与列宽可配置参数，支持业务侧按 provider 调整。
  - 兼容修复：保留无 `id` 的旧构造器，避免 NolonUI 既有预览代码破坏。
- 主工程改动：
  - `ProviderUsageUnifiedAccountCardGrid` 的 list 模式改为直接使用 `NolonUI.AccountListModeModule`。
  - `ProviderUsageView` 的 Codex 紧凑列表在「非多选」路径改为使用 `NolonUI.AccountListModeModule`；多选路径仍保留原实现以维持选择+拖拽行为。
- 验证：`CodexUsageTabPresentationTests` + `ViewComponentParadigmTests` 通过（64 tests, 0 failures）。
