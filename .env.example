# nolon Build Configuration
# Copy this file to .env and fill in your values

# === Code Signing ===
# Your Developer ID Application certificate identity
# Find with: security find-identity -v -p codesigning | grep "Developer ID Application"
SIGNING_IDENTITY="Developer ID Application: Your Name (TEAM_ID)"

# === Notarization ===
# Apple ID for notarization
APPLE_ID="your-apple-id@example.com"

# App-specific password (generate at https://appleid.apple.com/account/manage)
APPLE_APP_PASSWORD="xxxx-xxxx-xxxx-xxxx"

# Team ID (find in Apple Developer portal or with: security find-identity -v)
TEAM_ID="XXXXXXXXXX"

# === Optional ===
# Keychain profile name (if using stored credentials)
# Create with: xcrun notarytool store-credentials "nolon-notary" --apple-id $APPLE_ID --password $APPLE_APP_PASSWORD --team-id $TEAM_ID
NOTARY_PROFILE="nolon-notary"
