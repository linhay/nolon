# 2026-03-11

## Codex
- Codex auth 导入增强：支持批量数组 JSON（`[ {...}, {...} ]`）文件导入；导入时把顶层 token 字段规范化写入 `tokens.*`，并将 `expired` 映射为 `expires_at`（当缺失时）。
- Email 推断扩展：导入/展示时 `email` 候选包含 `user.email`（优先级：`email -> user.email -> nolon.account.email`）。
- 安全校验：若导入 JSON 含 `type` 且不为 `codex`（忽略大小写），候选项直接判无效。

## Claude
- Claude 账号配置第一阶段完成：模板开启账号能力（`supportsAccounts/supportsMultiAccount`），`claudeCode` 已可映射到 `UsageProvider.claude`（Usage 页与账号页可识别）。
- ProviderUsage 新增 `ClaudeUsageDescriptor`：复用 `CodexHTTPUsageQueryExecutor` 支持可配置 HTTP 用量查询，Claude 不再走 registry unsupported。
- `ClaudeAccountManager` 关键能力落地：
  1. 从当前 Claude `settings.json` 手动迁移导入
  2. 从 `cc-switch` SQLite 导入 Claude 账号
  3. 冲突时按“有效账号优先（在线探测）”替换
  4. 激活账号写回 `settings.json`
- 修复递归缺陷：`ensureStorage()` 与 `saveAccounts()/setActiveAccountID()` 互调导致栈溢出（SIGBUS），已拆分底层写入函数并补测试防回归。
- Claude 账号导入修复：`importFromCCSwitch` 新增 cc-switch 配置兼容层，支持从 `settings_config` 的 `env`、`*.env`、顶层 `apiKey/api_key` + `base_url/baseURL/apiEndpoint` 解析；并支持从 `~/Library/Application Support/com.ccswitch.desktop/app_paths.json` 读取 `app_config_dir_override` 以定位自定义 `cc-switch.db`。
- 增加回归测试 `importFromCCSwitchSupportsTopLevelKeys`，验证非 `env` 结构也能成功导入。
- Claude/Codex/Accounts 卡片样式收敛：新增通用 `AccountSummaryCard` 与 `AccountCardPresentation`，统一 Accounts 页摘要卡、Usage 页 Codex 卡、Usage 页 Claude 卡和 loading skeleton 的外层表面/选中态；`PiAccountSummaryCard` 已移除，PI 只读信息并回主卡片内容区。
- 验证结果：`./build.sh` 构建通过；`xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -only-testing:nolonTests/AccountCardPresentationTests` 通过 4/4。
- 账号体系边界调整：`PI` 已从统一账号体系移除，不再进入 `Accounts` 页账号卡片；`Gemini` 与 `Antigravity` 纳入账号型 provider 范围。
- 统一账号卡片第二阶段开始落地：新增统一 `AccountCardViewData` / `UnifiedAccountCard` 骨架，`Accounts` 页与 `Usage` 页开始复用同一套卡片渲染层；Codex active badge 改用 provider-neutral key，Claude 的 `baseURL` 不再在卡片内重复展示。

- Codex 导入账号修复：`导入账号` sheet 内的“选择文件”改为直接使用 `NSOpenPanel`，避免父层 `fileImporter` 在 macOS sheet 场景下不弹出 Finder；并补了“选择成功/取消”两条 BDD 测试。

- 历史 Token 消耗修复：范围切换现在只影响图表/表格窗口，不再污染 `Today / 7D / 30D` summary；同时新增 `ALL` 累计卡片，并补 Codex/Gemini 趋势服务测试。

- 历史 Token 消耗交互增强：新增 `1D` 范围；点击 `Today/7D/30D/ALL` 卡片可直接切换图表范围，`1D` 仅显示一个柱子；并补 `TokenTrendRange.days1` 展示/窗口测试。

- 历史 Token 消耗卡片继续优化：移除顶部 segmented control，范围切换统一收敛到 `Today/7D/30D/ALL` 卡片；卡片改为整卡可点击，并增加语义色渐变底色与选中描边。

- Codex Advanced 配置修复：
  - 根因：`Advanced` tab 的结构化控件在初始化灌值时可能触发自动保存，且保存链路用 `CodexConfigToml + TOMLEncoder` 整文件重写，导致现有 `config.toml` 中未建模字段、注释与额外 section 被裁剪。
  - 决策：改为文本级结构化草稿提取 + 增量 patch 保存，只修改受控键；新增 hydration guard，阻止首次加载阶段的自动保存。
  - 风险边界：受控 block 内部的原始键顺序与局部注释不保证完全保留，但 block 外的未知 TOML 内容会保留。
  - 验证：`xcodebuild -project nolon.xcodeproj -scheme nolon-app -destination 'platform=macOS' test -only-testing:nolonTests/CodexAdvancedConfigRoleDraftTests` 通过。

## References
- 新增参考项目：已将 `https://github.com/Wei-Shaw/sub2api` 克隆到 `references/sub2api`，供后续对照实现与接口行为参考。

## Codex Sub2API Export
- Codex 账号新增 `sub2api-data` 导出链路，UI 与 CLI 复用 `CodexAuthManager.exportAccountsAsSub2API(accountIDs:destinationURL:)`。
- UI：Codex 账号多选工具条新增 `导出 sub2api`，输出单个 JSON；成功时退出多选模式，若选择集中含 relay，则提示“跳过 X 个 relay 账号”。
- CLI：新增 `nolon codex auth export --format sub2api --output <path>`，目标集合支持 `--all`、`--account-id <uuid>`、`--email <email>`，并返回 `exportedCount / skippedRelayCount / skippedUnsupportedCount`。
- 导出范围仅支持 ChatGPT OAuth 与官方 OpenAI API Key；`relayProfile` 明确不映射到 sub2api，按跳过处理。
- 验证：`swift test --package-path libs/Providers --filter 'CodexAuthManagerTests|NolonCodexCLIEntrypointTests'` 已通过；补跑 `swift test --package-path libs/Providers --filter NolonCoreCLIKitTests` 时存在与本改动无关的既有失败（`gemini auth doctor` 预期、MCP 计数从 `5/5` 变为 `6/6`）。

## Codex Import Sheet
- `导入账号` 面板新增“导出 ZIP / 导出 sub2api”动作，直接基于当前已校验且已勾选的候选项导出，不要求先导入落盘。
- 底层新增 `CodexAuthManager.exportValidatedAuthFilesArchive(results:destinationURL:)` 与 `exportValidatedAuthFilesAsSub2API(results:destinationURL:)`，导入面板与正式账号页共用相同 sub2api 映射规则。
- 导入面板导出 sub2api 时，若候选集合包含 relay，仍生成结果文件，但会提示跳过的 relay 数量。
- 验证：`swift test --package-path libs/Providers --filter CodexAuthManagerTests` 通过；`xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/CodexUsageTabPresentationTests` 被现有无关编译错误阻塞：`AccountSummaryCard.swift` 引用 `DesignSystem.Colors.Component.fill`。

## Codex Import Online Test
- `导入账号` 面板中的在线测试改为仅在存在显式 HTTP usage query 配置时才执行 HTTP 请求；未配置时直接提示“已跳过在线测试，仍可继续导入”，不再回退到 Codex CLI / JSON-RPC。
- 补齐本地化 key `codex.import.sheet.test.skipped.no_explicit_http`，避免该跳过提示依赖硬编码默认值。
- 验证：`xcodebuild test -quiet -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/CodexUsageTabPresentationTests` 通过；`-only-testing:nolonTests/CodexUsageTabPresentationTests/testBDD_GivenPastedOAuthWithoutExplicitHTTPUsageQuery_WhenHandled_ThenSkipsOnlineTestInsteadOfFallingBackToCLI` 通过。

## Codex Import Search
- `导入账号` 面板新增本地搜索框，支持按邮箱、建议名称、文件名、来源组标题和失败原因过滤候选列表。
- 搜索只影响展示，不改变勾选态与导出/导入结果；无命中时显示单独的“无搜索结果”空状态。
- 导入 sheet 关闭或重新打开时会清空搜索词，避免旧筛选条件残留。
- 初始空态不再显示下方候选工具栏和列表区域；只有实际存在候选项后才展示候选区。
- 验证：`xcodebuild test -quiet -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/CodexUsageTabPresentationTests` 通过。

- 收尾：删除 Accounts 页残留的旧账号卡片实现与 PI 兼容扩展，Accounts/Usage 账号卡片统一收口到 UnifiedAccountCard；随后重新验证 ./build.sh 与聚焦账号测试均通过。

- 继续收口统一账号体系：新增 AccountRecord / AccountRecordBuilder / AccountCardViewDataMapper，Claude/Gemini/Codex 的账号卡片事实数据已从页面层剥离；Accounts 与 Usage 页改为页面只做动作分发。

- 2026-03-11 详情页编译修复：修正 SkillInstallationSection 的错误 `.flex` 调用为 `.frame(maxWidth:alignment:)`，并修复 SkillDetailContent 中的 ContentView 命名冲突与失效的 Markdown `.nolon` 主题引用。
- 验证：`./build.sh` 于 2026-03-11 16:37 全量通过。
- 全量测试阻塞：`xcodebuild test -project nolon.xcodeproj -scheme nolon-tests` 于 2026-03-11 16:38-16:42 在 `AddRepositoryViewModelTests` 运行期触发 malloc 崩溃（`pointer being freed was not allocated`），随后测试 bundle 重启失败，属于现存测试运行问题，不是本次详情页修复编译错误。

- 2026-03-11 详情页体验修复：SkillDetailContent 顶部工具条恢复可见的“在 Finder 中打开”按钮，直接定位到 skill.globalPath。
- 2026-03-11 Markdown 渲染修复：SkillDetailContent 对 `SKILL.md` 渲染前先执行 `FrontmatterParser.stripFrontmatter`，避免 `--- ... ---` frontmatter 被当正文显示；同时启用 `.markdownSoftBreakMode(.lineBreak)`，让 Markdown 单换行按换行展示。
- 验证：`./build.sh` 于 2026-03-11 16:49 全量通过。

- 修复 SkillDetailContent 子视图遗漏注入 Markdown 链接处理闭包导致的编译错误；SkillFileContentView 改为显式接收 handleMarkdownLink。

- 调整 Markdown 行内代码与代码块样式：去掉过重灰底，改为更轻的品牌淡色与边框容器，提升详情页正文可读性。

- 2026-03-11 Skill 详情页结构收口：远程已安装/未安装 skill 全部统一到 SkillDetailView 两栏宿主；RemoteSkillDetailView 降级为包装器，远程三栏本地详情页不再保留独立骨架。

- 资源中心左侧仓库栏在 `showsHeader: false` 时改为 52pt 紧凑标题条，并加底部分隔线，和 SkillDetail 顶栏层级对齐；搜索框保持自适应拉伸，关闭按钮仅关闭资源中心。

- 资源中心中间 Tab 导航栏移除系统 `navigationTitle`，改为与左右栏一致的 52pt 紧凑头部，统一三栏导航区视觉节奏。
