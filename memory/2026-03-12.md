## 全量测试修复
- 收敛了 `xcodebuild test -project nolon.xcodeproj -scheme nolon-tests` 的运行时崩溃链，最终全量测试通过。
- 关键修复包括：
  - `AddRepositoryViewModel` 将 pending import 预填充抽成纯函数，测试不再通过异步 UI 状态路径触发崩溃。
  - `MainSplitViewModel` 初始选择逻辑抽成纯函数，单测环境跳过资源监控、Codex binary 探测与后台轮询。
  - `ResourceCatalogGridViewModel` 删除执行与删除请求展示增加纯数据预览/展示 helper，降低对 `@Observable` 状态的测试耦合。
  - `RemoteRepositorySidebarViewModel` 从 `@Observable` 切到 `ObservableObject`，并把 `DispatchQueue.main.asyncAfter` 改成可取消的 `Task.sleep` 调度，修复 deinit 期间的 libdispatch/malloc 崩溃。
- 验证结果：
  - `xcodebuild test -project nolon.xcodeproj -scheme nolon-tests` 通过。
  - 之后会再跑 `./build.sh` 做最终构建回归。

- 资源中心安装后的延迟刷新从 `DispatchQueue.main.asyncAfter` 收口为可取消的 `Task.sleep` 调度，按 skill/workflow/mcp 分类取消旧任务，避免重复刷新与潜在生命周期问题。
- 验证结果：`ResourceCenterViewModelTests`、`RemoteRepositorySidebarViewModelTests`、`ResourceCatalogGridViewModelTests` 定向通过，`xcodebuild test -project nolon.xcodeproj -scheme nolon-tests` 全量通过。

- 继续清理剩余调度风险：`AddRepositorySheet` 的下一轮主线程清理改为 `Task { @MainActor ... }`，`PluginManagementView` 的日志首屏滚动改为 `Task`，`CodexAdvancedConfigRoleDraftTests` 去掉 `DispatchQueue.main.asyncAfter` 改用 `Task.sleep`。
- 当前代码库中生产代码显式 `DispatchQueue` 仅剩 2 处：CLI signal 处理的专用串行队列，以及 Combine `debounce` 的 `DispatchQueue.main` scheduler。
- 验证结果：`./build.sh` 通过，`AddRepositoryViewModelTests` 与 `CodexAdvancedConfigRoleDraftTests` 定向通过。

- Swift 6 并发 warning 清理（测试侧第一轮）：为 `AccountCardPresentationTests`、`CodexUsageCardPresentationPolicyTests`、`CodexPIDSystemLogServiceTests`、`XcodeMCPKitInstallServiceTests`、`ResourceCenterViewModelTests` 增加 `@MainActor` 隔离；用 actor 记录器替换 `GeminiUsageTokenTrendViewModelTests` 与 `RemoteInstallOrchestratorTests` 中的并发可变捕获；修复过时 `String(contentsOf...)` / `activateIgnoringOtherApps` / 多余 `try await` 等测试告警。
- 验证结果：相关定向测试通过，`xcodebuild test -project nolon.xcodeproj -scheme nolon-tests` 全量通过。

- 继续清理 Swift 6 并发 warning 后，2026-03-12 再次执行 `xcodebuild test -quiet -project nolon.xcodeproj -scheme nolon-tests 2>&1 | rg -n "warning:|error:"`，未命中任何编译器 warning/error；命令因 `rg` 无匹配返回 1，视为当前测试构建日志干净。

## Clawhub 远程列表 429 限流重试（2026-03-12）
- 现象：资源中心远程 skill 列表在 `https://clawhub.ai` 返回 HTTP 429 时直接失败，日志为 `Remote list failed with status 429`。
- 根因：`SkillsRepositoryFacade.performListRemoteResourcesRequest` 缺少与下载链路一致的同 host 限流重试；下载已有最多 3 次尝试，列表没有。
- 修复：
  - 为远程列表请求补齐 429 重试逻辑，沿用 `Retry-After` 解析与 1...30 秒裁剪。
  - 最大尝试次数统一为 3 次（首次请求 + 2 次重试）。
  - 不引入镜像域名回退，保持当前 `clawhub.ai` 单域行为。
- 测试：
  - 新增 `list remote resources retries once on same host after 429`
  - 新增 `list remote resources returns 429 after same-host retry exhaustion`
  - `swift test --package-path libs/Providers --filter SkillsRepositoryFacadeTests` 通过。

## Gemini 用量页激活账号重复展示修复（2026-03-12）
- 现象：Gemini 用量 tab 在账号卡下方又显示一张同一激活账号的 outcome 卡，看起来像出现两个相同账号。
- 根因：`ProviderUsageView.genericUsageContent` 同时渲染 `geminiAccounts` 与 `outcomes`；而 `GeminiUsageDescriptor` 只会为当前激活账号生成一条 outcome。
- 修复：
  - 新增纯函数 `displayedGenericUsageOutcomes`，当 Gemini/Antigravity 已有账号卡时隐藏重复 outcome 卡。
  - 保留无账号时的错误/空状态 outcome 展示，以及 Gemini token trend。
- 测试：
  - 新增 `testBDD_GivenGeminiAccountsPresent_WhenResolvingDisplayedGenericUsageOutcomes_ThenHidesDuplicateOutcomeCards`
  - 新增 `testBDD_GivenGeminiWithoutAccounts_WhenResolvingDisplayedGenericUsageOutcomes_ThenKeepsOutcomeCardsForEmptyState`
  - `xcodebuild test -quiet -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/CodexUsageTabPresentationTests` 通过。

## Codex 账号总览卡片交互修复（2026-03-12）
- 现象：`Accounts` 页里的 Codex 账号卡片右键没有复制能力，点击/按钮也无法真正切换账号，只会跳到 provider 页面。
- 根因：
  - `NolonAccountsView.accountCardGrid` 对统一卡片的 `onTap/onAction` 一律转成 `onSelectProvider(provider.id)`，动作没有下沉到账号服务。
  - `NolonAccountsViewModel.accountCards(for:)` 在 Codex 分支没有注入 `activate`、`copy account id`、`copy auth path` 等卡片动作，也没有标记当前激活账号态。
- 修复：
  - 为统一账号卡片动作增加 `copyAccountID` / `copyAuthPath`。
  - `Accounts` 页持有 Codex 当前激活账号 ID，并在 Codex 卡片上正确展示 active 态。
  - 非激活 Codex 快照卡片支持点击直接激活，右键菜单支持复制账号 ID 与 Auth 路径。
  - 卡片动作通过 `CodexAuthActivationCoordinator` / `CodexAuthManager` / `NSPasteboard` 真正执行，不再统一跳 provider。
- 测试：
  - 新增 `testBDD_GivenInactiveCodexSnapshotCard_WhenBuildingAccountCards_ThenIncludesActivateAndCopyMenu`
  - 新增 `testBDD_GivenActiveCodexSnapshotCard_WhenBuildingAccountCards_ThenShowsActiveStateWithoutActivateAction`
  - 新增 `testBDD_GivenCodexSnapshotID_WhenCopyingAuthPath_ThenPasteboardWriterReceivesSnapshotPath`
  - 新增 `testBDD_GivenCodexSnapshotID_WhenActivatingFromAccountsPage_ThenUsesActivationCoordinatorClosure`

## 主页面/子页面统一标签（2026-03-12）
- 目标：给主页面与当前子页面增加统一、稳定、可见的页面名标签，方便联调、截图与问题对齐。
- 文档：新增 `docs-dev/features/page-marker-route-labels-2026-03-12.md`，约定 `Accounts`、`Plugin Management`、Provider 页面/子页、`Resource Center`/子页都应显示轻量标签。
- 实现：
  - 新增统一组件 `PageMarkerView` 与纯解析器 `PageMarkerRouteResolver`。
  - `NolonAccountsView`、`PluginManagementView`、`ProviderContentTabView`、`ProviderDetailGridView`、`ResourceCenterView` 接入统一 `pageMarker(...)` modifier。
  - Provider 详情页显示 `Provider / Tab`，资源中心显示 `Resource Center / Tab`，导航页显示 provider 名。
- 第二轮增强：
  - 页面标签仅在 `DEBUG` 构建中可用。
  - 默认关闭，需通过菜单栏 `Debug -> Show Page Markers` 手动开启。
  - 标签自动附带调用点元信息 `#file #line #function`，并支持右键复制单个标签或整条路径。
- 测试：
  - 新增 `PageMarkerRouteResolverTests`，覆盖 `Accounts`、Provider 子页、Provider 导航页、Resource Center 子页的标签解析。
  - 补充覆盖 debug 开关启用条件、metadata 文本格式、复制文本拼接顺序。
  - 本轮已发起 `xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -only-testing:nolonTests/PageMarkerRouteResolverTests` 定向验证；由于 Xcode 依赖解包/编译耗时较长，当前会话内尚未拿到最终完成回执。

## 页面标签收敛为定位按钮（2026-03-12）
- 需求调整：页面顶部不再展示一串可交互标签，而是统一收敛为单个高亮“定位按钮”；点击后直接复制完整定位文本。
- 交互约束：
  - 仍仅在 `DEBUG` 构建可用，且默认关闭。
  - 需通过菜单栏 `Debug -> Show Page Markers` 手动开启。
  - 按钮文本包含页面路径与调用点元信息 `#file #line #function`。
- UI 方向：
  - 页面顶部按钮使用高对比橙色胶囊底，提升可见性。
  - 账号卡片与资源卡片右上角同步接入紧凑版定位按钮，点击同样复制完整文本。
- 代码落点：
  - `PageMarkerView` 改为单按钮复制模型，保留 `PageMarkerRouteResolver` 负责路径与 metadata 拼接。
  - `AccountSummaryCard`、`ResourceCardShell` 及远程 skill/workflow/mcp 卡片接入 `debugLocatorOverlay(...)`。
- 测试：
  - `PageMarkerRouteResolverTests` 从旧 `copyText/metadataItem` 语义切换到 `locatorText/metadataText`。
  - 已再次发起 `nolon-tests` 下的定向 `xcodebuild test`，当前会话结束前仍在大型依赖编译阶段，未拿到最终通过回执。

## DebugPageLocatable 协议收口（2026-03-12）
- 用户希望“我们自己定义的类型”统一支持 debug 定位按钮，因此在 `PageMarkerView` 上层新增了两套协议：
  - `DebugPageLocatable`：页面根视图声明 `debugPageMarkerItems` 后，复用统一 `debugPageLocator()` / `debugPageLocatorText(...)`
  - `DebugCardLocatable`：卡片根视图声明 `debugCardMarkerItems` 后，复用统一 `debugCardLocator()` / `debugCardLocatorText(...)`
- 已接入的页面类型：
  - `NolonAccountsView`
  - `PluginManagementView`
  - `ProviderContentTabView`
  - `ProviderDetailGridView`
  - `ResourceCenterView`
- 已接入的卡片类型：
  - `UnifiedAccountCard`
  - `ResourceCardShell`
- 测试：
  - `PageMarkerRouteResolverTests` 新增 fake page/card，覆盖协议默认实现生成的 locator text。
  - 本轮定向测试再次发起，仍在 Xcode 大型依赖编译阶段，当前未拿到最终完成回执。

## Debug 定位开关持久化（2026-03-12）
- 新需求：在 Debug 模式下记住 `Debug -> Show Page Markers` 的选择，避免每次重启 app 后重复手动开启。
- 实现：
  - `AppCommandState` 改为注入 `UserDefaults`，`isDebugPageMarkersEnabled` 在 `didSet` 时写入持久化存储。
  - `shared` 继续走默认 `.standard`，测试可通过自定义 suite 隔离验证。
- 测试：
  - 新增 `AppCommandStateTests`，覆盖“开启后重建状态对象仍保持 true”。
  - 已发起 `AppCommandStateTests + PageMarkerRouteResolverTests` 定向 `xcodebuild test`，当前仍在依赖编译阶段，未拿到最终回执。

## Page Marker 三项体验修复（2026-03-12）
- 用户反馈：
  - Page Marker 文本过长影响点击与可读性；
  - 侧边面板没有 Marker；
  - 部分自定义视图未统一标记。
- 修复：
  - `DebugLocatorButton` 改为高对比“纯图标”按钮（保留点击复制完整定位文本，tooltip 展示全文），避免长文本覆盖卡片点击区域。
  - 侧边面板补齐 marker：`ProviderSidebarView`、`RemoteRepositorySidebarView`、`ResourceCenterTabView`、`PluginManagementNavigationView`。
  - 主容器页与卡片协议接入保持统一：页面类型走 `DebugPageLocatable`，卡片类型走 `DebugCardLocatable`。
- 结果：
  - UI 上 Marker 统一为小图标；
  - 左侧/中间导航面板可见并可复制定位；
  - 当前主流程中的自定义页面与卡片容器已覆盖统一标记。
  - `xcodebuild build -project nolon.xcodeproj -scheme nolon -destination 'platform=macOS'` 通过（`BUILD SUCCEEDED`）。

## AccountSummaryCard 切换交互去重（2026-03-12）
- 交互结论：对于支持切换/激活的账号卡片，入口应为“点击整张卡片”，不再同时展示重复的 `Activate`/切换按钮。
- 实现：
  - 在 `AccountCardViewDataMapper` 增加去重规则：当 `tapBehavior == .activate` 时，自动移除重复的 `.activate` 主操作按钮。
  - 这样 `ProviderUsageView` 与 `NolonAccountsView` 里已有的整卡点击切换逻辑继续保留，但卡片底部不会再出现重复切换 CTA。
- 测试：
  - 新增 `AccountCardViewDataMapperTests`，覆盖“整卡切换时移除重复 activate 按钮”和“非整卡切换场景保留其它主操作”。
  - 首次定向测试被无关的 `PageMarkerRouteResolverTests.swift` 缺少 `import SwiftUI` 阻塞，已顺手补齐该测试导入。
  - 最终 `xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/AccountCardViewDataMapperTests` 通过，2 个用例全部成功。

## 18:39 Codex 账号卡片点击切换失效修复
- 现象：Provider Usage 中 Codex 卡片点击无效，无法触发账号切换。
- 根因：`UnifiedAccountCard` 在 `tapBehavior == .none` 时仍注册 `.onTapGesture`，空手势吞掉外层 `onTapGesture`（外层才负责 `requestActivateCodexAccount`）。
- 决策：仅在 `tapBehavior != .none` 时安装卡片点击手势，避免手势冲突。
- 变更：
  - `nolon/Skills/Views/Components/AccountSummaryCard.swift`
  - `nolonTests/UnifiedAccountCardTapBehaviorTests.swift`
- 验证：
  - `xcodebuild -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/UnifiedAccountCardTapBehaviorTests test` 通过（2/2）。

## 18:49 Codex 账号与用量骨架样式统一
- 需求：`ProviderDetailGridView -> ProviderUsageView` 中 Codex「账号与用量」加载骨架统一为「账号页」(`NolonAccountsView`) 的卡片骨架样式。
- BDD/TDD：先在 `ProviderUsageSkeletonPolicyTests` 新增“Codex loading 使用 unified account card”测试，先红后绿。
- 实现：
  - 新增 `CodexLoadingSkeletonStyle` 与 `ProviderUsageSkeletonPolicy.codexLoadingSkeletonStyle = .unifiedAccountCard`。
  - `ProviderUsageView.codexContent` 加载态将 `ProviderQuotaSection` 骨架替换为 `UnifiedAccountCardSkeleton(providerName:)`。
- 验证：
  - `xcodebuild -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/ProviderUsageSkeletonPolicyTests test`
  - 结果：4/4 通过。

## 18:54 账号页 Codex 卡片无法点击切换修复
- 现象：`NolonAccountsView` 中部分 Codex 卡片点击后未触发切换。
- 根因：账号页仅将“纯 UUID”视为可切换账号；对 `codex.<uuid>` 格式未解析，导致 `tapBehavior` 退化为 `.openProvider`。
- 修复：
  - 新增 `NolonAccountsViewModel.resolveCodexAccountID(from:)`（支持 `uuid` 与 `codex.<uuid>`）。
  - `NolonAccountsView` 中激活/复制操作与卡片可切换判断统一改用该解析器。
- 测试：
  - 新增 `nolonTests/NolonAccountsCodexIDResolverTests.swift`，覆盖 direct/prefixed/invalid 三个场景。
  - `xcodebuild ... -only-testing:nolonTests/NolonAccountsCodexIDResolverTests test` 通过（3/3）。

## 19:14 账号页复用 ProviderUsageViewModel（按厂商实例）
- 决策：账号页不再只复用底层 activation coordinator，而是按 provider 复用 `ProviderUsageViewModel`（一厂商一个 VM）。
- 实现：
  - `NolonAccountsViewModel` 新增 `providerUsageViewModelFactory` 与 `providerUsageViewModelsByProviderID` 缓存。
  - `refreshAsync` 改为优先从对应 `ProviderUsageViewModel` 执行 `load()` 并读取 `outcomes`/`usageProvider`。
  - `activateCodexAccount` 改为优先通过对应 `ProviderUsageViewModel.requestActivateCodexAccount + confirmActivate` 执行切换，缺省再回退原路径。
- 兼容性：保留原 `codexActivateAction` 注入能力，并桥接到 `ProviderUsageViewModel` 的激活闭包，避免现有测试注入失效。
- 验证：
  - `xcodebuild ... -only-testing:nolonTests/NolonAccountsViewModelTests/testBDD_GivenCodexSnapshotID_WhenActivatingFromAccountsPage_ThenUsesActivationCoordinatorClosure test` 通过。
  - `xcodebuild ... -only-testing:nolonTests/NolonAccountsCodexIDResolverTests test` 通过（3/3）。

## 19:24 账号页与账号用量页共享同一 ProviderUsageViewModel 实例
- 目标：`NolonAccountsView` 与 `ProviderDetailGridView -> ProviderUsageView` 对同一 provider 使用同一个 `ProviderUsageViewModel`（按 provider.id）。
- 实现：
  - 新增 `ProviderUsageViewModelStore`（`ProviderUsageViewModelStore.shared`）按 `Provider.ID` 缓存并复用 VM。
  - `ProviderUsageView` 初始化与 provider 切换改为从该 store 获取 VM，不再每次 `ProviderUsageViewModel(provider:)` 新建。
  - `NolonAccountsViewModel` 默认 VM 工厂改为从 store 取 VM（自定义依赖注入测试场景仍可走独立工厂，避免影响测试可控性）。
- 验证：
  - `xcodebuild ... -only-testing:nolonTests/ProviderUsageViewModelStoreTests test` 通过（1/1）。
  - `xcodebuild ... -only-testing:nolonTests/NolonAccountsViewModelTests/testBDD_GivenCodexSnapshotID_WhenActivatingFromAccountsPage_ThenUsesActivationCoordinatorClosure test` 通过。
- 备注：中途一次并发测试触发 `build.db locked`，串行重跑后通过。
