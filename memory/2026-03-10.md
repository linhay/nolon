## Provider vendor 分类与 Nolon Accounts

- 决策：Provider 一级分类继续保留 `vendor` / `project`，`vendor` 下新增二级分类 `original` / `integrated`。
- 决策：新增模板能力字段，至少包含 `vendorCategory`、`supportsNativeMcpConfig`、`supportsAccounts`、`supportsMultiAccount`、`secondaryResourceLabel`，避免继续依赖 `templateId` 特判。
- 决策：`pi` 模板按 `badlogic/pi-mono/packages/coding-agent` 官方文档建模，使用 `~/.pi/agent` 作为 vendor 根目录，项目级根目录为 `<project>/.pi`。
- 决策：`pi` 的 secondary resource 文案统一为 `Prompts`，不再误用 `Workflow`。
- 决策：`pi` 在统一账号页本次仅提供只读状态摘要，基于 `~/.pi/agent/auth.json` 检测账号状态，不开放登录、切换、删除。
- 结果：新增 `Accounts` 全局页面，并挂载到 Sidebar 的 `Tools` 分组；非 `pi` provider 复用现有 Usage 管理视图，`pi` 展示只读账号卡片。
- 结果：Add Provider、Edit Provider、Onboarding、Sidebar 已按原始厂商 / 集成厂商完成分组改造；`pi` 已作为内建 integrated vendor 模板接入。
- 验证：`swift test --package-path libs/Providers --filter ProviderCatalogTemplateTests --filter ProviderSettingsTests` 通过；`NO_SPM_UPDATE=1 SKIP_CLEAN=1 ./build.sh` 通过；`xcodebuild test -project nolon.xcodeproj -scheme nolon-app -only-testing:nolonTests/ProviderPresentationSectionsTests -only-testing:nolonTests/NolonAccountsViewModelTests -destination 'platform=macOS'` 通过。
- 备注：主 app 的测试 scheme 实际为 `nolon-app`，不是 `nolon`；中途出现过 `build.db` 锁和旧的 DerivedData 包目录异常，均通过改为串行测试规避。
- 测试基础设施：新增 shared scheme `nolon-tests`，只包含 `nolon` + `nolonTests`，不再把 `nolonUITests` 纳入测试图，便于后续单元测试独立执行。
- 风险：`nolon-tests` 虽然能正确生成测试构建产物，但当前 `xcodebuild test` / `test-without-building` 仍可能停在 Xcode 的测试执行调度层，未稳定拉起 `xctest` 进程；这是现有工程测试运行链路问题，和本次功能改动无关。
- 测试基础设施：补充根目录 `nolon-tests.xctestplan`，并将 `nolon-tests.xcscheme` 绑定到该 test plan；`xcodebuild -project nolon.xcodeproj -scheme nolon-tests -showTestPlans` 已能识别 `nolon-tests`。
- 验证：`xcodebuild build-for-testing -project nolon.xcodeproj -scheme nolon-tests -testPlan nolon-tests -only-testing:nolonTests/ProviderPresentationSectionsTests -only-testing:nolonTests/NolonAccountsViewModelTests -destination 'platform=macOS,arch=arm64'` 通过，说明新的 unit-only 测试入口可正常构建。
- 风险：`xcodebuild test-without-building -xctestrun .../nolon-tests_nolon-tests_macosx26.2-arm64.xctestrun` 仍停在 `IDERunDestination: Supported platforms for the buildables in the current scheme is empty.`，说明剩余阻塞点已经收敛到 Xcode 的 test launch / run destination 层。
- 对照结论：同样的问题可在原生 `nolon-app` 生成的 `.../nolon-app_nolon-app_macosx26.2-arm64.xctestrun` 上复现；因此 `test-without-building` 的 run destination 卡死是现有工程/Xcode 运行层问题，不是新增 `nolon-tests` scheme 或 test plan 引入的回归。
- 补充验证：`xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -testPlan nolon-tests -only-testing:nolonTests/ProviderPresentationSectionsTests -only-testing:nolonTests/NolonAccountsViewModelTests -destination 'platform=macOS'` 在当前机器上也会停在构建完成后的 runner 拉起前，进程树中未出现 `xctest` 或新的 `nolon.app` host；因此 `nolon-tests` 的日常执行也受同一层 Xcode 测试启动问题影响。
- 结构结论：`nolonTests` 中大量测试直接 `@testable import nolon`，一旦移除 `TEST_HOST/BUNDLE_LOADER`，会在链接阶段丢失 `nolon` 可执行目标符号；因此当前工程不能通过“仅修改 test target 配置”把 `nolonTests` 直接变成独立 non-hosted target。
- 落地调整：将 `ProviderPresentationSections` 与 `PiAuthStatus` 解析迁入 `libs/Providers/Sources/NolonResourceKit`，并在 `libs/Providers/Tests/ProvidersTests` 新增对应测试，作为真正不依赖 app host 的独立 SwiftPM test target 路径；app 层仅保留 UI 编排与本地化文案。
- Review 修复：`NolonAccountsView` 对 `PiAuthStatus` 的引用改为直接使用 `NolonResourceKit` 顶层类型，修复 Accounts 页编译错误；`ProviderTemplate.secondaryResourceLabel` 改为语义化 `SecondaryResourceKind + localization key/fallback`，由 app 层负责本地化，避免 `Prompts/Commands/Workflows` 英文字符串直接渲染到中文界面。
- 回归验证：`swift test --package-path libs/Providers --filter ProviderCatalogTemplateTests` 通过；`NO_SPM_UPDATE=1 SKIP_CLEAN=1 ./build.sh` 通过，确认 review 提到的编译失败已消除。
- Accounts 修正：`Tools -> Accounts` 不再嵌入完整 `ProviderUsageView`，改为统一聚合摘要卡片（总数/成功数/失败数/最近更新时间 + 账号邮箱 + 主窗口用量百分比），满足“用量聚合展示”诉求。
- Accounts 修正：新增 `codexXcode -> codex` 的 usage provider 映射，并在 `codex/codexXcode` 卡片读取 `CodexAuthManager` + provider auth 文件，补齐账号信息展示，修复 `codex(xcode)` 无账号摘要问题。
- 文档与测试：更新 `docs-dev/features/provider-vendor-categories-and-nolon-accounts-2026-03-10.md` 验收场景（新增 codexXcode 账号显示与用量聚合项）；`xcodebuild test -project nolon.xcodeproj -scheme nolon-app -only-testing:nolonTests/NolonAccountsViewModelTests -destination 'platform=macOS,arch=arm64'` 通过；`NO_SPM_UPDATE=1 SKIP_CLEAN=1 ./build.sh` 通过。
- 本地化与并发修正：补齐 `accounts.provider.unsupported`、`accounts.usage.aggregate`、`accounts.usage.primary_used`、`accounts.usage.updated_at` 中英文 key；并将 `UsageMonitorSettingsStore.shared` 从默认参数移入 `@MainActor` 初始化体，消除 Swift 6 并发预警。
- 交互修正：在 `MainSplitViewModel` 新增 `openProviderFromAccounts(_:)`，从 `Tools -> 账号` 卡片进入 provider 时强制落到 `Skills`，不再停留中间 `账号/用量` tab；`NolonAccountsViewModelTests` 仍全绿，`./build.sh` 通过。
- 决策更新：`Codex(Xcode)` 改为集成厂商（`vendorCategory = integrated`），并将 `supportsAccounts = false`，统一“集成厂商没有账号”口径。
- 账号页筛选收敛：`ProviderPresentationSections.accountProviders` 仅返回 `original + supportsAccounts` 的 vendor，`Tools -> Accounts` 不再渲染 `codexXcode/pi` 等集成厂商账号卡片。
- 结构修正：`MainSplitView` 移除中间 `NolonAccountsNavigationView`，`Tools -> Accounts` 不再显示中间“账号”tab；受影响测试 `ProviderCatalogTemplateTests`、`ProviderPresentationSectionsTests`、`NolonAccountsViewModelTests` 均通过。
- 样式对齐（账号页）：`NolonAccountsView` 按设计稿增强为深色卡片体系，新增顶部 `Refresh All + Add Account` 动作区，Provider 分组头部计数、账号卡状态点、ghost 进度行、Plan chip 与更新时间 footer，并保持原有账号数据来源与跳转行为不变。

## Claude 账号配置与自定义域名调研

- 结论：当前 Nolon 未实现 Claude 账号配置链路，缺少 `~/.claude/settings.json` 的读写能力，Claude 模板仍为 `supportsAccounts = false`。
- 结论：参考 `references/cc-switch`，Claude 配置核心字段可收敛为 `env.ANTHROPIC_AUTH_TOKEN/ANTHROPIC_API_KEY` + `env.ANTHROPIC_BASE_URL`。
- 决策建议：采用“单 Provider + 多账号快照”方案（类似 Codex 账号管理），不走“新增多个 Claude provider”路径。
- 文档落地：新增 `docs-dev/features/claude-account-config-and-custom-domain-2026-03-10.md`（需求/BDD）与 `docs-dev/dev/claude-account-config-and-custom-domain-research-2026-03-10.md`（技术调研/实现路线/TDD 计划）。
