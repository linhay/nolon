# 2026-02-27

## Add Repository 收尾（SDK -> App）
- 新增 `ProviderSettings.upsertRemoteRepository(_:)`，统一“存在则更新，不存在则新增”的仓库写入语义。
- 修复目录确认流程：`RemoteRepositorySidebarViewModel.confirmDirectorySelection` 改为 upsert，避免编辑仓库后二次新增同 id 记录。
- 目录候选预勾选逻辑调整：若仓库已有 `skillsPaths`，按路径精确预选；无配置时保持全选。
- Add Repository 页面收敛：
  - 移除 Clawdhub / GlobalSkills 在可添加模板中的入口（仅保留 Git / Local Folder）。
  - 移除 Git 手工 `Skills Paths` 输入区，改为“同步后自动扫描 + 目录多选确认”。
  - 保留 URL 导入子路径 hint，作为 `preferredSkillsPaths` 参与后续预勾选。
- 测试：
  - `nolonTests/AddRepositoryViewModelTests`
  - `nolonTests/RemoteRepositorySidebarViewModelTests`
  - `swift test --package-path libs/Providers --filter NolonResourceKit`
  - `xcodebuild -project nolon.xcodeproj -scheme nolon-app -configuration Debug build`
- 决策：`libs` 下仅保留 `Providers`，其余视为参考项目。
- 行动：将 `CodexBar/STFilePath/STJSON/agent-skills/codex/oh-my-opencode/opencode/SKProcessRunner/SKIntelligence` 移至 `reference-projects/`，并在 `.gitignore` 忽略 `reference-projects/`。
- 同步：`SKProcessRunner` 对齐 `origin/master` 后移出；`SKIntelligence` 对齐 `origin/main` 后移出。
- 清理：删除 `.gitmodules`（不再维护这些子模块配置）。
