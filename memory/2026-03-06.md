## Clawhub URL 导航搜索修复
- 现象：通过 `nln://clawhub.ai/steipete/gemini` 打开后，列表显示全量，未按 `gemini` 搜索。
- 根因：`ResourceCatalogGridView` 首次加载时 `normalizedSearchQuery` 仅读取 `debouncedSearchText`，而 URL 注入时 `searchText` 已有值但 `debouncedSearchText` 初始为空。
- 决策：增加 `resolveSearchQuery` 逻辑，在 Clawdhub 场景下优先用 debounced，空时回退到 `searchText`；并在 init 用 `searchText.wrappedValue` 初始化 debounced state。
- 验证：新增 `ResourceCatalogGridViewSearchQueryTests` 覆盖优先值、回退值、非 Clawdhub 三类路径；与 `ResourceCatalogGridViewModelTests` 一起通过。

## AppKitProgressView 约束警告修复
- 日志：`AppKitProgressView` 出现 `maximum length ... doesn't satisfy min <= max`。
- 根因判断：`SearchField` 将小号 `ProgressView` 强制为 `14x14`，小于 AppKit 指示器固有尺寸（约 16pt），触发布局抖动告警。
- 处理：改为 `.frame(minWidth: 16, minHeight: 16)`，避免强压缩。
- 备注：`AFIsDeviceGreymatterEligible Missing entitlements` 与 `Unable to obtain a task name port right` 属系统/调试权限噪音，通常不影响功能。

## Codex Runtime 进程 5s 自动轮询
- 目标：Runtime Tab 在不手动刷新时，也能自动看到进程增删。
- 决策：只轮询 `runtimeList`（每 5 秒），不在轮询周期内重复拉取 diagnostics，降低开销与干扰。
- 行为：
  - 进入 Tab 启动轮询，离开 Tab 停止轮询。
  - 若当前选中 PID 已退出，自动切换到首个可用 PID 并刷新该 PID 日志。
- 验证：`CodexRuntimeTabViewModelTests` 新增轮询场景并通过；`CodexRuntimeTabConfigurationTests` 通过。

## Codex Runtime 诊断信息下沉到条目
- 需求：`Provider/Accounts/Active/Running/Binary/Path Active/Executable/Hint` 不再放顶部汇总，改放到 `Runtime Processes` 具体条目中。
- 决策：仅在条目展开态展示该组诊断，避免列表拥挤；日志区域保留在展开态下方。
- 实现：`CodexRuntimeTabView` 移除顶部 `diagnosticsSection`，改为条目内 `processDiagnosticsSection`；`CodexRuntimeTabViewModel` 提供 `processDiagnosticsRows(for:)`。
- 验证：新增 `testBDD_GivenDiagnosticsAndProcess_WhenBuildingProcessDiagnosticRows_ThenIncludesExpectedFields` 并通过。

## Remote 视图布局噪音进一步收敛
- 用户仍反馈 `AppKitProgressView` / `PlatformTextFieldAdaptor` 的 min/max 约束日志。
- 处理：将 Remote 链路的 `ProgressView` 替换为 SF Symbol 加载态（SearchField、ResourceCatalogGridView、RemoteRepositorySidebarView），避免触发 AppKit spinner 约束路径；同时让搜索中提示占位常驻（通过 opacity）降低 TextField 宽度抖动。
- 验证：`nolon-app` 定向测试通过。

## Clawhub 安装失败（429）修复
- 复现：`nolon remote install --kind skill --slug gemini --base-url https://clawdhub.com` 出现 `Error [rate_limited]`，安装失败。
- 根因：`SkillsRepositoryFacade.downloadRemoteResource` 仅在 TLS/网络错误时做 `clawdhub.com -> clawhub.ai` 回退；HTTP 429 不触发回退。
- 修复：
  1) 下载接口收到 429 时，按 `Retry-After`（1~30 秒）同主机重试一次；
  2) 若仍失败且 base 为 `clawdhub.com`，触发镜像回退到 `clawhub.ai`。
- 验证：`swift test --package-path libs/Providers --filter SkillsRepositoryFacadeTests` 全部通过；`nolon remote install` 在 `--base-url https://clawdhub.com` 下复测成功。

## Clawhub 域名统一为 clawhub.ai
- 需求：移除 `clawdhub.com`，运行时统一使用 `clawhub.ai`。
- 处理：
  1) Providers 源码中所有默认 remote baseURL 改为 `https://clawhub.ai`（CLI parser/command、RemoteRepository template、ClawdhubRepository、SkillUpdateChecker 等）；
  2) Skill 安装/锁文件中记录的 `sourceUrl` 改为 `https://clawhub.ai/skills/{slug}`；
  3) `RepositoryDraftService` 的 Clawhub host 白名单移除 `clawdhub.com`；
  4) `SkillsRepositoryFacade` 关闭 `.com -> .ai` mirror fallback（域名迁移后不再需要）。
- 验证：`SkillsRepositoryFacadeTests`、`NolonResourceKitTests` 通过，并同步更新相关测试默认 URL 断言。
- 排查 UI 安装失败：新增多层日志（ResourceCatalogGridView 点击/超时、MainSplitViewModel 安装开始/成功/失败、SkillsRepositoryFacade 远程 list/download 请求/429 重试/invalid reuse fallback）。
- 验证：`swift test --package-path libs/Providers --filter SkillsRepositoryFacadeTests`（26 tests passed），`xcodebuild -project nolon.xcodeproj -scheme nolon-app -destination 'platform=macOS' -only-testing:nolonTests/ResourceCatalogGridViewSearchQueryTests -only-testing:nolonTests/ResourceCatalogGridViewModelTests test` 通过。
- 复现并定位 nln://clawhub.ai/steipete/gemini 安装失败：`/api/v1/download?slug=gemini|steipete/gemini` 在当前网络环境频繁返回 HTTP 429（含 Retry-After）。
- 修复：SkillsRepositoryFacade 下载从“429 仅重试 1 次”改为“最多 3 次同主机重试”；ResourceCatalogGridView 安装超时从 15s 提升到 45s，减少限流场景误判。
- 新旧回归通过：SkillsRepositoryFacadeTests（26 passed），ResourceCatalogGridView* tests（xcodebuild passed）。
- 根据用户运行日志定位：Resource list 请求仍指向 https://clawdhub.com，根因是 `remote_repositories` 本地持久化保留旧域名。
- 修复：ProviderSettings 在 loadSettings 时对 `.clawdhub` 仓库执行迁移，强制 `baseURL=https://clawhub.ai` 并回写；新增 ProviderSettingsTests 覆盖迁移行为。

## Codex Runtime 条目补齐 Active 邮箱反查
- 问题：Runtime 条目 `Active` 只能显示 account id，无法反查到邮箱/账号名。
- 根因：`CodexRuntimeCLIService.diagnostics` 仅调用 `authStatus`，未读取 `authList`，缺少 `id -> name/email` 映射来源。
- 修复：
  1) diagnostics 并行读取 `authStatus + authList`，用 active id 反查账号 `name/email`；
  2) 诊断快照新增 `activeAccountName/activeAccountEmail`；
  3) UI 的 `Active` 字段显示为 `Name (email) [id: xxxx...xxxx]`（无邮箱时回退 name 或 id）。
- 验证：先改测试触发编译红灯，再实现后通过 `xcodebuild test -project nolon.xcodeproj -scheme nolon-app -destination 'platform=macOS' -only-testing:nolonTests/CodexRuntimeTabViewModelTests`。

## 切换账号 Thread Performance Checker（QoS 反转）修复
- 现象：切换账号时出现 `User-initiated` 等待 `Utility` 的 priority inversion，栈落在 `CodexStatusProbe.fetch -> CodexCommandExecutor.resolveExecutable -> resolveExecutableInUserShellSync`。
- 决策：状态探测链路禁止同步 shell 解析；改走异步可执行路径解析，保留错误语义映射。
- 实现：
  1) `CodexCommandExecutor` 新增 `requireResolvedExecutableAsync()`；
  2) `CodexStatusProbe.fetch()` 改为 `await requireResolvedExecutableAsync()`；
  3) `CodexCLIError.executableNotFound` 映射为 `CodexStatusProbeError.codexNotInstalled`。
- 测试：
  - `CodexCLIKitEnvironmentTests` 新增 2 个异步解析用例（路径命中/缺失抛错）并通过。
  - 全量 `swift test` 仍有与本改动无关的既有失败：`NolonCoreCLIKitTests`（gemini doctor、plugin start）。
- 用户提供删除崩溃栈（MainSplitView.deleteRemoteSkill:204）。实施两层修复：
  1) 删除结果文案 `String(format:)` `%d` -> `%ld`，避免 Swift Int 在 64 位格式不匹配导致潜在崩溃；
  2) 删除链路加固：ResourceDeletionCoordinator 去主线程执行、删除前冻结 provider 快照并增加开始/结束日志。
- 回归：ResourceCatalogGridView* 测试通过；ProviderSettingsTests 通过。

## 2026-03-06 11:26 删除全局 skill(gemini) 崩溃排查
- 现象：删除全局技能 gemini 时主线程崩溃，日志大量出现 AppKit menu inconsistency 噪音。
- 结论：高风险点在删除结果文案 `String(format:)` 变参格式化（Int 与 `%d/%ld` 占位符不一致导致未定义行为）；同时删除链路加入 provider snapshot 与结构化日志。
- 变更：
  - `ResourceCatalogGridView.showDeleteResult` 改为非 printf 的令牌替换渲染，避免 C vararg 崩溃面。
  - 新增 `ResourceCatalogGridViewDeleteMessageTests` 覆盖 success/partial 两条消息路径。
  - 删除执行链路保留 `com.nolon` 日志，便于后续只看业务日志。
- 验证：`xcodebuild test -scheme nolon-app` 针对删除与资源中心相关测试全部通过。

## 2026-03-06 11:40 删除异步边界收敛
- 新堆栈显示崩点前移到 `outlined init with copy of RemoteSkill`，发生在删除回调把 `RemoteSkill` 穿过 `Task`/async thunk 时。
- 决策：删除链路不再跨异步边界传递 `RemoteSkill/RemoteWorkflow/RemoteMCP` 整体模型，统一改为 `ResourceDeleteRequest`，仅承载 `slug/displayName/resourceType`。
- 落地：
  - `MainSplitViewModel` 删除入口合并为 `deleteRemoteResource(_:target:)`。
  - `ResourceCenterView` / `ResourceCatalogGridView` 删除回调统一改成 `onDeleteResource`。
  - `ResourceCatalogGridView` 不再用持有远程模型的私有 `DeleteRequest` 枚举。
- 验证：`ResourceDeleteRequestTests`、`ResourceDeletionCoordinatorTests`、`ResourceCatalogGridViewDeleteMessageTests`、`ResourceCatalogGridViewSearchQueryTests` 通过。

## 2026-03-06 11:50 删除 async 参数进一步收窄
- 新堆栈仍崩在 `outlined init with copy of ResourceDeleteRequest`，说明问题是自定义删除请求对象穿过 async 边界本身，而不是具体字段内容。
- 决策：删除 async 签名继续收窄，只允许 `String slug` 穿过 `Task` / async thunk；`ResourceDeleteRequest` 仅保留在同步 UI 层用于展示确认弹窗。
- 落地：
  - `MainSplitViewModel` 改回三条删除入口：`deleteRemoteSkill(slug:)`、`deleteRemoteWorkflow(slug:)`、`deleteRemoteMCP(slug:)`。
  - `ResourceCenterView` / `ResourceCatalogGridView` 删除回调改为按资源类型分别传 `slug`。
  - `ResourceCatalogGridView.sheet(item:)` 内在创建 `Task` 前先抽出 `resourceSlug` / `resourceType`，避免闭包捕获 `ResourceDeleteRequest`。
- 验证：同一组资源删除相关测试通过。

## 2026-03-06 12:30 删除链路再次收窄到 provider ID 计划
- 新堆栈继续落在 `ResourceDeletionCoordinator.execute(resourceSlug:resourceType:target:providers:)` 入口的 retain，上游已排除 `RemoteSkill`、`ResourceDeleteRequest` 和 `OSLog` 插值。
- 决策：删除 async 边界不再传 `Provider` 数组或 `ResourceDeleteTarget`；UI 层先同步生成 `ResourceDeletionExecutionPlan(providerIDs, removeGlobalCache)`，ViewModel 再在本地把 `providerIDs` 解析成仅含 `id/name/defaultSkillsPath/workflowPath/templateId` 的 `ResourceDeletionProviderTarget` 快照。
- 落地：
  1) `ResourceCatalogGridView` 删除回调改为传 `slug + ResourceDeletionExecutionPlan`；
  2) `MainSplitViewModel` 删除入口改为按 plan 执行；
  3) `ResourceDeletionCoordinator.execute` 改为只接收 provider 快照数组和 `removeGlobalCache`；
  4) 新增 `ResourceDeletionExecutionPlanTests`，并更新 `ResourceDeletionCoordinatorTests`。
- 验证：`ResourceDeletionExecutionPlanTests`、`ResourceDeletionCoordinatorTests`、`ResourceDeleteRequestTests`、`ResourceCatalogGridViewDeleteMessageTests`、`ResourceCatalogGridViewSearchQueryTests` 全部通过。

## 2026-03-06 12:45 删除结果文案与资源中心主线程状态修复
- 新崩溃栈确认删除成功提示仍落在 `ResourceCatalogGridView.buildDeleteResultMessage`，异常为 `NSString stringByReplacingOccurrencesOfString:withString:` 收到 nil 参数。
- 决策：删除结果文案不再依赖 Foundation `replacingOccurrences`/`String(format:)`，统一改为纯 Swift 令牌模板渲染，并在本地化模板缺失或占位符不全时回退到内建模板。
- 补充：`ResourceCenterViewModel` 显式标记 `@MainActor`，资源中心初始化改为 `ResourceCenterViewModel(selectedTab:)`，避免视图重建时把选中 tab 的初始化逻辑散落在闭包里。
- 测试：
  1) `ResourceCatalogGridViewDeleteMessageTests` 新增空模板回退覆盖；
  2) `ResourceCenterViewModelTests` 改为 `MainActor.run` 方式验证初始化选中 tab，避免 XCTest 对 `@MainActor` 测试类的宿主崩溃。
- 验证：
  1) `xcodebuild test -project nolon.xcodeproj -scheme nolon-app -destination 'platform=macOS' -only-testing:nolonTests/ResourceCatalogGridViewDeleteMessageTests -only-testing:nolonTests/ResourceDeletionExecutionPlanTests -only-testing:nolonTests/ResourceDeletionCoordinatorTests -only-testing:nolonTests/ResourceDeleteRequestTests -only-testing:nolonTests/ResourceCatalogGridViewSearchQueryTests` 通过；
  2) `xcodebuild test -project nolon.xcodeproj -scheme nolon-app -destination 'platform=macOS' -only-testing:nolonTests/ResourceCenterViewModelTests` 通过。

## 2026-03-06 13:32 删除结果提示彻底移除通用 token renderer
- 新堆栈再次确认崩点落在 `ResourceCatalogGridView.replaceDeleteMessageToken` 的 `String.append`，说明即便不走 Foundation `replacingOccurrences`，通用模板替换仍然会暴露字符串对象生命周期/桥接风险。
- 决策：采纳 Gemini 的方向，只保留“局部值拷贝 + 主线程构建”，彻底删除通用 token renderer；删除结果文案改回受控格式化，并用 `Int64` 对应 `%ld` 占位，避免 Swift `Int` 与 C vararg 不匹配。
- 落地：
  1) `buildDeleteResultMessage` 先拷贝 `typeName/resourceSlug/failures` 到局部 `String`；
  2) success/partial 两条路径各自调用 `String.localizedStringWithFormat`；
  3) 删除 `renderDeleteMessageTemplate` / `replaceDeleteMessageToken`；
  4) 新增 malformed template 回退测试。
- 验证：
  1) `xcodebuild test -project nolon.xcodeproj -scheme nolon-app -destination 'platform=macOS' -only-testing:nolonTests/ResourceCatalogGridViewDeleteMessageTests -only-testing:nolonTests/ResourceDeletionExecutionPlanTests -only-testing:nolonTests/ResourceDeletionCoordinatorTests -only-testing:nolonTests/ResourceDeleteRequestTests -only-testing:nolonTests/ResourceCatalogGridViewSearchQueryTests` 通过；
  2) `xcodebuild test -project nolon.xcodeproj -scheme nolon-app -destination 'platform=macOS' -only-testing:nolonTests/ResourceCenterViewModelTests` 通过。

## 2026-03-06 13:44 删除结果提示改为零运行时格式化
- 背景：`ResourceCatalogGridView.buildDeleteResultMessage` 先后在自定义 token renderer 和 `String.localizedStringWithFormat` 路径上发生崩溃，用户堆栈分别落在 `append` / `swift_getObjectType` / `CVarArg` 桥接层。
- 决策：彻底移除删除结果提示中的运行时模板注入，不再使用 `String(format:)`、`String.localizedStringWithFormat`、`NSString.replacingOccurrences` 或自定义 token 替换。
- 实现：`ResourceCatalogGridView.buildDeleteResultMessage` 现在只根据首选语言分支直接拼接最终字符串；`localized(...)` 仅用于判断中英文分支，不再参与参数替换。
- 验证：`ResourceCatalogGridViewDeleteMessageTests` 新增首选语言为 `zh-Hans` 的成功文案用例，且与 `ResourceDeletionExecutionPlanTests`、`ResourceDeletionCoordinatorTests`、`ResourceDeleteRequestTests`、`ResourceCatalogGridViewSearchQueryTests`、`ResourceCenterViewModelTests` 一并通过。
- 影响：删除全局技能 `gemini` 后的 toast/message 构建路径已不再依赖 Foundation 变参格式化，可避免此前连续出现的崩溃链。

## 2026-03-06 13:58 删除结果提示进一步收敛
- 用户在删除全局技能 gemini 时继续崩溃，最新栈落在 ResourceCatalogGridView.buildDeleteResultMessage 的 Swift 字符串插值路径，而非 Foundation 格式化。
- 最终收敛策略：删除结果消息构造中不再使用任何字符串插值，success/partial 两条路径都改为局部 String 片段数组 joined()，并预先复制 slug/typeName/count/failures。
- 新增中文 partial 回归测试；相关删除链路回归测试通过。

## 2026-03-06 15:20 ResourceCatalogGrid 删除逻辑迁入 ViewModel
- 按 SwiftUI Expert / View Refactor / UI Patterns / Performance Audit 的要求，ResourceCatalogGridView 的删除请求、删除执行、删除结果 alert 和删除消息构造全部迁入 ResourceCatalogGridViewModel。
- View 层不再持有 deleteRequest/deleteResultMessage/showingDeleteResultAlert/pending delete state，只负责绑定 viewModel 状态。
- 删除结果消息构造改为使用请求入口的稳定 slug，而不是 ResourceDeleteExecutionResult.resourceSlug，避免删除后提示再次读取不稳定结果对象。
- 新增 ViewModel 删除回归测试，删除结果消息测试改为直接覆盖 ViewModel 纯函数。

## 2026-03-06 15:28 全局删除默认目标修复
- 用户反馈：删除全局技能时应用不再崩溃，但 `~/.nolon/skills/<slug>` 未被删除。
- 根因：`ResourceDeleteTargetSheet` 在 `preferredProvider == nil` 的全局入口仍默认选中 `providers.first`，导致删除计划被构造成“仅删单个 Provider”，`removeGlobalCache` 始终为 `false`。
- 决策：将删除目标 sheet 的初始选择抽成纯模型 `ResourceDeleteTargetSheetState`；全局入口默认 `deleteAll = true`、`selectedProviderID = nil`，仅在用户切回单 Provider 删除时才回退选择首个 Provider。
- 测试：新增 `ResourceDeleteTargetSheetStateTests`，覆盖 provider 入口、全局入口、切回 provider 的默认选择。
- 验证：`xcodebuild test -project nolon.xcodeproj -scheme nolon-app -destination 'platform=macOS' -only-testing:nolonTests/ResourceDeleteTargetSheetStateTests -only-testing:nolonTests/ResourceDeletionExecutionPlanTests -only-testing:nolonTests/ResourceCatalogGridViewModelTests` 通过。

## 2026-03-06 15:39 全局技能删除目标按仓库上下文修正
- 实机排查确认：用户真实环境中的 `~/.nolon/skills/gemini` 仍存在，说明此前修复只覆盖了 `preferredProvider == nil` 的场景，不足以处理“主界面当前选中了某个 Provider，但资源中心正在浏览 globalSkills 仓库”的路径。
- 根因：`ResourceCenterView` 一直将主界面 `targetProvider` 透传给 `ResourceCatalogGridView` 和安装状态刷新逻辑；当仓库类型是 `.globalSkills` 时，这会把全局删除误降级为 provider 删除。
- 决策：新增 `ResourceCenterViewModel.effectiveTargetProvider(for:fallback:)`，当选中仓库为 `.globalSkills` 时强制返回 `nil`，用于删除目标、已安装状态刷新以及安装后刷新闭包；只有非全局仓库才保留主界面的 provider 上下文。
- 测试：
  1) `MainSplitViewModelTests` 新增全局缓存删除集成测试，断言目录和已安装状态同时消失；
  2) `ResourceCenterViewModelTests` 新增 globalSkills / 非 globalSkills 的有效 targetProvider 分支测试；
  3) `ResourceDeleteTargetSheetStateTests` 保留全局默认删除目标覆盖。
- 验证：`xcodebuild test -project nolon.xcodeproj -scheme nolon-app -destination 'platform=macOS' -only-testing:nolonTests/MainSplitViewModelTests -only-testing:nolonTests/ResourceDeleteTargetSheetStateTests -only-testing:nolonTests/ResourceCenterViewModelTests` 通过。
- 15:54 修复资源中心仓库切换后的安装态刷新缺口：将 `refreshInstalledResources` 收口到 `ResourceCenterViewModel`，并在 `selectedRepository` 变化时触发刷新，确保从 provider 仓库切到 `.globalSkills` 时不再沿用旧 provider 作用域。新增 `ResourceCenterViewModelTests.testBDD_GivenGlobalSkillsRepository_WhenRefreshInstalledResources_ThenIgnoreProviderScopedInstallState`，并回归通过 `MainSplitViewModelTests` / `ResourceDeleteTargetSheetStateTests` / `ResourceCenterViewModelTests`。同时确认此前用户界面一直运行旧进程，已重启到 `15:54:15` 的最新 Debug 构建。
- 资源中心全局技能删除继续收敛：全局仓库中的删除请求不再进入 Provider 目标选择 sheet，而是直接确认 `allProvidersAndGlobalCache`；`ResourceDeleteRequest` 新增 `localPath` 与 `defaultTarget`，`ResourceDeletionExecutionPlan` 新增 `globalCachePathHint`。
- `MainSplitViewModel` 的全局删除改为优先删除 `globalCachePathHint` 指向的真实路径，再回落 `GlobalCacheRepository.removeFromCache(slug:type)`，用于覆盖 UI 卡片路径与 `NolonManager` canonical 路径不一致的场景。
- 新增并通过测试：`ResourceCatalogGridViewModelTests` 覆盖全局仓库直删与 path hint 透传；`MainSplitViewModelTests` 覆盖 hinted global path 删除；`ResourceDeletionExecutionPlanTests`、`ResourceDeleteRequestTests` 同步覆盖新增字段。
- 本地使用 AX + 键盘事件完成到“更多”菜单的自动化导航，但当前环境下 macOS 菜单桥接仍未稳定触发 app 的删除 action，最终 end-to-end 文件删除仍需继续以最新构建手工验证或补更稳定的 GUI 驱动。

## 2026-03-06 17:30 Codex 账号列表页定位与排序链路
- 页面入口：Codex 的“账号列表页”不在独立文件，入口是 `ProviderDetailGridView` 的 `.accounts` tab，实际渲染复用 `ProviderUsageView`。
- 截图来源：当前 `/Applications/nolon.app` 在本机启动会弹 crash report，改用仓库内已有截图 `screenshots/20260304/codex/20260304-codex-account-usage-after-v05.png` 作为稳定参考；当日现场截图里 `/Applications` 版本也确认了 Codex provider 与中间 tab 位置。
- 当前显示排序：
  1) `CodexAuthManager.loadAccountsFromAuthFolder()` 先按文件名读，再在内存里按 `createdAt` 倒序排序；
  2) `ProviderUsageViewModel.load()` 直接把该顺序写入 `codexAccounts`；
  3) `loadCachedCodexAccountOutcomes(accounts:)` 按 `codexAccounts` 顺序生成卡片；
  4) `reorderCodexAccountOutcomesForDisplay()` 也只按 `codexAccounts` 的去重后顺序重排。
- 结论：账号卡片的“显示顺序”当前是“创建时间倒序，新账号在前”，不是“当前激活账号置顶”。
- 补充：`orderedAccounts(activeId:)` 只用于刷新顺序（如 header refresh、初次刷新、自动刷新），会把 active account 提到刷新队列最前，但不会影响 UI 卡片顺序。

## 2026-03-06 18:10 macOS UI 自动化方案落地文档
- 用户要求先调研 macOS app UI 自动化实现方式，再对齐 `nolon` 的落地方案。
- 结论：`nolon` 的主回归链路应采用 Apple 官方 `XCUITest`，以 `Accessibility Inspector` 作为调试辅助，不应将 AppleScript、裸 `AXUIElement` 或 Appium Mac2 作为主测试体系。
- 已新增文档：`docs-dev/dev/macos-ui-automation-strategy-2026-03-06.md`。
- 文档明确了：
  1) `nolonUITests` 作为正式 UI Test target；
  2) `UITestSupport` 环境变量开关边界；
  3) 关键控件必须提供稳定 `accessibilityIdentifier`；
  4) 高脆弱交互采用测试专用直达入口；
  5) 所有 UI 测试必须使用临时 `NOLON_HOME` 隔离数据；
  6) 首批必跑场景为全局技能删除、搜索、安装。

## 2026-03-06 18:16 global gemini 删除 UI 测试跑通
- 继续收敛 `nolonUITests/GlobalSkillDeletionUITests`。
- 根因 1：UI test 启动环境误注入 `XCODE_RUNNING_FOR_PREVIEWS=1`，导致 app 只起窗口壳和菜单栏，SwiftUI 内容层未进入可交互状态；移除该环境变量后，XCUI 可正常看到主窗口内的测试删除按钮。
- 根因 2：`MainSplitView` 顶层 overlay 与 `ResourceCenterView` 右上角测试入口共用了 `uitest.delete-global-skill.<slug>` identifier，导致 XCUI 点击时报 “Multiple matching elements found”；测试改为 `app.buttons.matching(identifier: ...).firstMatch` 后消除歧义。
- 验证：`xcodebuild test -project nolon.xcodeproj -scheme nolon-app -destination 'platform=macOS' -only-testing:nolonUITests/GlobalSkillDeletionUITests` 通过，确认点击测试删除按钮后临时 `NOLON_HOME/skills/gemini` 被实际删除。
- 产物：`/Users/linhey/Library/Developer/Xcode/DerivedData/nolon-daifteoyynegwuevitolzuidfhnx/Logs/Test/Test-nolon-app-2026.03.06_18-14-32-+0800.xcresult`。

## 2026-03-06 21:41 全局 gemini 删除链路收口到 MainSplitViewModel 本地直跑
- 用户在真实删除日志中观察到：`Execute delete requested` 阶段 `removeGlobalCache=1`，但进入 `MainSplitViewModel.deleteRemoteSkill` 后日志变成 `resolvedProviderTargets=0 removeGlobalCache=0`，随后出现 `EXC_BAD_ACCESS`。
- 判断：`ResourceDeletionExecutionPlan` 以及后续 `ResourceDeletionCoordinator` 的泛化 async 闭包链在当前项目上下文里不稳定，跨 UI async 回调和 coordinator uninstall closure 都会丢状态或触发坏内存访问，不值得继续维持抽象层。
- 决策：删除执行固定收口到 `MainSplitViewModel` 本地直跑。
  1) `ResourceCenterView` / `ResourceCatalogGridView` 删除回调只传 `slug + ResourceDeleteTarget + globalCachePathHint`；
  2) `MainSplitViewModel` 内部根据当前 `settings.providers` 现算 `ResourceDeletionExecutionPlan`；
  3) 直接用 `ResourceInstaller(globalCache: GlobalCacheRepository(nolonManager: nolonManager))` 卸载 provider 资源；
  4) 全局缓存删除优先删 `globalCachePathHint`，再 `await GlobalCacheRepository.removeFromCache(slug:type)`；
  5) 不再依赖 `ResourceDeletionCoordinator.execute(...)`。
- 实现后先遇到编译问题：`GlobalCacheRepository.removeFromCache` 是 actor 隔离方法，需要将 `removeGlobalCacheResource` 改为 `async throws` 并在调用处 `await`。
- 验证：
  1) `xcodebuild test -project nolon.xcodeproj -scheme nolon-app -destination 'platform=macOS' -only-testing:nolonTests/MainSplitViewModelTests -only-testing:nolonTests/ResourceCatalogGridViewModelTests` 通过；
  2) `MainSplitViewModelTests.testBDD_GivenGlobalSkill_WhenDeleteRemoteSkillWithGlobalCachePlan_ThenFolderAndInstalledStateDisappear` 与 `...HintedGlobalSkillPath...` 通过，确认真实目录删除与 hinted path 删除都成立；
  3) `xcodebuild test -project nolon.xcodeproj -scheme nolon-app -destination 'platform=macOS' -only-testing:nolonUITests/GlobalSkillDeletionUITests` 通过，确认 UI 删除全局 `gemini` 后临时 `NOLON_HOME/skills/gemini` 被实际删除。
- 产物：
  1) 单测 xcresult：`/Users/linhey/Library/Developer/Xcode/DerivedData/nolon-daifteoyynegwuevitolzuidfhnx/Logs/Test/Test-nolon-app-2026.03.06_21-39-36-+0800.xcresult`
  2) UI 测试 xcresult：`/Users/linhey/Library/Developer/Xcode/DerivedData/nolon-daifteoyynegwuevitolzuidfhnx/Logs/Test/Test-nolon-app-2026.03.06_21-40-26-+0800.xcresult`

## 2026-03-06 21:56 删除链路移除 ResourceDeleteTarget 异步传递
- 背景：用户继续贴出崩溃栈，最新 fault 落在 `ResourceDeletionExecutionPlan.make(target:providers:globalCachePathHint:)`，并且 LLDB 显示 `target = provider` 时 `providerID` 为 `<uninitialized>`，说明 `ResourceDeleteTarget` 这个带关联值 enum 跨 SwiftUI + async thunk 边界依旧不稳定。
- 决策：删除回调不再跨异步边界传 `ResourceDeleteTarget`，统一改成纯标量：`slug + providerID + removeGlobalCache + globalCachePathHint`。
- 实现：
  1) `ResourceCenterView` / `ResourceCatalogGridView` / `MainSplitView` 的删除回调签名改为 `((String, String?, Bool, String?) async -> ResourceDeleteExecutionResult)?`；
  2) `ResourceCatalogGridViewModel.executeDelete` 在同步 UI 层把 `ResourceDeleteTarget` 展平为 `(providerID, removeGlobalCache)` 后再发起 async 删除；
  3) `MainSplitViewModel.deleteRemoteSkill/deleteRemoteWorkflow/deleteRemoteMCP` 内部基于标量参数重新构造 `ResourceDeletionExecutionPlan`；
  4) `ResourceDeletionExecutionPlan.make(...)` 改为接收 `providerID/removeGlobalCache`，不再依赖 enum；
  5) 对非法组合（`providerID == nil && removeGlobalCache == false` 等）返回失败结果，避免崩溃。
- 测试：
  1) `MainSplitViewModelTests` 新增 `GivenInvalidDeleteCombination_WhenDeleteRemoteSkill_ThenReturnFailureWithoutCrash`；
  2) `ResourceCatalogGridViewModelTests` 改为断言 `providerID/removeGlobalCache/globalCachePathHint`；
  3) `ResourceDeletionExecutionPlanTests`、`ResourceDeletionCoordinatorTests` 全部切到新接口；
  4) `xcodebuild test -project nolon.xcodeproj -scheme nolon-app -destination 'platform=macOS' -only-testing:nolonTests/MainSplitViewModelTests -only-testing:nolonTests/ResourceCatalogGridViewModelTests -only-testing:nolonTests/ResourceDeletionExecutionPlanTests -only-testing:nolonTests/ResourceDeletionCoordinatorTests` 通过；
  5) `xcodebuild test -project nolon.xcodeproj -scheme nolon-app -destination 'platform=macOS' -only-testing:nolonUITests/GlobalSkillDeletionUITests` 通过。
- 产物：
  1) 单测 xcresult：`/Users/linhey/Library/Developer/Xcode/DerivedData/nolon-daifteoyynegwuevitolzuidfhnx/Logs/Test/Test-nolon-app-2026.03.06_21-54-30-+0800.xcresult`
  2) UI 测试 xcresult：`/Users/linhey/Library/Developer/Xcode/DerivedData/nolon-daifteoyynegwuevitolzuidfhnx/Logs/Test/Test-nolon-app-2026.03.06_21-55-23-+0800.xcresult`

- 22:12 删除链路继续从 `providerID: String?` 收缩到 `providerIndex: Int?`，避免 async thunk 中 `String` 载荷损坏；对应 clean 单测（`MainSplitViewModelTests`、`ResourceCatalogGridViewModelTests`、`ResourceDeletionExecutionPlanTests`、`ResourceDeletionCoordinatorTests`）通过，clean UI 测 `GlobalSkillDeletionUITests` 通过，xcresult 为 `/Users/linhey/Library/Developer/Xcode/DerivedData/nolon-daifteoyynegwuevitolzuidfhnx/Logs/Test/Run-nolon-app-2026.03.06_22-06-43-+0800.xcresult`。

- 22:42 删除链路继续收敛为“同步 registerDeleteRequest + 异步 executeRegisteredDeleteRequest(id:)”两段式，彻底避免 `providerIndex/removeGlobalCache` 直接穿过 async thunk；新增 `RegisteredResourceDeleteRequest` registry，更新 `ResourceCenterView`/`ResourceCatalogGridView` 为 request token 执行。验证通过：`MainSplitViewModelTests`、`ResourceCatalogGridViewModelTests` 和 `GlobalSkillDeletionUITests`。单测 xcresult `/Users/linhey/Library/Developer/Xcode/DerivedData/nolon-daifteoyynegwuevitolzuidfhnx/Logs/Test/Test-nolon-app-2026.03.06_22-41-10-+0800.xcresult`，UI 测 xcresult `/Users/linhey/Library/Developer/Xcode/DerivedData/nolon-daifteoyynegwuevitolzuidfhnx/Logs/Test/Test-nolon-app-2026.03.06_22-40-11-+0800.xcresult`。

- 22:48 修复删除成功后仍提示 `Registered delete request not found`：`MainSplitViewModel.executeRegisteredDeleteRequest(id:)` 现在会缓存已完成 request 的结果，重复执行同一 requestID 时直接复用首次结果，不再报错；新增 `testBDD_GivenExecutedDeleteRequest_WhenExecuteRegisteredDeleteRequestAgain_ThenReusePreviousResult`。验证通过：`MainSplitViewModelTests` 和 `GlobalSkillDeletionUITests`，UI 测 xcresult `/Users/linhey/Library/Developer/Xcode/DerivedData/nolon-daifteoyynegwuevitolzuidfhnx/Logs/Test/Test-nolon-app-2026.03.06_22-46-19-+0800.xcresult`。
- 23:04 修复资源删除 request registry 的并发竞态：`executeRegisteredDeleteRequest(id:)` 新增 `inFlightDeleteRequestTasks`，同一 requestID 并发执行时复用首个进行中的 Task，避免 UI 出现“Delete Request: Registered delete request not found.”。新增 `MainSplitViewModelTests.testBDD_GivenConcurrentDeleteExecutions_WhenExecuteRegisteredDeleteRequestTwice_ThenReuseInFlightResult`，并通过 `MainSplitViewModelTests` 与 `GlobalSkillDeletionUITests` 验证。
- 23:13 修复删除请求执行阶段的第二条 async 参数损坏：`onExecuteDeleteRequest(Int)` 改为 `onMakeDeleteRequestExecutor(Int) -> (@MainActor () async -> ResourceDeleteExecutionResult)`，让 `requestID` 仅在同步层捕获，不再跨 async thunk 作为参数传递。更新 `ResourceCenterView`、`ResourceCatalogGridView`、`MainSplitView` 桥接，并新增 `ResourceCatalogGridViewModelTests.testBDD_GivenDeleteExecutorFactory_WhenExecuteDelete_ThenRequestIDNeverCrossesAsyncBoundary`。验证通过 `ResourceCatalogGridViewModelTests`、`MainSplitViewModelTests` 与 `GlobalSkillDeletionUITests`。
- 23:22 补齐 `MainSplitViewModel` 的 workflow/MCP 全局删除回归：新增 workflow/mcp 直接删除与 registered delete request 执行测试，验证 `userWorkflowsURL` 与 `mcpsURL` 下的全局缓存文件会被正确删除。未修改实现，现有删除链直接通过。
- 23:32 补齐 workflow/MCP 的 UI 删除回归。`UITestSupport` 新增初始 tab、workflow slug、MCP slug；`ResourceCenterView` 新增 workflow/MCP 直达删除按钮并复用统一 `executeUITestDelete`。`GlobalSkillDeletionUITests` 现在覆盖 global skill/workflow/MCP 三条删除路径，均通过。UI 测 xcresult：`/Users/linhey/Library/Developer/Xcode/DerivedData/nolon-daifteoyynegwuevitolzuidfhnx/Logs/Test/Test-nolon-app-2026.03.06_23-30-55-+0800.xcresult`。
- 23:47 清理重复的 UI test delete identifier：移除 `MainSplitView` 顶层重复的 `uitest.delete-global-skill.*` overlay，只保留 `ResourceCenterView` 内统一生成的 skill/workflow/MCP 测试删除入口；`GlobalSkillDeletionUITests` 改为直接用唯一 identifier 查找按钮，不再依赖 `matching(...).firstMatch`。验证：整组 `GlobalSkillDeletionUITests` 重新通过，xcresult `/Users/linhey/Library/Developer/Xcode/DerivedData/nolon-daifteoyynegwuevitolzuidfhnx/Logs/Test/Test-nolon-app-2026.03.06_23-45-54-+0800.xcresult`。
- 23:53 将 `GlobalSkillDeletionUITests` 重构为 `ResourceDeletionUITests`，并新增 `ResourceDeletionUITestSupport` 抽出临时 `NOLON_HOME`、fixture seed、launchEnvironment、waitUntil、进程清理等公共逻辑；同时更新 `nolon.xcodeproj/project.pbxproj` 里的 `nolonUITests` 源文件引用。验证：`xcodebuild test -project nolon.xcodeproj -scheme nolon-app -destination 'platform=macOS' -only-testing:nolonUITests/ResourceDeletionUITests` 通过，xcresult `/Users/linhey/Library/Developer/Xcode/DerivedData/nolon-daifteoyynegwuevitolzuidfhnx/Logs/Test/Test-nolon-app-2026.03.06_23-50-49-+0800.xcresult`。
