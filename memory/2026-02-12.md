# 2026-02-12

- 完成 Codex 代码精简第二/三轮：删除 Provider 层重复 RPC 客户端，统一到 CodexAppServerKit typed API。
- 将二进制路径与 CODEX_HOME 解析统一到 CodexCLIKit.CodexCommandExecutor。
- 新增 CodexRuntimeSupport，统一 runtime service 生命周期，避免 CodexHelper/CodexCreditsFetcher 重复实现。
- 通过 `swift test --package-path libs/Providers` 全量回归（35 tests）。
- 继续精简：CodexLoginRunner 复用统一 executable resolver；新增登录路径测试；RuntimeAccountSwitcher cache key 改为基于归一化 binary。
- 新增 Switcher 可测试性注入，补齐缓存键行为测试；修正 resolvedBinary 在传入 "codex" 时未走统一 resolver 的问题。
- 收尾：补齐 docs-linhay/api、docs-linhay/features、docs-linhay/ops 目录骨架。
- 验证：执行 ./build.sh（2026-02-12）通过，主工程 Release 构建成功；存在 CodexBinaryManager 并发隔离 warning（当前未阻塞）。
- qmd 仍不可用（command not found），待安装后执行 qmd update && qmd embed。
- 继续推进收尾：Codex rollout 对 custom_tool_call/custom_tool_call_output 改为结构化 JSON 类型，避免字符串化丢信息。
- 新增 URLSchemeHandler BDD 测试并抽出可测 normalize 方法，去除 app 层残留 print，统一 OSLog。
- 回归通过：`swift test --package-path libs/Providers`、URLSchemeHandler 单测、`./build.sh`。
- 补齐文档体系：新增 `docs-linhay/features/codex-full-support-spec.md`（目标/范围/BDD 验收）与 `docs-linhay/dev/ops/codex-runtime-runbook.md`（运行与排障）。
- 更新 `docs-linhay/features/README.md` 与 `docs-linhay/dev/ops/README.md` 导航，形成 Codex 交付入口。
- 执行记忆索引闭环：`qmd update && qmd embed`。
- 新增 `docs-linhay/dev/api/codex-app-server-rpc.md`：沉淀 Nolon 消费的 Codex app-server JSON-RPC 契约（核心方法、通知、错误语义、兼容约束）。
- 新增 `docs-linhay/dev/codex-rollout-coverage-matrix.md`：会话 rollout 解析覆盖矩阵（top-level/response_item/event_msg）与待补强测试清单。
- 更新 `docs-linhay/dev/api/README.md` 导航到 RPC 契约文档。
- 按覆盖矩阵补齐解析测试第一批：新增 response_item（function_call_output/local_shell_call/web_search_call/ghost_snapshot）、event_msg（agent/error/warning/turn_complete）与 unknown fallback 断言。
- `CodexGeneratedFilesParserTests` 扩展后通过，`swift test --package-path libs/Providers` 全量回归仍通过（40 tests）。
- 更新 `docs-linhay/dev/codex-rollout-coverage-matrix.md`，同步“已覆盖/待补强”状态。
- 清理本地化噪音：将 `AgentDocCardView` 与 `RuleCardView` 的 `Text(" ")` 占位替换为布局占位（`Color.clear`），避免抽取空白本地化 key。
- 删除 `Localizable.xcstrings` 中空白 key `" "`。
- 回归验证：`./build.sh` 通过。
- rollout 解析补强完成：新增 `event_msg.task_started/task_complete`、`response_item.reasoning/compaction_summary`、`event_msg.payload.token_count` 的回归测试并通过。
- 新增 `CodexGeneratedFilesSnapshot` 与 `CodexGeneratedFilesParser.loadAllGeneratedFiles(codexHome:includeArchived:)`，统一解析 `auth.json/history.jsonl/config.toml/managed_config.toml/sessions`。
- 对 `parseHistoryLines` 与 `parseRolloutLines` 抽取公共 JSONL helper，减少重复代码。
- 回归：`swift test --package-path libs/Providers`（42 tests）通过；`./build.sh`（Release）通过。
- qmd 可用并已执行索引同步（本轮执行 `qmd update && qmd embed`）。
- 精简收口（不改行为）：`CodexGeneratedFilesParser` 新增文件名常量与通用文件加载 helper，统一 `auth/history/config` 读取路径。
- 新增回归：`loadAllGeneratedFiles(..., includeArchived: false)` 仅加载 `sessions`，排除 `archived_sessions`。
- 新增文档：`docs-linhay/dev/codex-provider-orchestration-guide.md`，明确 app 仅编排、Providers 承担 Codex CLI/RPC/文件解析。
- 回归：`swift test --package-path libs/Providers`（43 tests）通过；`./build.sh` 通过。
- STFilePath 下沉（Providers 第一批）完成：`FileTokenAccountStore` 改用 `STFile`，`CodexModelsCache` 增加 `STFile/STPath` 入口，`CodexGeneratedFilesParser` 增加 `STFolder/STPath` 入口并将会话文件扫描切到 `STFolder.allSubFilePaths`。
- 保留 URL 兼容入口，避免调用方一次性迁移造成破坏；内部实现优先 STFilePath。
- 新增回归：`ProviderUsageTokenAccountsTests`、`CodexModelsCacheTests.loadsFromSTFile`、`CodexGeneratedFilesParserTests.loadAllGeneratedFilesFromSTFolder`。
- 全量回归：`swift test --package-path libs/Providers` 通过（46 tests）。
- 文档同步：更新 `docs-linhay/features/codex-full-support-spec.md` 与 `docs-linhay/dev/codex-runtime-refactor.md`，记录 STFilePath 迁移范围与验收。
- STFilePath 下沉第二批完成（CostUsage）：
  - `CostUsageCacheIO` 增加 STFolder/STFile 入口并保留 URL 兼容入口。
  - `CostUsageScanner.Options` 路径类型改为 `STFolder?`，新增 URL 兼容构造器。
  - `CostUsageScanner` 的 sessions 文件发现链路切换到 STFilePath（`STFolder` + `STFile`）。
- 新增测试 `CostUsageStoragePathTests`，并通过 `swift test --package-path libs/Providers` 全量回归（48 tests）。
- STFilePath 下沉第三批完成（CLI/登录链路）：
  - `CodexCommandExecutor` 新增 `codexHomeDirectory(environment:) -> STFolder`，URL/String 旧入口兼容。
  - `CodexLoginRunner` 新增 `startLogin(..., codexHome: STFolder)`，并将进程 executable URL 组装改为 `STFile` 链路。
  - `CodexHelper` models cache 读取切换到 `STFolder.file(...)` + `STFile` 入口。
- 新增/更新测试：
  - `CodexCLIKitEnvironmentTests.codexHomeFolderOverride`
  - `CodexLoginRunnerTests.startLoginWithSTFolder`
- 回归：`swift test --package-path libs/Providers` 全量通过（50 tests）。
- STFilePath 下沉第四批完成（ProviderCatalog + ProvidersShared）：
  - `ProviderTemplate` / `ProviderTemplateLoader` 切换到 STFilePath 内部路径与文件读取实现（URL 外部接口兼容）。
  - `TTYCommandRunner` 的可执行判定与 executableURL 组装切换到 STFilePath。
- 新增测试：
  - `ProviderCatalogTemplateTests`
  - `TTYCommandRunnerTests`
- 回归：`swift test --package-path libs/Providers` 全量通过（54 tests）。
- STFilePath 下沉第五批完成：
  - `Provider` 新增 `path: STPath` 与 `additionalPaths: [STPath]`，URL 属性转调 STPath 视图保持兼容。
  - `CodexAppServerSession` executable URL 组装切换到 `STFile(...).url`。
- 新增测试：`ProviderCatalogTemplateTests.providerPathViews`。
- 回归：`swift test --package-path libs/Providers` 全量通过（55 tests）。
- STFilePath 下沉第六批完成（收口）：
  - `CostUsageCacheIO.defaultCacheRoot` fallback 去除 `FileManager` 枚举，改为 STFilePath 组合路径 `~/Library/Caches/CodexBar`。
  - 新增 `CostUsageStoragePathTests.costUsageCacheDefaultPathLayout`。
- 回归：`swift test --package-path libs/Providers` 全量通过（56 tests）。
- 收口检查：`rg -n "FileManager|Data(contentsOf:|URL(fileURLWithPath:" libs/Providers/Sources` 无结果。
- 远程仓库能力下沉推进：新增 `ProviderCatalog.RemoteGitRepositorySupport`，统一 Git URL 规范化/组件提取、clone/pull、skills 目录发现（含 `libs/agent-skills` 风格层级）。
- app 编排层去重：`RemoteRepository` 与 `GitRepository` 改为委托 `RemoteGitRepositorySupport`，移除 app 层重复的 Git URL 解析、SSH 检测与目录扫描逻辑。
- 测试：`swift test --package-path libs/Providers` 全量通过（60 tests）。
- 当前阻塞：`xcodebuild`/`./build.sh` 仍因既有工程依赖解析问题失败（`Unable to find module dependency: ProviderCatalog/ProviderUsage`），需后续单独修复工程依赖图。
- 技能标准解析推进：按 agentskills 规范新增 `SkillParser.parseStandardMetadata(content:directoryName:)`，支持 `name/description/license/compatibility/metadata/argument-hint/allowed-tools` 结构化解析。
- 兼容策略：保留 `SkillParser.parse` 旧回退路径（无 frontmatter 时继续默认 name=id / 默认 description），并将规范校验以 warning 形式输出，不阻断现有技能导入。
- 新增测试 `SkillParserSpecificationTests`，覆盖标准字段解析、name 目录不一致告警、name 格式告警、compatibility 与 allowed-tools 解析、旧技能兼容场景。
- 验证：`xcodebuild ... -only-testing:nolonTests/SkillParserSpecificationTests test` 通过；`./build.sh` 通过。
- 技能标准解析下沉（核心能力向 CLI/SDK 迁移）完成一批：新增 `ProviderCatalog.SkillSpecificationParser`，将 `SKILL.md` 规范字段解析能力放到 `libs/Providers` 统一复用。
- `RemoteGitRepositorySupport.detectSkillsDirectories` 接入 `SkillSpecificationParser`：目录发现时优先读取 frontmatter `name` 作为技能展示名，失败回退目录名；`skillNames` 输出稳定排序。
- App 层 `SkillParser.parseStandardMetadata` 改为委托 `ProviderCatalog.SkillSpecificationParser`，消除双份规范解析实现。
- 新增/更新测试：`SkillSpecificationParserTests`、`RemoteGitRepositorySupportTests`（校验 agent-skills 目录 + frontmatter name）、`SkillParserSpecificationTests` 回归通过。
- 验证：`swift test --package-path libs/Providers` 全量通过（63 tests）；`xcodebuild -only-testing:nolonTests/SkillParserSpecificationTests test` 通过。
- 文档：新增 `docs-linhay/dev/skills-standard-parser-downsink.md`，记录下沉范围、接口与验收。
- 第七轮下沉（CLI/SDK Facade）完成：新增 `ProviderCatalog.SkillsRepositoryFacade`，统一远程仓库导入计划（plan）、同步（sync）、资源发现（discover）、技能规范解析（parse）入口。
- 新增测试 `SkillsRepositoryFacadeTests`（shorthand+subpath 计划生成、SSH 计划生成、frontmatter 名称发现）。
- App 编排层继续瘦身：`GitRepository.sync/syncRepository/detectSkillsDirectories` 改为调用 `SkillsRepositoryFacade`，减少直接依赖底层 support。
- 新增文档：`docs-linhay/features/nolon-core-cli-milestones.md`（业务域与里程碑）与 `docs-linhay/dev/nolon-cli-sdk-facade-guide.md`（边界与调用流）。
- 回归通过：`swift test --package-path libs/Providers`（66 tests）与 `./build.sh`（BUILD SUCCEEDED）。
- 第八轮下沉（CLI 命令面）完成：在 `libs/Providers` 新增 `NolonCoreCLIKit` 与可执行 target `nolon-core`，命令覆盖 `skills repo plan/sync`、`skills discover`、`skills parse`。
- CLI 输出协议落地：成功统一 stdout JSON（`ok=true`），失败统一 stderr JSON（`ok=false`，`invalid_arguments/execution_failed`），退出码 `0/2`。
- 第九轮下沉（资源发现）完成：`RemoteGitRepositorySupport` 新增 `detectRepositoryResources`，统一发现 `skillsDirectories/workflows/mcps`，并通过 `SkillsRepositoryFacade.discoverRepositoryResources` 暴露到 SDK。
- CLI 同步扩展 `resources discover` 命令，返回仓库资源结构化清单。
- 新增/更新测试：`NolonCoreCLIKitTests`（命令解析/执行 JSON 输出）、`RemoteGitRepositorySupportTests.discoverRepositoryResources`。
- 验证：`swift test --package-path libs/Providers` 全量通过（73 tests）；`swift run --package-path libs/Providers nolon-core ...` 命令实测通过。
- 文档同步：更新 `docs-linhay/features/nolon-core-cli-command-surface-spec.md`、`docs-linhay/features/nolon-core-cli-milestones.md`、`docs-linhay/dev/nolon-cli-sdk-facade-guide.md`、`libs/Providers/README.md`。
- 第十轮推进：`nolon-core skills repo sync` 输出增强，新增 `resources` 字段（skillsDirectories/workflows/mcps），减少同步后客户端二次扫描调用。
- 对应回归：新增 `NolonCoreCLIKitTests.runnerRendersSyncWithResources`，`swift test --package-path libs/Providers` 全量通过（74 tests）。
- 第十一轮推进（App 编排侧收口）：`GitRepository` 新增 `detectRepositoryResources(at:)`（skills/workflows/mcp），`RemoteRepositorySidebarView` 回退扫描改为调用该统一入口，不再仅依赖旧 `detectSkillsDirectories`。
- 新增 app 侧回归测试 `nolonTests/GitRepositoryResourceDetectionTests`，验证仓库资源检测可同时识别 skills/workflow/mcp。
- 验证通过：`xcodebuild -project nolon.xcodeproj -scheme nolon -destination 'platform=macOS' -only-testing:nolonTests/GitRepositoryResourceDetectionTests test`。
- 第十二轮推进：`GitRepository.SyncResult` 扩展为携带 `workflowPaths/mcpPaths`，`syncRepository` 在 clone/pull 后统一基于 façade 发现并回传 skills/workflows/mcp 资源清单。
- 新增测试：`GitRepositoryResourceDetectionTests.testSyncResultSuccess_CarriesResourcePaths`。
- 验证：`xcodebuild -project nolon.xcodeproj -scheme nolon -destination 'platform=macOS' -only-testing:nolonTests/GitRepositoryResourceDetectionTests test` 通过。
- 第十三轮推进（skills 标准解析加固）：`ProviderCatalog.SkillSpecificationParser` 改为基于 `Yams` 解析 frontmatter，补齐多行 `description`、`allowed-tools` YAML 数组、`argument-hint`、`metadata` 混合类型告警等规范场景。
- 依赖调整：`libs/Providers` 新增 `Yams` 依赖，`ProviderCatalog` target 显式引入 `.product(name: "Yams", package: "Yams")`。
- 新增测试：`SkillSpecificationParserTests` 增补 `parse block description and mixed metadata values`、`warn invalid name format`、`parse allowed tools from yaml list`。
- 回归：`swift test --package-path libs/Providers` 全量通过（77 tests）。
- 当前已知阻塞（非本轮引入）：App 工程定向测试在编译阶段失败，缺少 `DGCharts` 模块依赖（`CodexDailyCostBarChartView.swift`）。
- 第十四轮推进（远程资源发现规则补齐）：`RemoteGitRepositorySupport.detectRepositoryResources` 扩展支持 `.github/workflows/*.{yml,yaml}` 与 workflow 路径下的 YAML/Markdown 文件识别，提升与 GitHub Actions 仓库布局兼容性。
- 新增测试：`RemoteGitRepositorySupportTests.discoverRepositoryResourcesInGitHubWorkflowFolder`（先红后绿），并保留 `.agents/skills` 隐藏目录布局回归。
- 验证：`swift test --package-path libs/Providers --filter RemoteGitRepositorySupportTests` 与 `swift test --package-path libs/Providers` 全量通过（79 tests）。
- 文档更新：`docs-linhay/dev/nolon-cli-sdk-facade-guide.md` 补充资源发现约定（skills/workflows/mcp 模式）。
- 第十五轮推进（Facade 边界收口）：`SkillsRepositoryFacade` 新增 Git URL 工具入口（`normalizeGitURL`、`extractSubpath`、`extractURLComponents`、`suggestedClonePath`），用于替代 app 层对 `RemoteGitRepositorySupport` 的直接依赖。
- App 侧收口：`RemoteRepository.localClonePath/extractURLComponents/normalizeGitURL/extractSubpath` 全部改为调用 `SkillsRepositoryFacade`，保持“app 只编排，底层能力在 libs/Providers”。
- 新增测试：`SkillsRepositoryFacadeTests` 增补 URL 归一化、subpath 提取、URL 组件提取、建议 clone path 场景。
- 验证通过：`swift test --package-path libs/Providers --filter SkillsRepositoryFacadeTests` 与 `xcodebuild -project nolon.xcodeproj -scheme nolon -destination 'platform=macOS' -only-testing:nolonTests/RemoteRepositoryTests test`。
- 代码同步：已从 `origin/main` 执行合并检查，当前分支与 main 无差异（Already up to date）。
- 第十六轮推进（错误边界收口）：`SkillsRepositoryFacade` 新增统一 `SyncError`（invalidURL/sshNotAvailable/cloneFailed/pullFailed/commandFailed），内部映射 `RemoteGitRepositorySupport.SyncError`，避免向 app 泄漏底层错误类型。
- App 侧 `GitRepository` 同步链路改为仅捕获 `SkillsRepositoryFacade.SyncError`，移除对 `RemoteGitRepositorySupport.SyncError` 的直接依赖。
- 新增测试：`SkillsRepositoryFacadeTests.sync maps invalid URL to facade sync error`，验证非法 URL 错误经 façade 统一输出。
- 回归通过：`swift test --package-path libs/Providers --filter SkillsRepositoryFacadeTests`（6 tests）与 `xcodebuild -project nolon.xcodeproj -scheme nolon -destination 'platform=macOS' -only-testing:nolonTests/RemoteRepositoryTests test`。
- 第十七轮推进（类型边界收口）：`SkillsRepositoryFacade` 不再对外暴露 `RemoteGitRepositorySupport` 类型，新增 façade 自有 `GitSyncMode/SkillsDirectoryCandidate/ResourceFile/RepositoryResources`。
- 同步语义统一：底层 `pull` 在 façade 映射为 `GitSyncMode.updated`；`NolonCoreCLIKit` 为保持兼容输出仍映射为 `"pulled"`。
- 新增回归：`SkillsRepositoryFacadeTests.git sync mode exposes facade semantic updated`（先红后绿）。
- 回归通过：`swift test --package-path libs/Providers --filter 'SkillsRepositoryFacadeTests|NolonCoreCLIKitTests'`；`xcodebuild -project nolon.xcodeproj -scheme nolon -destination 'platform=macOS' -only-testing:nolonTests/GitRepositoryResourceDetectionTests -only-testing:nolonTests/RemoteRepositoryTests test`。
- 第十八轮推进（M4 第 1 轮：仓库身份语义收口）：`SkillsRepositoryFacade` 新增 `RepositoryIdentity`、`parseRepositoryIdentity(from:)`、`detectGitProvider(from:)`，统一 repoName/repoFullName/provider 判定来源。
- App 侧 `RemoteRepository.extractRepoName/extractRepoFullName/detectProvider/localClonePath` 回退链路改为优先委托 façade，减少 `GitProvider` 内部解析规则承载。
- 新增回归（先红后绿）：
  - `SkillsRepositoryFacadeTests.parse repository identity from shorthand/ssh url`
  - `RemoteRepositoryTests.testExtractRepoName_FromShorthandAndSSH`
  - `RemoteRepositoryTests.testExtractRepoFullName_FromShorthandAndHTTPS`
  - `RemoteRepositoryTests.testDetectProvider_UsesFacadeSemantics`
- 回归通过：
  - `swift test --package-path libs/Providers --filter SkillsRepositoryFacadeTests`
  - `xcodebuild -project nolon.xcodeproj -scheme nolon -destination 'platform=macOS' -only-testing:nolonTests/RemoteRepositoryTests test`
- 第十九轮推进（skills 标准解析加固第二轮）：`SkillSpecificationParser` 增加规范级容错与告警能力，覆盖未知顶层字段、空必填字段，以及 `allowed-tools` 兼容解析（逗号分隔字符串/混合类型数组）。
- 新增测试（先红后绿）：
  - `SkillSpecificationParserTests.warnUnknownTopLevelFields`
  - `SkillSpecificationParserTests.warnEmptyRequiredFields`
  - `SkillSpecificationParserTests.parseAllowedToolsFlexibleInput`
- 回归通过：
  - `swift test --package-path libs/Providers --filter SkillSpecificationParserTests`
  - `swift test --package-path libs/Providers --filter NolonCoreCLIKitTests`
  - `xcodebuild -project nolon.xcodeproj -scheme nolon -destination 'platform=macOS' -only-testing:nolonTests/SkillParserSpecificationTests test`
- 文档同步：更新 `docs-linhay/features/skills-standard-parsing-spec.md`、`docs-linhay/dev/skills-standard-parser-downsink.md`、`docs-linhay/features/nolon-core-cli-milestones.md`。
- 第二十轮推进（仓库生命周期下沉第 1 批）：
  - `RemoteGitRepositorySupport` 新增 `SyncOptions`：
    - `pullStrategy`：`ff-only/rebase/merge`
    - `credentialStrategy`：`automatic/prefer-ssh/token-only/ssh-only`
  - 新增 `credentialMode`（`local/ssh/https_token/https_anonymous`）与 `defaultBranch` 探测并纳入同步结果。
  - 新增 `SyncError.accessTokenRequired`，并在 façade 与 app 映射链路打通。
  - `skills repo sync` 命令新增 `--pull-strategy` / `--credential-strategy` 参数并下发到 façade。
- 新增测试（先红后绿）：
  - `RemoteGitRepositorySupportTests.syncTokenOnlyRequiresAccessToken`
  - `RemoteGitRepositorySupportTests.detectDefaultBranch`
  - `NolonCoreCLIKitTests.parseSkillsRepoSyncWithStrategies`
  - `SkillsRepositoryFacadeTests.syncTokenOnlyRequiresToken`
- 回归通过：
  - `swift test --package-path libs/Providers --filter 'RemoteGitRepositorySupportTests|NolonCoreCLIKitTests'`
  - `swift test --package-path libs/Providers --filter SkillsRepositoryFacadeTests`
  - `xcodebuild -project nolon.xcodeproj -scheme nolon -destination 'platform=macOS' -only-testing:nolonTests/RemoteRepositoryTests test`
- 文档同步：更新 `docs-linhay/features/nolon-core-cli-command-surface-spec.md`、`docs-linhay/dev/nolon-cli-sdk-facade-guide.md`、`docs-linhay/features/nolon-core-cli-milestones.md`。
- 第二十一轮推进（仓库生命周期下沉第 2 批）：
  - `SkillsRepositoryFacade` 新增 `preflightSync(source:accessToken:options:)`，在 plan 阶段统一输出同步预检语义：
    - `isValidURL`
    - `requiresAccessToken`
    - `credentialMode`
    - `warnings`
  - `nolon-core` 新增命令 `skills repo preflight`，支持 `--pull-strategy` / `--credential-strategy` / `--access-token`。
  - CLI 命令解析与执行链路接入 preflight payload；不触发 clone/pull，仅做策略预检。
- 新增测试（先红后绿）：
  - `NolonCoreCLIKitTests.parseSkillsRepoPreflight`
  - `NolonCoreCLIKitTests.runnerRendersPreflight`
  - `SkillsRepositoryFacadeTests.preflightTokenOnlyWithoutToken`
- 回归通过：
  - `swift test --package-path libs/Providers --filter 'NolonCoreCLIKitTests|SkillsRepositoryFacadeTests'`
  - `swift test --package-path libs/Providers --filter RemoteGitRepositorySupportTests`
- 文档同步：更新 `docs-linhay/features/nolon-core-cli-command-surface-spec.md`、`docs-linhay/dev/nolon-cli-sdk-facade-guide.md`、`docs-linhay/features/nolon-core-cli-milestones.md`。
- 第二十二轮推进（仓库生命周期下沉第 3 批）：
  - `skills repo plan` 升级为返回 `plan + preflight`（同一响应完成导入计划与策略预检）。
  - `NolonCoreCLICommand.skillsRepoPlan` 增加策略参数（`pull/credential/access-token`），与 `preflight/sync` 参数语义对齐。
  - `NolonCoreCLIRunner` 在 `plan` 分支调用 `preflightGitSync` 并输出 `PlanPayload(preflight)`。
- 新增/更新测试（先红后绿）：
  - `NolonCoreCLIKitTests.parseSkillsRepoPlan`（断言默认策略参数）
  - `NolonCoreCLIKitTests.runnerRendersSuccessJSON`（断言 `preflight` 字段）
- 回归通过：
  - `swift test --package-path libs/Providers --filter NolonCoreCLIKitTests`
  - `swift test --package-path libs/Providers --filter SkillsRepositoryFacadeTests`
  - `xcodebuild -project nolon.xcodeproj -scheme nolon -destination 'platform=macOS' -only-testing:nolonTests/RemoteRepositoryTests test`
- 第二十三轮推进（仓库生命周期下沉第 4 批：preflight 类型化）：
  - `SkillsRepositoryFacade.GitSyncPreflight` 新增结构化 `issues`（`code/severity/message`），并保留 `warnings` 兼容输出。
  - 新增 issue code：`invalid_git_url`、`access_token_required`、`token_strategy_requires_https`、`ssh_strategy_requires_ssh`。
  - `NolonCoreCLIKit` 的 `NolonGitSyncPreflight` 同步新增 `issues` 字段，`skills repo plan/preflight` JSON 输出可直接被 UI/脚本按 code 消费。
  - 新增/更新测试（先红后绿）：
    - `SkillsRepositoryFacadeTests.preflightTokenOnlyWithoutToken`（断言 `issues.access_token_required`）
    - `SkillsRepositoryFacadeTests.preflightInvalidURLIssue`
    - `NolonCoreCLIKitTests.runnerRendersPreflight`（断言输出包含 `access_token_required`）
  - 回归通过：`swift test --package-path libs/Providers --filter 'SkillsRepositoryFacadeTests|NolonCoreCLIKitTests'`。
- 第二十四轮推进（仓库生命周期下沉第 5 批：sync 结构化错误码）：
  - `NolonCoreCLIError` 新增 `syncFailed(code:message:)`，CLI stderr `error.code` 不再固定 `execution_failed`。
  - `NolonLiveSkillsRepositoryService.syncGitRepository` 将 `SkillsRepositoryFacade.SyncError` 映射为稳定错误码：
    - `invalid_git_url` / `access_token_required` / `ssh_not_available` / `git_clone_failed` / `git_pull_failed` / `git_command_failed`
  - 新增测试（先红后绿）：
    - `NolonCoreCLIKitTests.runnerRendersStructuredSyncErrorCode`
    - `NolonSkillsRepositoryServiceTests.syncMapsAccessTokenRequired`
  - 回归通过：`swift test --package-path libs/Providers --filter 'NolonCoreCLIKitTests|NolonLiveSkillsRepositoryService'`。
- 第二十五轮推进（仓库生命周期下沉第 6 批：sync 错误 detail）：
  - `NolonCoreCLIError.syncFailed` 新增 `detail`，stderr 错误输出支持机器可读上下文。
  - `NolonCLIErrorEnvelope` 新增可选 `error.detail` 字段。
  - `NolonLiveSkillsRepositoryService.mapSyncError` 映射补齐 detail：
    - 基础：`git_url`、`pull_strategy`、`credential_strategy`、`has_access_token`
    - 扩展：`phase`（clone/pull/command）、`host`（ssh_not_available）
  - 新增/更新测试：
    - `NolonCoreCLIKitTests.runnerRendersStructuredSyncErrorCode`（断言 detail）
    - `NolonSkillsRepositoryServiceTests.syncMapsAccessTokenRequired`（断言 detail 字段）
  - 回归通过：`swift test --package-path libs/Providers`（98 tests）。
- 第二十六轮推进（仓库生命周期下沉第 7 批：typed error detail）：
  - `NolonGitSyncErrorDetail` 落地为固定结构体（`git_url/pull_strategy/credential_strategy/has_access_token/phase/host`），替代 `syncFailed` 中的 `[String:String]`。
  - `NolonCLIErrorEnvelope.error.detail` 改为该 typed 结构并保持 JSON snake_case 编码稳定。
  - 更新测试：
    - `NolonCoreCLIKitTests.runnerRendersStructuredSyncErrorCode`（兼容原断言）
    - `NolonSkillsRepositoryServiceTests.syncMapsAccessTokenRequired`（改为 typed 字段断言）
  - 回归通过：`swift test --package-path libs/Providers`（98 tests）。
- 第二十七轮推进（仓库生命周期下沉第 8 批：preflight issue 强类型）：
  - `NolonGitSyncPreflightIssue` 的 `code/severity` 从字符串升级为 enum：
    - `NolonGitSyncPreflightIssueCode`
    - `NolonGitSyncPreflightIssueSeverity`
  - `NolonLiveSkillsRepositoryService` 新增 issue 映射函数，显式将 façade issue code/severity 映射到 CLI 强类型。
  - 保持 JSON 输出兼容（rawValue 不变），同时降低业务层字符串拼写漂移风险。
  - 回归通过：`swift test --package-path libs/Providers --filter 'NolonCoreCLIKitTests|NolonLiveSkillsRepositoryService'` 与全量 `swift test --package-path libs/Providers`。
- 第二十八轮推进（仓库生命周期下沉第 9 批：策略参数类型化）：
  - `NolonCoreCLIKit` 将 `pullStrategy/credentialStrategy` 从字符串升级为 enum：
    - `NolonGitPullStrategy`：`ff-only|rebase`
    - `NolonGitCredentialStrategy`：`automatic|token-only|ssh-only`
  - 命令解析阶段即校验非法策略值，新增：
    - `invalid pull strategy is rejected by parser`
    - `invalid credential strategy is rejected by parser`
  - service 协议与错误 detail 同步改为 typed strategy，移除运行时字符串解析分支。
  - 回归通过：
    - `swift test --package-path libs/Providers --filter 'NolonCoreCLIKitTests|NolonSkillsRepositoryServiceTests'`
    - `swift test --package-path libs/Providers`（100 tests）。
- 第二十九轮推进（安装能力下沉到 CLI/SDK）：
  - `NolonCoreCLIKit` 新增技能安装能力：
    - `skills install --skill-path --provider-path [--install-method symlink|copy] [--skill-id]`
    - `skills uninstall --skill-id --provider-path`
  - 新增 typed 模型：
    - `NolonSkillInstallMethod`
    - `NolonSkillInstallResult`
    - `NolonSkillUninstallResult`
  - `NolonSkillsRepositoryServing` 协议新增 `installSkill`/`uninstallSkill`，live service 基于 `STFilePath` 实现 symlink/copy 与删除。
  - 新增/更新测试（先红后绿）：
    - `NolonCoreCLIKitTests.parseSkillsInstall`
    - `NolonCoreCLIKitTests.parseSkillsUninstall`
    - `NolonCoreCLIKitTests.runnerRendersInstallResult`
    - `NolonCoreCLIKitTests.runnerRendersUninstallResult`
    - `NolonSkillsRepositoryServiceTests.installSkillCreatesSymlink`
    - `NolonSkillsRepositoryServiceTests.uninstallSkillRemovesTarget`
  - 回归通过：
    - `swift test --package-path libs/Providers --filter 'NolonCoreCLIKitTests|NolonSkillsRepositoryServiceTests'`
    - `swift test --package-path libs/Providers`（106 tests）。
- 第三十轮推进（资源安装能力下沉到 CLI/SDK）：
  - `NolonCoreCLIKit` 新增资源命令：
    - `resources install --kind workflow|mcp --file-path --target-path [--resource-name] [--install-method symlink|copy]`
    - `resources uninstall --kind workflow|mcp --resource-name --target-path`
  - 新增 typed 模型：
    - `NolonResourceKind`
    - `NolonResourceInstallResult`
    - `NolonResourceUninstallResult`
  - `NolonSkillsRepositoryServing` 协议新增 `installResource`/`uninstallResource`，live service 使用 `STFilePath` 完成复制/软链接/删除。
  - 新增/更新测试（先红后绿）：
    - `NolonCoreCLIKitTests.parseResourcesInstall`
    - `NolonCoreCLIKitTests.parseResourcesUninstall`
    - `NolonCoreCLIKitTests.runnerRendersResourcesInstallResult`
    - `NolonCoreCLIKitTests.runnerRendersResourcesUninstallResult`
    - `NolonSkillsRepositoryServiceTests.installResourceCopiesFile`
    - `NolonSkillsRepositoryServiceTests.uninstallResourceRemovesTarget`
  - 回归通过：
    - `swift test --package-path libs/Providers --filter 'NolonCoreCLIKitTests|NolonSkillsRepositoryServiceTests'`
    - `swift test --package-path libs/Providers`（112 tests）。
- 第三十一轮推进（迁移助手核心下沉到 CLI/SDK）：
  - `NolonCoreCLIKit` 新增迁移命令：
    - `skills migrate scan --provider-path --global-skills-path`
    - `skills migrate apply --skill-id --provider-path --global-skills-path [--install-method symlink|copy]`
  - 新增 typed 模型：
    - `NolonProviderSkillStateKind`
    - `NolonProviderSkillState`
    - `NolonSkillMigrateScanResult`
  - `NolonSkillsRepositoryServing` 协议新增：
    - `scanProviderSkills`
    - `migrateSkill`
  - live service 实现：
    - 扫描 provider 目录并输出 `installed/orphaned/broken`
    - 从 global skills 路径按 install-method 修复迁移（复用 installSkill）
  - 新增/更新测试（先红后绿）：
    - `NolonCoreCLIKitTests.parseSkillsMigrateScan`
    - `NolonCoreCLIKitTests.parseSkillsMigrateApply`
    - `NolonCoreCLIKitTests.runnerRendersMigrateScanResult`
    - `NolonCoreCLIKitTests.runnerRendersMigrateApplyResult`
    - `NolonSkillsRepositoryServiceTests.scanProviderSkillsReportsOrphaned`
    - `NolonSkillsRepositoryServiceTests.migrateSkillLinksFromGlobal`
  - 回归通过：
    - `swift test --package-path libs/Providers --filter 'NolonCoreCLIKitTests|NolonSkillsRepositoryServiceTests'`
    - `swift test --package-path libs/Providers`（118 tests）。
- 第三十二轮推进（远程目录/下载下沉到 CLI/SDK）：
  - `NolonCoreCLIKit` 新增远程命令：
    - `remote list --kind skill|workflow|mcp [--query] [--limit] [--base-url]`
    - `remote download --kind skill|workflow|mcp --slug [--version] [--base-url]`
  - 新增 typed 模型：
    - `NolonRemoteCatalogKind`
    - `NolonRemoteCatalogItem`
    - `NolonRemoteListResult`
    - `NolonRemoteDownloadResult`
  - `NolonSkillsRepositoryServing` 协议新增：
    - `listRemoteResources`
    - `downloadRemoteResource`
  - live service 实现复用 Clawdhub API 形状（skills/workflows/mcps 列表与下载端点），统一到 `NolonCoreCLIKit`。
  - 新增/更新测试（先红后绿）：
    - `NolonCoreCLIKitTests.parseRemoteList`
    - `NolonCoreCLIKitTests.parseRemoteDownload`
    - `NolonCoreCLIKitTests.runnerRendersRemoteListResult`
    - `NolonCoreCLIKitTests.runnerRendersRemoteDownloadResult`
  - 回归通过：
    - `swift test --package-path libs/Providers --filter NolonCoreCLIKitTests`
    - `swift test --package-path libs/Providers`（122 tests）。
- 第三十三轮推进（skills 标准解析结构化校验）：
  - `ProviderCatalog.SkillSpecificationParser` 新增结构化校验输出：
    - `IssueCode` / `IssueSeverity` / `ValidationIssue`
    - `StandardMetadata.issues`
    - `StandardMetadata.isValid`（有 error issue 即 false）
  - 必填字段策略升级：
    - `name`/`description` 缺失或空值标记为 `error`（`missing_name` / `missing_description`）
    - `unknown_top_level_field`、`allowed_tools_non_string_item` 等保留 warning 级别
  - `NolonCoreCLIKit` `skills parse` 输出增强：
    - `NolonSkillStandardMetadata` 新增 `is_valid` 与 `issues`（含 code/severity/message）
  - 新增/更新测试（先红后绿）：
    - `SkillSpecificationParserTests`：覆盖 `isValid` 与 issue code/severity 断言
    - `NolonCoreCLIKitTests.runnerRendersParseResult`：断言 `is_valid` / `issues`
  - 回归通过：
    - `swift test --package-path libs/Providers --filter 'SkillSpecificationParserTests|NolonCoreCLIKitTests'`
    - `swift test --package-path libs/Providers`（123 tests）。
- 第三十四轮推进（远程目录读取从 UI 直连下沉到 façade）：
  - `ProviderCatalog.SkillsRepositoryFacade` 新增：
    - `RemoteCatalogKind`
    - `RemoteCatalogItem`
    - `RemoteListResult`
    - `listRemoteResources(kind:query:limit:baseURL:loader:)`
  - `NolonLiveSkillsRepositoryService.listRemoteResources` 改为复用 façade，删除与 Clawdhub 列表解析的并行实现路径。
  - App 侧编排替换：
    - `RemoteSkillsGridViewModel`（`.clawdhub` 分支）改为调用 façade list API 并映射为 `RemoteSkill/RemoteWorkflow/RemoteMCP`
    - `RemoteContentTabViewModel`（计数加载）改为调用 façade list API
  - 新增测试（先红后绿）：
    - `SkillsRepositoryFacadeTests.listRemoteSkills`
    - `SkillsRepositoryFacadeTests.listRemoteSearch`
  - 回归通过：
    - `swift test --package-path libs/Providers --filter SkillsRepositoryFacadeTests`
    - `swift test --package-path libs/Providers`（125 tests）
    - `./build.sh`（Release）成功。

## 第三十五轮推进（Facade远程下载下沉）
- 新增 `SkillsRepositoryFacade.downloadRemoteResource`，统一 skill/workflow/mcp 下载入口，App 不再直连 `ClawdhubRepository` 下载。
- MainSplitViewModel/ProviderDetailGridViewModel/ProviderSkillsViewModel/UpdatesViewModel/SkillUpdateChecker 已改走 ProviderCatalog façade。
- 新增并通过下载相关测试（skill/workflow/http错误），`swift test --package-path libs/Providers` 全绿，`./build.sh` 通过。

## 第三十六轮推进（CLI路径抽象下沉到 STFilePath）
- 目标：把 `NolonCoreCLIKit` 执行层路径处理从 `URL(fileURLWithPath:)` 下沉为 `STPath/STFolder`，减少本地 URL 构造耦合，贴合 CLI/SDK 统一路径抽象。
- 变更：
  - `NolonSkillsRepositoryServing` 协议签名下沉：
    - `repositoriesRoot/repositoryPath/providerPath/globalSkillsPath/targetPath` -> `STFolder`
    - `skillPath/filePath` -> `STPath`
  - `NolonCoreCLIRunner`：命令参数字符串直接转 `STFolder/STPath` 传入 service，不再使用 `URL(fileURLWithPath:)`。
  - `NolonLiveSkillsRepositoryService`：
    - 与 façade 交互处使用 `.url` 适配；
    - install/uninstall/migrate/scan/resource 安装卸载流程改为 STPath 原语；
    - `scanProviderSkills` 去掉 `FileManager.contentsOfDirectory`，改用 `STFolder.subFilePaths(.skipsHiddenFiles)` + `isSymbolicLink/destinationOfSymbolicLink`。
  - 测试桩同步改为 STPath 签名：`NolonCoreCLIKitTests`、`NolonSkillsRepositoryServiceTests`。
- 兼容处理：
  - `NolonGitImportPlan.localClonePath` 继续保持 `URL`（因当前依赖版本 `STFolder` 不具备 Codable 合成能力），仅执行层下沉到 STPath。
- 验证：
  - `swift test --package-path libs/Providers --filter NolonCoreCLIKitTests --filter NolonSkillsRepositoryServiceTests` 通过（36 tests）。
  - `swift test --package-path libs/Providers` 全量通过（128 tests）。

## 第三十七轮推进（远程下载去重 + App 安装路径继续 STPath 化）
- `NolonLiveSkillsRepositoryService.downloadRemoteResource` 已改为复用 `SkillsRepositoryFacade.downloadRemoteResource`，移除 CLI 层自建 URL/下载实现，避免与 façade 并行维护。
- 删除 `NolonSkillsRepositoryService.swift` 中已无用代码：
  - `jsonDecoder` 成员
  - `buildURL` 辅助方法
  - Clawdhub 私有解码结构体残留
- App 侧继续下沉本地路径构造：
  - `MainSplitView`：workflow/mcp 本地安装从 `URL(fileURLWithPath:)` 改为 `STPath(localPath).url`
  - `ProviderDetailGridViewModel`：
    - 远程 workflow/mcp 本地安装同样改为 `STPath(localPath).url`
    - `createAgentDocDraft` 与 `createRuleDraft` 改为 `STPath/STFolder` 路径判断与目录创建（减少 `FileManager.fileExists/createDirectory` 直用）
- 验证：
  - `swift test --package-path libs/Providers --filter NolonCoreCLIKitTests --filter NolonSkillsRepositoryServiceTests` 通过
  - `swift test --package-path libs/Providers --filter SkillsRepositoryFacadeTests` 通过
  - `./build.sh` 通过（BUILD SUCCEEDED）

## 第三十八轮推进（Codex 配置/规则路径 STFilePath 收敛）
- 将 `ProviderDetailGridViewModel` 的 Codex rules/agents/models cache 读取进一步收敛到 STFilePath：
  - rules 递归扫描由 STFolder files/folders 组合实现，保留隐藏文件过滤与 `.rules` 筛选。
  - agents 文件存在判断改为 `STFile.isExists`。
  - models cache 与 TOML 配置读取改为 `STFile + Data(contentsOf:)`。
- 将 `CodexBinaryConfigViewModel`、`CodexAdvancedConfigViewModel` 的配置文件创建/读取路径收敛到 STFilePath：
  - 配置目录创建使用 `STFolder(...).createIfNotExists()`。
  - 配置文件默认路径与解析路径统一由 `STFile` 构建。
  - Xcode link folder 操作改为 `STPath.createSymbolicLink/deleteIncludingBrokenSymlink`。
- 新增测试场景：
  - `CodexRulesTabTests` 增加“隐藏 rules 文件应被忽略”场景。
- 验证结果：
  - `xcodebuild test -only-testing:nolonTests/CodexRulesTabTests` 通过。
  - `xcodebuild test -only-testing:nolonTests/CodexAgentsTabTests` 通过。
  - `./build.sh` Release 构建通过。

## 第三十九轮推进（Codex Auth 下沉到 Providers Phase 1）
- 目标达成：`CodexAuthService` 核心能力已下沉到 `libs/Providers`，app 层改为薄桥接。
- 库侧新增：
  - `ProviderUsage/CodexAuthManager.swift`
  - `ProviderUsage/CodexAuthAccount.swift`（含 `CodexAuthSummary`）
  - `CodexAuthManagerTests`（3 个 BDD 场景：token 读取、激活 clean sync、CLI finalize）
- app 侧调整：
  - `nolon/Skills/Infrastructure/CodexAuthService.swift` 改为桥接 `CodexAuthManager`
  - `nolon/Skills/Models/CodexAuthAccount.swift` 改为 `ProviderUsage` 类型别名
- 并发兼容修复：
  - 去除共享静态 `ISO8601DateFormatter`（按需创建）
  - 桥接层跨 actor 调用改为 `async/await`
  - `CLILoginError.errorDescription` 提升为 `public`
- 验证通过：
  - `swift test --package-path libs/Providers --filter CodexAuthManagerTests`
  - `xcodebuild test -project nolon.xcodeproj -scheme nolon -destination 'platform=macOS' -only-testing:nolonTests/CodexAuthServiceTests`
- 风险与下一步：
  - app 侧 watcher 事件策略（去抖/rename policy）尚未下沉；下一阶段可并入 `ProviderUsage`。
  - runtime switcher 与 auth manager 尚未统一门面。

## 第四十轮推进（Codex auth 事件策略下沉）
- 下沉内容：
  - `CodexAuthEventPolicy`（已知账户 rename 过滤）
  - `CodexAuthChangeSuppressionStore`（auth 变更短窗口抑制）
- app 接入：`ProviderUsageViewModel` 去除本地 suppression 字典与本地 rename 判断，统一调用 ProviderUsage 下沉实现。
- 测试：
  - Providers 新增 `CodexAuthEventPolicyTests` / `CodexAuthChangeSuppressionStoreTests`
  - `nolonTests/CodexAuthServiceTests` 删除本地重复策略定义并改为验证库类型
- 验证通过：
  - `swift test --package-path libs/Providers --filter 'CodexAuthEventPolicyTests|CodexAuthChangeSuppressionStoreTests'`
  - `xcodebuild test -project nolon.xcodeproj -scheme nolon -destination 'platform=macOS' -only-testing:nolonTests/CodexAuthServiceTests`

## 第四十一轮推进（Auth + Runtime 统一门面）
- 新增 `ProviderUsage.CodexAuthRuntimeCoordinator`：统一“从 snapshot 读取 token + runtime switch”流程。
- 错误模型：`CodexAuthRuntimeCoordinatorError`（`tokenPairMissing` / `runtimeSwitchFailed`）。
- 新增测试：`CodexAuthRuntimeCoordinatorTests`（成功切换/缺失 token/runtime 失败映射）。
- app 接入：`ProviderUsageViewModel.confirmActivate` 在激活后调用 runtime coordinator，失败仅日志，不阻塞激活。
- 验证通过：
  - `swift test --package-path libs/Providers --filter 'CodexAuthRuntimeCoordinatorTests|CodexAuthEventPolicyTests|CodexAuthChangeSuppressionStoreTests'`
  - `xcodebuild test -project nolon.xcodeproj -scheme nolon -destination 'platform=macOS' -only-testing:nolonTests/CodexAuthServiceTests`

## 第四十二轮推进（Session 事件类型化 + 统一解析 API）
- 新增 `CodexSessionEventParser`（CodexProvider）：
  - `CodexSessionEvent` 覆盖 `response_item/event_msg/compacted/turn_context/session_meta/token_count`
  - `CodexSessionUsageEvent` 提供 token 统计 fast-path
- `CostUsageScanner` 已切换使用 `parseUsageEventLine(data:)`，token 统计链路统一走 parser API。
- 新增测试：`CodexSessionEventParserTests`（完整事件 + fast-path），并回归 `CostUsageStoragePathTests`。
- 验证通过：
  - `swift test --package-path libs/Providers --filter 'CodexSessionEventParserTests|CostUsageStoragePathTests'`

## 第四十三轮推进（Binary 双实现清理）
- 决策：删除 app 侧 `CodexBinaryManager/CodexBinaryManifest` 遗留实现，确保 Binary 能力单一来源为 `CodexProvider`。
- BDD/TDD：
  - 新增 `CodexBinaryDownsinkMigrationTests` 锁定 app 类型与 `CodexProvider` 类型一致。
  - 清理后出现红灯：`String.nonEmpty` 丢失引发编译失败。
  - 最小修复：
    - 新增 `nolon/Skills/Models/String+Nolon.swift`
    - `nolonTests/CodexBinaryAutoUpdateBehaviorTests` 显式 `import CodexProvider`
  - 回归转绿。
- 验证通过：
  - `xcodebuild test -project nolon.xcodeproj -scheme nolon -destination 'platform=macOS' -only-testing:nolonTests/CodexBinaryDownsinkMigrationTests -only-testing:nolonTests/CodexBinaryAutoUpdateBehaviorTests -only-testing:nolonTests/CodexAuthServiceTests`

## 第四十四轮推进（Auth 包装层移除）
- 决策：`CodexAuthService` 从 app 包装 actor 收敛为库侧别名，避免与 `ProviderUsage.CodexAuthManager` 双维护。
- BDD/TDD：
  - 新增迁移守卫测试：
    - `CodexAuthServiceTests.testBDD_GivenAppAuthServiceType_WhenCheckingMigrationBoundary_ThenUsesProviderUsageManagerType`
  - 红灯确认后，将 `nolon/Skills/Infrastructure/CodexAuthService.swift` 改为：
    - `typealias CodexAuthService = CodexAuthManager`
  - 删除原 1:1 转发实现，调用点保持不变。
- 回归通过：
  - `xcodebuild test -project nolon.xcodeproj -scheme nolon -destination 'platform=macOS' -only-testing:nolonTests/CodexAuthServiceTests/testBDD_GivenAppAuthServiceType_WhenCheckingMigrationBoundary_ThenUsesProviderUsageManagerType`
  - `xcodebuild test -project nolon.xcodeproj -scheme nolon -destination 'platform=macOS' -only-testing:nolonTests/CodexAuthServiceTests -only-testing:nolonTests/CodexBinaryDownsinkMigrationTests -only-testing:nolonTests/CodexBinaryAutoUpdateBehaviorTests`

## 第四十五轮推进（Auth 旧名清理）
- 决策：去掉 app 侧 `CodexAuthService` 旧名，统一以 `CodexAuthManager` 表达下沉后真实边界。
- 变更：
  - 删除 `nolon/Skills/Infrastructure/CodexAuthService.swift`（别名文件）
  - `ProviderUsageViewModel` 依赖字段改为 `codexAuthManager`
  - `CodexAuthServiceTests.swift` 中调用统一替换为 `CodexAuthManager`（类名同步为 `CodexAuthManagerTests`）
- 回归通过：
  - `xcodebuild test -project nolon.xcodeproj -scheme nolon -destination 'platform=macOS' -only-testing:nolonTests/CodexAuthManagerTests -only-testing:nolonTests/CodexAuthTokenExtractionTests -only-testing:nolonTests/CodexAuthActiveAccountRegistryTests -only-testing:nolonTests/CodexAuthCompatSyncTests -only-testing:nolonTests/CodexBinaryDownsinkMigrationTests -only-testing:nolonTests/CodexBinaryAutoUpdateBehaviorTests`

## 第四十六轮推进（CLI 登录快照编排下沉）
- BDD 目标：将 CLI 登录后“账号匹配/更新 + 登录/同步时间戳写入”从 `ProviderUsageViewModel` 下沉到 `ProviderUsage.CodexAuthManager`，app 仅保留流程触发。
- TDD：
  - 先补红灯：`CodexAuthManagerTests.recordCLILoginSnapshotUpdatesPreferredAndMetadata`（调用新 API 前编译失败，确认红灯）。
  - 最小实现：
    - `CodexAuthManager` 新增 `recordCLILoginSnapshot(authJSONString:preferredAccountID:loginAt:)`，统一封装：
      - `upsertAccountFromCLILogin`
      - `updateLoginSuccess`
      - `updateSyncSuccess`
    - `ProviderUsageViewModel.runCLILoginFlow` 改为调用单点 API，移除本地 email/hash 匹配和 add/update 细节。
- 额外修正：
  - `ProviderUsageViewModelCLILoginTests` 旧断言与现行为不一致（现行为“运行中再次请求会重启并携带 preferred account”），更新测试名与断言：
    - `...ThenFlowRestartsWithPreferredAccount`
- 验证通过：
  - `swift test --package-path libs/Providers --filter CodexAuthManagerTests`
  - `xcodebuild test -project nolon.xcodeproj -scheme nolon -destination 'platform=macOS' -only-testing:nolonTests/CodexAuthManagerTests -only-testing:nolonTests/ProviderUsageViewModelCLILoginTests`

## 第四十七轮推进（激活编排门面下沉）
- BDD 目标：将 `confirmActivate()` 的 “auth 激活 + runtime 切换 + 失败策略” 下沉到库侧统一门面，app 层仅处理 UI 状态与日志。
- TDD：
  - 新增库侧测试 `CodexAuthActivationCoordinatorTests`，覆盖：
    - auth/runtime 成功
    - auth 成功但 runtime 失败（不抛错，返回失败信息）
    - auth 失败（抛错，runtime 不执行）
  - 最小实现：
    - 新增 `CodexAuthActivationCoordinator`
    - 新增 `CodexAuthActivationResult`
  - app 接入：
    - `ProviderUsageViewModel.confirmActivate` 改为调用 `CodexAuthActivationCoordinator.shared.activate(...)`
    - 保持现有策略：runtime 失败仅日志，不阻断激活与后续 `load()`。
- 验证通过：
  - `swift test --package-path libs/Providers --filter CodexAuthActivationCoordinatorTests`
  - `xcodebuild test -project nolon.xcodeproj -scheme nolon -destination 'platform=macOS' -only-testing:nolonTests/CodexAuthManagerTests -only-testing:nolonTests/ProviderUsageViewModelCLILoginTests`

## 第四十八轮推进（激活后 active registry 标记下沉）
- BDD 目标：统一激活后必须同步写入 active registry，确保即使 provider `auth.json` 缺失也可识别当前 active account。
- TDD：
  - 先补红灯：`CodexAuthManagerTests.activateAccountAndMarkActivePersistsRegistry`（调用新 API 前编译失败）。
  - 最小实现：
    - `CodexAuthManager` 新增 `activateAccountAndMarkActive(_:, for:)`，封装 `activateAccount + setActiveAccount`。
    - `CodexAuthActivationCoordinator` 默认 auth 激活流程切换到 `activateAccountAndMarkActive`。
- 验证通过：
  - `swift test --package-path libs/Providers --filter CodexAuthManagerTests`
  - `swift test --package-path libs/Providers --filter CodexAuthActivationCoordinatorTests`
  - `xcodebuild test -project nolon.xcodeproj -scheme nolon -destination 'platform=macOS' -only-testing:nolonTests/CodexAuthManagerTests -only-testing:nolonTests/ProviderUsageViewModelCLILoginTests`

## 第四十九轮推进（confirmActivate 行为测试补齐）
- BDD 目标：在 app 层锁定 `confirmActivate()` 两条关键行为：
  - runtime 失败不阻断激活后续流程
  - 激活抛错时正确展示 alert 并保留 pending
- 变更：
  - `ProviderUsageViewModel` 增加可测试注入点（默认行为不变）：
    - `codexActivateAction`
    - `postActivationLoadAction`
  - 新增 `ProviderUsageViewModelActivationTests` 两个用例：
    - `...ThenActivationContinuesAndReloadRuns`
    - `...ThenShowsActivationAlert`
  - 修复测试细节：
    - 避免在 `XCTAssert` autoclosure 内 `await`
    - alert 断言改用 `NSLocalizedString` key 适配中文/英文环境
- 验证通过：
  - `xcodebuild test -project nolon.xcodeproj -scheme nolon -destination 'platform=macOS' -only-testing:nolonTests/ProviderUsageViewModelActivationTests`
  - `xcodebuild test -project nolon.xcodeproj -scheme nolon -destination 'platform=macOS' -only-testing:nolonTests/CodexAuthManagerTests -only-testing:nolonTests/ProviderUsageViewModelCLILoginTests -only-testing:nolonTests/ProviderUsageViewModelActivationTests`

## 第五十轮推进（Usage parser 归约 API 统一）
- BDD 目标：让 token 统计链路中的“model 回退、total 差分、cached clamp、last usage 增量”统一收敛到 `CodexSessionEventParser`，`CostUsageScanner` 不再持有重复解析逻辑。
- TDD：
  - 先补红灯：`CodexSessionEventParserTests.reduceUsageLineForTokenDelta`（调用新 API 前编译失败）。
  - 最小实现：
    - `CodexSessionEventParser` 新增：
      - `CodexSessionTokenTotals`
      - `CodexSessionTokenDelta`
      - `CodexSessionUsageReduction`
      - `reduceUsageLine(data:currentModel:previousTotals:)`
    - `CostUsageScanner.parseCodexFile` 改为消费 `reduceUsageLine` 返回值推进 `sessionId/currentModel/previousTotals` 与日聚合。
- 验证通过：
  - `swift test --package-path libs/Providers --filter CodexSessionEventParserTests`
  - `swift test --package-path libs/Providers --filter CostUsageStoragePathTests`
  - `swift test --package-path libs/Providers --filter CostUsageFetcherTests`
- 风险记录：
  - 本轮仅覆盖 libs/Providers 相关测试，未执行全量 `swift test --package-path libs/Providers` 与 app 全量 `xcodebuild test`。
  - `CostUsageScanner` 对 EOF 最后一行的扫描边界可能影响用例稳定性；新增集成测试通过在末尾追加无关事件规避边界噪声，后续可独立评估 `CostUsageJsonl.scan` EOF 行为并决定是否修复。

## 第五十一轮推进（JSONL EOF 最后一行扫描修复）
- BDD 目标：`rollout.jsonl` 最后一行无 trailing newline 时，token 事件仍应被 usage 聚合统计。
- TDD：
  - 先补红灯：`CostUsageStoragePathTests.costUsageScannerParsesLastLineWithoutTrailingNewline`（期望 `[103,83,22]`，实际仅 `[100,80,20]`）。
  - 最小实现：
    - `ProvidersShared.CostUsageJsonl.scan` 在 EOF 时处理 `buffer` 残留为最后一行后再 `flushLine`。
    - 抽出 `appendLinePart(...)` 统一换行与 EOF 残留的 line 拼装/截断规则。
- 验证通过：
  - `swift test --package-path libs/Providers --filter CostUsageStoragePathTests`
  - `swift test --package-path libs/Providers --filter CodexSessionEventParserTests`
  - `swift test --package-path libs/Providers --filter CostUsageFetcherTests`

## 第五十二轮推进（UsageMonitor 环境拼装继续下沉）
- BDD 目标：将 app 侧 `UsageMonitorService` 中的 codex 环境拼装（managed env + `CODEX_CLI_PATH`）下沉到 `ProviderUsageMonitorService`，app 仅做委托。
- TDD：
  - 新增库侧测试 `ProviderUsageMonitorServiceTests`：
    - `resolveCodexEnvironmentMergesManagedValues`
    - `resolveNonCodexEnvironmentKeepsBase`
  - 最小实现：
    - `ProviderUsageMonitorService` 新增注入点与默认实现：
      - `codexManagedEnvironmentLoader`
      - `codexCLIPathLoader`
    - 新增 `resolveEnvironmentForFetch(provider:)`，在 codex 下自动合并环境。
    - `fetchOutcomes` 改为使用统一解析后的 environment 构造 `ProviderFetchContext`。
    - `nolon/Skills/Infrastructure/UsageMonitorService` 删除本地 codex 环境拼装逻辑，改为直接调用库侧 monitor。
- 验证通过：
  - `swift test --package-path libs/Providers --filter ProviderUsageMonitorServiceTests`
  - `swift test --package-path libs/Providers --filter ProviderUsageTokenAccountsTests`
  - `swift test --package-path libs/Providers --filter ProviderUsageRegistryTests`
  - `xcodebuild test -project nolon.xcodeproj -scheme nolon -destination 'platform=macOS' -only-testing:nolonTests/UsageMonitorServiceTests`

## 第五十三轮推进（Token account 默认路径规则下沉）
- BDD 目标：`token-accounts.json` 默认路径规则由 `ProviderUsage` 统一提供，app 不再持有重复路径拼装细节。
- TDD：
  - 先补红灯：`ProviderUsageTokenAccountsTests.defaultTokenAccountsFilePathLayout`（新增 `ProviderUsagePaths` 前编译失败）。
  - 最小实现：
    - `ProviderUsage` 新增 `ProviderUsagePaths.defaultTokenAccountsFileURL(baseDirectory:)`。
    - `UsageMonitorService.defaultTokenAccountsFileURL()` 改为委托该库侧 API。
- 验证通过：
  - `swift test --package-path libs/Providers --filter ProviderUsageTokenAccountsTests`
  - `swift test --package-path libs/Providers --filter ProviderUsageMonitorServiceTests`
  - `xcodebuild test -project nolon.xcodeproj -scheme nolon -destination 'platform=macOS' -only-testing:nolonTests/UsageMonitorServiceTests`

## 第五十四轮推进（app 层去除 token path 静态桥接）
- BDD 目标：app 监听 token account 文件时直接依赖库侧 `ProviderUsagePaths`，删除 `UsageMonitorService.defaultTokenAccountsFileURL()` 过渡桥接。
- 变更：
  - `ProviderUsageViewModel.updateUsageFileWatcher()` 直接使用 `ProviderUsagePaths.defaultTokenAccountsFileURL().path`
  - `UsageMonitorService` 初始化直接使用 `ProviderUsagePaths.defaultTokenAccountsFileURL()`
  - 删除 `UsageMonitorService.defaultTokenAccountsFileURL()`
  - `UsageMonitorServiceTests` 改为直接断言 `ProviderUsagePaths` 路径输出
- 验证通过：
  - `swift test --package-path libs/Providers --filter ProviderUsageTokenAccountsTests`
  - `swift test --package-path libs/Providers --filter ProviderUsageMonitorServiceTests`
  - `xcodebuild test -project nolon.xcodeproj -scheme nolon -destination 'platform=macOS' -only-testing:nolonTests/UsageMonitorServiceTests`

## 第五十五轮推进（ViewModel 直接依赖 ProviderUsageMonitorService）
- BDD 目标：继续收敛 app 编排层，`ProviderUsageViewModel` 直接调用库侧 `ProviderUsageMonitorService` 拉取 usage，去掉对 `UsageMonitorService` 包装层的运行时依赖。
- TDD：
  - 先补红灯：`UsageMonitorServiceTests` 新增 cost window 回退规则用例，初始因 `ProviderUsageViewModel.resolveCostWindowDays` 不存在而失败。
  - 最小实现：
    - `ProviderUsageViewModel` 新增 `usageMonitor` 依赖并默认构造 `ProviderUsageMonitorService`。
    - `load()` 改为直接调用 `usageMonitor.fetchOutcomes(...)`。
    - 新增 `resolveCostWindowDays(selectedCostWindowDays:settings:)`，保持原 `selected ?? settings` 语义。
  - 测试修正：新增用例标注 `@MainActor`，满足 `ProviderUsageViewModel` 主线程隔离约束。
- 验证通过：
  - `xcodebuild test -project nolon.xcodeproj -scheme nolon -destination 'platform=macOS' -only-testing:nolonTests/UsageMonitorServiceTests`
  - `swift test --package-path libs/Providers --filter ProviderUsageMonitorServiceTests`
- 风险与后续：
  - `nolon/Skills/Infrastructure/UsageMonitorService.swift` 现已无 app 调用点，属于可删除的迁移遗留文件；后续可单独一提交清理（若删除需同步确认 Xcode 工程文件引用）。

## 第五十六轮推进（删除 app 层 UsageMonitor 遗留包装）
- BDD 目标：清理 `ProviderUsageViewModel` 直连库侧 monitor 后的遗留壳层，避免 app 编排层继续保留无调用的迁移代码。
- 变更：
  - 删除 `nolon/Skills/Infrastructure/UsageMonitorService.swift`。
- 迁移守卫：
  - 清理前 `rg` 仅命中该文件自身定义。
  - 删除后 app 源码中不再存在 `UsageMonitorService` 调用/定义。
- 验证通过：
  - `xcodebuild test -project nolon.xcodeproj -scheme nolon -destination 'platform=macOS' -only-testing:nolonTests/UsageMonitorServiceTests`

## 第五十七轮推进（cost window 回退规则下沉）
- BDD 目标：把 `selected ?? settings` 的 cost window 回退规则从 app ViewModel 下沉到 `ProviderUsageMonitorSettings`，减少 app 侧重复业务规则。
- TDD：
  - 先补红灯：新增 `ProviderUsageMonitorSettingsTests`，调用 `effectiveCostWindowDays(selected:)` 时编译失败。
  - 最小实现：
    - `ProviderUsageMonitorSettings` 新增 `effectiveCostWindowDays(selected:)`。
    - `ProviderUsageViewModel.load()` 改用该库侧 API。
    - 删除 app 侧 `resolveCostWindowDays(...)` 重复方法。
    - `UsageMonitorServiceTests` 调整为断言 settings API。
- 验证通过：
  - `swift test --package-path libs/Providers --filter ProviderUsageMonitorSettingsTests`
  - `swift test --package-path libs/Providers --filter ProviderUsageMonitorServiceTests`
  - `xcodebuild test -project nolon.xcodeproj -scheme nolon -destination 'platform=macOS' -only-testing:nolonTests/UsageMonitorServiceTests`

## 第五十八轮推进（fetchOutcomes 窗口回退收敛）
- BDD 目标：把 `fetchOutcomes` 的 cost window 默认/override 规则收敛到 `ProviderUsageMonitorService`，避免调用方需要先计算 effective window。
- 变更：
  - `ProviderUsageMonitorService.fetchOutcomes` 默认 `costWindowDays` 由 `30` 改为 `nil`。
  - 新增 `resolveCostWindowDaysForFetch(settings:overrideCostWindowDays:)`，内部统一使用 `settings.effectiveCostWindowDays(selected:)`。
  - `ProviderUsageViewModel.load()` 去掉本地窗口计算，直接传 `codexCostWindowDays`。
- 测试：
  - `ProviderUsageMonitorServiceTests` 新增两条用例覆盖“override 优先 / 否则回退 settings”。
- 验证通过：
  - `swift test --package-path libs/Providers --filter ProviderUsageMonitorServiceTests`
  - `swift test --package-path libs/Providers --filter ProviderUsageMonitorSettingsTests`
  - `xcodebuild test -project nolon.xcodeproj -scheme nolon -destination 'platform=macOS' -only-testing:nolonTests/UsageMonitorServiceTests`

## 第五十九轮（CLI 解析器切换：swift-argument-parser）
- 决策：用户明确要求“不要自研”，本轮将 `nolon` CLI 新增 Codex 命令的参数解析切换到 `swift-argument-parser`。
- 工程变更：
  - `libs/Providers/Package.swift` 新增 `swift-argument-parser` 依赖。
  - 可执行从 `nolon-core` 改为 `nolon`，新增 target `NolonCLI`。
  - 删除 `Sources/NolonCoreCLI/main.swift`，新增 `Sources/NolonCLI/main.swift`。
  - `NolonCoreCLIKit` 新增 `NolonCodexCLI.swift`，提供 `codex auth/binary/status` 命令编排与 JSON 输出。
- 测试：
  - 新增 `NolonCodexCLIEntrypointTests`（4 个场景）并通过。
  - 回归 `NolonCoreCLIKitTests` 与新 suite 联跑通过。
- 验证命令：
  - `swift test --package-path libs/Providers --filter NolonCodexCLIEntrypointTests`
  - `swift test --package-path libs/Providers --filter 'NolonCoreCLIKitTests|NolonCodexCLIEntrypointTests'`
  - `swift run --package-path libs/Providers nolon codex binary list`

## 第六十轮（Codex CLI 路由测试补齐）
- 新增 `NolonCodexCLIEntrypointTests` 覆盖剩余命令路由与错误分支：
  - `auth list/status/login`
  - `binary current/use/doctor`
  - `unsupported command` 与 `preferred-account-id` 非 UUID 错误
- 回归：`NolonCoreCLIKitTests` + `NolonCodexCLIEntrypointTests` 联跑通过。
- 结论：`nolon codex` Phase 1 命令面已具备完整路由级回归保护。

## 第六十一轮（Codex CLI 结构化错误码）
- `NolonCoreCLIError` 新增 `domainFailed(code:message:)`，用于保留业务错误码。
- `nolon codex` 关键错误（如 `codex_binary_not_found`）不再折叠为 `execution_failed`。
- 测试新增：`domain error keeps structured code`。

## 第六十二轮（status provider 参数校验）
- `nolon codex status probe --provider` 在路由层执行合法性校验（`codex` / `codex-xcode`）。
- 非法 provider 统一返回 `invalid_arguments`。

## 第六十三轮（auth provider 参数校验一致化）
- `nolon codex auth list/status/activate/login` 全部在路由层前置 provider 校验。
- 防止替换 service/mock 时出现校验缺口，保证命令行为一致。

## 第六十四轮（provider alias canonical 化）
- 将 `codexxcode` / `codex-xcode` 统一 canonical 为 `codex-xcode`。
- CLI 路由与 status probe 输出统一使用 canonical provider id。

## 第六十五轮（CLI/Service provider 语义对齐）
- 新增 `NolonCodexCLIServiceTests`，覆盖 service 直调场景。
- `NolonLiveCodexCLIService` 在 auth/status 输出中统一 canonical provider id。

## 第六十六轮（未知路由错误码细化）
- 未支持命令路由从 `invalid_arguments` 细化为 `unsupported_command`。
- 便于 app/脚本端做稳定错误分支处理。
