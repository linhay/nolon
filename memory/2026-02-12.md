# 2026-02-12

- 完成 Codex 代码精简第二/三轮：删除 Provider 层重复 RPC 客户端，统一到 CodexAppServerKit typed API。
- 将二进制路径与 CODEX_HOME 解析统一到 CodexCLIKit.CodexCommandExecutor。
- 新增 CodexRuntimeSupport，统一 runtime service 生命周期，避免 CodexHelper/CodexCreditsFetcher 重复实现。
- 通过 `swift test --package-path libs/Providers` 全量回归（35 tests）。
- 继续精简：CodexLoginRunner 复用统一 executable resolver；新增登录路径测试；RuntimeAccountSwitcher cache key 改为基于归一化 binary。
- 新增 Switcher 可测试性注入，补齐缓存键行为测试；修正 resolvedBinary 在传入 "codex" 时未走统一 resolver 的问题。
- 收尾：补齐 docs-dev/api、docs-dev/features、docs-dev/ops 目录骨架。
- 验证：执行 ./build.sh（2026-02-12）通过，主工程 Release 构建成功；存在 CodexBinaryManager 并发隔离 warning（当前未阻塞）。
- qmd 仍不可用（command not found），待安装后执行 qmd update && qmd embed。
- 继续推进收尾：Codex rollout 对 custom_tool_call/custom_tool_call_output 改为结构化 JSON 类型，避免字符串化丢信息。
- 新增 URLSchemeHandler BDD 测试并抽出可测 normalize 方法，去除 app 层残留 print，统一 OSLog。
- 回归通过：`swift test --package-path libs/Providers`、URLSchemeHandler 单测、`./build.sh`。
- 补齐文档体系：新增 `docs-dev/features/codex-full-support-spec.md`（目标/范围/BDD 验收）与 `docs-dev/ops/codex-runtime-runbook.md`（运行与排障）。
- 更新 `docs-dev/features/README.md` 与 `docs-dev/ops/README.md` 导航，形成 Codex 交付入口。
- 执行记忆索引闭环：`qmd update && qmd embed`。
- 新增 `docs-dev/api/codex-app-server-rpc.md`：沉淀 Nolon 消费的 Codex app-server JSON-RPC 契约（核心方法、通知、错误语义、兼容约束）。
- 新增 `docs-dev/dev/codex-rollout-coverage-matrix.md`：会话 rollout 解析覆盖矩阵（top-level/response_item/event_msg）与待补强测试清单。
- 更新 `docs-dev/api/README.md` 导航到 RPC 契约文档。
- 按覆盖矩阵补齐解析测试第一批：新增 response_item（function_call_output/local_shell_call/web_search_call/ghost_snapshot）、event_msg（agent/error/warning/turn_complete）与 unknown fallback 断言。
- `CodexGeneratedFilesParserTests` 扩展后通过，`swift test --package-path libs/Providers` 全量回归仍通过（40 tests）。
- 更新 `docs-dev/dev/codex-rollout-coverage-matrix.md`，同步“已覆盖/待补强”状态。
- 清理本地化噪音：将 `AgentDocCardView` 与 `RuleCardView` 的 `Text(" ")` 占位替换为布局占位（`Color.clear`），避免抽取空白本地化 key。
- 删除 `Localizable.xcstrings` 中空白 key `" "`。
- 回归验证：`./build.sh` 通过。
- rollout 解析补强完成：新增 `event_msg.task_started/task_complete`、`response_item.reasoning/compaction_summary`、`event_msg.payload.token_count` 的回归测试并通过。
- 新增 `CodexGeneratedFilesSnapshot` 与 `CodexGeneratedFilesParser.loadAllGeneratedFiles(codexHome:includeArchived:)`，统一解析 `auth.json/history.jsonl/config.toml/managed_config.toml/sessions`。
- 对 `parseHistoryLines` 与 `parseRolloutLines` 抽取公共 JSONL helper，减少重复代码。
- 回归：`swift test --package-path libs/Providers`（42 tests）通过；`./build.sh`（Release）通过。
- qmd 可用并已执行索引同步（本轮执行 `qmd update && qmd embed`）。
- 精简收口（不改行为）：`CodexGeneratedFilesParser` 新增文件名常量与通用文件加载 helper，统一 `auth/history/config` 读取路径。
- 新增回归：`loadAllGeneratedFiles(..., includeArchived: false)` 仅加载 `sessions`，排除 `archived_sessions`。
- 新增文档：`docs-dev/dev/codex-provider-orchestration-guide.md`，明确 app 仅编排、Providers 承担 Codex CLI/RPC/文件解析。
- 回归：`swift test --package-path libs/Providers`（43 tests）通过；`./build.sh` 通过。
