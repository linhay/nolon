- 修复 `ProviderQuotaSection` 重构后的两个回归：
  - 恢复 `Localizable.xcstrings` 中 `usage.metric.resets_at`，避免 `CodexUsageDescriptor` 在 zh-Hans 下回退英文。
  - detailed credits footer 恢复区分 `creditsRefreshedAt` 与 `credits.updatedAt`，主行显示“刷新于”，存在独立快照时间时追加“快照于”。
- 新增回归测试：
  - `ProviderQuotaSectionTests.testBDD_GivenCodexDescriptorUsesResetsAtLabel_WhenCheckingStringCatalog_ThenUsageMetricResetsAtExists`
  - `ProviderQuotaSectionViewDataTests.testBDD_GivenCreditsRefreshTime_WhenResolvingDisplayedTimestamp_ThenPrefersRefreshTime`
  - `ProviderQuotaSectionViewDataTests.testBDD_GivenSeparateCreditsRefreshTime_WhenBuildingMetadataLines_ThenIncludesRefreshAndSnapshotTimes`
- 确认 Codex 卡片右上角刷新控件当前是“常驻按钮”设计，不是刷新状态未清理；`isRefreshing` 只负责把图标切成 `ProgressView`。
- 复盘 Codex 卡片“1 小时前同步但右上角一直转”的真实根因：
  - `~/.nolon/codex/auth` watcher 在 rename / 临时文件切换时触发磁盘重载；
  - `CodexAuthManager.loadAccountsFromAuthFolder()` 之前依赖 `STFilePath` 的目录项属性排序，遇到瞬时消失的 snapshot 文件会在读取属性时触发 fatal；
  - 已改为使用 `FileManager.contentsOfDirectory(atPath:)` 只枚举稳定的 `.json` 文件名，并在 `loadAccount` 中用安全的 URL resource values 读取时间戳，避免文件竞态导致崩溃。
- 新增 auth 目录重载回归测试：
  - `CodexAuthManagerTests.testBDD_GivenAuthFolderContainsTransientArtifacts_WhenLoadingAccounts_ThenOnlyStableJSONSnapshotsAreReturned`
- 验证：
  - `xcodebuild test -project nolon.xcodeproj -scheme nolon-app -destination 'platform=macOS' -only-testing:nolonTests/CodexAuthServiceTests/testBDD_GivenAuthFolderContainsTransientArtifacts_WhenLoadingAccounts_ThenOnlyStableJSONSnapshotsAreReturned -only-testing:nolonTests/CodexAuthServiceTests/testBDD_GivenAccountRefreshHangs_WhenRefreshingCodexAccount_ThenRefreshingStateClearsAfterTimeout`
  - 结果：`TEST SUCCEEDED`
- 根据用户反馈，撤销了“切换 tab / 页面重新 appear 时自动刷新 usage”的行为：
  - 删除 `ProviderUsageView` 的 `onAppear -> handleUsageViewAppear()` 触发链路；
  - `handleUsageViewAppear()` 收敛为空操作，避免 tab 切换触发隐式刷新；
  - 新增测试 `ProviderUsageViewModelManualRefreshTests.testBDD_GivenUsageViewAppearsAgain_WhenHandlingAppear_ThenDoesNotTriggerCodexRefresh`
- 为了让现有测试链路重新可编译，顺手修复了工作区里两个编译问题：
  - `ProviderQuotaSection.swift` 的 `HStack(alignment: .baseline)` 改为 `.firstTextBaseline`
  - `ProviderUsageView.swift` 中不存在的 `outcome.accountID` 改为复用已有的 `accountId`
- 重新验证：
  - `xcodebuild test -project nolon.xcodeproj -scheme nolon-app -destination 'platform=macOS' -only-testing:nolonTests/CodexAuthServiceTests/testBDD_GivenUsageViewAppearsAgain_WhenHandlingAppear_ThenDoesNotTriggerCodexRefresh -only-testing:nolonTests/CodexAuthServiceTests/testBDD_GivenAuthFolderContainsTransientArtifacts_WhenLoadingAccounts_ThenOnlyStableJSONSnapshotsAreReturned`
  - 结果：`TEST SUCCEEDED`
- 根据用户对“新增 API Key”表单的反馈，进一步收敛配置编辑器规则：
  - `新增 API Key` 模式改为 `名称 + API Key` 必填；
  - `Base URL` 归入可选高级项，并默认预填 OpenAI 官方地址 `https://api.openai.com/v1`；
  - 保存前新增 `name required` 校验，底部主按钮在 `名称` 或 `API Key` 为空时禁用。
- 同步更新：
  - `CodexConfigEditorSheet`：API Key 模式新增 `Advanced` 折叠区展示可选 `Base URL`；
  - `ProviderUsageViewModel`：`beginNewCodexAPIKeyAccount()` 预填官方 `Base URL`，`saveCodexConfigEditor()` 强制校验 `名称` 和 `API Key`；
  - `CodexUsageTabPresentationTests`：新增 API Key 默认值 / 副标题 / 空名称保存失败三个回归用例；
  - `docs-dev/features/codex-account-usage-tab-update-2026-03-02.md`：补充“API Key 模式最小必填 + Base URL 默认值”的 BDD；
  - `Localizable.xcstrings`：补充 `codex.accounts.config.error.name_required` 与 API Key 高级区说明文案。
- 追查 Codex usage load 时的 `signal 13 (SIGPIPE)`：
  - `Codex active snapshot backup created ... reason= usage_load` 只是正常备份日志，不是崩溃点；
  - 真正风险在 `JsonRPCLineProcessSession.sendLineData(...)`，之前直接对 `stdinHandle.write(...)` 写 JSON-RPC 行，如果 app-server/CLI 已提前关闭 stdin，Darwin 下会触发 `SIGPIPE`；
  - 已将写入改为显式 fd 循环写入，启动时对该写端启用 `SIGPIPE` 防护，并在子进程提前退出时主动 fail 掉所有 pending request，避免 UI 无限挂起。
- 新增回归测试：
  - `JsonRPCKitTests.closedStdinDuringSendReturnsTransportError`
- 验证：
  - `swift test --package-path libs/Providers --filter JsonRPCKitTests`
  - 结果：`12 tests passed`
- 为了稳定复现 Codex 用量页的 GUI 验证链路，新增 UI Test 启动参数：
  - `NOLON_UI_TEST_SELECTED_PROVIDER_TAB`
  - `MainSplitViewModel.setup()` 在已指定 `NOLON_UI_TEST_SELECTED_PROVIDER_INDEX` 时，会在该 provider 可用的前提下直接切到目标 tab
  - `UITestSupport.environmentOverride` 仅用于单元测试覆写环境，不影响正式运行时
- 新增测试：
  - `MainSplitViewModelTests.testBDD_GivenUITestLaunchSelection_WhenSetupRuns_ThenSelectedProviderAndUsageTabAreApplied`
- 手工 Launch Services 验证：
  - 用 `launchctl setenv` 注入 `NOLON_UI_TEST_MODE=1`、`NOLON_UI_TEST_SKIP_ONBOARDING=1`、`NOLON_UI_TEST_SELECTED_PROVIDER_INDEX=0`、`NOLON_UI_TEST_SELECTED_PROVIDER_TAB=usage`
  - 通过 `open -n /tmp/nolon-derived-api-key-form-2/Build/Products/Debug/nolon.app` 成功直接打开普通 `Codex` 的 `账号与用量` 页
  - 通过 `AXUIElement` 命中顶部 `刷新` 按钮后，app 未崩溃；界面仍存活并更新为最新同步时间
  - 本次 live 账号返回成功，因此观察到的是成功刷新态，不是失败态；失败分支仍主要由 `JsonRPCKitTests.closedStdinDuringSendReturnsTransportError` 和 `CodexAuthServiceTests.testBDD_GivenAccountRefreshHangs_WhenRefreshingCodexAccount_ThenRefreshingStateClearsAfterTimeout` 覆盖
- 为 `chatgptAccount` 落地默认 HTTP usage query 优先策略：
  - `CodexHTTPUsageQueryExecutor.resolveConfiguration(from:)` 在无显式 `nolon.usage_query` 且 `cardKind == .chatgptAccount` 时，会运行时合成默认模板；
  - 默认模板对齐上游 `references/codex`：`GET https://chatgpt.com/backend-api/wham/usage`，headers 带 `Authorization: Bearer {{accessToken}}` 和 `chatgpt-account-id: {{userID}}`；
  - 默认模板会映射 `plan_type`、`credits.balance`、`rate_limit.primary_window.*`、`rate_limit.secondary_window.*`。
- `CodexUsageDescriptor` 新策略：
  - 显式 `usage_query` 成功走 `.web`，失败仍直接返回 HTTP failure，不回退 CLI；
  - 默认 `chatgptAccount` HTTP 成功走 `.web`，失败自动回退现有 CLI / app-server 链路。
- 新增/更新回归测试并通过：
  - `CodexHTTPUsageQueryExecutorTests.resolveConfigurationSynthesizesDefaultChatGPTQuery`
  - `CodexUsageDescriptorTests.fallsBackToCLIWhenDefaultChatGPTHTTPFails`
  - `swift test --package-path libs/Providers --filter CodexHTTPUsageQueryExecutorTests`
  - `swift test --package-path libs/Providers --filter CodexUsageDescriptorTests`
  - 结果：两组测试均通过
- 继续修正 refresh skip 策略：
  - 之前 `CodexAuthFailureClassifier.shouldSkipRefresh(summary:)` 只要看到 `lastSyncFailedAt / lastSyncFailureMessage` 就一律跳过，导致默认 ChatGPT HTTP 虽已实现，真实 `free` 账号仍会被 `failed_before` 挡住；
  - 现改为：`chatgptAccount` 不再因旧失败元数据被跳过，API Key / Relay 仍保留原跳过策略。
- 新增/更新验证：
  - `NolonCodexCLIServiceTests.authUsageRefreshRetriesChatGPTAccountsAfterPreviousFailure`
  - `NolonCodexCLIServiceTests.authUsageRefreshStillSkipsConfiguredAccountsAfterPreviousFailure`
  - `swift test --package-path libs/Providers --filter NolonCodexCLIServiceTests`
  - 结果：通过
- 本地 live 验证：
  - `nujewicizo770@gmail.com`（team）刷新前 `usage_cache.fetchKind=cli`，刷新后写回为 `web / HTTP`
  - `is.linhey@outlook.com`（free，之前会被 `failed_before` 跳过）在放开 skip 后再次验证，刷新前 `cli / CLI`，刷新后也成功写回为 `web / HTTP`
