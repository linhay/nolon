# 2026-02-20

- 完成 `nolon skills` 两轮CLI体验闭环（含 agent-browser 对照 Clawhub 页面检验）。
- 修复：`skills remove` 支持 `--provider/--provider-id`，不再强依赖 `--provider-path`。
- 修复：`skills list` 无 `--provider` 的路径容错已生效（先前源码修复，重装CLI后二进制验证通过）。
- 新增测试：
  - `parse skills remove command with provider id`
  - `parse skills remove command rejects missing target selector`
  - `skills remove supports provider selector`
- 体验结论：当前主要剩余改进点是 `skills list` 的输出模式可选化与 `skills search` 到 `skills add` 的直连安装链路。
- 新增 UX 改动：`skills list` 文本输出由宽表格改为紧凑列表（`- provider/skill [state] path`），提升终端可读性与可复制性。
- 发现并确认：并行执行安装与命令验证会造成旧/新二进制输出混淆；二进制哈希已核对一致，不是安装脚本错误。
- 新增 `skills list --state installed|orphaned|broken` 过滤能力（文本/JSON同步）。
- `skills list` 输出现在可组合 `--provider` + `--state` 精确筛选；并在文本输出显示 `state_filter`。
- 复测时出现 shell hash 缓存导致的旧行为假象，执行 `hash -r` 后与安装二进制行为一致。
## 2026-02-20 01:20 技能CLI体验循环（agent-browser + codex-cli）
- 体验范围：`nolon skills list/search/add/remove/sync`，并用 `agent-browser` 对照 Clawhub 搜索页结果。
- 发现问题1（可复现）：`nolon skills sync --source ...` 强制要求 `--repositories-root`，不符合 NOLON_HOME 默认路径策略。
- 发现问题2（可复现）：`nolon skills repo` / `nolon skills migrate` 返回 `Unsupported parsed command type`，错误提示不可用。
- 修复策略（TDD）：先新增失败测试，再改解析逻辑。
  - `NolonSkillsSyncCommand.repositoriesRoot` 增加默认值 `NolonCoreCLIPathDefaults.repositoriesRootPath()`。
  - 手写解析 `skills sync` 同步改为可选 `--repositories-root`，缺省走默认路径。
  - 对 `NolonSkillsRepoGroupCommand` / `NolonSkillsMigrateGroupCommand` 提供明确错误：
    - `Missing command. Expected: skills repo <action> ...`
    - `Missing command. Expected: skills migrate <action> ...`
- 回归：新增的 4 个定向测试全部通过；CLI 实测报错文案已更新。
## 2026-02-20 01:27 技能CLI体验循环（search 输出引导增强）
- 体验工具：agent-browser 对照 Clawhub 检索、codex-cli 给 DX 改进建议。
- 发现：`nolon skills search` 仅返回列表，缺少下一步安装提示；空结果时也缺少可执行动作引导。
- 修复（TDD）：
  - 新增测试断言 search 文本输出包含下一步动作提示与空结果提示。
  - `formatSkillsSearchText` 增加：
    - 非空结果尾部提示：`下一步: nolon skills add <slug> [--provider <id>]`
    - 空结果提示：`nolon skills sync --source <owner/repo>` 或更换关键词。
- 复测：`nolon skills search xcode` 与 `nolon skills search <无结果关键词>` 均输出引导文案。
- codex-cli 本轮建议：优先做搜索-安装打通（一键安装或 search --install）。
## 2026-02-20 01:36 技能CLI体验循环（search --install + --yes）
- 体验请求继续按 agent-browser + codex-cli 闭环执行。
- 本轮新增能力：`nolon skills search ... --install`（唯一命中可直接走安装链路）。
  - 支持参数：`--provider/--provider-id`、`--install-method`、`--dry-run`。
  - 多命中时返回明确错误并附下一步建议（改进 query 或用 `skills add <slug>`）。
  - 空 query + `--install` 返回明确错误。
- 基于 codex-cli 建议补充安全阈：
  - 新增 `--yes`。
  - `--install` 且非 `--dry-run` 时必须显式 `--yes`，否则拒绝执行，避免误装。
- 关键验证：
  - `nolon skills search xcodebuildmcp --install --provider codex` => 要求 `--yes`。
  - `nolon skills search xcodebuildmcp --install --provider codex --yes --dry-run` => 输出安装计划。
## 2026-02-20 01:44 技能CLI体验循环（search --install 精确slug优先）
- 体验问题：`search --install` 在多命中时一律报歧义，导致像 `xcode` 这类“本身就是slug”的查询仍无法安装。
- codex-cli 建议采纳：多命中时优先按 slug 精确匹配（忽略大小写）；命中则直接安装。
- 实现：`resolveSingleSearchInstallMatch` 先做精确 slug 匹配，再回退到“单结果安装/多结果报错”。
- 测试：
  - 多结果 + 精确 slug 查询 => 安装成功。
  - 多结果 + 无精确 slug => 仍报歧义错误。
- CLI复测：
  - `nolon skills search xcode --install --provider codex --dry-run` 现可直接输出安装计划。
  - `nolon skills search xc --install --provider codex --dry-run` 仍保留歧义保护。
## 2026-02-20 01:47 技能CLI体验循环（search --install 错误文案可执行化）
- codex-cli 建议：优先优化 `--install` 缺确认时的错误提示，给出可复制下一步命令。
- 实施：将 `--install` 非 dry-run 且未传 `--yes` 的报错改为中文行动指令+示例。
- 新文案：检测到写入操作。请先用 --dry-run 预览，确认后再加 --yes 执行。示例：nolon skills search <query> --install --dry-run --provider <id>
- 测试：扩展 `parse skills search install requires yes or dry-run`，验证文案包含 `--yes`、`--dry-run` 与示例命令。
- 复测：CLI 输出与预期一致，下一步动作明确可复制。
## 2026-02-20 01:50 技能CLI体验循环（歧义报错追加可复制Next命令）
- 体验问题：`search --install` 歧义报错后仍需手改命令，无法直接复制。
- codex-cli 建议采纳：在歧义错误中追加可复制 `Next:` 命令，自动填入首个候选 slug 与 provider。
- 实现：`resolveSingleSearchInstallMatch` 的歧义分支追加 `Next: nolon skills add <first-slug> [--provider ...] --dry-run`。
- 测试：扩展 `runner search install keeps ambiguity error when no exact slug` 断言 `Next:` 命令。
- 复测：`nolon skills search xc --install --provider codex --dry-run` 错误中已出现可复制的 Next 命令。
## 2026-02-20 01:53 技能CLI体验循环（Next命令改为search精确重试）
- codex-cli 建议：歧义报错中的 Next 命令应沿用原命令结构，复制成功率更高。
- 调整：`Next` 从 `nolon skills add <slug> ...` 改为 `nolon skills search <slug> --install ... --dry-run`。
- 价值：用户无需切换命令心智，直接复制即可触发精确 slug 命中安装链路。
- 测试：更新歧义报错断言，验证新的 Next 命令文案。
- 复测：
  - `nolon skills search xc --install --provider codex --dry-run` 返回新 Next 文案。
  - `nolon skills search xcode --install --provider codex --dry-run` 可直接成功输出计划。
## 2026-02-20 体验迭代（skills CLI）
- 按 "$agent-browser + $codex-cli" 流程完成一轮体验 -> 报告 -> 修改 -> 复测。
- 变更1：`nolon skills list` 末尾新增修复建议，并在存在 orphaned/broken 时给出可执行命令。
- 变更2：修复建议由占位符改为基于扫描结果自动填充真实命令（示例：`nolon skills add agent-browser --provider claude`）。
- 变更3：`nolon skills --help` 改为走 `NolonCoreCLIHelpResolver`，输出任务导向帮助与 Examples，避免与运行时帮助输出不一致。
- 测试：新增/更新用例覆盖 list 修复建议与 skills 帮助示例；定向 `swift test --package-path libs/Providers --filter ...` 通过。
## 2026-02-20 体验迭代（skills search 可执行性）
- 按 "$agent-browser + $codex-cli" 再跑一轮体验闭环。
- 问题：`nolon skills search` 结果可读但执行链路弱，末尾提示单一。
- 改进：每个搜索结果项新增 `install: nolon skills add <slug>` 可复制命令；末尾提示改为双路径：`add` 直接安装 + `search --install --dry-run` 预览安装计划。
- 验证：`nolon skills search xcode` 已输出逐条 install 命令与新提示。
## 2026-02-20 体验迭代（skills search 序号与总数）
- 使用 agent-browser + codex-cli 体验后，落地改进：`nolon skills search` 输出新增 `匹配结果总数` 与 `稳定序号`。
- 输出结构更新为 `[index] slug + version/updated/install`，增强从搜索到安装的定位效率。
- 现状风险：summary 详情在 live 数据中偏长，且出现空行；下一轮优先做摘要单行截断与紧凑排版。
## 2026-02-20 体验迭代（skills search summary 紧凑化）
- 按 agent-browser + codex-cli 体验，确认问题：summary 前置空行、长摘要破坏扫描体验。
- 改进：`nolon skills search` summary 输出改为单行紧凑文本（压缩多空白/换行），并在超过 140 字符时截断为 `...`。
- 同步修复：去除 summary 前多余空行，保持每个条目紧凑四行结构（slug/version/updated/summary/install）。
- 验证：实机 `nolon skills search swift` 输出已无空行，长 summary 已截断。
## 2026-02-20 体验迭代（skills search 序号语义提示）
- 通过 agent-browser + codex-cli 复测后，新增 UX 提示：`提示: 序号仅用于阅读，安装请使用 slug。`
- 背景：避免用户把显示序号误当成可安装参数，提高“搜索->安装”命令成功率。
- 验证：`nolon skills search swiftui` 末尾已出现稳定提示。
## 2026-02-20 体验迭代（skills search --install 歧义提示）
- 体验点：`nolon skills search <query> --install --dry-run` 在多命中时，旧文案是逗号长串，不易扫读。
- 改进：`matches` 改为带序号单行列表格式（`matches(8): [1] ...; [2] ...`），保留 Next 可复制命令。
- 验证：`nolon skills search swiftui- --install --dry-run` 已输出编号候选列表。
## 2026-02-20 体验迭代（skills search 歧义计数动态化）
- 问题：歧义错误文案历史为固定 `matches(8)`，可能与实际候选数不一致。
- 改进：`matches(n)` 改为根据实际展示候选数量动态注入（最多展示前 8 个）。
- 说明：本轮实机歧义样例仍为 8 个候选，因此输出 `matches(8)` 为正确值；单测已覆盖 `matches(2)` 场景验证动态行为。
## 2026-02-20 体验迭代（skills search --install 未命中提示）
- 问题：`nolon skills search <query> --install --dry-run` 未命中时仅返回 `skill_not_found`，缺少下一步指引。
- 改进：保留 `skill_not_found` 错误码，但 message 增加可执行建议：
  - `nolon skills search <query>`
  - `nolon skills sync --source <owner/repo>`
- 验证：`nolon skills search nomatchkeyword123 --install --dry-run` 已输出含建议的错误 message。
## 2026-02-20 体验迭代（skills search --install 空 query 提示）
- 问题：`nolon skills search --install --dry-run`（未提供 query）只返回抽象错误，无修复指引。
- 改进：`invalid_arguments` message 增加两条可执行示例命令：
  - `nolon skills search <keyword> --install --dry-run`
  - `nolon skills search --query <text> --install --dry-run`
- 验证：实机命令已输出带 Try 列表的新错误文案。
## 2026-02-20 体验迭代（skills search 写入确认错误文案）
- 问题：`nolon skills search <q> --install` 在缺少 `--yes/--dry-run` 时仅给单示例，且可读性一般。
- 改进：错误文案改为双示例列表：
  - `nolon skills search <keyword> --install --dry-run`
  - `nolon skills search --query <text> --install --dry-run`
- 同步更新 ArgumentParser 和手写解析器错误文案，保证一致。
- 验证：实机命令已输出双示例列表。

## 2026-02-20 CLI 体验闭环（skills）
- 执行了 `agent-browser` 与 `codex` 体验评审，聚焦 `nolon skills search` 参数错误可执行性。
- 改进 1：当同时传 `positional <query>` 与 `--query` 时，错误文案新增冲突值和可复制示例命令。
- 改进 2：`--limit <= 0` 错误文案新增收到的值与修复建议（`Try --limit 10`）。
- 代码一致性：`NolonCoreCLIArgumentParser` 与 `NolonCoreCLICommand` 两条解析路径同步同一校验与文案，避免行为漂移。
- 验证：定向测试通过（duplicate query / non-positive limit），并重装 CLI 后实机验证输出符合预期。

## 2026-02-20 skills 体验循环（agent-browser + codex）
- 针对 `nolon skills add --dry-run` 做两轮体验修复。
- 修复 A：新增首行强提示 `[DRY-RUN] No changes applied`，避免误判为已安装。
- 修复 B：dry-run 结果文案从 `ok/failed` 改为 `planned/invalid`，避免与真实执行混淆。
- 测试：`runnerSkillsAddDryRunDoesNotInstall` 先红后绿；`runnerRendersSkillsAddConciseTextOutput` 回归通过。

## 2026-02-20 skills 体验循环追加
- 修复 `skills list --provider <id> --state <state>` 空结果文案误导：改为携带筛选上下文（provider/state）。
- 新增 `skills search --install --pick <index>` 消歧能力：多匹配时可直接选择索引进入安装/预览链路。
- 多匹配报错追加可执行提示：`Or disambiguate with --pick: ...`。
- 验证：解析测试、runner 测试与实机命令均通过。

## 2026-02-20 skills 体验循环追加（help 一致性）
- 发现并修复 `nolon skills --help` 与 `nolon skills search --help` 的占位词不一致问题。
- 顶层 `skills --help` 的 search 概要改为 `<keyword>`，与子命令帮助保持一致。
- 同步新增测试断言，防止后续回归遗漏 `--pick` 与占位词一致性。

## 2026-02-20 skills 体验循环追加（429 限流可执行提示）
- 体验发现远端 429 仅返回 execution_failed，缺少可执行建议。
- 修复：在 `NolonCoreCLIRunner` 统一错误出口增加 429 识别并映射为 `rate_limited`，返回中文重试与排查建议。
- 覆盖测试：`runnerMapsRemote429ToActionableRateLimitError`（红->绿）并回归 `runnerRendersSkillsSearchTextList`。
- 实机验证：`nolon skills search ...` 在限流场景输出 `rate_limited` 与重试文案。

## 2026-02-20 skills 体验循环追加（--install/--pick 帮助文案）
- 体验发现 `skills search --help` 中 `--install` 描述已过时，未体现 `--pick` 消歧能力。
- 修复：更新 `--install` / `--pick` 帮助文案，强调多匹配可用 `--pick <index>`。
- 测试：新增 `skillsSearchHelpExplainsInstallPickRelation`（红->绿），并回归 `skillsHelpIncludesExamples`。
- 实机复测：`nolon skills search --help` 已显示新的可执行提示。

## 2026-02-20 skills 体验循环追加（--query 占位统一）
- 体验发现顶层 `skills --help` 与子命令 `skills search --help` 的 `--query` 占位词不一致（<text> vs <query>）。
- 修复：顶层帮助改为 `search [<keyword> | --query <query>]`，与子命令保持一致。
- 测试：`skillsHelpIncludesExamples`（红->绿）通过。
- 实机与 codex 评审验收：通过。

## 2026-02-20 skills 体验循环追加（search 列表提示与 --pick 一致）
- 体验发现 search 列表提示“序号仅用于阅读”与新增 `--pick` 能力冲突。
- 修复：提示文案改为“序号可用于 `--pick` 安装；也可使用 slug 安装。”
- 测试：`runnerRendersSkillsSearchTextList` 断言更新并通过。
- 实机与 codex 验收：通过。

## 2026-02-20 skills 体验循环追加（下一步文案补齐 --pick 快速路径）
- 体验发现 search 列表“下一步”文案未突出 `--pick` 快速安装路径。
- 修复：下一步文案改为优先提示 `nolon skills search <keyword> --install --pick <index> ... --dry-run`。
- 测试：`runnerRendersSkillsSearchTextList` 断言更新并通过。
- 实机与 codex 验收：通过。

## 2026-02-20 skills 体验循环追加（list 可发现性 + 权限错误映射）
- 体验来源：按用户要求使用 `agent-browser`（Clawhub 页面）+ `codex`（开发者视角）复核 `nolon skills`。
- 改进 1（list 可发现性）：新增 `nolon skills list --verbose` 能力并将默认输出改为不带路径；默认输出追加提示：`nolon skills list --verbose` 可查看安装路径。
- 改进 2（search 错误分层）：`NolonCoreCLIRunner` 新增权限错误归一化，将 `Operation not permitted` / `Permission denied` 映射为 `permission_denied`，并给出可执行建议（检查权限、必要时设置 `NOLON_HOME` 到可写目录）。
- 改进 3（help 任务导向）：`nolon skills --help` 改为“场景: 发现技能/安装技能/修复异常”分组，降低首次使用门槛。
- 测试：新增/更新定向测试（parse verbose、permission_denied 映射、list verbose 路径、help 场景文案）全部通过。
- 实机复验：`./scripts/install-nolon-cli.sh --force` 后，`skills --help`、`skills list`、`skills list --verbose`、`skills search xcode` 输出符合预期。

## 2026-02-20 skills 体验循环追加（修复建议多 provider 语义）
- 体验输入：`agent-browser` + `codex` 复核发现 `nolon skills list --state broken` 在多 provider 异常时仅显示一条修复命令，易被误解为只需修复一个 provider。
- 决策：不改变既有“省略 --provider 时 add/search-install 默认分发到全部已安装 provider”的行为，仅优化修复建议文案语义，避免误导。
- 实现：`skills list` 修复段落改为 `修复建议（示例命令）`，且在未指定 provider 时追加提示“多个 provider 需分别执行”。
- 测试：更新 `NolonCoreCLIKitTests` 中 skills list 文本断言并通过。
- 实机：`nolon skills list --state broken` 输出已新增“示例命令”和“按 provider 分别执行”提示。

## 2026-02-20 skills 体验循环追加（多 provider 安装安全提示）
- 体验来源：`agent-browser` + `codex` 复核 `nolon skills add/search --install --dry-run`。
- 发现：未指定 `--provider` 时默认会分发到多个 provider，虽是既定行为，但误操作风险较高。
- 决策：不改变默认分发行为，仅新增安全提示，降低误操作概率。
- 实现：在 `skills add` 文本输出新增 `safety` 段；当目标 provider 数 > 1 时，显示 provider 数量、列表及 `--provider <id>` 建议（dry-run 与执行态分别文案）。
- 测试：新增 `skillsAddSafetyWarningForMultiProviderTargets` 并回归 `runnerSkillsAddDryRunDoesNotInstall`、`runnerRendersSkillsAddConciseTextOutput`。
- 实机：`nolon skills add xcode --dry-run` 与 `nolon skills search xcode --install --dry-run` 均出现 `safety` 告警段。

## 2026-02-20 skills 体验循环追加（批量安装范围预告）
- 体验来源：`agent-browser` + `codex` 复核后，建议在 safety 文案前增加“结果预告”，提前明确是批量安装。
- 实现：`skills add` 文本输出新增 `scope: multi-provider (N: ...)` 行（仅多 provider 时显示），位置在 `result` 之前。
- 原则：不改变默认行为，仅增强文案可见性，降低误操作风险。
- 测试：在 `skillsAddSafetyWarningForMultiProviderTargets` 中增加 `makeInstallScopeLabel` 断言，并回归 `runnerSkillsAddDryRunDoesNotInstall` / `runnerRendersSkillsAddConciseTextOutput`。
- 实机：`nolon skills add xcode --dry-run` 与 `nolon skills search xcode --install --dry-run` 已显示 `scope: multi-provider (...)`。

## 2026-02-20 skills 体验循环追加（help 前置批量风险提示）
- 体验来源：`agent-browser` + `codex` 复核，发现虽然 add/search 输出有 scope+safety，但 `skills --help` 未提前显式提示省略 --provider 的批量写入风险。
- 实现：在 `nolon skills --help` 的 `add` 说明下新增前置风险文案：省略 `--provider` 可能触发多 provider 批量写入/覆盖，建议先 `--dry-run`。
- 测试：更新 `NolonCodexCLIEntrypointTests/skillsHelpIncludesExamples` 断言并通过。
- 实机：`nolon skills --help` 已显示新增风险提示文案。

## 2026-02-20 skills 体验循环追加（add/search 子命令 help 风险对齐）
- 体验来源：`agent-browser` + `codex` 复核，发现 top-level `skills --help` 已有批量风险提示，但 `skills add --help` / `skills search --help` 缺同级提示。
- 实现：为 `NolonSkillsAddCommand` 与 `NolonSkillsSearchCommand` 的 `--provider` / `--provider-id` 选项补充说明：省略将分发到全部检测到的 provider。
- 测试：新增并通过 `skills add --help warns about provider omission scope`，并更新 `skills search --help explains install and pick relation` 断言。
- 实机：`nolon skills add --help` 与 `nolon skills search --help` 均显示 provider 省略范围提示。

## 2026-02-20 skills 体验循环追加（search 结果页单目标优先提示）
- 体验来源：`agent-browser` + `codex` 复核后，`skills search` 结果页“下一步”仍可能被理解为直接批量安装入口。
- 实现：在 `formatSkillsSearchText` 的“下一步”后新增 `安全提示` 行，明确“默认优先单目标安装，建议显式传 `--provider`，批量前先 `--dry-run`”。
- 测试：更新 `runnerRendersSkillsSearchTextList` 断言并通过；相关帮助测试回归通过。
- 实机：`nolon skills search xcode` 已显示新增安全提示行。

## 2026-02-20 skills 体验循环追加（search 每项安装命令单目标化）
- 体验来源：`agent-browser` + `codex` 复核后，`skills search` 每项 `install:` 示例原先不带 provider，容易被直接复制为批量语义。
- 实现：将每项安装示例改为显式单目标命令：`nolon skills add <slug> --provider <id>`。
- 测试：更新 `runnerRendersSkillsSearchTextList` 与 `runnerCompactsAndTruncatesLongSummaryInSkillsSearchTextList` 断言并通过。
- 实机：`nolon skills search xcode` 每条结果均显示带 `--provider <id>` 的 install 示例。

## 2026-02-20 skills 体验循环追加（add/search help 安全示例）
- 体验来源：`agent-browser` + `codex` 复核，发现 `skills add/search --help` 缺可直接复制的安全示例命令。
- 实现：在 `NolonSkillsAddCommand` 与 `NolonSkillsSearchCommand` 的 `CommandConfiguration` 增加 `discussion` 示例，均采用 `--provider ... --dry-run` 安全路径。
- 测试：`skillsSearchHelpExplainsInstallPickRelation` 与 `skillsAddHelpWarnsAboutProviderOmissionScope` 增加示例断言并通过。
- 实机：`nolon skills add --help` / `nolon skills search --help` 均显示 `Examples:` 安全命令。

## 2026-02-20 skills 体验循环追加（remove help 防误删）
- 体验来源：`agent-browser` + `codex` 复核后，`nolon skills remove --help` 缺少高可见防误删提示与可复制示例。
- 实现：在 `NolonSkillsRemoveCommand` 增加 discussion：先 `skills list` 确认 skill-id，再 remove；并补安全示例命令。
- 同步：为 `--skill-id`、`--provider-path`、`--provider`、`--provider-id` 增加更明确的 help 文案。
- 测试：新增 `skills remove --help shows safety note and example`，并回归 `skills add --help warns about provider omission scope`。
- 实机：`nolon skills remove --help` 已显示注意事项与示例。

## 2026-02-20 skills 体验循环追加（list 修复建议按 provider 展开）
- 体验来源：`agent-browser` + `codex` 复核发现 `skills list --state broken` 在多 provider 场景仅给一条示例命令，可执行性不足。
- 实现：`formatSkillsListText` 的修复区改为按异常项逐条输出命令（orphaned/broken 分组），每个 provider/skill 都给可复制命令。
- 结果：多 provider broken 场景下会输出多条 `remove -> add` 组合命令，不再只给单条 sample。
- 测试：更新 `runnerRendersSkillsListTextAsCompactList` 断言覆盖新格式并通过。
- 实机：`nolon skills list --state broken` 已显示 claude/opencode 两条独立修复命令。
- skills 体验循环：改进 CLI 可执行性文案
  - `skills list` 增加 `providers_matched`，解决 `providers_scanned` 与可见列表不一致的理解偏差。
  - `skills search` 每条结果新增可直接执行安装示例：`install: nolon skills add <slug> --dry-run` 与 `install_single: ... --provider codex --dry-run`。
  - 测试：更新 `NolonCoreCLIKitTests` 相关断言；当前套件剩余 1 个历史失败（`parse skills sync command defaults repositories root`，受环境态影响）。
- skills 体验循环（agent-browser + codex-cli）
  - 通过 agent-browser 打开 Clawhub 搜索页，验证网页链路可达；通过 nolon CLI 复测 `skills list/search/add --dry-run`。
  - 发现并修复：`skills search` 文案占位符混用 `<id>` 与 `<provider-id>`，现统一为 `<provider-id>`（含 safety 文案与下一步提示）。
  - 注意：并行执行 `swift test` 与 `install-nolon-cli.sh` 会造成“新代码/旧二进制”观测偏差，后续改为串行安装复测。
- skills 体验循环（agent-browser + codex-cli，日期可解释性）
  - codex-cli 体验反馈：`skills search` 的 `updated` 出现未来日期（相对 2026-02-19）易引起误解；已在 CLI 文本渲染增加未来标记 `future +Nd`。
  - 实现：`formatUpdatedDate` 对 `updatedAt > today` 增加后缀（按天）。
  - 测试：新增 `runner annotates future updated date in skills search text list`，使用固定未来日期样例覆盖。
  - 备注：本轮在线 `nolon skills search xcode` 受 429 限流，实机未能直接展示新标记，但单测已覆盖渲染行为。
  - 额外发现：本机 codex-cli 令牌处于 `refresh_token_reused` 状态，`codex exec` 仍可返回结果但有鉴权告警。
- skills 体验循环（agent-browser + codex-cli，限流可恢复性）
  - 复现：`nolon skills search xcode` 在部分请求下返回 `rate_limited(429)`，旧文案仅提示等待重试，缺少立即可执行的替代路径。
  - 改进：429 错误文案增加本地回退指令：`nolon skills sync --source <owner/repo>` 与 `nolon skills add <slug> --dry-run`。
  - 测试：更新 `runner maps remote 429 to actionable rate limit error` 断言，要求包含两条回退命令（注意 JSON 斜杠转义）。
  - 说明：本轮在线搜索最终一次成功返回 7 条结果，未触发 429；但模拟测试已覆盖 429 文案路径。
- skills 体验循环（agent-browser + codex-cli，结果来源可解释性）
  - 现象：网页搜索（clawhub.ai 页面）与 CLI 搜索结果存在展示差异，用户难判断数据来源是否一致。
  - 改进：`nolon skills search` 文本输出新增 `source: remote-api (<base_url>)`，明确结果来自 API 列表而非网页当前可见卡片。
  - 测试：更新 `runner renders skills search text list` 断言，要求包含来源标记。
  - 验证：重装 CLI 后 `nolon skills search xcode` 首屏已显示来源行。
- skills 体验循环（agent-browser + codex-cli，网页/API 差异提示）
  - 现象：`nolon skills search` 已展示 API 来源，但用户仍可能困惑网页可见列表与 CLI 结果不一致。
  - 改进：在 `skills search` 输出增加解释行：`提示: CLI 基于远端 API 索引，网页展示可能受筛选/排序/索引延迟影响。`
  - 测试：`runner renders skills search text list` 新增断言覆盖该提示行。
  - 验证：重装后 `nolon skills search xcode` 首屏已出现来源+解释提示。
- skills 体验循环（agent-browser + codex-cli，默认行为文案一致性）
  - 问题：`skills --help` 描述“省略 --provider 默认分发到全部 provider”，但 `skills search` 安全提示写成“默认优先单目标安装”，存在矛盾。
  - 修复：统一为真实默认行为：省略 `--provider` 会分发到全部已安装 providers，并提示显式传参避免批量写入。
  - 测试：更新 `runner renders skills search text list` 断言。
  - 验证：重建并重装后 `nolon skills search xcode` 输出安全提示已更新为一致文案。
- skills 体验循环（agent-browser + codex-cli，updated 字段语义澄清）
  - 问题：`skills search` 中 `updated` 容易被理解为本地缓存同步时间，实际是远端目录时间。
  - 修复：新增固定字段说明：`字段说明: updated 为远端目录时间（非本地缓存同步时间）。`
  - 测试：`runner renders skills search text list` 新增断言覆盖字段说明。
  - 验证：重建+重装后 `nolon skills search xcode` 首屏已显示该说明。
- skills 体验循环（agent-browser + codex-cli，快速安装可发现性）
  - 反馈：`skills search` 信息完整但首屏缺少“立刻安装单个结果”的直达示例，用户仍要向下扫描。
  - 改进：在头部新增 `快速安装(单目标示例): nolon skills add <slug> --provider codex --dry-run`。
  - 测试：`runner renders skills search text list` 新增断言。
  - 验证：重建/重装后首屏已展示快速安装示例。
- skills 体验循环（agent-browser + codex-cli，安装示例安全一致性）
  - 问题：结果项里 `install` 之前是无 `--provider` 命令，与“默认会批量分发”的安全提示冲突。
  - 修复：结果项改为
    - `install`: 单目标推荐（带 `--provider codex --dry-run`）
    - `install_all`: 明确批量分发（无 `--provider`）
  - 测试：更新 `runner renders skills search text list` 与 `runner compacts and truncates long summary in skills search text list` 断言。
  - 验证：重装后 `nolon skills search xcode` 已显示 `install` + `install_all` 新格式。
- skills 体验循环（agent-browser + codex-cli，pick 占位符一致性）
  - 问题：`skills --help` 中 `--pick <n>` 与搜索输出提示 `--pick <index>` 不一致。
  - 修复：统一为 `--pick <index>`（帮助文案与搜索提示一致）。
  - 变更文件：`NolonCoreCLIHelp.swift`、`NolonCodexCLIEntrypointTests.swift`。
  - 验证：`nolon skills --help` 已显示 `--pick <index>`。
- [CLI][skills] 体验循环（agent-browser + codex-cli）后完成两轮改进：
  1) `skills list` 默认模式改为异常优先（隐藏 installed）、新增 `异常项(orphaned/broken)` 与 `health(installed/total)` 摘要、修复建议改为单条可执行命令。
  2) `skills search` 权限失败文案补充可执行下一步（`NOLON_HOME=/tmp/nolon-home ...` + `nolon skills list`）。
- [测试] 新增/更新用例并按函数名定向通过：
  - `runnerMapsPermissionDeniedToActionableError`
  - `runnerRendersSkillsListTextAsCompactList`
  - `runnerHidesInstalledItemsInDefaultListMode`
  - `runnerRendersSkillsListWithStateFilter`
- [实测] `./scripts/install-nolon-cli.sh --force` 后，`nolon skills list` 已输出 health+异常聚焦；`nolon skills search xcode` 保持可安装提示链路。
- [CLI][skills] 新一轮体验回归后优化 `skills list` 输出可执行性：
  - 修复建议由扁平列表改为分组+序号：`orphaned(n)` / `broken(n)`。
  - 每组命令可直接复制，降低多异常场景下的认知负担。
- [测试] 先改断言再实现，定向通过：
  - `runnerRendersSkillsListTextAsCompactList`
  - `runnerRendersSkillsListWithStateFilter`
- [实测] 重新安装 CLI 后，`nolon skills list` 已显示分组修复建议；`skills search xcode` 输出保持稳定。
- [CLI][skills] 新一轮体验后完成失败场景可读性增强（最小改动）：
  - `NolonCoreCLIRunner` 在 text 模式下对 `permission_denied` / `rate_limited` 输出改为人类可读文本：
    `Error [code]` + message；`--json` 模式保持原 JSON envelope。
  - 修复建议继续保留命令级可执行文案。
- [测试] 新增/更新并通过：
  - `runnerMapsPermissionDeniedToActionableError`
  - `runnerMapsRemote429ToActionableRateLimitError`
  - `runnerKeepsJSONEnvelopeForPermissionDeniedInJSONMode`
- [边界] `invalid_arguments` 等非上述 code 仍保持 JSON 输出（当前行为不变）。
- [CLI][skills] 新一轮体验后提升修复效率：`skills list` 在 `修复建议` 末尾新增 `一键修复` 命令（将 orphaned/broken 建议自动按顺序串联为单条可执行命令）。
- [测试] 先改测试再实现，定向通过：
  - `runnerRendersSkillsListTextAsCompactList`
  - `runnerRendersSkillsListWithStateFilter`
- [实测] 安装 CLI 后 `nolon skills list` 已输出 `一键修复: <long command>`，可直接复制执行。
- [CLI][skills] 又一轮体验后优化一键执行体验：
  - 在 `skills list` 的修复段新增分场景一键命令：
    - `一键清理(orphaned)`
    - `一键修复(broken)`
    - `一键修复(all)`（保留）
  - 让用户可以复制更短命令，避免总命令过长。
- [测试] 更新断言并通过：
  - `runnerRendersSkillsListTextAsCompactList`
  - `runnerRendersSkillsListWithStateFilter`
- [CLI][skills] 新一轮体验后，`skills list` 增加“异常提供方”汇总行（按 provider 去重+排序），用于快速定位受影响 provider。
- [测试] 定向通过：
  - `runnerRendersSkillsListTextAsCompactList`
  - `runnerRendersSkillsListWithStateFilter`
- [实测] `nolon skills list` 现已显示：`异常提供方: claude, gemini, opencode`。
- [CLI][skills] 继续小步优化：`skills list` 的异常提供方行改为带数量格式：`异常提供方(N): ...`，提高扫读效率。
- [测试] 定向通过：
  - `runnerRendersSkillsListTextAsCompactList`
  - `runnerRendersSkillsListWithStateFilter`
- [实测] CLI 输出示例：`异常提供方(3): claude, gemini, opencode`。
- [CLI][skills] 新一轮体验后补充“下一步筛查”指引：当存在异常 provider 时，`skills list` 增加提示行：
  `下一步：按 provider 查看安装路径，运行 nolon skills list --provider <provider>`。
- [测试] 定向通过：
  - `runnerRendersSkillsListTextAsCompactList`
  - `runnerRendersSkillsListWithStateFilter`
- [实测] 输出示例已出现 `--provider claude` 的引导示例。
- [CLI][skills] 新一轮体验后补充 broken 快速筛查提示：
  - 当存在 broken 项时，在 `skills list` 输出增加：
    `快速筛坏链：nolon skills list --state broken`（并给出 provider 版本示例）。
- [测试] 定向通过：
  - `runnerHidesInstalledItemsInDefaultListMode`
- [实测] `nolon skills list` 已出现 broken 快速筛查提示，便于先缩小问题范围再执行修复。
- [CLI][skills] 新一轮体验后补齐 orphaned 快速筛查：
  - `skills list` 在存在 orphaned 项时新增：
    `快速筛孤链：nolon skills list --state orphaned`（含 provider 示例）。
  - 与 broken 快速筛查形成对称提示。
- [测试] 定向通过：
  - `runnerRendersSkillsListTextAsCompactList`
  - `runnerHidesInstalledItemsInDefaultListMode`
- [实测] `nolon skills list` 已同时出现 `快速筛坏链` 与 `快速筛孤链`。

## 18:06 CLI Skills 体验闭环（agent-browser + codex-cli）
- 背景：多轮体验反馈显示 `nolon skills list` 默认输出修复脚本过长、风险信息和诊断信息混排，首屏决策成本高。
- 决策：新增 `skills list --show-fixes` 开关；默认仅输出异常摘要与下一步筛查命令，不展开修复脚本。
- 实现：
  - `NolonSkillsListCommand` 增加 `showFixes` flag。
  - `NolonCoreCLICommand.skillsList` 增加 `showFixes` 参数。
  - `NolonCoreCLIRunner.formatSkillsListText` 增加 `showFixes` 分支：默认提示 `nolon skills list --show-fixes`。
  - `NolonCoreCLIHelp` 更新 `skills list` 用法。
- 测试：`swift test --package-path libs/Providers --filter NolonCoreCLIKit` 全通过（113 tests）。
- 体验结论：默认输出更聚焦排障入口；需要修复命令时再显式展开。

## 18:10 CLI Skills 体验闭环（重复迭代）
- 使用 `agent-browser` + `codex-cli` 再次体验后，落地两项输出层优化：
  1. `nolon skills list` 将重复提示合并为 `修复建议（可复制）` 区块（最多3条）。
  2. 在 `health/state` 增加百分比展示（如 `22/27 (81.5%)`）。
- 代码：`NolonCoreCLIRunner.formatSkillsListText`。
- 测试：更新 `NolonCoreCLIKitTests` 断言并通过定向测试；`NolonCoreCLIKit` 全量跑时出现 1 个与环境变量污染相关的既有不稳定用例（默认 repositories root 断言），非本次改动路径。
- 实机复测：`~/.nolon/bin/nolon skills list` 已展示百分比与新建议区块；`--show-fixes` 仍可展开详细修复命令。

## 18:14 CLI Skills 体验闭环（search 文案迭代）
- 流程：agent-browser + codex-cli 体验 -> 输出问题 -> 最小改动 -> 测试/复测。
- 改动1：`skills search` 输出将 `install_all` 重命名为 `install_all_providers`，并追加风险标识 `[可能批量写入]`。
- 改动2：统一索引差异提示文案为“结果来自远端 API 索引；若与网页不一致，通常由筛选、排序或索引延迟导致。”
- 测试：`runnerRendersSkillsSearchTextList`、`runnerCompactsAndTruncatesLongSummaryInSkillsSearchTextList` 通过。
- 实机：`~/.nolon/bin/nolon skills search xcode` 已显示新字段名与新文案。

## 18:16 CLI Skills 体验闭环（search 风险提示前置）
- 来源：agent-browser + codex-cli 体验指出 `skills search` 的批量写入风险提示出现过晚。
- 改动：将风险提示前置到“快速安装示例”之后，文案统一为：
  - 注意：未指定 `--provider` 将分发到全部 providers（可能批量写入），建议先 `--dry-run`。
- 同时移除末尾重复的安全提示段，减少冗余。
- 测试：`runnerRendersSkillsSearchTextList` 通过。
- 实机：`~/.nolon/bin/nolon skills search xcode` 已显示前置风险提示。

## 18:18 CLI Skills 体验闭环（search 下一步示例）
- 来源：codex-cli 体验建议将“下一步”改为可直接执行的当前查询示例，减少参数拼接成本。
- 改动：`skills search` 输出新增：
  - 下一步(当前查询示例): `nolon skills search xcode --install --pick 1 --provider codex --dry-run`
- 保留原通用下一步说明，兼容其它查询场景。
- 测试：`runnerRendersSkillsSearchTextList` 通过。
- 实机：`~/.nolon/bin/nolon skills search xcode` 已展示新示例命令。

## 18:20 CLI Skills 体验闭环（search 去重下一步）
- 来源：codex-cli 体验建议 `skills search` 存在两条“下一步”提示，信息重复。
- 改动：移除通用 `下一步:` 行，仅保留 `下一步(当前查询示例)`。
- 测试：`runnerRendersSkillsSearchTextList` 通过。
- 实机：`~/.nolon/bin/nolon skills search xcode` 已只保留一条下一步命令。

## 18:25 CLI Skills 体验闭环（add dry-run 状态压缩）
- 体验建议：`skills add --dry-run` 的 `dry_run: true` 与 warning 重复表达同一状态。
- 改动：
  - 文本输出改为 `status: dry-run (no cache writes, no installation)`。
  - 移除自动追加的 dry-run warning，避免重复。
  - `result` 文案中文化：`1 计划, 0 无效` / `1 已安装, 0 失败`。
- 测试通过：
  - `runnerExecutesSkillsSearchInstallDryRunForUniqueMatch`
  - `runnerRendersSkillsAddConciseTextOutput`
  - `runnerSkillsAddDryRunDoesNotInstall`
- 实机：`~/.nolon/bin/nolon skills add xcode --provider codex --dry-run` 输出已生效。

## 18:27 CLI Skills 体验闭环（add result 键值化）
- 来源：codex-cli 复评建议 `skills add --dry-run` 的 result 行改为键值格式，提升扫读和机器解析性。
- 改动：
  - dry-run: `result: planned=1, invalid=0`
  - apply: `result: installed=1, failed=0`
- 测试通过：
  - `runnerRendersSkillsAddConciseTextOutput`
  - `runnerSkillsAddDryRunDoesNotInstall`
- 实机：`~/.nolon/bin/nolon skills add xcode --provider codex --dry-run` 已生效。
