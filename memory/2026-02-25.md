## 02:10 show-fixes [详情] 过滤条件前置（skills + mcp）

- 背景：`--show-fixes` 输出在 [详情] 区块里先看到统计再看到过滤条件，阅读路径不直观。
- 决策：在 `formatSkillsListText` 与 `formatResourceListText` 中新增 `appendFiltersIfNeeded()`；当 `showFixes=true` 时，先输出 `provider_filter/state_filter`，再输出 `providers_scanned/providers_matched/...`。
- 实现：通过局部 `filtersAppended` 防止过滤条件重复追加，保持非 `show-fixes` 场景行为不变。
- 验证：`swift test --package-path libs/Providers --filter NolonCoreCLIKitTests` 通过（162 tests）。
- 测试补强：新增顺序断言，确保 `provider_filter: codex` 出现在 `providers_scanned: 1` 之前。
- 风险：仅文本渲染顺序调整，JSON 契约未变；低风险。

## 02:13 codex-cli 体验回路：缩短“可选复检”命令

- 体验来源：用 `codex exec` 跑 `skills/mcp/workflow list --show-fixes/--verbose` 可读性评审，命中问题“可选复检命令过长（含变量赋值与分号）”。
- 改动：将“可选复检”统一改为两行短命令：
  - `直接运行: nolon ...`
  - `源码模式: swift run --package-path libs/Providers nolon ...`
- 实现点：`NolonCoreCLIRunner` 新增 `appendOptionalRecheck(...)` 与 `sourceModeCommand(...)`，skills/mcp/workflow 空结果的 show-fixes 分支统一复用。
- 测试：更新 `runner skills show-fixes with installed filter prints explicit no-op hint` 断言，并跑 `swift test --package-path libs/Providers --filter NolonCoreCLIKitTests`（162 tests 通过）。
- 风险：仅文本提示样式调整，不改执行语义；低风险。

## 02:14 show-fixes 健康场景去重（mcp/workflow）

- 目标（BDD）：当 `list --verbose --show-fixes` 且无异常时，输出应保持两层：机器摘要 + 一行自然语言健康摘要，避免“结论：全部健康...”与其它字段重复。
- 红灯测试：新增 `runner mcp verbose show-fixes uses unified healthy summary line`，先失败（缺少 `健康：...`，且存在 `结论：全部健康...`）。
- 实现：在 `formatResourceListText` 中移除 verbose 专用 `结论：全部健康...`，统一无异常场景输出 `健康：installed/total...` 一行。
- 绿灯验证：
  - `swift test --package-path libs/Providers --filter runnerMcpVerboseShowFixesUsesUnifiedHealthySummaryLine` 通过。
  - `swift test --package-path libs/Providers --filter NolonCoreCLIKitTests` 通过（163 tests）。
- 风险：仅文本输出去重与文案统一，不涉及状态计算与 JSON 协议；低风险。

## 02:18 codex-cli 体验回路：统一 verbose 提示与异常条目格式

- 体验报告（codex-cli）剩余高优先级：
  1) `skills` 的 `--verbose` 提示文案与 `workflow/mcp` 不一致（仅“查看安装路径”）；
  2) `workflow/mcp --verbose` 异常条目使用 `[状态] provider/id`，与 `skills` 的 `provider/id [状态]` 不一致。
- 红灯测试：
  - 在 `runnerRendersSkillsListTextAsCompactList` 增加断言：`提示: 使用 \`nolon skills list --verbose\` 查看安装路径与来源。`（先失败）。
  - 新增 `runner workflow verbose abnormal items keep trailing state label format`，要求异常条目不再是前置状态标签（先失败）。
- 实现：
  - `formatSkillsListText`：提示统一为“查看安装路径与来源”。
  - `formatResourceListText`（workflow/mcp）：verbose 异常条目改为 `provider/id [状态]`。
- 验证：
  - 两条针对性测试转绿。
  - `swift test --package-path libs/Providers --filter NolonCoreCLIKitTests` 通过（164 tests）。
- 风险：仅文本渲染一致性改动，不影响资源状态判定与执行逻辑；低风险。

## 02:20 list 指标同构化（skills 默认状态行对齐 workflow/mcp）

- 目标：三类 list 文本在默认（非 `--show-fixes`）场景的指标行更同构，降低横向扫读成本。
- 改动：
  - `skills` 去掉 `健康度(已安装/总数): ...` 行。
  - `skills` 状态行改为与 `workflow/mcp` 同模板：
    `状态(已安装/失效链接/损坏): installed/orphaned/broken (..%/..%/..%)`。
- 测试：更新 `runnerRendersSkillsListTextAsCompactList` 断言；定向+全量 `NolonCoreCLIKitTests` 通过（164 tests）。
- 影响：仅文本输出一致性调整，不影响状态计算或修复命令逻辑。

## 02:24 list total 键名同构化（skills/workflow/mcp）

- 体验来源：codex-cli 评审指出 total 汇总键名不一致（`skills_total` vs `workflows_total`/`mcps_total`）。
- 改动：`formatResourceListText` 改为显式键名映射：
  - `workflow_total`
  - `mcp_total`
  使三类命令统一为“命令名 + _total”。
- 测试：
  - `runnerRendersWorkflowListTextWithStateFilterLabel` 新增 `workflow_total` 断言；
  - `runnerRendersMcpListTextWithProviderFilterLabel` 新增 `mcp_total` 断言；
  - 修正一次误放置断言后，全量 `NolonCoreCLIKitTests` 通过（164 tests）。
- 风险：文本键名变更可能影响依赖旧键名的外部 grep/脚本；当前仓内测试通过。

## 02:29 show-fixes 异常状态行同构（workflow/mcp 对齐 skills）

- 体验来源：codex-cli 指出 `--show-fixes` 且有异常时，`skills` 与 `workflow/mcp` 状态行句式不一致。
- 改动：`formatResourceListText` 在 `showFixes && issueCount > 0` 分支改为统一模板：
  `状态(已安装/失效链接/损坏): installed/orphaned/broken (pct/pct/pct)`。
- 测试：在 `runnerWorkflowShowFixesHidesCompactSummaryAndKeepsDetailedCommands` 的异常分支增加同构状态行断言，并确保不再出现旧句式前缀 `状态：已安装`。
- 验证：`swift test --package-path libs/Providers --filter NolonCoreCLIKitTests` 通过（164 tests）。
- 风险：仅文本文案模板对齐，不影响修复命令生成与状态统计。

## 02:31 skills verbose 混合状态分组对齐（异常/已安装）

- 问题：`skills list --verbose` 在“异常+已安装混合”时未分段；而 `workflow/mcp` 已按 `[异常]` / `[已安装]` 分组。
- TDD：新增 `runner skills verbose mixed states are grouped into abnormal and installed sections`，先失败，确认当前输出缺少分组标题。
- 实现：
  - `formatSkillsListText` 按 `problematicItems` 与 `installedItems` 分段渲染；
  - 复用既有条目行格式，保持 `[state]` 与 `path/origin` 语义不变。
- 验证：`swift test --package-path libs/Providers --filter NolonCoreCLIKitTests` 通过（165 tests）。
- 风险：仅文本布局分组调整；命令与状态计算逻辑未变。

## 02:42 show-fixes 过滤无修复项提示 + 术语统一（继续 codex-cli 体验闭环）

- 体验来源：`codex exec` UX 报告指出两点：
  1) `--show-fixes` 在过滤条件下缺少“可恢复”提示；
  2) 文案出现“坏链/失效链接”术语混用。
- 改动：
  - `skills/workflow/mcp` 在 `--show-fixes` 且当前筛选结果无可修复项时，统一输出：
    `当前筛选条件下无可修复项；请移除筛选后重试 --show-fixes。`
  - `skills` 快捷动作文案由 `查看坏链详情` 改为 `查看损坏详情`。
- 测试：
  - 新增/更新 installed + show-fixes 场景断言（含新提示、排除旧提示）。
  - 更新“默认列表隐藏已安装项”断言为“查看损坏详情”，并断言不再出现“查看坏链详情”。
  - 全量：`swift test --package-path libs/Providers --filter NolonCoreCLIKitTests` 通过（165 tests）。
- 影响：仅 CLI 文本输出与可读性增强，不影响状态扫描和修复命令生成。

## 02:45 workflow/mcp list --help 选项说明补齐（继续 codex-cli 体验闭环）

- 体验来源：codex-cli 报告指出 `workflow/mcp list --help` 缺少关键选项解释，与 `skills list --help` 不对齐。
- 改动：
  - `NolonWorkflowListCommand` 与 `NolonMcpListCommand` 补齐说明：
    - `--provider` / `--provider-id`（多 provider 风险提示）
    - `--verbose`（显示完整安装路径）
    - `--show-fixes`（显示修复命令）
- 测试（TDD）：
  - 新增 `workflowListHelpIncludesOptionDescriptions`
  - 新增 `mcpListHelpIncludesOptionDescriptions`
  - 首次因 help 文本自动换行导致整句匹配失败（红灯），调整为分片断言后通过（绿灯）。
  - 全量：`swift test --package-path libs/Providers --filter NolonCoreCLIKitTests` 通过（167 tests）。
- 影响：仅 CLI `--help` 文案增强，不影响命令解析或执行路径。

## 02:48 skills list 命令意图文案修正（继续 codex-cli 体验闭环）

- 体验来源：codex-cli 报告指出 `skills list` 默认输出偏“异常巡检”，与旧 overview 文案“List installed skills by provider.”不一致。
- TDD：新增 `skillsListHelpSummaryReflectsDefaultAbnormalFocus`，先红灯确认旧文案仍存在。
- 改动：
  - `NolonSkillsListCommand` 的 `abstract` 改为：
    `Inspect skill install states by provider (defaults to orphaned/broken view).`
- 测试：
  - 新增测试通过；
  - 全量 `swift test --package-path libs/Providers --filter NolonCoreCLIKitTests` 通过（168 tests）。
- 影响：仅 `--help` 总览文案，不影响命令行为。

## 02:50 provider 过滤错误补充可恢复提示（继续 codex-cli 体验闭环）

- 体验来源：codex-cli 报告指出 `--provider` 非法时仅报错，缺少下一步建议。
- TDD：新增 `runnerSkillsListUnsupportedProviderIncludesRecoveryHint`，先红灯。
- 改动：
  - 在 `NolonCoreCLIRunner` 内新增 `unsupportedProviderHint(flag:value:)`；
  - 将 `--provider/--provider-id` 的 Unsupported 错误统一改为包含：
    `Run \`nolon provider list\` to view available providers.`
  - 覆盖路径：skills/workflow/mcp 的 list 目标解析，以及 provider-id 到路径解析。
- 测试：
  - 新增用例通过；
  - `runnerRejectsUnsupportedProviderIDForRemoteInstallSkill` 保持通过；
  - 全量 `swift test --package-path libs/Providers --filter NolonCoreCLIKitTests` 通过（169 tests）。
- 影响：错误提示可恢复性增强，不改变解析/执行结果码。

## 02:52 文本模式筛选字段本地化（继续 codex-cli 体验闭环）

- 体验来源：codex-cli 报告建议 text 模式避免中英混排机器字段。
- 改动：
  - `provider_filter:` -> `provider筛选:`
  - `state_filter:` -> `state筛选:`
  - 覆盖 `skills` 与 `workflow/mcp` 的文本渲染路径。
- 测试：
  - 同步更新相关断言（多处）。
  - 全量 `swift test --package-path libs/Providers --filter NolonCoreCLIKitTests` 通过（169 tests）。
- 影响：仅 text 输出标签可读性提升，不影响 JSON 输出键名。

## 02:54 默认列表压缩重复信息（移除“摘要”行）

- 体验来源：持续 CLI 体验反馈，`[结论]` 区域存在“需处理异常/行动建议/摘要”语义重叠。
- TDD：先把相关断言改为 `摘要:` 不应出现，定向测试红灯后再实现。
- 改动：
  - `skills list` 默认（非 `--show-fixes`）输出删除 `摘要:` 行。
  - `workflow/mcp list` 默认（非 `--show-fixes`）输出删除 `摘要:` 行。
  - 保留：`状态` + `需处理异常` + `行动建议`。
- 验证：
  - 定向：`runnerRendersSkillsListTextAsCompactList`、`runnerWorkflowFixHintsKeepProviderFilterInCommands` 通过。
  - 全量：`swift test --package-path libs/Providers --filter NolonCoreCLIKitTests` 通过（169 tests）。
- 影响：仅文案压缩，不影响异常判定、修复命令和返回码。

## 02:56 state筛选值本地化（英文值 -> 中文状态词）

- 体验来源：继续优化 text 模式可读性，`state筛选` 的值仍是 `installed/orphaned/broken` 英文。
- 改动：
  - 文本渲染中 `state筛选` 值改为 `localizedStateLabel` 输出：
    - `已安装` / `失效链接` / `损坏`
  - 覆盖 `skills` 与 `workflow/mcp` 两条列表渲染路径。
- 测试：
  - 更新相关断言（`state筛选: ...`）到中文状态值。
  - 全量 `swift test --package-path libs/Providers --filter NolonCoreCLIKitTests` 通过（169 tests）。
- 影响：仅 text 输出本地化，不影响 JSON 结构和内部状态枚举。

## 02:58 筛选键名统一为中文（筛选-提供方/筛选-状态）

- 体验来源：继续收敛 text 输出的一致性与可读性，避免 `provider筛选/state筛选` 的混合形式。
- 改动：
  - `provider筛选:` -> `筛选-提供方:`
  - `state筛选:` -> `筛选-状态:`
  - 覆盖 `skills` 与 `workflow/mcp` 文本渲染路径。
- 测试：
  - 同步更新相关断言（多处）。
  - 全量 `swift test --package-path libs/Providers --filter NolonCoreCLIKitTests` 通过（169 tests）。
- 影响：仅 text 字段文案，不影响 JSON/命令逻辑。

## 03:01 分段提交：修复命令去掉 NOLON_CMD 前缀
- 背景：用户反馈 `--show-fixes` 与默认修复建议里仍有前缀变量步骤，复制体验不够直观。
- 决策：文本模式修复命令统一直接输出 `nolon ...`，不再要求先执行 `NOLON_CMD='nolon'`。
- 代码：`NolonCoreCLIRunner.copyableCommand(_:)` 改为原样返回；移除 skills/workflow/mcp 修复建议中的前缀变量提示行。
- 测试：更新 `NolonCoreCLIKitTests` 相关断言（从 `$NOLON_CMD ...` 改为 `nolon ...`，并验证前缀提示不再出现）。
- 验证：`swift test --package-path libs/Providers --filter runnerRendersSkillsListWithShowFixes` 与 `--filter NolonCoreCLIKitTests` 全通过（169 tests）。

## 03:04 体验回路：筛选无异常场景补“复检命令”
- 体验结论：`--show-fixes` 在 provider/state 筛选后若无异常，仅有提示文案，缺少可复制命令，下一步不够直接。
- 调整：在 skills/workflow/mcp 的“当前筛选条件下无可修复项”分支追加：`复检命令: nolon <kind> list --show-fixes`。
- 测试：新增 `runner workflow installed verbose show-fixes does not add extra blank lines before installed section`，并更新相关 installed-filter 断言。
- 验证：`NolonCoreCLIKitTests` 全量 170 tests 通过。

## 03:07 体验回路：show-fixes 摘要行全中文
- 体验结论：源码模式 `list --show-fixes` 仍有英文摘要键（`summary/issues/action`），与其余中文输出不一致。
- 调整：skills/workflow/mcp 的 show-fixes 摘要统一改为中文：`摘要: 异常=... | 已安装=... | 修复动作=...`。
- 测试：更新 `NolonCoreCLIKitTests` 中相关断言，新增校验旧英文键不再出现。
- 验证：`swift test --package-path libs/Providers --filter NolonCoreCLIKitTests` 全量 170 tests 通过。

## 03:09 体验回路：skills 在筛选无异常时的下一步文案与 workflow/mcp 对齐
- 体验结论：`skills list --show-fixes` 在 `effectiveItems.isEmpty` 分支输出“可选复检（两条）”，而 workflow/mcp 输出“下一步 + 复检命令”，风格不一致。
- 调整：skills 该分支改为统一样式：`[下一步（可复制执行）]` + 无可修复提示 + `复检命令`。
- 测试：更新 `runnerSkillsShowFixesWithInstalledFilterPrintsNoOpHint` 断言，校验新文案并确保不再出现“可选复检”。
- 验证：`swift test --package-path libs/Providers --filter NolonCoreCLIKitTests` 全量 170 tests 通过。

## 03:21 体验回路：skills 已安装列表前多余空行修复
- 体验结论：`skills list --state installed --show-fixes` 在摘要与 `[已安装]` 之间出现双空行，视觉上比 workflow/mcp 更松散。
- 根因：`formatSkillsListText` 在进入列表区前存在无条件 `lines.append("")`，同时 `[已安装]` 分支也会补空行。
- 调整：移除无条件空行；仅在 `[异常]` 分组前显式补空行，保持分组间距一致。
- 测试：在 `runnerSkillsInstalledFilterOmitsInstalledTagInCompactMode` 新增断言，防止 `\n\n\n[已安装]` 回归。
- 验证：`swift test --package-path libs/Providers --filter NolonCoreCLIKitTests` 全量 170 tests 通过。

## 03:23 体验回路：空结果提示中的 state 值本地化
- 体验结论：空结果提示仍出现 `state=broken/installed/orphaned` 英文枚举值，和中文输出不一致。
- 调整：在 skills/workflow/mcp 空结果分支中，`state=` 后的值改为本地化状态词（已安装/失效链接/损坏）。
- 测试：更新 `NolonCoreCLIKitTests` 里相关断言（skills/workflow/mcp 场景）。
- 验证：`swift test --package-path libs/Providers --filter NolonCoreCLIKitTests` 全量 170 tests 通过。

## 10:54 体验回路：skills/workflow/mcp help 参数展开为逐行注释
- 需求：用户反馈 Subcommands 单行参数过密，要求“一行一个参数 + 注释”。
- 调整：`NolonCoreCLIHelp.swift` 中 skills/workflow/mcp 三组帮助文本改为：子命令标题 + 每个参数单独一行 + 中文注释。
- 兼容：保留原有“场景”示例和 add 的安全说明段。
- 测试：新增 `skillsHelpTextRendersOneParameterPerLineWithComments`；并通过 `NolonCoreCLIKitTests` 全量（171 tests）。
- 体验验证：`swift run --package-path libs/Providers nolon skills --help` 输出已按新格式展示。

## 11:17 体验回路：remote help 同步改为逐行参数注释
- 需求延续：help 输出密集，要求每参数单独一行并带说明。
- 调整：`remote` 的 `list/download/sync/sync-install/install` 全部改为“子命令 + 参数逐行 + 中文注释”，并对 skill 与 workflow/mcp 的目标参数分组展示。
- 测试：新增 `remoteHelpTextRendersOneParameterPerLineWithComments`，并保留 `remote --help` 入口测试。
- 验证：`NolonCoreCLIKitTests` 全量 172 tests 通过；`swift run ... nolon remote --help` 已确认新格式。

## 11:25 体验回路：help 全面展开收口（skills/mcp/workflow/skills repo/codex）
- 体验结论：历史紧凑帮助文案与新“每参数一行+注释”样式不一致，且入口测试仍含旧断言。
- 调整：
  - `NolonCoreCLIHelp.swift`：`skills repo` 帮助改为动作分块 + 参数逐行注释。
  - `NolonCodexCLIHelp.swift`：`codex auth/binary/status/runtime` 帮助改为动作分块 + 参数逐行注释。
  - `NolonCodexCLIEntrypointTests.swift`：同步更新旧断言（场景文案、参数行匹配、状态筛选中文文案）。
  - `NolonCoreCLIKitTests.swift`：新增覆盖 `skills repo` 与 `codex auth` 展开式帮助测试。
- 验证：
  - `swift test --package-path libs/Providers --filter NolonCoreCLIKitTests` 通过（174 tests）。
  - `swift test --package-path libs/Providers --filter NolonCodexCLIEntrypointTests` 通过（114 tests）。
  - 体验命令：`swift run --package-path libs/Providers nolon mcp --help`、`swift run --package-path libs/Providers nolon skills --help` 均为逐行参数注释格式。
