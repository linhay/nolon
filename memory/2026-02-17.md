## 13:38 路径模型收敛收尾
- 完成 `libs/Providers` 中 `FileManager.default.temporaryDirectory` / `NSTemporaryDirectory()` 的测试侧收敛，统一改为 `STFolder`/`STFile` 路径模型。
- 关键覆盖文件包含 `SkillsRepositoryFacadeTests`、`CodexAuthManagerTests`、`RemoteGitRepositorySupportTests` 等；`SkillsRepositoryFacade` 的 fallback 临时目录也改为 `STFolder("/tmp")`。
- 全量验证通过：`swift test --package-path libs/Providers`（305 tests, 38 suites, 0 failure）。
## 13:40 SKProcessRunner 收敛（测试侧）
- 将 `SkillsRepositoryFacadeTests`、`NolonSkillsRepositoryServiceTests` 里压缩 zip 的 `Process` 调用改为 `SKProcessRunner.runSync` + `SKProcessPayload`。
- `libs/Providers/Tests/ProvidersTests` 中 `Process` 直连已清零（`rg -n "\\bProcess\\b"` 无匹配）。
- 定向回归通过：
  - `swift test --package-path libs/Providers --filter SkillsRepositoryFacadeTests`
  - `swift test --package-path libs/Providers --filter NolonSkillsRepositoryServiceTests`
## 13:42 STFilePath 测试收敛补齐
- `NolonSkillsRepositoryServiceTests` 继续收敛路径模型：
  - `makeRemoteSkillZip` 入参从 `URL` 改为 `STFolder`，返回 `STFile`。
  - 远程安装场景中 staged skill 与 `SKILL.md` 存在性改为 `STFolder/STFile` 判定。
  - 多处 `FileManager.default.fileExists` 断言改为 `STPath(...).isExists` / `isSymbolicLink`。
- 回归验证：
  - `swift test --package-path libs/Providers --filter NolonSkillsRepositoryServiceTests`
  - `swift test --package-path libs/Providers`（305/38 全通过）
## 15:41 持续收敛（STFilePath + 测试侧文件操作）
- `NolonSkillsRepositoryService` 的 `resolveNolonSkillsRootFolder` 改为以 `STFolder` 为主模型（home 场景走 `STFolder(sanbox: .home)`）。
- 测试侧进一步收敛：
  - `ProviderUsageTokenAccountsTests` 的 base path 改为 `STFolder`。
  - `SkillsRepositoryFacadeTests` 的 repositoriesRoot 改为 `STFolder.url`，下载结果存在性断言改为 `STPath(...).isExists`。
  - `CodexCLIKitEnvironmentTests`、`CodexRuntimeAccountSwitcherTests`、`TTYCommandRunnerTests`、`CodexLoginRunnerTests` 中脚本写入与权限设置改为 `STFile.overlay + set(permissions:)`。
- 回归：多组定向 + `swift test --package-path libs/Providers` 全量通过（305/38）。
## 16:24 回归稳定后继续收敛
- 尝试将 `JsonRPCLineProcessSession` 迁移到 `SKProcessPipeSession` 时触发 `JsonRPCKitTests` 运行时崩溃（signal 4），已回退该文件到稳定实现，保证主线可回归。
- 继续完成低风险收敛：
  - `CodexAccountRuntimeServiceTests` / `CodexAppServerKitTests` / `JsonRPCKitTests` 的 `/usr/bin/python3` 可执行探测改为 `STPath(...).permission.contains(.executable)`。
- 验证：上述定向 suite 通过；`swift test --package-path libs/Providers` 全量通过（305/38）。
## 16:30 NolonHomeEnvironment TDD 收敛
- 新增测试：`NolonHomeEnvironmentTests`
  - 覆盖绝对路径、相对路径、默认回退（user home + `.nolon`）三种场景。
- 实现收敛：`NolonHomeEnvironment.normalizeDirectoryPath` 改为 `STFolder` 主模型。
- `NolonCodexCLI` 进程探测命令路径：`/bin/ps` 切换为 `STPath("/bin/ps").url`。
- 验证通过：
  - `swift test --package-path libs/Providers --filter NolonHomeEnvironmentTests`
  - `swift test --package-path libs/Providers --filter NolonCodexCLIServiceTests`
  - `swift test --package-path libs/Providers`（308 tests, 39 suites, 0 failure）
## 16:33 NolonManager 路径视图下沉
- `NolonManager` 新增 `STFolder/STFile` 视图属性：`rootFolder/skillsFolder/.../providersConfigFile`，保留现有 `URL` 字段兼容。
- `ensureDirectoriesExist` 改为直接使用 `STFolder` 视图，减少 URL->STFolder 来回桥接。
- 新增测试 `NolonManager exposes STPath views while keeping URL compatibility`，验证路径视图与 URL 字段一致。
- 回归通过：
  - `swift test --package-path libs/Providers --filter NolonResourceKitTests`
  - `swift test --package-path libs/Providers`（309 tests, 39 suites, 0 failure）
## 16:35 ProviderTemplateLoader 路径收敛
- `ProviderTemplateLoader.normalizeDirectoryPath` 改为 `STFolder` 主路径模型：
  - 绝对路径：`STFolder(expanded).url`
  - 相对路径：`STFolder(currentDirectory).folder(expanded).url`
- 新增测试：`loader resolves relative NOLON_HOME against current directory`（`ProviderCatalogTemplateTests`）。
- 另外收敛：`CodexAuthManager` 中 `STPath(URL(fileURLWithPath: ...))` 改为 `STPath(string)`。
- 验证通过：
  - `swift test --package-path libs/Providers --filter ProviderCatalogTemplateTests`
  - `swift test --package-path libs/Providers --filter CodexAuthManagerTests`
  - `swift test --package-path libs/Providers`（310 tests, 39 suites, 0 failure）
## 17:19 Watcher 路径归一化收敛
- 将 watcher 相关实现中的 `URL(fileURLWithPath: raw).standardizedFileURL.path` 统一为 `STPath(raw).url.standardizedFileURL.path`：
  - `ProviderUsage/UsageMonitorFileWatcher`
  - `NolonResourceKit/RemoteRepositoryWatchCenter`
  - `NolonResourceKit/ProviderResourceMonitor`
- `ProviderTemplateLoader` 增补 relative NOLON_HOME 场景测试后，路径归一化实现已改为 `STFolder` 主模型。
- 一次全量测试遇到 `Codex (Live)` 外网瞬时失败（Fetch credits），复跑 live suite 通过，随后全量通过。
- 当前验证：`swift test --package-path libs/Providers` 通过（310 tests, 39 suites, 0 failure）。
## 19:46 Provider+CodexPaths 路径视图下沉
- `Provider+CodexPaths` 新增 STFilePath 视图属性：
  - `codexHomeFolder` / `codexRulesFolder`
  - `codexDefaultRulesFile` / `codexAgentsFile` / `codexAgentsOverrideFile`
- 兼容层保留：原 `codex*URL` 属性改为由上述 ST 视图派生，保持外部 API 不变。
- 新增测试 `Provider codex paths expose STPath views while keeping URL compatibility`（`NolonResourceKitTests`）。
- 验证通过：
  - `swift test --package-path libs/Providers --filter NolonResourceKitTests`
  - `swift test --package-path libs/Providers`（311 tests, 39 suites, 0 failure）
## 19:48 ProviderSettings 路径视图收敛
- `ProviderSettings` 新增 `pathFolder(for:) -> STFolder`，原 `path(for:) -> URL` 改为复用该 ST 视图。
- 新增测试 `ProviderSettings exposes STFolder path view while keeping URL compatibility`（隔离 UserDefaults + NolonManager）。
- 验证通过：
  - `swift test --package-path libs/Providers --filter ProviderSettingsTests`
  - `swift test --package-path libs/Providers`（312 tests, 40 suites, 0 failure）
## 19:52 RemoteRepository 本地路径视图收敛
- `RemoteRepository.localClonePath` 在 `localFolder` 分支由 `URL(fileURLWithPath:)` 改为 `STPath(localPath).url`。
- 新增 `localCloneFolder: STFolder`，提供路径模型视图并与 URL 兼容层共存。
- 新增测试：`RemoteRepository local clone exposes STFolder view while keeping URL compatibility`（`NolonResourceKitTests`）。
- 验证通过：
  - `swift test --package-path libs/Providers --filter NolonResourceKitTests`
  - `swift test --package-path libs/Providers`（313 tests, 40 suites, 0 failure）
## 19:55 ProviderTemplateLoader + ProviderSettings 文件读写收敛
- `ProviderTemplateLoader.bootstrapTemplateFileIfNeeded`：去掉 `FileManager.removeItem/createFile`，改为 `STFile.overlay(with: Data)`。
- `ProviderSettings`：
  - 新增 `providersFile: STFile` 访问器（复用 `NolonManager.providersConfigFile`）。
  - `loadSettings` 改为 `providersFile.isExists + providersFile.data()`。
  - `saveProviders` 改为 `providersFile.overlay(with: encoded)`。
- 新增测试：`ProviderSettingsTests`，覆盖 `pathFolder(for:)` 与 URL 兼容。
- 全量验证通过：`swift test --package-path libs/Providers`（313 tests, 40 suites, 0 failure）。
## 19:57 可执行路径桥接收敛（无行为变更）
- 将多处固定可执行路径从 `URL(fileURLWithPath:)` 改为 `STPath(...).url`：
  - `ProviderCatalog/SkillsRepositoryFacade` (`/usr/bin/ditto`)
  - `ProviderCatalog/RemoteGitRepositorySupport` (`/usr/bin/git`, `/usr/bin/ssh`)
  - `NolonResourceKit/SkillInstaller` (`/usr/bin/ditto`)
  - `NolonResourceKit/GlobalCacheRepository` (`/usr/bin/ditto`)
  - `CodexProvider/CodexBinaryManager`（`shellPath`、`activePath`、`executablePath`、`/usr/bin/env`）
- 目标：统一路径模型桥接层，避免直接 URL 构造散落。
- 验证通过：
  - `swift test --package-path libs/Providers --filter SkillsRepositoryFacadeTests`
  - `swift test --package-path libs/Providers --filter RemoteGitRepositorySupportTests`
  - `swift test --package-path libs/Providers --filter CodexBinaryManagerEnvironmentTests`
  - `swift test --package-path libs/Providers`（313 tests, 40 suites, 0 failure）

## 20:08 response_item drift guard 补齐
- 新增 `CodexGeneratedFilesParser.supportedResponseItemTypesForCompatibility`，集中维护 provider 当前兼容的 `response_item.type` 集合。
- `CodexEventMsgSchemaDriftGuardTests` 新增 `ResponseItem` 守卫：
  - 读取 `libs/codex/codex-rs/protocol/src/models.rs` 的 `pub enum ResponseItem`；
  - 自动映射 variant 到 snake_case，并对比 provider 兼容集合；
  - 忽略 `Other` 兜底项，避免误报。
- 结论：`event_msg + response_item` 两条线都具备源协议漂移红灯能力，后续 codex 协议升级时能第一时间暴露缺口。
- 验证：
  - `swift test --package-path libs/Providers --filter CodexEventMsgSchemaDriftGuardTests` 通过。
  - `swift test --package-path libs/Providers` 全量通过（314 tests, 40 suites, 0 failure）。

## 20:14 App shell 执行收敛到 SKProcessRunner
- 将 `nolon/Skills/Views/Provider/ProviderContentTabView.swift` 中 `CodexTerminalLauncher` 的两处直接 `Process` 调用收敛为 `SKProcessRunner.runSync`：
  - `runAppleScript`：`/usr/bin/osascript -e ...`
  - `launchViaOpen`：`/usr/bin/open -b ... --args ...`
- 目标：继续减少 app 侧 shell 调用风格分叉，保持与 Providers/CLI 侧一致。
- 验证：`./build.sh` 通过（`BUILD SUCCEEDED`）。

## 20:22 Process 收敛守卫测试
- 新增 `ProcessRunnerConvergenceTests`（`libs/Providers/Tests/ProvidersTests/ProcessRunnerConvergenceTests.swift`）：
  - 扫描 `libs/Providers/Sources` 生产源码；
  - 仅允许 `JsonRPCKit/JsonRPCLineProcessSession.swift` 为 `Process` 直连例外；
  - 其余若出现 `Process` 直接使用即红灯。
- 目的：把“Process -> SKProcessRunner 收敛”从人工约束变成自动化门禁，避免回归。
- 验证：
  - `swift test --package-path libs/Providers --filter ProcessRunnerConvergenceTests` 通过。
  - `swift test --package-path libs/Providers` 全量通过（315 tests, 41 suites, 0 failure）。
