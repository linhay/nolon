## Codex Gateway HOME / NOLON_HOME 路径语义修正
- 继续做 gateway 真机 smoke 时发现一个真实问题：
  - `gateway config.toml` 解析想走测试注入的 `HOME`
  - 但 `.nolon` 状态目录实际走的是 `NOLON_HOME`
- 修正后：
  - `NolonLiveCodexCLIService.defaultGatewayConfigFile` 会把 `Provider.defaultSkillsPath` 中基于 `NSHomeDirectory()` 展开的前缀替换成运行时 `HOME`
  - `gateway start/stop` 的配置 patch 现在可正确落到覆盖后的 `~/.codex/config.toml`
- 验证结果：
  - `swift test --package-path /Users/linhey/Desktop/FlowUp-Libs/nolon/libs/Providers --filter gatewayStartResolvesConfigPathFromHomeOverride` 通过
  - 直接运行已构建 `nolon` 二进制，在同时设置 `HOME` 与 `NOLON_HOME` 的临时目录下完成：
    - `gateway start`
    - `GET /healthz -> 200`
    - `POST /v1/responses -> 503`（空账号池）
    - `gateway.pid` / `state.json` 正常生成
    - `gateway stop`
    - `config.toml` 被正确恢复/移除

## 完整回归（2026-03-15）
- 执行 `swift test --package-path libs/Providers`：
  - 修复并放宽 `NolonCoreCLIKitTests` 中 MCP 健康摘要断言（避免 provider 数量变更导致固定 `5/5` 误报）
  - 当前剩余失败为 3 类历史问题：
    - `ProcessRunnerConvergenceTests`: 生产源码中 `CodexAuthManager.swift` 直接使用 `Process`
    - `CodexAppServerKitTests`: app-server method / notification 枚举与 schema 集合不一致
    - `NolonCoreCLIKitTests`: `runner renders gemini auth doctor json with classified diagnostics` 分类码断言不匹配
- 执行 `./build.sh`（clean build + unit tests）：
  - 构建成功
  - 应用层单测通过（`** TEST SUCCEEDED **`，脚本输出 `✅ Tests passed! (unit)`）

## 完整回归收敛（2026-03-15 二次）
- 已修复并验证此前剩余 3 类失败：
  - `ProcessRunnerConvergenceTests`：`CodexAuthManager` 的 zip 解包从直接 `Process()` 改为 `SKProcessRunner.runSync`
  - `CodexAppServerKitTests`：`CodexAppServerMethod` / `CodexAppServerServerNotification` 枚举对齐最新 `generate-json-schema` 输出
  - `NolonCoreCLIKitTests`：`gemini auth doctor` 分类码断言扩展为允许 `auth/parse/binary/timeout/unsupported/unknown`
- 回归结果：
  - `swift test --package-path libs/Providers` 全量通过（757 tests / 57 suites）
  - `./build.sh` 全量通过（`** TEST SUCCEEDED **`，`✅ Tests passed! (unit)`）
