# 2026-02-13

## Codex CLI（round 67-69）
- 决策：继续坚持 `nolon` 单入口，不回退 `nolon-core` 兼容路径。
- 新增：`nolon codex auth delete --provider --account-id`，用于删除本地 auth 快照。
- 语义收敛：删除不存在账号时，返回结构化业务错误 `codex_auth_account_not_found`，避免静默成功。
- 参数门禁：`codex binary install/use --version` 增加空白值校验，统一返回 `invalid_arguments`。
- 测试：
  - `NolonCodexCLIEntrypointTests` 新增 delete 路由与 version 参数校验场景。
  - `NolonCodexCLIServiceTests` 新增 delete 缺失账号错误场景。
- 风险结论：`auth delete` 当前仅处理本地快照与 active map，不做 runtime 强制切换（保持最小副作用，后续按编排需求评估）。

## Codex NOLON_HOME 下沉
- 新增 `NOLON_HOME` 路径解析器（ProvidersShared/NolonHomeEnvironment），统一根目录解析逻辑。
- `CodexAuthManager` 默认根目录改为：显式 `rootURL` > `NOLON_HOME` > `~/.nolon`。
- `CodexBinaryManager` 默认根目录改为：显式 `nolonHomeURL` > `NOLON_HOME` > `~/.nolon`，并将 PATH 注入行改为基于解析后的根目录动态生成。
- 测试新增：
  - `CodexAuthManagerTests.defaultRootRespectsNolonHomeEnv`
  - `CodexBinaryManagerEnvironmentTests.usesNolonHomeEnvironment`
  - `CodexBinaryManagerEnvironmentTests.explicitNolonHomeWinsOverEnvironment`
- STFilePath 对齐：新增断言使用 `STFolder == STFolder`，避免字符串路径比较。

## Codex EventMsg 全量兼容（libs/codex 对齐）
- 决策：按 `libs/codex` schema 扩展 `event_msg` 兼容类型，不采用严格报错。
- 策略：
  - 核心事件维持强类型（token_count/user_message/agent_message/error/warning/task_*）。
  - schema 内其他事件统一映射 `.known(type:payload:)`。
  - schema 外未知事件保持 `.other(type:payload:)`。
- 新增测试：`CodexEventMsgCompatibilityTests`
  - `EventMsg` 全量列表映射测试（token_count 特判为顶层 `.tokenCount`）
  - `ResponseItem` 全量类型映射测试
- 结论：usage 与 CLI 回归均通过，兼容扩展未引入行为回归。

## CLI 事件类型建模偏好（新增）
- 用户偏好变更：后续新增事件类型字符串，优先采用可扩展常量封装（如 `RawRepresentable + ExpressibleByStringLiteral` / `enum + unknown`），避免分散硬编码字符串。
- 约束：先不做破坏性重构；从“新代码必须遵循”开始，并逐步回收旧硬编码。
- 适用范围：`CodexGeneratedFiles` 的 `event_msg.type` 映射、命令路由关键 type 常量。

## nolon CLI 安装脚本（~/.nolon/bin）
- 新增 `scripts/install-nolon-cli.sh`：构建 `libs/Providers` 的 `nolon` 可执行并安装到 `<NOLON_HOME or ~/.nolon>/bin/nolon`。
- 支持参数：`--nolon-home`、`--package-path`、`--configuration`、`--force`、`--print-path`。
- 新增 smoke：`scripts/tests/install-nolon-cli-smoke.sh`，覆盖默认安装、隔离安装、覆盖保护、force 覆盖、print-path。
- 约束：默认不覆盖既有目标，需显式 `--force`。

## nolon 空参帮助输出
- 变更：`nolon` 无参数时不再返回 JSON 错误，改为输出帮助文本并 `exitCode=0`。
- 兼容：`codex ...` 既有路由与错误 JSON 行为保持不变。
- 测试：`NolonCodexCLIEntrypointTests.noArgumentsPrintsHelp`。

## Codex CLI 路由复杂度治理（命令路径常量化）
- 变更：`NolonCLIEntrypoint.executeCommand` 从大块内联 `switch + 业务逻辑` 拆分为独立 handler（`executeAuthList/...`）。
- 新增：`NolonCodexCommandPath`（`RawRepresentable + ExpressibleByStringLiteral`），统一定义路由常量（`codex.auth.list` 等），避免分散硬编码。
- 行为：外部 CLI 行为不变，仅内部结构降复杂度并提高可扩展性。
- 验证：
  - `swift test --package-path libs/Providers --filter NolonCodexCLIEntrypointTests`
  - `swift test --package-path libs/Providers --filter 'NolonCodexCLIServiceTests|NolonCoreCLIKitTests'`

## Codex CLI 分层帮助输出（ArgumentParser 习惯对齐）
- 新增行为：
  - `nolon codex --help` 输出 codex 级帮助并 `exitCode=0`。
  - `nolon codex auth --help` 输出 auth 级帮助并 `exitCode=0`。
- 保持不变：业务/参数错误仍走 JSON 错误包络（`ok:false`）。
- 目的：在不破坏现有 JSON 协议前提下，先对齐 CLI 交互习惯与可发现性。
- 新增 CLI 帮助覆盖测试：`codex binary --help` 与 `codex status --help`，确保分层帮助完整且 exitCode=0。
- 新增 action 级 help：`nolon codex auth list --help` 与 `nolon codex binary install --help`，避免 `ArgumentParser.ParserError` JSON 化暴露给用户。
- `auth list/status` 参数解析已迁移到 `ParsableCommand`（从 `ParsableArguments` 渐进迁移），并补齐 `nolon codex auth status --help` action 级帮助。
