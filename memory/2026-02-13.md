# 2026-02-13

## Codex CLI（round 67-69）
- 决策：继续坚持 `nolon` 单入口，不回退 `nolon-core` 兼容路径。
- 新增：`nolon codex auth delete --provider --account-id`，用于删除本地 auth 快照。
- 语义收敛：删除不存在账号时，返回结构化业务错误 `codex_auth_account_not_found`，避免静默成功。
- 参数门禁：`codex binary install/use --version` 增加空白值校验，统一返回 `invalid_arguments`。
- 测试：
  - `NolonCodexCLIEntrypointTests` 新增 delete 路由与 version 参数校验场景。
  - `NolonCodexCLIServiceTests` 新增 delete 缺失账号错误场景。
- 风险结论：`auth delete` 当前仅处理本地快照与 active map，不做 runtime 强制切换（保持最小副作用，后续按编排需求评估）。

## Codex NOLON_HOME 下沉
- 新增 `NOLON_HOME` 路径解析器（ProvidersShared/NolonHomeEnvironment），统一根目录解析逻辑。
- `CodexAuthManager` 默认根目录改为：显式 `rootURL` > `NOLON_HOME` > `~/.nolon`。
- `CodexBinaryManager` 默认根目录改为：显式 `nolonHomeURL` > `NOLON_HOME` > `~/.nolon`，并将 PATH 注入行改为基于解析后的根目录动态生成。
- 测试新增：
  - `CodexAuthManagerTests.defaultRootRespectsNolonHomeEnv`
  - `CodexBinaryManagerEnvironmentTests.usesNolonHomeEnvironment`
  - `CodexBinaryManagerEnvironmentTests.explicitNolonHomeWinsOverEnvironment`
- STFilePath 对齐：新增断言使用 `STFolder == STFolder`，避免字符串路径比较。

## Codex EventMsg 全量兼容（libs/codex 对齐）
- 决策：按 `libs/codex` schema 扩展 `event_msg` 兼容类型，不采用严格报错。
- 策略：
  - 核心事件维持强类型（token_count/user_message/agent_message/error/warning/task_*）。
  - schema 内其他事件统一映射 `.known(type:payload:)`。
  - schema 外未知事件保持 `.other(type:payload:)`。
- 新增测试：`CodexEventMsgCompatibilityTests`
  - `EventMsg` 全量列表映射测试（token_count 特判为顶层 `.tokenCount`）
  - `ResponseItem` 全量类型映射测试
- 结论：usage 与 CLI 回归均通过，兼容扩展未引入行为回归。

## CLI 事件类型建模偏好（新增）
- 用户偏好变更：后续新增事件类型字符串，优先采用可扩展常量封装（如 `RawRepresentable + ExpressibleByStringLiteral` / `enum + unknown`），避免分散硬编码字符串。
- 约束：先不做破坏性重构；从“新代码必须遵循”开始，并逐步回收旧硬编码。
- 适用范围：`CodexGeneratedFiles` 的 `event_msg.type` 映射、命令路由关键 type 常量。

## nolon CLI 安装脚本（~/.nolon/bin）
- 新增 `scripts/install-nolon-cli.sh`：构建 `libs/Providers` 的 `nolon` 可执行并安装到 `<NOLON_HOME or ~/.nolon>/bin/nolon`。
- 支持参数：`--nolon-home`、`--package-path`、`--configuration`、`--force`、`--print-path`。
- 新增 smoke：`scripts/tests/install-nolon-cli-smoke.sh`，覆盖默认安装、隔离安装、覆盖保护、force 覆盖、print-path。
- 约束：默认不覆盖既有目标，需显式 `--force`。

## nolon 空参帮助输出
- 变更：`nolon` 无参数时不再返回 JSON 错误，改为输出帮助文本并 `exitCode=0`。
- 兼容：`codex ...` 既有路由与错误 JSON 行为保持不变。
- 测试：`NolonCodexCLIEntrypointTests.noArgumentsPrintsHelp`。

## Codex CLI 路由复杂度治理（命令路径常量化）
- 变更：`NolonCLIEntrypoint.executeCommand` 从大块内联 `switch + 业务逻辑` 拆分为独立 handler（`executeAuthList/...`）。
- 新增：`NolonCodexCommandPath`（`RawRepresentable + ExpressibleByStringLiteral`），统一定义路由常量（`codex.auth.list` 等），避免分散硬编码。
- 行为：外部 CLI 行为不变，仅内部结构降复杂度并提高可扩展性。
- 验证：
  - `swift test --package-path libs/Providers --filter NolonCodexCLIEntrypointTests`
  - `swift test --package-path libs/Providers --filter 'NolonCodexCLIServiceTests|NolonCoreCLIKitTests'`

## Codex CLI 分层帮助输出（ArgumentParser 习惯对齐）
- 新增行为：
  - `nolon codex --help` 输出 codex 级帮助并 `exitCode=0`。
  - `nolon codex auth --help` 输出 auth 级帮助并 `exitCode=0`。
- 保持不变：业务/参数错误仍走 JSON 错误包络（`ok:false`）。
- 目的：在不破坏现有 JSON 协议前提下，先对齐 CLI 交互习惯与可发现性。
- 新增 CLI 帮助覆盖测试：`codex binary --help` 与 `codex status --help`，确保分层帮助完整且 exitCode=0。
- 新增 action 级 help：`nolon codex auth list --help` 与 `nolon codex binary install --help`，避免 `ArgumentParser.ParserError` JSON 化暴露给用户。
- `auth list/status` 参数解析已迁移到 `ParsableCommand`（从 `ParsableArguments` 渐进迁移），并补齐 `nolon codex auth status --help` action 级帮助。
- `auth activate/login/delete` 迁移至 `ParsableCommand`，并补齐对应 action 级 `--help` 输出。

- `binary install/use` 与 `status probe` 迁移至 `ParsableCommand`，并新增对应 action 级 `--help` 输出。

- `NolonCLIEntrypoint` 已切到 `NolonRootCommand.parseAsRoot` 命令树解析（nolon->codex->group->action），移除手写 route 字符串分发；保留 `unsupported_command` 兼容语义。

- 补齐 `binary list/current/doctor --help`，避免命令树下 `HelpCommand` 落入 JSON 错误。

- 帮助路由改为 `NolonCLIHelpPath` 常量模型统一解析，替换长 `if` 链，新增命令时仅需补路径映射与 help 文本。

- 命令树定义已从 `NolonCodexCLI.swift` 拆分到 `NolonCodexCommands.swift`，减少单文件体积与耦合；CLI 行为保持不变。

- Help 体系已拆分到 `NolonCodexCLIHelp.swift`（包含 `NolonCLIHelpPath` 与文本 resolver），主执行文件职责进一步收敛。

## Codex CLI 执行器拆分（结构降复杂度）
- 变更：新增 `NolonCodexCLIExecutor.swift`，将 `NolonCLIEntrypoint` 的执行分发与参数校验（provider/version/unsupported route）整体下沉到独立执行器。
- 结构：`NolonCodexCLI.swift` 仅保留 `NolonLiveCodexCLIService` 与入口 `execute`；输出命令路径常量 `NolonCodexCommandPath` 随执行器归位。
- 关联：`NolonCLIExecutionContext` 已独立到 `NolonCLIExecutionContext.swift`，用于统一 success/error JSON 编码。
- 验证：`swift test --package-path libs/Providers --filter 'NolonCodexCLIEntrypointTests|NolonCoreCLIKitTests|NolonCodexCLIServiceTests'` 通过（69 tests）。

## Codex event_msg 类型常量化（可扩展结构）
- 背景：按用户偏好，新增代码避免分散硬编码事件 type 字符串。
- 变更：`CodexGeneratedFiles` 引入 `CodexEventMessageType`（`RawRepresentable + ExpressibleByStringLiteral`），统一管理 `event_msg` 类型常量。
- 行为：`parseEventMessage` 与 `knownCodexEventMessageTypes` 全量切到类型常量；`mcp_startup_update/complete` 与既有兼容逻辑保持不变。
- 验证：`CodexGeneratedFilesParserTests`、`CodexEventMsgCompatibilityTests`、`CodexSessionEventParserTests`、`CostUsageStoragePathTests` 全通过（30 tests）。

## Codex CLI 能力补强文档落位
- 新增 `docs-dev/dev/nolon-codex-cli-capability-gap-2026-02-13.md`。
- 内容：当前 CLI 能力盘点、P0/P1/P2 缺口、BDD 验收场景、建议提交序列。
- 目的：将后续推进从口头共识转成可执行清单，便于连续小步提交。

## Codex 协议漂移守卫（EventMsg）
- 新增测试 `CodexEventMsgSchemaDriftGuardTests`：运行时读取 `libs/codex/codex-rs/protocol/src/protocol.rs` 的 `EventMsg` 枚举，校验 Provider 侧兼容集合覆盖。
- 新增 `CodexGeneratedFilesParser.supportedEventMessageTypesForCompatibility`，作为事件兼容覆盖集合单一来源。
- 语义：当 codex protocol 新增事件而 Provider 未补齐时，测试会直接失败，避免 silently fallback 漏兼容。

## CLI 安装脚本 E2E 与产物路径修复
- 扩展 `install-nolon-cli` smoke：安装后实际执行 `nolon codex --help` 与 `nolon codex status probe --help`。
- 发现问题：原脚本回退 `find .build` 会命中旧产物，导致安装到非当前源码构建的 `nolon`。
- 修复：`resolve_built_binary_path` 优先使用 `swift build --show-bin-path` 定位当前包产物，再回退 `find`。
- 验证：`bash scripts/tests/install-nolon-cli-smoke.sh` 通过。

## Codex CLI 登录等待逻辑下沉
- 新增 `CodexLoginRunner.loginAndAwaitAuthJSONString(...)`（URL/STFolder 双入口），统一处理：启动 login、等待 `auth.json`、超时、进程提前退出宽限、UTF-8 校验。
- `NolonLiveCodexCLIService.authLogin` 改为直接调用该 API，移除服务层重复轮询逻辑。
- 新增测试：
  - `CodexLoginRunnerTests.loginAndAwaitAuthJSONStringReturnsAuth`
  - `CodexLoginRunnerTests.loginAndAwaitAuthJSONStringThrowsWhenAuthMissing`
- 回归：`CodexLoginRunnerTests|NolonCodexCLIServiceTests|NolonCodexCLIEntrypointTests` 全通过。

## Codex CLI 能力文档同步（P0进展）
- 更新 `docs-dev/dev/nolon-codex-cli-capability-gap-2026-02-13.md`：
  - 标记 `EventMsg` 协议漂移守卫已落地（schema drift guard）。
  - 标记 `auth login` 等待 `auth.json` 已下沉到 `CodexLoginRunner`。

## `nolon codex` 裸命令行为改造（ArgumentParser 习惯对齐）
- 背景：`nolon codex`（以及 `codex auth/binary/status`）此前会走到“缺少 action”错误，不符合 CLI 常见帮助回退体验。
- 变更：`NolonCodexCLIHelpResolver` 增加裸路径映射：`[]`、`["codex"]`、`["codex","auth"]`、`["codex","binary"]`、`["codex","status"]` 直接返回对应 help 文本。
- 测试：`NolonCodexCLIEntrypointTests` 新增 4 个场景（codex/auth/binary/status 裸命令均返回 help，exitCode=0）。
- 验证：`swift test --package-path libs/Providers --filter NolonCodexCLIEntrypointTests` 全通过；`swift run --package-path libs/Providers nolon codex` 输出帮助。

## CLI 行为调整：`nolon codex binary list` 返回纯文本列表
- 需求：用户要求 `binary list` 返回列表而非 JSON。
- 变更：`NolonCodexCLIExecutor.executeBinaryList` 改为调用 `formatBinaryList`，输出逐行版本列表（选中版本前缀 `*`，未选中前缀空格）。
- 兼容：仅调整 `codex binary list` 输出格式，其他命令继续保持结构化 JSON。
- 测试：新增 `NolonCodexCLIEntrypointTests.binaryListPrintsPlainText`，断言非 JSON 且包含纯文本版本行。
- 验证：`swift test --package-path libs/Providers --filter NolonCodexCLIEntrypointTests` 通过。

## CLI 行为调整：`nolon codex binary list` 输出详细信息
- 需求：用户要求 `binary list` 不仅是列表，还要显示详细信息。
- 变更：`NolonCodexCLIExecutor.formatBinaryList` 调整为每个版本输出 6 个字段：`version`、`id`、`name`、`source`、`imported_at`、`selected`，并保留选中标记（`*`）。
- 范围：仅影响 `codex binary list`，其他命令输出协议保持不变。
- 测试：`NolonCodexCLIEntrypointTests.binaryListPrintsDetailedPlainText`（先失败后通过），覆盖“纯文本且含详细字段，且非 JSON”。
- 验证：`swift test --package-path libs/Providers --filter NolonCodexCLIEntrypointTests` 通过。

## CLI 输出风格统一：`nolon codex` 其余成功命令改为详细纯文本
- 范围：除 `--help` 与错误输出外，`auth/status/binary/status probe` 的成功结果统一从 JSON envelope 改为可读详细文本。
- 命令覆盖：
  - auth: `list/status/activate/login/delete`
  - binary: `list/current/install/use/doctor`
  - status: `probe`
- 设计：每个命令输出稳定键名行（snake_case），空值统一 `-`，便于人读与脚本 grep。
- 测试：更新 `NolonCodexCLIEntrypointTests` 中所有成功路由断言，从 `"command"` JSON 断言切换为详细字段断言；整套 42 测试通过。
- 现状：错误路径仍保持结构化 JSON（`{"ok":false,"error":...}`），不影响上层错误处理链路。

## CLI 交互优化：`codex binary list` 改为单行简短显示
- 需求：用户要求列表项“一行简短显示”。
- 变更：`formatBinaryList` 从多行详情改为单行摘要：
  - `* <version> | <name> | <source> | <id>`（当前选中）
  - `  <version> | <name> | <source> | <id>`（非选中）
- 测试：`NolonCodexCLIEntrypointTests.binaryListPrintsConciseOneLineItems`（先失败后通过），并断言不再出现多行字段块。
- 验证：`swift test --package-path libs/Providers --filter NolonCodexCLIEntrypointTests` 通过。

## CLI 细节优化：`codex binary list` 列分隔符严格对齐
- 需求：用户要求 `|` 列分隔符严格对齐。
- 实现：`formatBinaryList` 先扫描版本列表计算 `version/name/source` 三列最大宽度，再右侧补空格后拼接单行输出。
- 输出样式：`<marker> <version-padded> | <name-padded> | <source-padded> | <id>`。
- 测试：`binaryListPrintsConciseOneLineItems` 使用不同长度样例（`0.26.0` vs `0.9.0`，`Codex 0.26.0` vs `X`），断言两行 `|` 索引完全一致。
- 验证：`swift test --package-path libs/Providers --filter NolonCodexCLIEntrypointTests` 通过。

## CLI 对齐扩展：`codex auth list` 账户列表改为表格对齐
- 需求：用户确认将 `auth list` 也改成和 `binary list` 一致的对齐表格风格。
- 实现：`formatAuthList` 账户行从多行详情改为单行表格，先计算 `name`、`auth_path` 列宽，再补空格对齐：
  - `* <name-padded> | <account-id> | <created_at> | <auth_path-padded>`
- 验证：新增 `authListPrintsAlignedTableRows`，使用不同长度样本并断言两行 `|` 索引一致；`NolonCodexCLIEntrypointTests` 全通过（43 tests）。
