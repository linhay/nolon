- Codex Advanced Config: 为 Common Options 的 `approval_policy` 与 `model_verbosity` 增加了与 Feature Flags 一致的描述文案展示（左侧标签下方）。
- 新增本地化键：`codex.config.description.approval_policy`、`codex.config.description.model_verbosity`（en + zh-Hans）。
- 实现方式：`commonOptionPickerRow` / `alignedConfigRow` 支持可选 `description` 参数，不影响其它行布局与即时落盘逻辑。
- 验证：`./build.sh` 通过（BUILD SUCCEEDED）。
- Codex Advanced Config 布局调整：Common Options 左侧标签列不再固定宽度；右侧控件改为自适应宽度（min 200 / max 420）并保持右对齐，解决描述换行与控件僵硬宽度问题。
- 改动位置：CodexAdvancedConfigView.alignedConfigRow。
- 验证：`./build.sh` 通过（BUILD SUCCEEDED）。
- Feature Flags 单卡片间距优化：卡片主容器改为 `HStack(alignment: .top, spacing: 12)`，文本区 `VStack(spacing: 6)`，卡片垂直内边距由 8 调整到 10，右侧 switch 顶部补偿 `padding(.top, 2)`，解决控件垂直过挤问题。
- 验证：`./build.sh` 通过（BUILD SUCCEEDED）。
- Codex 用量页命名与操作区更新：`codex/codexXcode` 的 Usage Tab 改为 `账号与用量`，非 Codex 维持 `用量` 不变。
- Codex 头部操作改为固定三按钮顺序：`刷新 | 登录 | 导入`，其中“刷新”为全部刷新（复用现有多账号并行刷新链路）。
- 新增测试：`CodexUsageTabPresentationTests`，覆盖 Tab 文案分流与头部按钮顺序。
- 验证：`xcodebuild -scheme nolon-app -only-testing:nolonTests/CodexUsageTabPresentationTests CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO test` 通过；`CodexBinaryTabConfigurationTests` 回归通过。
- Codex Advanced Config（Multi-Agent Roles）交互调整：`添加角色 -> 新增空白角色` 改为创建全空白草稿（不再自动命名 `role_n`）。
- 角色编辑区改为 `sheet`：角色列表从行内 `DisclosureGroup` 编辑改为“摘要卡片 + 编辑按钮”；新增空白角色后会直接弹出角色编辑 sheet。
- 新增可测试工厂：`CodexAdvancedConfigViewModel.makeEmptyRoleDraft()`，`addRoleDraft()` 改为复用该工厂。
- 新增测试：`CodexAdvancedConfigRoleDraftTests.testBDD_GivenAdvancedConfig_WhenCreateEmptyRoleDraft_ThenNewRoleFieldsAreBlank`。
- 验证：`xcodebuild test -project nolon.xcodeproj -scheme nolon-app -destination 'platform=macOS' -only-testing:nolonTests/CodexAdvancedConfigRoleDraftTests -only-testing:nolonTests/CodexBinaryTabConfigurationTests` 通过。
- 崩溃修复：`CodexAdvancedConfigViewModel` 在析构时触发 `abort()`（栈含 `swift_task_deinitOnExecutorMainActorBackDeploy`）已复现并修复。
- 修复决策：为 `@MainActor` 的 `CodexAdvancedConfigViewModel` 添加显式 `nonisolated deinit {}`，采用最小改动绕开释放路径崩溃。
- 回归测试新增：`CodexAdvancedConfigRoleDraftTests.testBDD_GivenMainActorViewModel_WhenReleaseInstance_ThenDeinitDoesNotCrash`。
- 验证：`xcodebuild test -project nolon.xcodeproj -scheme nolon-app -destination 'platform=macOS,arch=arm64' -only-testing:nolonTests/CodexAdvancedConfigRoleDraftTests -quiet` 通过。
- Multi-Agent Roles 新建流程补充：`新增空白角色` 不再立即落入 `roleDrafts`，改为先打开 sheet 编辑，只有点击 sheet 的保存按钮时才提交并创建角色。
- 实现：新增角色编辑上下文 `RoleEditorTarget(mode: .creating/.existing)` 与 `pendingNewRoleDraft`；`addRoleDraft` 支持提交指定草稿（用于“保存时提交”）。
- 新增测试：`testBDD_GivenRoleDraft_WhenCommitOnSave_ThenAppendsDraftToRoles`。
- 补充一致性：内置角色菜单（Add/Override）在“新建”路径也改为保存后创建；若命中已有同名角色则保留“覆盖并编辑已有角色”的语义。
- Codex 账号与用量页刷新修复：顶部“刷新”按钮不再走 `load()`，改为 `refreshFromHeader()`，在多账号模式下执行“全账号刷新”。
- 刷新策略调整：顶部刷新目标改为 `codexHeaderRefreshTargets()`（按当前激活账号优先排序），不再受 `shouldSkipRefresh` 失败跳过策略影响，报错账号也会参与刷新。
- 测试先红后绿：新增 `ProviderUsageViewModelManualRefreshTests` 两条用例，覆盖“顶部刷新触发全账号刷新”与“失败账号不被跳过”。
- 验证：`xcodebuild test -project nolon.xcodeproj -scheme nolon-app -destination 'platform=macOS' -only-testing:nolonTests/ProviderUsageViewModelManualRefreshTests -quiet` 通过。
- Codex 多账号一致性修复落地：`CodexAuthManager` 新增 preflight（文件锁 + 判真值 + 漂移恢复 + active 备份 + 指纹持久化）。
- 匹配优先级调整：`matchAccount` 改为 `email -> account.id -> OPENAI_API_KEY`，最后回退 cleaned JSON 全等；`CodexAuthSummary` 新增 `accountID` 提取。
- `codexXcode` 兼容修复：`codexHomeFolder(for:)` 支持 `codexXcode`；`ProviderUsageViewModel.mapToUsageProvider` 将 `codexXcode` 映射到 `.codex`。
- 后台无监听自愈：`nolonApp` 新增 `CodexAuthBackgroundPoller`，默认开启，60s 轮询对 `codex/codexXcode` 执行 preflight。
- 备份与恢复策略：仅 active 快照，5 分钟间隔，最多 10 份且清理 30 天前；检测到 identity 漂移时恢复 active 并承接漂移数据到新/匹配快照。
- 新增验证：`swift test --package-path libs/Providers --filter CodexAuthManagerTests` 通过；`xcodebuild test -project nolon.xcodeproj -scheme nolon-app -destination 'platform=macOS' -only-testing:nolonTests/ProviderUsageViewModelManualRefreshTests` 通过。
- 崩溃修复：`ProviderUsageViewModel.reorderCodexAccountOutcomesForDisplay` 在重复账号 outcome 场景下使用 `Dictionary(uniqueKeysWithValues:)` 触发断言崩溃（SIGTRAP）已修复。
- 修复方式：改为 `Dictionary(_:uniquingKeysWith:)`，重复 key 采用“后者覆盖前者”，确保保留最新 outcome 且不崩溃。
- 回归测试新增：`ProviderUsageViewModelOutcomeOrderingTests.testBDD_GivenDuplicatedAccountOutcomes_WhenReordering_ThenKeepsLatestWithoutCrash`。
- 验证：`xcodebuild test -project nolon.xcodeproj -scheme nolon-app -destination 'platform=macOS' -only-testing:nolonTests/ProviderUsageViewModelOutcomeOrderingTests -only-testing:nolonTests/ProviderUsageViewModelManualRefreshTests` 通过。
- 提交：`6d40f4b fix(usage): avoid crash when duplicate codex outcomes are reordered`。
- 新问题修复：`CodexAuthManager.matchAccount` 不再使用 `OPENAI_API_KEY` 后缀匹配，改为完整值精确匹配，避免“后缀碰撞”导致账号文件被误覆盖并出现快照重复/账号漂移。
- 测试先红后绿：新增 `CodexAuthManagerTests.recordCLILoginSnapshotMatchesExactAPIKeyWithoutSuffixDrift`，复现“两个账号后缀相同时写错文件”并验证修复。
- 验证：`swift test --package-path libs/Providers --filter CodexAuthManagerTests` 通过。
- 现场排查确认：`~/.nolon/codex/auth` 出现历史脏数据，`robbinsterry3456-gmail-com.json` 内部内容指向 `linhan.bigl055@gmail.com`，且 `nolon.account.id` 与 `linhan-bigl055-gmail-com.json` 重复，`relativeAuthPath` 也错指到 `auth/linhan-bigl055-gmail-com.json`。
- 新增自愈修复：`loadAccountsFromAuthFolder` 增加脏数据修复，自动纠正 `nolon.account.relativeAuthPath` 并修复跨文件重复 `nolon.account.id`（后续重复项重置 UUID，保持 token 原文不变）。
- 测试先红后绿：新增 `CodexAuthManagerTests.loadAccountsHealsDuplicateIDsAndWrongRelativePath`，验证重复 ID + 错误路径元数据可自动修复。
- 验证：`swift test --package-path libs/Providers --filter CodexAuthManagerTests` 通过（19 tests）。
- 运营修复（用户现场）：先备份 `~/.nolon/codex` 到 `/Users/linhey/.nolon/codex-backup-20260302-183906.tar.gz`，再用 CLI 执行 `nolon codex auth list` 触发自愈。
- 后续清理（CLI）：执行 `auth activate --account-id A95DC3D6-D1C1-47A4-A2C0-C8C1E1A9B0F6` 与 `auth delete --account-id 7235A928-6093-4BC7-9E4C-E3153F959A3F`，移除污染账号文件 `auth/robbinsterry3456-gmail-com.json`。
- 风险备注：`auth activate` 返回 `runtimeSwitched=false`（等待 `account/updated` 超时），但文件层激活与 registry 已生效；需后续单独观察 runtime 通知稳定性。
- 回归现场（18:42）再次出现：`auth/robbinsterry3456-gmail-com.json` 被重建且 payload 与 `linhan-bigl055-gmail-com.json` 完全一致（去除 `nolon` 后 hash 一致），表现为“重复文件+账号漂移复发”。
- 新增防复发自愈：`loadAccountsFromAuthFolder` 在修复重复 ID 后，继续按 cleaned payload hash 去重，自动删除重复 payload 文件并同步 active map（优先保留 active 映射项）。
- 新增测试：`CodexAuthManagerTests.loadAccountsPrunesDuplicatedSnapshotPayloadFile`；全量 `CodexAuthManagerTests` 20 条通过。
- 现场再次 CLI 修复前备份：`/Users/linhey/.nolon/codex-backup-20260302-185107-pre-prune.tar.gz`；执行 `nolon codex auth list --provider codex --json` 后重复文件被自动清除。
- 新增“激活即基线”修复：`CodexAuthManager.activateAccountAndMarkActive` 改为文件锁内执行 `activate+setActive+reconcile`，并立即 `backupActiveSnapshotIfNeeded(force: true)` + `persistActiveFingerprintIfNeeded`，避免切换后短时间漂移缺少可恢复基线。
- 新增测试：`CodexAuthManagerTests.activateAccountAndMarkActiveSeedsDriftRestoreBaseline`，覆盖“切换后立刻发生漂移，下一次 preflight 仍可恢复 active 并保留漂移账号”。
- 验证：`swift test --package-path libs/Providers --filter CodexAuthManagerTests` 通过（21 tests）。
- 现场回归（19:03~19:04）：CLI 连续切换 `robbinsterry3456@gmail.com -> linhan.bigl055@gmail.com` 后，未再出现新的重复文件；`auth` 目录 20 秒轮询无新增漂移写入。
- 现场备份新增：`/Users/linhey/.nolon/codex-backup-20260302-190316-post-fix.tar.gz`。
- 用户决策变更：账号切换流程改为“仅切磁盘层，不自动切 runtime”；runtime 切换保留为手动能力。
- 实现：`CodexAuthActivationCoordinator.activate` 仅执行 `authActivate` 并返回 `runtimeSwitched=false`；新增 `activateRuntime(account:)` 作为手动 runtime 切换入口。
- 新增测试：`CodexAuthActivationCoordinatorTests.activateSkipsRuntimeByDefault`、`activateRuntimeSuccess`、`activateRuntimeFailure`；验证默认不触发 runtime，手动路径仍可独立成功/失败返回。
- 验证：`swift test --package-path libs/Providers --filter CodexAuthActivationCoordinatorTests`、`--filter NolonCodexCLIServiceTests`、`--filter CodexAuthManagerTests` 全通过。
- CLI 实测：`nolon codex auth activate --email robbinsterry3456@gmail.com --json` 耗时约 0.69s（不再出现 8s runtime 通知等待）。
- 回归发现并修复：引入“按邮箱重命名快照文件”后，`auth.json` 在 preflight 首轮可能先断链，且 registry 因旧 ID 失配无法回链，导致后续漂移恢复链路中断。
- 实现修复：`reconcileProviderAuthWithSnapshotsIfNeeded` 在 `auth.json` 不存在场景新增兜底回链策略（优先 active 指纹匹配，其次单快照回链）；`reconcileActiveSymlinkDriftIfNeeded` 在恢复后按最新快照 ID 重取账号再 relink，避免使用重命名前旧路径。
- 稳定性修复：新增 `removeFileOrSymlinkIfPresent`，软链重建改为无条件尝试删除路径（含断链），修复 `auth.json` “File exists” 冲突；`finalizeCLILogin` 改为复用 `activateAccountAndMarkActive`，保证激活后立即建立基线与指纹。
- 测试更新：`CodexAuthManagerTests` 适配“路径会在自愈后变化”的新行为（按账号 ID/邮箱断言，不再依赖旧文件名）。
- 验证：`swift test --package-path libs/Providers --filter CodexAuth` 通过（37 tests）；`xcodebuild test -project nolon.xcodeproj -scheme nolon-app -destination 'platform=macOS' -only-testing:nolonTests/ProviderUsageViewModelOutcomeOrderingTests` 通过（3 tests）。
- 新增登录链路修复：`app-server` 登录完成后改为统一调用 `CodexLoginRunner.awaitAuthResult`（8 秒轮询窗口）等待 `auth.json` 落盘，再执行 `recordCLILoginSnapshot`，覆盖 CLI 工具链（`NolonCodexCLI.loginViaAppServer`）与 App UI 链路（`ProviderUsageViewModel.runAppServerLoginFlow`）。
- 风险结论：此前存在“login completed 通知先到、auth.json 仍未同步”竞态，可能触发 fallback direct login 并把旧账号 payload 回写到现有快照，表现为“登录成功但不新增卡片/文件”。
- 新增测试：`CodexLoginRunnerTests.awaitAuthResultWaitsForDelayedWrite`、`awaitAuthResultThrowsWhenTimeoutExpires`。
- 验证：`swift test --package-path libs/Providers --filter CodexLoginRunnerTests` 通过（8 tests）；`xcodebuild test -project nolon.xcodeproj -scheme nolon-app -destination 'platform=macOS' -only-testing:nolonTests/ProviderUsageViewModelCLILoginTests` 通过（8 tests）。
- 新增稳定化修复：Codex 登录目录从临时目录切换为固定目录 `~/.nolon/{provider}/cli-login-home/{provider}`，覆盖 `ProviderUsageViewModel` 与 `NolonCodexCLI.authLogin` 两条链路，保证登录后落盘路径可预测。
- 登录前强制写入 `config.toml`：`cli_auth_credentials_store = "file"`；并在每次登录前清理隔离目录旧 `auth.json`，避免读取陈旧 payload。
- 新增测试（先红后绿）：`ProviderUsageViewModelCLILoginTests.testBDD_GivenIsolatedNolonHome_WhenPreparingCLILoginHome_ThenUsesStableFolderAndForcesFileStore`、`NolonCodexCLIServiceTests.prepareIsolatedLoginHomeEnforcesFileStoreAndClearsStaleAuth`。
- 验证：`xcodebuild test -project nolon.xcodeproj -scheme nolon-app -destination 'platform=macOS' -only-testing:nolonTests/ProviderUsageViewModelCLILoginTests` 通过（9 tests）；`swift test --package-path libs/Providers --filter prepareIsolatedLoginHomeEnforcesFileStoreAndClearsStaleAuth` 通过（1 test）。
- 新增兜底修复：app-server 登录链路不再依赖 `login completed` 事件作为前置条件，改为“`auth.json` 落盘优先”。`completion` 事件改为并行 best-effort，失败不会阻断快照写入。
- 影响范围：`ProviderUsageViewModel.runAppServerLoginFlow`、`NolonCodexCLI.loginViaAppServer` 同步切换为 `CodexLoginRunner.awaitAuthResultPreferFile`。
- 新增测试：`CodexLoginRunnerTests.awaitAuthResultPreferFileIgnoresCompletionWaiterFailure`（completion waiter 抛错但 `auth.json` 正常落盘仍成功）。
- 验证：`swift test --package-path libs/Providers --filter awaitAuthResultPreferFileIgnoresCompletionWaiterFailure` 通过（1 test）；`xcodebuild test -project nolon.xcodeproj -scheme nolon-app -destination 'platform=macOS' -only-testing:nolonTests/ProviderUsageViewModelCLILoginTests` 通过（9 tests）。
- 用户偏好变更：app-server 登录不再自动打开系统浏览器。
- 实现：移除 App 侧 `NSWorkspace.shared.open(started.authURL)` 与 CLI 侧 `/usr/bin/open <auth_url>`；登录入口仅提供 URL（UI 弹框/CLI 输出）供手动打开。
- 验证：`swift test --package-path libs/Providers --filter awaitAuthResultPreferFileIgnoresCompletionWaiterFailure` 通过；`xcodebuild build -project nolon.xcodeproj -scheme nolon-app -destination 'platform=macOS' CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO` 通过。`xcodebuild test` 在当前机器存在 `nolonTests.xctest` bundle 环境问题（与本次改动无关）。
- 登录弹框交互收敛：`ProviderUsageView` 的登录 URL sheet 增加 `onDismiss` 回调，弹框消失即触发 `handleLoginURLSheetDismissed()` 并取消当前登录任务；覆盖 Close/Esc/外部关闭路径。
- 文案调整：`codex.login.sheet.open` 按钮文案统一改为“在浏览器中打开”（`ProviderUsageLoginSheet` + `Localizable.xcstrings`）。
- OAuth originator 对齐：app-server 登录初始化 `clientName` 从 `nolon` 调整为 `codex`（App `ProviderUsageViewModel.runAppServerLoginFlow` 与 CLI `NolonCodexCLI.loginViaAppServer`）。
- 稳定性修复：`CodexLoginRunner.awaitAuthResult` 对 `auth.json` 的瞬时无效 UTF-8 改为继续轮询；仅在超时后仍无效时抛 `authInvalidUTF8`，避免“文件已创建但流程未成功”误判。
- 新增测试：
  - `ProviderUsageViewModelCLILoginTests.testBDD_GivenCLILoginRunning_WhenLoginURLSheetDismisses_ThenLoginFlowCancelsImmediately`
  - `CodexLoginRunnerTests.awaitAuthResultRetriesWhenAuthUTF8IsTemporarilyInvalid`
  - `CodexLoginRunnerTests.awaitAuthResultThrowsInvalidUTF8WhenStillInvalidAtTimeout`
- 验证：
  - `swift test --package-path libs/Providers --filter CodexLoginRunnerTests` 通过（11 tests）。
  - `xcodebuild test -project nolon.xcodeproj -scheme nolon-app -destination 'platform=macOS' -only-testing:nolonTests/ProviderUsageViewModelCLILoginTests CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO` 通过（10 tests）。
