# 2026-03-20

- 修复：`账号与用量`（Codex）Tab 顶部操作栏仅显示第一个动作导致“登录”按钮不可见。
- 实现：在 `ProviderUsageView` 新增 `visibleCodexPrimaryHeaderActions`，非多选模式下显示前两个主动作（刷新 + 登录）。
- 测试：新增 `CodexUsageTabPresentationTests.testBDD_GivenCodexHeaderActions_WhenResolvingVisibleActions_ThenShowsRefreshAndLoginInTopBar`，验证顶部动作可见性策略。
- 验证：目标用例本地通过（`xcodebuild test -scheme nolon-app -only-testing:...`）。
- 规则升级：`CodexAuthManager` 在保存账号快照 `auth.json` 时，统一使用 `prettyPrinted + sortedKeys` 输出，确保落盘即格式化且键稳定排序。
- 回归保障：新增 `CodexAuthManagerTests.addAccountPersistsPrettySortedAuthJSON`，并验证相关快照路径用例通过。
- 交互增强：`账号与用量` 的「网关卡片」分组支持展开/折叠，交互与账号分组折叠保持一致（chevron + 内容显隐动画）。
- 回归测试：新增 `CodexAccountDisplaySectionsTests.testBDD_GivenGatewayCardsSection_WhenTogglingCollapse_ThenExpandedStateFlips` 并通过。
- 修复：网关卡片点击不触发激活的问题，`gatewayCardView` 由普通 `.onTapGesture` 调整为 `.highPriorityGesture(TapGesture, including: .gesture)`，降低被 `dropDestination`/上下文菜单手势链路吞掉的风险，同时不影响卡内“添加账号/移除成员”按钮。
- 验证：`CodexAccountDisplaySectionsTests` 与 `GatewayCardVisualRegressionSnapshotTests` 回归通过；Debug 版 `nolon.app` 已重新编译并启动用于实机复测。
- 功能补齐：账号卡片与网关卡片的右键菜单新增“激活”操作（仅在未激活状态显示），与点击卡片激活逻辑保持一致。
- 测试补充：新增 `CodexUsageTabPresentationTests` 两个激活菜单可见性策略用例并通过。
- 交互优化：右键菜单触发账号激活改为“直切”，不再进入 `pendingActivate` 二次确认弹框；普通激活流程仍保留确认逻辑。
- 回归验证：新增 `testBDD_GivenGatewayCardSelected_WhenActivatingAccountImmediately_ThenSkipsConfirmAndClearsGatewaySelection`，并与原确认路径测试同时通过。
- 根因定位：网关守护进程默认使用 `CommandLine.arguments[0]` 作为启动可执行；在 `.app` 场景该路径是 GUI 入口，子进程无法稳定进入 `codex gateway serve`，导致 `healthz` 超时。
- 修复：`NolonLiveCodexCLIService.gatewayStart` 引入 daemon 启动模式解析：优先 companion CLI（若存在），否则 `.app` 场景自动回退到进程内嵌网关 daemon；并在 `gatewayStatus/gatewayStop` 增加嵌入式 daemon 状态与清理逻辑。
- 测试：新增启动模式解析与 `.app` 嵌入式启动用例，`swift test --package-path libs/Providers --filter NolonCodexCLIServiceTests` 共 34 条通过。
- 修复：Codex 账号归并与自愈链路对 `account_id` 过度匹配，导致“同工作空间不同用户”被误覆盖。
- 新规则：含 `account_id` 的 payload 仅在 `account_id + email` 命中时覆盖；若缺失 email，仅 `account_id + nolon.account.id` 命中时覆盖；仅 `account_id` 命中一律新建快照。
- 范围：统一落地到 `upsertAccountFromCLILogin`、`matchAccount`、`upsertSnapshotFromProviderData` 与 drift `isSameIdentity` 判定。
- 回归：新增/更新 `CodexAuthManagerTests`（登录导入、detached reconcile、drift repair）并通过 `swift test --package-path libs/Providers --filter CodexAuthManagerTests`（47/47 通过）。
- 文档：已同步更新 `docs-dev/features/codex-auth-preflight-and-drift-repair-2026-03-02.md` 的匹配与自愈规则描述。
- 功能：`Main / Codex / 账号与用量` 新增“列表/卡片”模式切换，账号区与网关卡片区均支持按模式展示。
- 持久化：`ProviderUsageMonitorSettings` 新增 `codexUseListLayout`，默认卡片模式，切换后可跨会话保留。
- 测试：新增 `CodexUsageTabPresentationTests` 两个布局模式用例，并扩展 `ProviderUsageMonitorSettingsTests` 的 Codable 回归用例。
- 验证：`xcodebuild test -quiet -project nolon.xcodeproj -scheme nolon-app -destination 'platform=macOS' -only-testing:nolonTests/CodexUsageTabPresentationTests` 通过；`swift test -q --package-path libs/Providers --scratch-path /tmp/providers-scratch-layout-mode --filter ProviderUsageMonitorSettingsTests` 通过（4 tests）。
- 根因确认：`codex-cli 0.107.0` 在网关场景下仅改 `config.toml` 顶层 `base_url/openai_base_url` 不足以接管 OpenAI 内建 provider，请求仍可能直达 `https://api.openai.com/v1/responses`。
- 修复策略：网关启动改为写入自定义 provider `nolon_gateway`（`model_provider` + `[model_providers.nolon_gateway] base_url=/v1 wire_api=responses`），并在停止时优先按原始配置文本完整恢复。
- 转发修复：`CodexGatewayResponsesForwarder` 增加关键请求头透传（`Accept/OpenAI-Beta/...`）与流式默认 Accept 推断（`stream=true` -> `text/event-stream`），同时识别上游 `text/event-stream` 为 SSE 内容类型，避免流完成事件被误处理。
- 回归：`CodexGatewayKit` 新增 SSE 与头透传测试，相关网关套件与 `NolonCodexCLIService` 网关启动/停止关键用例通过。
- 实链路复盘：网关下游曾返回 `stream closed before response.completed`，抓包确认上游实际返回 `200 + HTML`（ChatGPT 登录页），并非 SSE；根因是候选账号可用性与协议不匹配被静默吞掉。
- 兜底策略：`CodexGatewayResponsesForwarder` 在 `stream=true` 时增加协议校验（拒绝 HTML、拒绝无 SSE 语义、拒绝缺失 `response.completed`），触发调度层 failover。
- 调度增强：`CodexGatewayScheduler` 将 `401/403` 纳入 failoverable；当全部候选失败时，`503` 错误体附带 `Last failure` 明确失败原因，便于 UI/用户定位具体阻塞。
- 账号健康复核：执行 `nolon codex auth usage --provider codex --refresh --json`，14 个账号全部 `status=healthy`，并通过 `codex exec` 直接链路返回 `OK`，确认账号池本身可用。
- 网关路径修复：`CodexGatewayResponsesForwarder.resolveUpstreamURL` 改为“保留上游 base path + 去除请求侧冗余 v1 前缀”拼接策略，确保 `https://chatgpt.com/backend-api/codex` + `/v1/responses` 正确转发到 `/backend-api/codex/responses`。
- 回归补强：更新 `CodexGatewayKitTests` 的 v1 归一化断言，并新增 ChatGPT backend path 保留测试；`swift test --package-path libs/Providers --filter CodexGatewayKitTests` 与 `--filter CodexGatewaySchedulerTests` 通过。
- 真实切换验证：`gateway start` 后 `config.toml` 出现 `model_provider = "nolon_gateway"`，`auth.json` 切为虚拟网关账号（`auth_mode=apikey` + `OPENAI_API_KEY=nolon-gateway-virtual-api-key` + `nolon_gateway_virtual=1`）；网关下 `codex exec` 返回 `GATEWAY_OK`；`gateway stop` 后 `auth.json` 恢复账号模式并可再次 `codex exec` 返回 `ACCOUNT_OK`。
- 测试隔离修复：`NolonCodexCLIServiceTests.gatewayStartUsesEmbeddedDaemonForAppExecutable` 改为使用 `providerID = codex-xcode`（原为 `codex`），避免静态 `embeddedGatewayRuntime` 在并发执行时与 `codex` 网关启动用例互相占用运行槽位，导致 `codex_gateway_already_running` 偶发失败。
- 回归：`swift test --package-path libs/Providers --filter NolonCodexCLIServiceTests --parallel --num-workers 8` 连续 3 轮通过；`CodexGatewayKitTests` 与 `CodexGatewaySchedulerTests` 通过。
- 修复：处理 URL 导入（如 `https://github.com/Dimillian/Skills`）时，`pendingImportURL` 在资源中心链路存在同帧重复响应风险；将 `RemoteRepositorySidebarView` 与 `ResourceCenterView` 的监听从 `.onChange(of: settings.pendingImportURL)` 统一改为 `.task(id: settings.pendingImportURL)`，并新增 `RemoteRepositorySidebarViewModel.shouldPresentAddRepositorySheet` 防止弹窗重复触发。
- 测试：新增 `RemoteRepositorySidebarViewModelTests` 3 个用例（Git URL 触发、Clawhub URL 不触发、弹窗已展示时不重复触发）；执行 `xcodebuild test -project nolon.xcodeproj -scheme nolon-tests -destination 'platform=macOS' -only-testing:nolonTests/RemoteRepositorySidebarViewModelTests` 通过（5/5）。
- 架构拆包（Sidebar Phase 1）：新增 `libs/NolonUIFoundation`（Sidebar 轻量模型 + `SidebarSelectionKey` + `SidebarSectionBuilder`）与 `libs/NolonUI`（`ProviderSidebarComponent` 与样式）。
- 主工程回接：`ProviderSidebarView` 改为依赖 `NolonUI` 组件；新增 `ProviderSidebarAdapter` 将 `ProviderCatalog.Provider` 映射为 UI Foundation 模型，主工程保留编排逻辑（选择/删除/移动/添加）。
- 预览改进：Sidebar 不同样式视图已拆到独立文件并各自提供 `#Preview`（Component / ProviderRow / ToolRow / SectionHeader），支持在包内直接假数据预览，不依赖 App 启动。
- 验证：`swift test --package-path libs/NolonUIFoundation`、`swift test --package-path libs/NolonUI`、`xcodebuild -project nolon.xcodeproj -scheme nolon-app -destination 'platform=macOS,arch=arm64' build` 通过。
- 结构整理：将 Sidebar 相关文件按目录归档。`NolonUI` 调整为 `Components/Sidebar/*` + `Styles/*`，`NolonUIFoundation` 调整为 `Sidebar/*`；仅文件组织变更，无行为改动，包测试与主工程构建均通过。
- DesignSystem 迁移（NolonUI 内聚）：新增 `libs/NolonUI/Sources/NolonUI/DesignSystem/Core`，落地 `DesignSystem` 核心 token（Colors/Metrics/Typography/Animations/Transitions）；`ProviderSidebarStyle` 默认值改为回落到 `NolonUI.DesignSystem`，Sidebar 行与分组标题字体改为使用 `DesignSystem.Typography`。
- 验证：`swift test --package-path libs/NolonUI`（4 tests）通过；`xcodebuild -project nolon.xcodeproj -scheme nolon-app -destination 'platform=macOS,arch=arm64' build` 通过。
