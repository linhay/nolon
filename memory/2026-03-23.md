- 完成资源中心卡片抽离到 NolonUI：新增 ResourceSkillCardView / ResourceWorkflowCardView / ResourceMcpCardView 与 ResourceCardScaffold；主工程 RemoteSkillCardView、RemoteWorkflowCardView、RemoteMCPCardView 改为 wrapper。
- 保留原有行为：安装弹窗选择 provider、Finder reveal、删除、MCP copy command、debug marker（context menu + debugCardLocator）。
- 回归结果：`swift test --package-path libs/NolonUI` 通过（23 tests）；`xcodebuild ... -only-testing:nolonTests/ProviderContentTabViewVisibilityTests -only-testing:nolonTests/RemoteRepositorySidebarViewModelTests test` 通过。
- Skill 详情页框架抽离到 NolonUI：新增 SkillDetailScaffold + SkillDetailSidebarContainer，主工程 SkillDetailView / SkillDetailSidebar 改为桥接调用；业务 section 与 viewModel 仍留在 app 层。
- 回归：NolonUI 单测 24 项通过；主工程定向测试（ProviderContentTabViewVisibilityTests + RemoteRepositorySidebarViewModelTests）通过。
- SkillDetailScaffold 底层布局从手写 HStack 改为 NolonUI ThreeColumnScaffold(twoColumn)；通过 sidebarWidth(min/ideal/max=280) 固定侧栏宽度，保留关闭按钮、背景、忽略安全区行为。
- SkillDetailSidebarContainer 去除固定宽度约束，避免与 NavigationSplitView 列宽双重约束。
- 回归结果：`swift test --package-path libs/NolonUI` 通过；`xcodebuild ... -only-testing:nolonTests/ProviderContentTabViewVisibilityTests -only-testing:nolonTests/RemoteRepositorySidebarViewModelTests test` 通过。

## 2026-03-23 通用关闭按钮抽离
- 将 `ResourceCenterCloseButton` 底层实现抽离为 NolonUI 通用组件 `FloatingCloseButton`。
- `SkillDetailScaffold` 右上角关闭按钮改为复用 `FloatingCloseButton`，并保持 `.cancelAction` 快捷键行为。
- 回归验证：`swift test --package-path libs/NolonUI` 通过；`ProviderContentTabViewVisibilityTests`、`RemoteRepositorySidebarViewModelTests` 通过。

## 2026-03-23 Skill Detail 独立窗口改造
- 将 Skill Detail 从 sheet 改为独立窗口（Window + openWindow），统一窗口 ID: `skill-detail`。
- 新增 `SkillDetailWindowCoordinator` 作为本地/远程 skill 详情窗口桥接，支持复用 `SkillDetailView`。
- Provider 详情页与资源中心技能卡片点击均改为打开独立窗口；workflow/mcp 详情仍保留 sheet。
- 验证通过：SkillDetailWindowCoordinatorTests、ProviderContentTabViewVisibilityTests、RemoteRepositorySidebarViewModelTests。

## 2026-03-23 ResourceCenter 独立窗口化
- 资源中心从 MainSplit overlay / ProviderDetail sheet 统一切换为独立窗口 `resource-center`。
- 新增 `ResourceCenterWindowCoordinator` 作为窗口上下文桥接；MainSplit 与 ProviderDetail 统一通过 `openWindow` 打开。
- 新增/更新测试：`ResourceCenterWindowCoordinatorTests`、`MainSplitViewModelTests`（窗口 ID 稳定性）。
- 定向回归通过：`ResourceCenterWindowCoordinatorTests`、`SkillDetailWindowCoordinatorTests`、`ProviderContentTabViewVisibilityTests`、`RemoteRepositorySidebarViewModelTests`、`MainSplitViewModelTests`。
