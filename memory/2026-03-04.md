# 2026-03-04

## 资源中心删除能力落地
- 完成范围：资源中心 Skills / Workflows / MCP Installed 卡片菜单支持删除；删除目标支持单 Provider 与“全部 Provider + 全局缓存”。
- 删除策略：批量删除采用部分成功语义，汇总失败项并回传 UI。
- 可维护性：删除编排逻辑抽到 `ResourceDeletionCoordinator`，视图只负责触发与结果呈现。

## 测试与验证
- 新增测试：`nolonTests/ResourceDeletionCoordinatorTests.swift`。
- 回归命令：
  - `xcodebuild -project nolon.xcodeproj -scheme nolon-app -configuration Debug test -only-testing:nolonTests/ResourceDeletionCoordinatorTests`
- 结果：3/3 通过。

## 交付证据
- 功能文档：`docs-dev/features/resource-center-delete-and-text-selection-2026-03-04.md`
- 实现文档：`docs-dev/dev/resource-center-delete-implementation-2026-03-04.md`
- 截图目录：`screenshots/20260304/resource-center/`

## 风险结论
- 当前会话中系统事件驱动 UI 自动化存在不稳定，已保留可复现的流程截图；如需严格“逐按钮级”截图，建议在本机前台交互会话复拍。

## XcodeMCPKit 启动按钮实现
- 在 Plugin Management 增加 XcodeMCPKit 运行控制（Start/Stop/Retry Start）。
- 新增 `XcodeMCPKitRuntimeService`，采用会话内守护进程模型，不做系统级持久保活。
- 运行时实际拉起 `xcode-mcp-proxy-server`（而非 `xcode-mcp-proxy`，后者为 STDIO 适配器）。
- 状态同步通过 `onStateChange` 回传到 `PluginManagementViewModel`。
- 测试：`PluginManagementViewModelTests` 增加 runtime 相关用例并通过。

## Codex 账号与用量刷新交互修复
- 关键决策：将卡片刷新态从“整段刷新流程”收敛为“仅覆盖 fetch 阶段”，避免请求结束后仍显示转圈。
- 关键决策：头部刷新加入会话 ID 防抖，防止“旧任务收尾”覆盖“新任务状态”。
- 交互行为：头部 `刷新` 支持刷新中再次点击中断；中断后可立即再次触发刷新。

## 验证结果
- 定向测试：
  - `ProviderUsageViewModelManualRefreshTests.testBDD_GivenAccountRefreshCompletes_WhenRefreshingCodexAccount_ThenRefreshingStateClears`
  - `ProviderUsageViewModelManualRefreshTests.testBDD_GivenHeaderRefreshInProgress_WhenTappingRefreshAgain_ThenCanInterruptAndRefreshAgain`
- 回归结果：`ProviderUsageViewModelManualRefreshTests` 5/5 通过。

## 关联文档
- `docs-dev/features/codex-account-usage-refresh-interrupt-2026-03-04.md`

## CLI Plugin 子命令落地（先 CLI 后 App）
- 按用户要求优先实现 `nolon plugin`，不再把 `cli mcp` 作为插件管理入口。
- 新增子命令：`list/status/install/upgrade/uninstall/start/stop`，首期插件白名单为 `xcodemcpkit`。
- install/upgrade/uninstall 通过 MCP server 配置变更（`xcode` -> `xcode-mcp-proxy`）与 `installed_version.txt` 管理实现。
- 默认离线化 release 检查（`NOLON_PLUGIN_CHECK_LATEST=1` 才联网），避免 CLI 在网络异常时阻塞。
- 修复 `plugin status` 卡死：`ps` 管道读取改为先读后 wait，避免 wait-before-read 死锁。

## CLI 验证结果（2026-03-04）
- 回归测试：`swift test --package-path libs/Providers --filter NolonCoreCLIKitTests --parallel` 通过。
- 流程实测（临时 `NOLON_HOME`）：
  - install: `v0.3.6` -> status 显示已安装
  - upgrade: `v0.3.7` -> status 版本更新
  - uninstall: 移除后 status 显示未安装

## CLI Plugin 深度回归（继续测试）
- 覆盖 JSON 输出契约、start/stop 幂等、offline/online latest 分支。
- 结果：
  - start 二次调用返回 already running（同 PID）；stop 二次调用返回 not running。
  - `NOLON_PLUGIN_CHECK_LATEST=0` 下 upgrade 无 `--to-version` 保持现版本不变。
  - `NOLON_PLUGIN_CHECK_LATEST=1` 下 status 可返回 `latest_version`。

## CLI Plugin 全局化重构（资源中心-全局）
- 决策更新：plugin install/upgrade/uninstall 不再自动写当前 provider MCP；统一写 `~/.nolon/mcps/<plugin>.json`。
- 卸载保护：仅允许删除带 `nolon_plugin.managed=true` 且 `plugin_id` 匹配的条目；否则返回 `plugin_not_managed_by_nolon`。
- 扩展策略：plugin 能力改为 capability 模型（`mcp_global_install` / `runtime_control`），满足“插件不一定是 MCP”。
- 风险修复：修复 uninstall 运行态检测误杀 CLI 自身进程（先 marker 校验，且 PID 匹配排除当前进程并只匹配可执行命令）。
- 验证：NolonCoreCLIKitTests 全绿；源码 CLI 安装/升级/卸载与拒删流程实测通过。

## CLI Plugin 运行态修复补充
- 修复：`plugin stop` 在发 SIGTERM/SIGKILL 后加入短轮询等待，降低 stop 后立即重试的时序误判。
- 修复：运行态检测新增 shell 脚本进程匹配分支，避免二次 start 重复拉起。
- 验证：`NolonCoreCLIKitTests` 197 项全通过，CLI 深测确认 second start=already running。

## CLI Plugin 异常 JSON 行为统一
- 修复：全局 MCP JSON 损坏时，不再冒泡 execution_failed；统一判定为非 plugin 管理条目。
- 行为：`plugin uninstall` 返回 `plugin_not_managed_by_nolon`，且不删除文件。
- 验证：NolonCoreCLIKitTests 198/198 通过，CLI 负向实测一致。

## App 侧插件运行与内置 MCP 对齐
- 将内置 xcodemcpkit MCP 配置命令改为 `xcode-mcp-server`，与 CLI 全局写入语义保持一致。
- 将 Plugin Runtime 启动命令改为 `xcodemcpkit`，避免继续依赖旧二进制名 `xcode-mcp-proxy-server`。
- 定向测试通过：ResourceCatalogGridViewModelTests + PluginManagementViewModelTests（8/8）。

## App 插件安装状态判定修正
- 按用户决策落地：插件安装状态不再依赖 `installed_version.txt`，改为读取二进制 `xcodemcpkit` 是否存在。
- 版本来源仍读取 `installed_version.txt`，但仅在 `isInstalled=true` 时显示，避免“未安装但显示版本”。
- Plugin 卡片新增 `Status: Installed / Not Installed` 文案，并补齐 `Localizable.xcstrings` key。
- 回归验证：`PluginManagementViewModelTests` 新增二进制存在/缺失组合用例，定向测试通过（12/12）。

## App 运行控制与安装状态联动修复
- 问题：插件未安装时，卡片仍显示 `Stopped` 与 `Start`，造成误导。
- 原因：运行控制仅依赖 `runtimeState`，未约束 `isInstalled`。
- 修复：未安装时 runtime 文案改为 `Not Installed`，按钮文案为 `Install First` 且禁用点击。
- 验证：新增 `testRuntimeControls_WhenPluginNotInstalled_DisabledAndShowsInstallRequired`，`PluginManagementViewModelTests` 全通过（10/10）。

## App 插件未安装主按钮改为可安装
- 用户确认：未安装状态主按钮应执行下载安装。
- 调整：主按钮文案改为 `Install`，未安装时按钮保持可点击。
- 行为升级：点击后不再跳浏览器，改为应用内下载 GitHub release 包并安装到 `~/.nolon/bin`。
- 兼容策略：release 内旧二进制名自动映射为新命名（`xcode-mcp-proxy-server -> xcodemcpkit`、`xcode-mcp-proxy -> xcode-mcp-server`）。
- 安装后写回：`installed_version.txt` + `~/.nolon/mcps/xcodemcpkit.json`，并立即刷新插件状态。
- 验证：新增 `testInstallPlugin_WhenNotInstalled_InvokesInstallerAndRefreshesInstalledState`，`PluginManagementViewModelTests` 11/11 通过。

## App 安装服务集成测试补齐
- 新增 `XcodeMCPKitInstallServiceTests`，覆盖“release API + tar 包下载 + 解压 + 二进制落盘 + 元数据写回”完整路径。
- 测试输入使用 mock URLProtocol（不依赖外网），但执行真实 tar 解压与本地文件权限校验。
- 验证点：
  - `~/.nolon/bin/xcodemcpkit` 与 `~/.nolon/bin/xcode-mcp-server` 可执行
  - `installed_version.txt` 写入目标版本
  - `~/.nolon/mcps/xcodemcpkit.json` command 为 `xcode-mcp-server`
- 定向回归：`PluginManagementViewModelTests + XcodeMCPKitInstallServiceTests` 全通过。

## App 安装失败分支测试补齐
- `XcodeMCPKitInstallServiceTests` 新增：
  - 无稳定 release -> `releaseNotFound`
  - 无兼容 macOS 资产 -> `assetNotFound`
- `PluginManagementViewModelTests` 新增：
  - 安装失败时写入 `errorMessage`，并恢复 `isInstalling=false`，按钮文案回到 `Install`
- 回归：两组测试共 14 条全部通过。

## App 安装动作幂等与安装中态补齐
- `PluginManagementViewModelTests` 新增：
  - 已安装时点击 Install 不应再次调用安装器
  - 安装进行中按钮应显示 `Installing...` 且不可点击
- 结果：`PluginManagementViewModelTests` 扩展至 14 条并全部通过。

## App 安装服务网络与损坏包健壮性补齐
- 安装服务增强：GitHub API 请求补 `Accept/User-Agent`，并对 HTTP 非 2xx 返回统一抛 `httpFailed(code)`。
- 新增 `XcodeMCPKitInstallServiceTests`：
  - release 请求 429 -> `httpFailed(429)`
  - 下载到损坏 tar 包 -> `extractionFailed`
- 回归结果：`XcodeMCPKitInstallServiceTests` 5/5 + `PluginManagementViewModelTests` 14/14 全通过。

## App 升级流程（应用内）落地
- `Upgrade` 按钮改为应用内执行，不再跳转 release 页面。
- 升级实现复用安装服务：`upgradePlugin()` 调用 `installLatest()`，成功后 reload 并刷新 hasUpgrade 状态。
- UI 状态补齐：新增 `isUpgrading` 与 `Upgrading...` 文案，升级中禁用按钮。
- 测试新增：
  - 有升级时可成功升级并清除 hasUpgrade
  - 无升级时不触发安装器
  - 升级失败时写入错误并恢复状态
- 结果：`PluginManagementViewModelTests` 扩展至 17 条并全部通过。

## Runtime 崩溃修复（task already launched）
- 现象：`NSInvalidArgumentException: task already launched`，堆栈指向 `XcodeMCPKitRuntimeService.handleTermination`.
- 根因：Process 已启动后，在终止回调中再次写 `standardOutput/standardError`（Foundation 不允许）。
- 修复：移除 `handleTermination` 中对 `process.standardOutput/standardError` 的重新赋值。
- 验证：`PluginManagementViewModelTests` + `XcodeMCPKitInstallServiceTests` 全通过。

## SKProcessRunner 接入与依赖回刷（2026-03-04）
- 完成上游 `SKProcessRunner` 改造并推送：新增 `SKProcessPipeSession.isRunning()` / `sendSignal(_:)`，提交 `1512ca7`。
- nolon app runtime 服务已从 `Process` 迁移到 `SKProcessPipeSession`，修复 `task already launched` 生命周期问题。
- 增加 `XcodeMCPKitRuntimeServiceTests` 覆盖：binary missing、start/stop、non-zero exit。
- 依赖已回刷：`libs/Providers/Package.swift` 与 `Package.resolved` 均指向 `1512ca7`。
- CLI 插件流程冒烟结论：text 模式 install/start/stop/upgrade/uninstall 可执行；`--json` 对 plugin 子命令仍返回 unsupported parsed command（已记录为后续问题）。

## CLI plugin --json 路由修复（2026-03-04）
- 修复 `nolon --json plugin ...` 误路由到 codex executor 的问题。
- 触发条件：`--json` 位于首参数时，旧逻辑把 root 识别为 `--json`。
- 修复点：`NolonCLIEntrypoint.shouldRouteToCoreCLI` 先剔除 `--json` 再识别 root。
- 验证：新增两条入口测试（前置/后置 --json），并实测 `plugin status/install/uninstall` 的 `--json` 输出正常。

## 全量回归修复收口（2026-03-04）
- CLI `swift test` 全量中发现并修复 3 类问题：
  1) `CodexLoginRunner` 在高并发回归下偶发超时/URL 捕获抖动：增加登录 URL 短暂补采样，放宽脆弱时序断言阈值。
  2) `CodexAppServerKit` 方法枚举与生成 schema 漂移：移除 schema 未声明的枚举项，恢复枚举一致性测试。
  3) `NolonCoreCLIRunner` 仍直接使用 `Process`：改为 `SKProcessRunner`，并将 runtime 进程探测改为 `pgrep -f` 优先（回退 `ps`），修复 plugin start 幂等判断。
- 回归结果：
  - `libs/Providers` 全量 `swift test` 618/618 通过。
  - app 侧 `xcodebuild -project nolon.xcodeproj -scheme nolon-app -configuration Debug test` 成功返回。
