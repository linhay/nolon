- Codex 多账号删除行为修复：`ProviderUsageViewModel.confirmDeleteCodexAccount` 删除成功后不再调用 `load()`，改为 `reloadCodexFromDisk(refreshUsage: false)`，避免删除“其他账号”时触发当前账号刷新与快照回写。
- 事件抑制补强：删除前对目标账号 auth 文件打短窗口抑制（复用 `CodexAuthChangeSuppressionStore`），减少 watcher 对 `deleted` 事件造成的二次刷新。
- 回归测试新增：`ProviderUsageViewModelDeleteTests.testBDD_GivenDeletingNonActiveAccount_WhenConfirmDeleteWithoutHook_ThenDoesNotRefreshRemainingSnapshot`，验证删除非激活账号后剩余账号快照内容不被刷新链路改写。
- 验证：`xcodebuild test -quiet -project nolon.xcodeproj -scheme nolon-app -destination 'platform=macOS' -only-testing:nolonTests/ProviderUsageViewModelDeleteTests` 通过。
- 登录态收口修复：`runAppServerLoginFlow` / `runCLILoginFlow` 在 `recordCLILoginSnapshot` 成功后不再 `await load()`，改为 `schedulePostLoginReload()` 后台刷新，避免 `load()` 阻塞导致 UI 长时间停留“登录中”。
- 事实确认：当前对 `cli-login-home/{provider}/auth.json` 采用轮询（`CodexLoginRunner.awaitAuthResult*`），不是文件 watcher；轮询间隔 0.2~0.25s，超时默认 10 分钟（调用侧）。
- 验证：`xcodebuild test -quiet -project nolon.xcodeproj -scheme nolon-app -destination 'platform=macOS' -only-testing:nolonTests/ProviderUsageViewModelCLILoginTests` 通过。
- Codex runtime-home skills 收敛：新增共享模板目录 `~/.nolon/codex/runtime-skills-template`，并把 `runtime-home/<account-id>/skills` 统一改为软链接到模板（整目录共享）。
- 策略确认：对已有账号 runtime-home 的 `skills` 采用“懒迁移 + 强制删除后替换”为软链接，不做备份。
- 代码接入点：
  - `CodexAuthManager.ensureRuntimeSkillsSymlink(accountID:)`
  - `CodexAuthRuntimeCoordinator.activateAccountInRuntime(...)`
  - `NolonLiveCodexCLIService.refreshUsageCaches(...)`
  - `NolonLiveCodexCLIService.liveAuthRefreshRunner(...)`
- 新增测试：
  - `CodexAuthManagerTests`（3 条 runtime skills 链接用例）
  - `CodexAuthRuntimeCoordinatorTests.activateRuntimePreparesRuntimeSkillsBeforeSwitch`
  - `NolonCodexCLIServiceTests.authUsageRefreshWritesBackPerAccount` 增加 runtime skills 链接断言
- 验证结果：
  - `swift test --package-path libs/Providers --filter CodexAuthManagerTests` 通过；
  - `swift test --package-path libs/Providers --filter CodexAuthRuntimeCoordinatorTests` 通过；
  - `swift test --package-path libs/Providers --filter NolonCodexCLIServiceTests` 通过；
  - `swift test --package-path libs/Providers` 在本地 live 尾段出现挂起，人工终止，未形成一次完整全量收敛。
- 登录命中策略收敛：`upsertAccountFromCLILogin` 改为“同 email 必须 account_id 精确命中才更新，否则新建快照”；`preferredAccountID` 仍保持最高优先级。
- 字段白名单收敛：`CodexAuthSummary` 的 email/account_id/api key 读取改为
  - `email`: `email` > `nolon.account.email`
  - `account_id`: `tokens.account_id` > `nolon.account.id`
  - `api key`: 仅 `OPENAI_API_KEY`
  并移除 `user/profile/JWT/access_token` 等兜底来源。
- 快照命名自愈升级：`alignSnapshotFileNamesWithEmailIfNeeded` 改为对齐 `email(account_id).json`，使用安全字符清洗并保留括号格式。
- 匹配顺序微调：`matchAccount` 优先级调整为 `account_id -> OPENAI_API_KEY -> email -> cleaned JSON`。
- 新增/调整测试：
  - `CodexAuthManagerTests.recordCLILoginSnapshotCreatesNewWhenEmailMatchesButAccountIDDiffers`
  - `CodexAuthManagerTests.recordCLILoginSnapshotOverwritesWhenEmailAndAccountIDMatch`
  - `CodexAuthManagerTests.loadAccountsAlignsSnapshotFileNameWithEmailAndAccountID`
  - `NolonCodexCLIServiceTests` 夹具 auth payload 统一改为顶层 `email` 字段。
- 验证结果：
  - `swift test --package-path libs/Providers --filter CodexAuthManagerTests` 通过（26 tests）
  - `swift test --package-path libs/Providers --filter NolonCodexCLIServiceTests` 通过（17 tests）
  - `xcodebuild test -quiet -project nolon.xcodeproj -scheme nolon-app -destination 'platform=macOS' -only-testing:nolonTests/CodexAuthServiceTests` 通过（带既有 Swift 6 actor-isolated 警告）。
- 登录事件联动根因修复：`CodexLoginRunner.awaitAuthResultPreferFile` 之前使用 `withThrowingTaskGroup` 并发等待 completion waiter；当 waiter 不响应取消时，`auth.json` 已生成也会阻塞退出，导致 UI 长时间停留“登录中”。现改为独立 `Task` 承载 waiter，主流程仅等待文件结果并在返回时取消 waiter，不再阻塞。
- 新增回归测试：`CodexLoginRunnerTests.awaitAuthResultPreferFileDoesNotBlockOnNonCancellableCompletionWaiter`，验证 completion waiter 忽略取消时，只要 `auth.json` 就绪即可快速返回。
- 验证：`swift test --package-path libs/Providers --filter CodexLoginRunnerTests` 通过（12 tests）；`xcodebuild test -quiet -project nolon.xcodeproj -scheme nolon-app -destination 'platform=macOS' -only-testing:nolonTests/CodexAuthServiceTests` 通过。
- QoS 优先级反转修复（登录/运行时链路）：将 Codex runtime 路径上的可执行文件解析从同步 `resolveExecutableInUserShellSync` 切到异步 `resolveExecutableInUserShell`，避免在 user-initiated 任务内阻塞等待低 QoS 线程。
- 代码调整：
  - `CodexCommandExecutor` 新增 `resolveExecutableAsync()`（保持原同步 API 不变）。
  - `CodexRuntimeSupport.withRuntimeService` 改为使用 `resolvedBinaryAsync(...)`。
  - `CodexRuntimeAccountSwitcher.serviceFor` 改为 await 异步解析。
- 新增测试：`CodexCLIKitEnvironmentTests` 增加 2 条 async resolver 用例（env override / explicit path）。
- 验证：
  - `swift test --package-path libs/Providers --filter CodexCLIKitEnvironmentTests` 通过（7 tests）。
  - `swift test --package-path libs/Providers --filter CodexRuntimeAccountSwitcherTests` 通过（2 tests）。
  - `xcodebuild ... CodexAuthServiceTests` 本机失败原因为测试 bundle 签名（Command CodeSign failed），非逻辑失败。
- 结论校正："无 email 是没执行用量刷新" 只对一半。用量刷新可拿到 identity.email，但此前仅更新内存摘要，不会回写 auth 快照文件，因此快照命名自愈（依赖文件内 email/account_id）仍可能跳过并保留 `account.json`。
- 修复：新增 `CodexAuthManager.backfillEmailIfMissing(for:email:)`，当用量刷新拿到 email 时回写到 `email` 与 `nolon.account.email`（仅缺失时写入）。
- 接入点：
  - app 端 `ProviderUsageViewModel.applyRefreshedCodexOutcome` 成功分支。
  - CLI 端 `NolonLiveCodexCLIService.refreshUsageCaches` 成功分支。
- 新增测试：`CodexAuthManagerTests.backfillEmailEnablesEmailAccountIDAlignment`，验证从 `auth/account.json`（无 email）回填后，`loadAccounts` 可自愈重命名为 `email(account_id).json`。
- 验证：
  - `swift test --package-path libs/Providers --filter backfillEmailEnablesEmailAccountIDAlignment` 通过。
  - `swift test --package-path libs/Providers --filter NolonCodexCLIServiceTests` 通过（17 tests）。
- 登录后即时自愈增强：`ProviderUsageViewModel` 的 CLI/AppServer 登录流在 `recordCLILoginSnapshot` 后，改为先执行 `backfillLoginSnapshotEmailIfMissing(account:)`（单账号即时探测，拿到 identity.email 即回写快照），再 `load()`；不再依赖“下一次用量刷新”触发 email 回填。
- 影响：无 email 的新快照在登录后的首轮 reload 就可命中 `alignSnapshotFileNamesWithEmailIfNeeded`，从 `account.json` 自愈为 `email(account_id).json`（若探测成功拿到 email）。
- 验证：
  - `xcodebuild test -quiet -project nolon.xcodeproj -scheme nolon-app -destination 'platform=macOS' -only-testing:nolonTests/CodexAuthServiceTests` 通过（保留既有 actor-isolated warnings）。
  - `swift test --package-path libs/Providers --filter NolonCodexCLIServiceTests` 通过（17 tests）。
- 按需求补全“登录即刷新用量”：`schedulePostLoginReload` 现为三步串行：
  1) 登录后立即单账号探测 usage（`probeLoginSnapshotUsageAndBackfillEmailIfMissing`）并回填 email；
  2) 执行 `load()` 触发文件命名自愈；
  3) 将刚才探测得到的 usage outcome 直接应用到该账号（`applyRefreshedCodexOutcome`），确保登录完成当次就有最新用量状态/缓存，不等下一次刷新。
- 验证：
  - `xcodebuild test -quiet -project nolon.xcodeproj -scheme nolon-app -destination 'platform=macOS' -only-testing:nolonTests/CodexAuthServiceTests` 通过（仅既有 warning）。
  - `swift test --package-path libs/Providers --filter CodexAuthManagerTests` 通过（27 tests）。
- 登录后卡片仍未更新的补救：`schedulePostLoginReload` 改为在 `load()` 完成后，定位到该登录账号并强制执行 `refreshCodexAccountOutcome` 一次，绕过 `shouldSkipRefresh` 条件，确保登录当次就刷新卡片用量。
- 验证：
  - `xcodebuild test -quiet -project nolon.xcodeproj -scheme nolon-app -destination 'platform=macOS' -only-testing:nolonTests/CodexAuthServiceTests` 通过（既有 warning）。
  - `swift test --package-path libs/Providers --filter NolonCodexCLIServiceTests` 通过（17 tests）。

- 修复 Codex 账号卡片删除失败：`CodexAuthManager.deleteAccount` 增加 provider 感知清理，删除快照时同步清理命中的 provider auth symlink/regular file，并刷新 active fingerprint，避免删除后回流。
- 修复 UI 幽灵卡片：`ProviderUsageViewModel.reorderCodexAccountOutcomesForDisplay` 只保留当前 `codexAccounts` 的 outcome，丢弃 stale outcome；`requestDeleteCodexAccount` 对不存在快照 id 的卡片做本地清理。
- 新增测试：`CodexAuthManagerTests.deleteAccountRemovesProviderAuthSymlinkForDeletedActiveAccount`、`ProviderUsageViewModelOutcomeOrderingTests.testBDD_GivenStaleOutcomeWithoutSnapshot_WhenReordering_ThenDropsStaleOutcomeCard`。

- 将 Codex auth 监听抑制从时间窗口模式升级为操作级精确抑制：新增 `CodexAuthSuppressionKind` 与 `markOperation/consumeSuppression`，按事件类型匹配并在首个命中后消费，避免误抑制后续真实事件。
- ProviderUsageViewModel 接入新抑制：删除操作注册 `.deleted` 抑制；watcher 收到事件时按 kind 精确消费。
- 新增 watcher reload 单飞队列：`enqueueCodexReload` 合并多次文件事件，仅串行执行 `reloadCodexFromDisk`，减少重复 reload 与竞态。
- 新增测试覆盖：kind mismatch 不抑制、同操作只抑制一次、文件夹级子路径删除可命中抑制。

- 修复删除后仅收到 renamed 事件导致卡片残留：`CodexAuthEventPolicy.shouldIgnoreKnownAuthRename` 调整为仅在 auth 目录外（如 Trash）才可忽略已知文件重命名；auth 目录内 renamed 一律不忽略，确保触发 reload。
- 补充双端测试：ProviderTests 与 nolonTests 均新增“known rename inside auth folder -> not ignored”。
- auth 事件联动收口：`UsageMonitorFileWatcher` 移除内部 debounce，改为文件夹事件直通；UI 侧统一使用 `Combine debounce(300ms)` 聚合后触发 `enqueueCodexReload`，避免双层抑制带来的不可预测行为。
- 抑制模式调整：`ProviderUsageViewModel` 下线 auth suppression mark 写入/消费路径，保留“cache write / refresh in-flight”运行时防重入过滤，减少误吞真实删除事件。
- 新增 BDD 回归：`ProviderUsageViewModelAuthSignalAggregationTests` 覆盖“突发事件合并为一次 reload”和“跨窗口事件分别 reload”。
- 验证结果：
  - `xcodebuild test ... -only-testing:nolonTests/ProviderUsageViewModelAuthSignalAggregationTests` 通过（2 tests）。
  - `xcodebuild test ... -only-testing:nolonTests/ProviderUsageViewModelDeleteTests -only-testing:nolonTests/ProviderUsageViewModelOutcomeOrderingTests -only-testing:nolonTests/CodexAuthEventPolicyTests` 通过。
  - `swift test --package-path libs/Providers --filter CodexAuthEventPolicyTests` 通过。
  - `swift test --package-path libs/Providers --filter CodexAuthManagerTests` 通过（28 tests）。
- Codex 账号卡片时间文案收敛：登录时间与同步时间合并同一行，展示为 `登录于yyyy/MM/dd HH · 同步于 xhxxmxxs 之前`；当任一字段缺失时只显示已有段。
- 新增 `CodexAccountInlineTimeFormatter`：
  - `loginTimestamp` 固定格式 `yyyy/MM/dd HH`（24h）
  - `syncAgoDuration` 格式 `hMMmSSs`（仅 m/s 补零）
  - `joinInlineTimeLine` 负责单段/双段拼接与分隔符处理。
- 本地化新增：
  - `codex.accounts.time.login.inline`
  - `codex.accounts.time.sync.inline`
- 新增测试：`ProviderUsageInlineTimeFormatterTests`（6 条）覆盖日期格式、相对时长边界、未来时间钳制、单/双段拼接。
- 验证：
  - `xcodebuild test -quiet -project nolon.xcodeproj -scheme nolon-app -destination 'platform=macOS' -only-testing:nolonTests/ProviderUsageInlineTimeFormatterTests -only-testing:nolonTests/CodexUsageTabPresentationTests` 通过。
- 卡片时间文案优化：去掉 `0h00m00s` 机械串，改为“刚刚同步 / Xm 前 / XhYm 前 / MM/dd HH:mm”四段规则；登录时间改为短格式 `登录 MM/dd HH:mm`。
- UI 细节：时间行保持单行并右对齐（trailing），避免卡片底部视觉噪音。
- 本地化新增：
  - `codex.accounts.time.sync.just_now`
  - `codex.accounts.time.sync.absolute`
  并更新 `codex.accounts.time.sync.inline` 中文为“同步于 %@ 前”。
- 新增/调整测试：`ProviderUsageInlineTimeFormatterTests` 扩展为 8 条，覆盖 just-now、分钟/小时相对文案、超过 24h 绝对文案、未来时间钳制与拼接行为。
- 验证：
  - `xcodebuild test -quiet -project nolon.xcodeproj -scheme nolon-app -destination 'platform=macOS' -only-testing:nolonTests/ProviderUsageInlineTimeFormatterTests` 通过。

## 15:27 新增参考项目 XcodeMCPKit
- 行动：将 `https://github.com/lynnswap/XcodeMCPKit` 克隆到 `reference-projects/XcodeMCPKit`。
- 验证：`git -C reference-projects/XcodeMCPKit remote -v` 指向目标仓库；当前分支 `main`，HEAD `f6d5215`。

## 15:56 新增代码协议文档
- 行动：新增 `docs-dev/dev/code-protocol.md`，固化 Nolon 代码边界、BDD/TDD 流程、测试门禁、提交发布与 DoD。
- 约束：协议适用于 `nolon/` 与 `libs/Providers/`，并要求文档与记忆同步更新。

## 16:47 新增 GPL-3.0 许可证
- 行动：新增根目录 `LICENSE`（GNU GPL v3.0 正文），并在 `README.md` 与 `README_ZH.md` 增加许可证声明段落。
- 结果：仓库具备项目级开源许可证文件与双语入口说明，后续发布可被平台识别许可证信息。

## 17:48 Swift Testing 截图测试接入
- 行动：`nolonTests` 接入 `pointfreeco/swift-snapshot-testing`，新增 `ToastViewSnapshotTests`（Swift Testing 套件）与基线快照。
- 代码：
  - `nolon.xcodeproj/project.pbxproj` 增加 `swift-snapshot-testing` 依赖并绑定 `nolonTests` target。
  - 新增 `nolonTests/ToastViewSnapshotTests.swift` 与 `nolonTests/__Snapshots__/ToastViewSnapshotTests/*.png`。
  - 新增 `docs-dev/dev/swift-testing-snapshot-testing.md` 说明运行方式。
- 验证：`xcodebuild test -quiet -project nolon.xcodeproj -scheme nolon-app -destination 'platform=macOS' -only-testing:nolonTests/ToastViewSnapshotTests` 通过。

## 18:12 快照基线路径规范化
- 调整：将截图基线目录从 SnapshotTesting 默认 `nolonTests/__Snapshots__/...` 迁移为显式规范路径 `nolonTests/Snapshots/ToastViewSnapshotTests/*.png`。
- 实现：`ToastViewSnapshotTests` 改用 `verifySnapshot(..., snapshotDirectory: ...)` 指定快照目录；清理默认目录空文件夹。
- 验证：`xcodebuild test -quiet -project nolon.xcodeproj -scheme nolon-app -destination 'platform=macOS' -only-testing:nolonTests/ToastViewSnapshotTests` 通过。

## 18:38 Token Trend 新增“亿”单位
- 需求：Token Trend 数值展示新增 `亿` 单位（>= 100,000,000）。
- 实现：
  - App 端新增 `TokenCountCompactFormatter`，并接入 `ProviderUsageView` 的 trend summary/table 格式化。
  - CLI 端 `NolonCodexCLIExecutor.formatTokensInMillions` 增加 `亿` 分支，用于 `codex auth usage-trend` summary。
- 测试：
  - 新增 `nolonTests/TokenCountCompactFormatterTests`（4 条，覆盖 raw/K/M/亿）。
  - 更新 `ProvidersTests/NolonCodexCLIEntrypointTests.routesAuthUsageTrendTable`，断言 `summary.today | 1.2亿`。
- 文档：新增 `docs-dev/features/codex-token-trend-yi-unit-2026-03-03.md`。
- 验证：
  - `swift test --package-path libs/Providers --filter NolonCodexCLIEntrypointTests/routesAuthUsageTrendTable` 通过。
  - `xcodebuild test -project nolon.xcodeproj -scheme nolon-app -destination 'platform=macOS' -only-testing:nolonTests/TokenCountCompactFormatterTests` 通过。
- 2026-03-03 18:50 +0800: 完成“插件管理”独立页面改造（从 Provider/MCP 子页解耦为主导航独立入口），内置 XcodeMCPKit 条目并接入 GitHub Releases 稳定版升级检测（忽略 pre-release），升级按钮可直达 release 页面；同步补充 SDK/App 测试与 docs-dev 文档；提交并推送 `67fabb7`。

## 20:10 可 Fork 独立榜单模板落地
- 决策：采用单仓自包含模板，fork 后可在 GitHub/GitLab 独立部署，不依赖中心 API。
- Submission 契约：移除 `platform`、移除 `rangeMode`；`points` 强制包含 `tool`。
- 实现：新增 `projects/leaderboard-template`，包含校验脚本、快照聚合脚本、静态站、GitHub/GitLab CI、工具别名映射。
- 风控：用户名正则校验；去重键为 `userId+date+tool`；超阈值 token（>100,000,000）不入榜。
- 测试：新增 Python 单元测试覆盖 validate/build snapshot 关键场景并通过。
- 2026-03-03 22:32 +0800: 完成 GitHub Pages 官网首版（docs 源）落地：中英双语单页、主控制台截图、7+ provider 口径、Codex 深度支持区块、projects/leaderboard-template 生态入口；提交 ee821e0 并推送 main。
