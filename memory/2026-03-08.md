# 2026-03-08

- 调整 Codex 账号卡片排序菜单交互：
  - 未选中的排序项不再显示方向箭头
  - 仅当前选中排序项在菜单文案中显示 `↑/↓`
  - 排序菜单项切换到其他排序项时，会恢复该项默认方向，不保留旧方向状态
- Codex 排序语义继续固定为：
  - `按剩余额度`：默认降序
  - `按到期时间`：按最早 `resetsAt`，默认升序
  - `按名称`：默认升序
  - `按 Xh/Xd 剩余比例`：默认降序
- 已验证：
  - `xcodebuild test -project nolon.xcodeproj -scheme nolon-app -destination 'platform=macOS' -only-testing:nolonTests/CodexAccountDisplaySectionsTests`
  - `xcodebuild test -project nolon.xcodeproj -scheme nolon-app -destination 'platform=macOS' -only-testing:nolonTests/CodexUsageTabPresentationTests`
- 修复 Codex reviewer 指出的两处交互回归：
  - 配置型卡片激活时，头部主按钮重新保留 `登录` 与 `导入`，同时继续提供 `编辑` / `验证`
  - 导入页“粘贴”不再只识别 `localhost:1455/success?...`，改为直接复用 Codex callback 解析器，接受合法的 loopback `/success` 与 `/auth/callback` URL
- 已验证 reviewer 修复：
  - `xcodebuild test -project nolon.xcodeproj -scheme nolon-app -destination 'platform=macOS' -only-testing:nolonTests/CodexUsageTabPresentationTests -only-testing:nolonTests/CodexAccountDisplaySectionsTests`
- 对齐 Codex upstream 的 `userID/account_id` 语义：
  - `CodexHTTPUsageQuery` 继续只从持久化的 `tokens.account_id` / 顶层 `account_id` 读取 `{{userID}}`
  - 不在 HTTP usage query 执行阶段从 JWT claims 临时回退推导
  - callback 导入阶段已负责把 JWT 里的 `chatgpt_account_id` 写入 `tokens.account_id`
- 已新增 Provider 回归测试：
  - `swift test --package-path libs/Providers --filter CodexHTTPUsageQueryExecutorTests`
- 接入 Gemini 官方 quota 读取链路：
  - 新增 `GeminiQuotaFetcher`，通过本机已安装 `gemini` npm 包内置的 `@google/gemini-cli-core` 调用 `retrieveUserQuota`
  - `GeminiUsageDescriptor` 对 OAuth 账号展示真实 `Pro/Flash` 剩余额度与 reset 时间；API key / Vertex 维持身份快照
  - `sourceMode == .auto` 时 quota 拉取失败回退为成功身份快照；`sourceMode == .cli` 时保留失败，供 `gemini auth doctor` 诊断
- 新增文档与测试：
  - `docs-dev/features/gemini-quota-usage-2026-03-08.md`
  - `GeminiQuotaFetcherTests`
  - `GeminiUsageDescriptorTests`
  - `NolonCoreCLIKitTests` Gemini doctor 断言更新为接受真实分类结果
- 已验证：
  - `swift test --package-path libs/Providers --filter GeminiUsageDescriptorTests --filter GeminiQuotaFetcherTests`
  - `swift test --package-path libs/Providers --filter runnerRendersGeminiAuth --filter GeminiUsageDescriptorTests --filter GeminiQuotaFetcherTests`
- 修复 Gemini quota 真实账号解析失败：
  - 根因是 `@google/gemini-cli-core` 在 stdout 的 JSON 前额外输出 `Loaded cached credentials.`，导致 `GeminiQuotaFetcher` 直接整段 JSON decode 失败
  - 现改为先从混合 stdout 中提取 JSON 对象再解码，并补了前缀日志污染的回归测试
- 已再次验证真实链路：
  - `swift run --package-path libs/Providers nolon gemini auth usage --provider gemini` => `primary=12% secondary=15%`
  - `swift run --package-path libs/Providers nolon gemini auth doctor --provider gemini` => `healthy: yes`
- Gemini Usage 卡片改为支持全量模型窗口展示：
  - `UsageSnapshot` 新增具名 `windows` 列表，兼容保留 `primary/secondary/tertiary`
  - Gemini OAuth quota 现在把全部 bucket 映射为窗口，标题直接使用 `modelId`
  - App 侧 Usage 卡片与快照视图优先渲染 `windows`，因此会展示 `flash-lite / flash / pro / preview / vertex` 等完整额度项
  - CLI `nolon gemini auth usage --provider gemini` 也同步输出逐模型窗口列表
- 已验证全量展示改动：
  - `swift test --package-path libs/Providers --filter GeminiQuotaFetcherTests --filter GeminiUsageDescriptorTests`
  - `xcodebuild test -project nolon.xcodeproj -scheme nolon-app -destination 'platform=macOS' -only-testing:nolonTests/CodexUsageTabPresentationTests`
  - `swift run --package-path libs/Providers nolon gemini auth usage --provider gemini`
- Gemini 历史 token 消耗实现改为两段式：
  - 先把 Codex 的历史 token 消耗 UI 抽成通用 `ProviderTokenTrendSection`
  - 再让 Gemini 复用同一组件展示基于 session 文件聚合出来的日维度 token 趋势
- Gemini 历史 token 数据源对齐 upstream `gemini-cli` 的本地 session 记录：
  - 从账号 runtime home 下的 `tmp/**/chats/session-*.json` 读取 `type == "gemini"` 消息
  - 聚合 `tokens.input/output/cached/total` 为 `ProviderTokenTrendSnapshot`
  - 支持 `7D / 30D / ALL` 视图范围，view model 切换范围时会立即刷新
- 本次新增/调整的关键实现：
  - `ProviderTokenTrendModels` 提供通用趋势点/快照模型，Codex 旧模型改为 typealias 兼容
  - `GeminiTokenTrendService` 负责 active Gemini 账号的 session token 聚合
  - `ProviderUsageViewModel` 的 codex 专属趋势状态改为 provider 通用趋势状态
  - `ProviderUsageView` / `ProviderTokenTrendSection` 统一承载 Codex 与 Gemini 的历史 token 消耗视图
- 已验证通用趋势改造：
  - `swift test --package-path libs/Providers --filter GeminiTokenTrendServiceTests`
  - `xcodebuild test -project nolon.xcodeproj -scheme nolon-app -destination 'platform=macOS' -only-testing:nolonTests/CodexUsageTabPresentationTests`
- 继续补齐 Gemini 历史 token 收尾：
  - `ProviderUsageViewModel` 新增 Gemini token trend 注入点与 `refreshTokenTrendForTesting()`，便于 app 层直接验证 Gemini provider 会按当前 range 刷新趋势快照
  - `nolon gemini auth usage --provider gemini` 现在同步输出 `token_trend`
    - JSON 模式：`token_trend.today_tokens / last_7_days_tokens / last_30_days_tokens / points`
    - text 模式：新增 `token_trend: today=... last7=... last30=...` 摘要行
- 新增测试：
  - `nolonTests/GeminiUsageTokenTrendViewModelTests`
  - `NolonCoreCLIKitTests` 增加 Gemini usage JSON/Text token trend 断言
- 已验证：
  - `swift test --package-path libs/Providers --filter runnerRendersGeminiAuthUsageJSON --filter runnerRendersGeminiAuthUsageTextWithTokenTrend`
  - `swift test --package-path libs/Providers --filter GeminiTokenTrendServiceTests`
  - `xcodebuild test -project nolon.xcodeproj -scheme nolon-app -destination 'platform=macOS' -only-testing:nolonTests/GeminiUsageTokenTrendViewModelTests -only-testing:nolonTests/CodexUsageTabPresentationTests`
- 修复 Gemini 历史 token 页面无数据：
  - 根因：`GeminiTokenTrendService` 只扫描 Nolon 账号 runtime home 下的 `tmp/**/chats/session-*.json`，但真实 Gemini 聊天记录落在全局 `~/.gemini/tmp/**/chats/session-*.json`
  - 处理：服务改为多根目录扫描，优先 runtime home，同时回退全局 `GEMINI_CLI_HOME` / `~/.gemini`，并对根路径去重
  - 回归测试：新增 “runtime home 无 session 时回退全局 `.gemini`” 用例
  - 实机验证：`swift run --package-path libs/Providers nolon gemini auth usage --provider gemini` 现返回 `token_trend: today=30345333 last7=76799270 last30=108033572`
  - app 验证：`xcodebuild test -project nolon.xcodeproj -scheme nolon-app -destination 'platform=macOS' -only-testing:nolonTests/GeminiUsageTokenTrendViewModelTests`
- 进一步收紧 Gemini 历史 token 数据源定义：
  - 历史 token 只读取全局 `~/.gemini/tmp/**/chats/session-*.json`
  - Nolon Gemini runtime home 仅用于认证隔离与 quota 查询，不参与历史 token 聚合
  - `GeminiTokenTrendService` 已删除 runtime home 扫描逻辑，仅在存在活跃账号时读取全局 `.gemini`
  - 测试与 feature 文档已同步更新为 “只读全局 `.gemini`” 口径
  - 已验证：
    - `swift test --package-path libs/Providers --filter GeminiTokenTrendServiceTests`
    - `swift run --package-path libs/Providers nolon gemini auth usage --provider gemini`
    - `xcodebuild test -project nolon.xcodeproj -scheme nolon-app -destination 'platform=macOS' -only-testing:nolonTests/GeminiUsageTokenTrendViewModelTests`
- quota 卡片展示也已组件化：
  - 新增通用 `ProviderQuotaSection`，统一承载额度窗口解析和配额行 UI
  - `ProviderUsageView` 中的 Codex/Gemini 紧凑卡片与 `ProviderUsageSnapshotView` 的详细卡片现在共用同一套 quota 组件
  - 窗口解析策略统一为：优先 `UsageSnapshot.windows`，为空时回退 `primary/secondary/tertiary`
  - 新增 `ProviderQuotaSectionTests` 覆盖显式窗口优先和 legacy 字段回退
  - 已验证：
    - `xcodebuild test -project nolon.xcodeproj -scheme nolon-app -destination 'platform=macOS' -only-testing:nolonTests/ProviderQuotaSectionTests -only-testing:nolonTests/GeminiUsageTokenTrendViewModelTests -only-testing:nolonTests/CodexUsageTabPresentationTests`

## 2026-03-09 Milestone: Provider Usage UI Overhaul
- **Task**: Universalize and polish Provider Usage UI for Gemini and Codex.
- **Decision**: Adopted 'Ultra Compact List' for Quotas and 'Stacked Bar Chart' for Trends to maximize information density.
- **Logic**: Unified UsageIdentity rendering across providers and implemented custom progress bars to bypass bulky system defaults.
- **Verification**: Validated logic via ProviderQuotaSectionViewDataTests.swift and manual layout audit against HTML prototypes.
